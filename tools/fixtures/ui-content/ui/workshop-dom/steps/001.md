---
title: Nadpis 001
---

# --description--

Do stránky přidej nadpis `h1` s textem `Krok 001`.

# --hints--

Stránka má nadpis `h1` s textem `Krok 001`. Podle `wait.txt` test chvíli počká (číslo v ms), ať jde během kontroly upravit kód, nebo se zasekne (`smycka`).

```js
if (files['wait.txt'].trim() === 'smycka') while (true) {}
await helpers.wait(Number(files['wait.txt']) || 0);
assert.equal(document.querySelector('h1')?.textContent, 'Krok 001');
```

# --seed--

## --file-- index.html

```html
<!DOCTYPE html>
<html lang="cs">
  <body>
--edit--

--edit--
  </body>
</html>
```

## --file-- wait.txt

```txt
0
```

# --solution--

## --file-- index.html

```html
<!DOCTYPE html>
<html lang="cs">
  <body>
    <h1>Krok 001</h1>
  </body>
</html>
```
