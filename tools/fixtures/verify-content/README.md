# Fixture pro verify-unit.test.js

Každá sekce a modul tu schválně porušuje pravidla verify (kontrakt kap. 10), aby test
ověřil jejich kódy. Běhy runneru test simuluje: test projde, když soubory deklarují
všechny proměnné `hodnotaN`, které test používá; výstup konzole jsou řádky `// log: …`.
