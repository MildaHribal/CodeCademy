# --description--

Nastav nadpisu `h1` tučné písmo.

# --hints--

`h1` je tučný.

```js
assert.equal(getComputedStyle(document.querySelector('h1')).fontWeight, '700');
```

# --seed--

## --file-- index.html

```html
<!DOCTYPE html>
<html lang="cs">
  <head><link rel="stylesheet" href="styles.css"></head>
  <body><h1>Ahoj</h1></body>
</html>
```

## --file-- styles.css

```css
h1 { color: blue; }
```

# --solution--

## --file-- styles.css

```css
h1 { color: blue; font-weight: 700; }
```
