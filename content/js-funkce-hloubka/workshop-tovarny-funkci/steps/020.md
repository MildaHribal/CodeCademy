---
title: Našeptávač s mezipamětí
see: js-funkce-hloubka/closures#closure-a-pamet
---

# --description--

Uživatelé často mažou a píšou znovu: „Brn", „Br", „Brn". Stejný dotaz nemá cenu počítat dvakrát.

**Úkol:** Uprav `createSuggester` tak, aby `suggest` pro dotaz, který už jednou dostal, vrátil **totéž pole** z mezipaměti. Každý našeptávač má vlastní mezipaměť a všechno ostatní z minulého kroku platí dál.

Nástroj na to už v souboru máš.

# --hints--

Stejný dotaz vrátí podruhé totéž pole.

```js
const suggest = createSuggester(['Cheb', 'Chomutov', 'Kolín']);
const first = suggest('ch');
const second = suggest('ch');
assert.deepEqual(first, ['Cheb', 'Chomutov'], "suggest('ch') má vrátit ['Cheb', 'Chomutov']");
assert.equal(second, first, "druhé suggest('ch') má vrátit totéž pole z mezipaměti, ne nově spočítané");
```

Dva našeptávače mají každý svou mezipaměť.

```js
const trains = createSuggester(['Kolín', 'Kladno']);
const buses = createSuggester(['Kroměříž', 'Krnov']);
assert.deepEqual(trains('k'), ['Kladno', 'Kolín'], "trains('k') má vrátit ['Kladno', 'Kolín']");
assert.deepEqual(buses('k'), ['Krnov', 'Kroměříž'], "buses('k') má vrátit ['Krnov', 'Kroměříž'], ne výsledek z mezipaměti jiného našeptávače");
```

Řazení, limit a prázdný dotaz fungují dál.

```js
const names = ['Hradec Králové hl. n.', 'Cheb', 'České Budějovice', 'Brno hl. n.', 'Chomutov', 'Cvikov'];
const suggest = createSuggester(names, 2);
assert.deepEqual(suggest('c'), ['Cvikov', 'České Budějovice'], "createSuggester([…], 2)('c') má vrátit ['Cvikov', 'České Budějovice']");
assert.deepEqual(suggest('  '), [], "suggest('  ') má vrátit []");
```

# --help--

## --tip--

Jde o mezipaměť v prostředí továrny — viz [Closure a paměť](see:js-funkce-hloubka/closures#closure-a-pamet).

## --tip--

Funkci, kterou továrna vrací, obal tvou `memoize`. Mezipaměť tak vznikne při každém zavolání `createSuggester` znovu.

## --tip--

Na jiných datech: `const priceFor = memoize((code) => lookupPrice(catalog, code));` uvnitř `createShop` dá každému obchodu vlastní mezipaměť cen.

# --seed--

## --file-- script.js

```js
// Stanice pro našeptávač, seřazené podle počtu cestujících.
const stations = [
  'Praha hl. n.',
  'Brno hl. n.',
  'Ostrava hl. n.',
  'Plzeň hl. n.',
  'Olomouc hl. n.',
  'Pardubice hl. n.',
  'Praha-Smíchov',
  'České Budějovice',
  'Hradec Králové hl. n.',
  'Ústí nad Labem hl. n.',
  'Liberec',
  'Zlín střed',
  'Česká Třebová',
  'Kolín',
  'Jihlava',
  'Cheb',
  'Chomutov',
  'Děčín hl. n.',
];

// Pomocné funkce pro pozdější kroky. Počítadla ukazují, kolikrát se opravdu spustily.
let timetableLoads = 0;
let travelCalls = 0;

// Načte jízdní řád (ve skutečné aplikaci by ho stahovala ze serveru).
function loadTimetable() {
  timetableLoads += 1;
  console.log('Načítám jízdní řád…');
  return { updated: '14. 9. 2026', trains: 1840 };
}

// Doba jízdy v minutách, zjednodušeně podle vzdálenosti od Prahy
// (ve skutečné aplikaci pomalý výpočet trasy).
const kmFromPrague = {
  'Praha hl. n.': 0,
  Kolín: 62,
  'Pardubice hl. n.': 104,
  'Česká Třebová': 164,
  'Olomouc hl. n.': 252,
  'Ostrava hl. n.': 356,
  'Plzeň hl. n.': 114,
  Cheb: 220,
};

function travelMinutes(from, to) {
  travelCalls += 1;
  const km = Math.abs(kmFromPrague[from] - kmFromPrague[to]);
  return Math.round((km / 96) * 60);
}

// Cena jízdenky v Kč: 1,60 Kč za kilometr a sleva podle tarifu.
function ticketPrice(tariff, km) {
  const discounts = { adult: 0, student: 0.25, senior: 0.5 };
  return Math.round(km * 1.6 * (1 - discounts[tariff]));
}

// Stanice, které obsahují dotaz, bez ohledu na velikost písmen.
function findStations(names, query) {
  const needle = query.toLowerCase();
  return names.filter((name) => name.toLowerCase().includes(needle));
}

console.log(findStations(stations, 'praha'));

// Nové pole stanic seřazené podle české abecedy.
function sortStations(names) {
  return names.toSorted((a, b) => a.localeCompare(b, 'cs'));
}

console.log(sortStations(findStations(stations, 'hl. n.')));

// Vyrobí funkci, která pozná, jestli název stanice obsahuje dotaz.
function matchesQuery(query) {
  const needle = query.toLowerCase();
  return (name) => name.toLowerCase().includes(needle);
}

console.log(stations.filter(matchesQuery('praha')));

// Počítadlo: každé zavolání přičte jedničku a vrátí nový stav.
function createCounter() {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
}

const countSearches = createCounter();
countSearches();
console.log('Hledání:', countSearches());

// Historie hledání: nejnovější dotaz první, nejvýš limit dotazů.
function createSearchHistory(limit) {
  let queries = [];
  return {
    add(query) {
      queries = [query, ...queries].slice(0, limit);
    },
    list() {
      return [...queries];
    },
  };
}

const recentSearches = createSearchHistory(3);
recentSearches.add('Brno');
recentSearches.add('Cheb');
console.log(recentSearches.list());

// Od kolegy: klávesové zkratky 1, 2, 3… pro oblíbené stanice.
// Každá zkratka je funkce, která vrátí svou stanici.
function createShortcuts(names) {
  const shortcuts = [];
  for (let i = 0; i < names.length; i++) {
    shortcuts.push(() => names[i]);
  }
  return shortcuts;
}

// Obal, který zavolá fn jen poprvé a potom vrací uložený výsledek.
function once(fn) {
  let called = false;
  let result;
  return (...args) => {
    if (!called) {
      called = true;
      result = fn(...args);
    }
    return result;
  };
}

const getTimetable = once(loadTimetable);
getTimetable();
console.log(getTimetable(), 'načteno:', timetableLoads);

// Částečná aplikace: předvyplní první argumenty funkce.
function partial(fn, ...presetArgs) {
  return (...laterArgs) => fn(...presetArgs, ...laterArgs);
}

const studentPrice = partial(ticketPrice, 'student');
console.log('Student Praha–Brno:', studentPrice(255), 'Kč');

// Čísla rezervací: prefix a pořadové číslo doplněné nulami na tři místa.
function createIdGenerator(prefix) {
  let lastNumber = 0;
  return () => {
    lastNumber += 1;
    return `${prefix}-${String(lastNumber).padStart(3, '0')}`;
  };
}

const nextReservation = createIdGenerator('R');
console.log(nextReservation(), nextReservation());

// Mezipaměť: pro stejné argumenty spočítá výsledek jen jednou.
function memoize(fn) {
  const cache = new Map();
  return (...args) => {
    const key = JSON.stringify(args);
    if (!cache.has(key)) {
      cache.set(key, fn(...args));
    }
    return cache.get(key);
  };
}

const minutesFromPrague = memoize((station) => travelMinutes('Praha hl. n.', station));
minutesFromPrague('Olomouc hl. n.');
console.log(minutesFromPrague('Olomouc hl. n.'), 'min, výpočtů:', travelCalls);

// Od kolegy: doba jízdy pro tabuli spojení, má se počítat jen jednou.
const travelCache = memoize(travelMinutes);

function cachedTravelMinutes(from, to) {
  return travelCache(from, to);
}

// Debounce: zavolá fn až po delay ms klidu, s argumenty posledního zavolání.
function debounce(fn, delay) {
  let timer;
  return (...args) => {
    clearTimeout(timer);
    timer = setTimeout(() => fn(...args), delay);
  };
}

const logSearch = debounce((query) => console.log('Hledám:', query), 300);
logSearch('P');
logSearch('Pra');
logSearch('Praha');

// Od kolegy: zpožděné hledání pro pole Odkud a Kam.
function createDelayedSearch(search, delay) {
  let searchTimer;
  return (query) => {
    clearTimeout(searchTimer);
    searchTimer = setTimeout(() => search(query), delay);
  };
}

// Throttle: zavolá fn nejvýš jednou za interval ms, zavolání mezitím zahodí.
function throttle(fn, interval) {
  let lastRun = 0;
  return (...args) => {
    const now = Date.now();
    if (now - lastRun >= interval) {
      lastRun = now;
      fn(...args);
    }
  };
}

const refreshLog = throttle(() => console.log('Obnovuji odjezdy'), 1000);
refreshLog();
refreshLog();

// Obal, který fn zavolá nejvýš max-krát; další zavolání vrátí undefined.
function limitCalls(fn, max) {
  let calls = 0;
  return (...args) => {
    if (calls >= max) {
      return undefined;
    }
    calls += 1;
    return fn(...args);
  };
}

const freeSearch = limitCalls(findStations, 2);
console.log(freeSearch(stations, 'Brno'), freeSearch(stations, 'Cheb'), freeSearch(stations, 'Kolín'));

--edit--
// Našeptávač: stanice podle dotazu, česky seřazené, nejvýš limit výsledků.
function createSuggester(names, limit = 5) {
  const sorted = sortStations(names);
  return (query) => {
    const trimmed = query.trim();
    if (trimmed === '') {
      return [];
    }
    return sorted.filter(matchesQuery(trimmed)).slice(0, limit);
  };
}

const suggest = createSuggester(stations);
console.log(suggest('hl'));
--edit--
```

# --solution--

## --file-- script.js

```js
// Stanice pro našeptávač, seřazené podle počtu cestujících.
const stations = [
  'Praha hl. n.',
  'Brno hl. n.',
  'Ostrava hl. n.',
  'Plzeň hl. n.',
  'Olomouc hl. n.',
  'Pardubice hl. n.',
  'Praha-Smíchov',
  'České Budějovice',
  'Hradec Králové hl. n.',
  'Ústí nad Labem hl. n.',
  'Liberec',
  'Zlín střed',
  'Česká Třebová',
  'Kolín',
  'Jihlava',
  'Cheb',
  'Chomutov',
  'Děčín hl. n.',
];

// Pomocné funkce pro pozdější kroky. Počítadla ukazují, kolikrát se opravdu spustily.
let timetableLoads = 0;
let travelCalls = 0;

// Načte jízdní řád (ve skutečné aplikaci by ho stahovala ze serveru).
function loadTimetable() {
  timetableLoads += 1;
  console.log('Načítám jízdní řád…');
  return { updated: '14. 9. 2026', trains: 1840 };
}

// Doba jízdy v minutách, zjednodušeně podle vzdálenosti od Prahy
// (ve skutečné aplikaci pomalý výpočet trasy).
const kmFromPrague = {
  'Praha hl. n.': 0,
  Kolín: 62,
  'Pardubice hl. n.': 104,
  'Česká Třebová': 164,
  'Olomouc hl. n.': 252,
  'Ostrava hl. n.': 356,
  'Plzeň hl. n.': 114,
  Cheb: 220,
};

function travelMinutes(from, to) {
  travelCalls += 1;
  const km = Math.abs(kmFromPrague[from] - kmFromPrague[to]);
  return Math.round((km / 96) * 60);
}

// Cena jízdenky v Kč: 1,60 Kč za kilometr a sleva podle tarifu.
function ticketPrice(tariff, km) {
  const discounts = { adult: 0, student: 0.25, senior: 0.5 };
  return Math.round(km * 1.6 * (1 - discounts[tariff]));
}

// Stanice, které obsahují dotaz, bez ohledu na velikost písmen.
function findStations(names, query) {
  const needle = query.toLowerCase();
  return names.filter((name) => name.toLowerCase().includes(needle));
}

console.log(findStations(stations, 'praha'));

// Nové pole stanic seřazené podle české abecedy.
function sortStations(names) {
  return names.toSorted((a, b) => a.localeCompare(b, 'cs'));
}

console.log(sortStations(findStations(stations, 'hl. n.')));

// Vyrobí funkci, která pozná, jestli název stanice obsahuje dotaz.
function matchesQuery(query) {
  const needle = query.toLowerCase();
  return (name) => name.toLowerCase().includes(needle);
}

console.log(stations.filter(matchesQuery('praha')));

// Počítadlo: každé zavolání přičte jedničku a vrátí nový stav.
function createCounter() {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
}

const countSearches = createCounter();
countSearches();
console.log('Hledání:', countSearches());

// Historie hledání: nejnovější dotaz první, nejvýš limit dotazů.
function createSearchHistory(limit) {
  let queries = [];
  return {
    add(query) {
      queries = [query, ...queries].slice(0, limit);
    },
    list() {
      return [...queries];
    },
  };
}

const recentSearches = createSearchHistory(3);
recentSearches.add('Brno');
recentSearches.add('Cheb');
console.log(recentSearches.list());

// Od kolegy: klávesové zkratky 1, 2, 3… pro oblíbené stanice.
// Každá zkratka je funkce, která vrátí svou stanici.
function createShortcuts(names) {
  const shortcuts = [];
  for (let i = 0; i < names.length; i++) {
    shortcuts.push(() => names[i]);
  }
  return shortcuts;
}

// Obal, který zavolá fn jen poprvé a potom vrací uložený výsledek.
function once(fn) {
  let called = false;
  let result;
  return (...args) => {
    if (!called) {
      called = true;
      result = fn(...args);
    }
    return result;
  };
}

const getTimetable = once(loadTimetable);
getTimetable();
console.log(getTimetable(), 'načteno:', timetableLoads);

// Částečná aplikace: předvyplní první argumenty funkce.
function partial(fn, ...presetArgs) {
  return (...laterArgs) => fn(...presetArgs, ...laterArgs);
}

const studentPrice = partial(ticketPrice, 'student');
console.log('Student Praha–Brno:', studentPrice(255), 'Kč');

// Čísla rezervací: prefix a pořadové číslo doplněné nulami na tři místa.
function createIdGenerator(prefix) {
  let lastNumber = 0;
  return () => {
    lastNumber += 1;
    return `${prefix}-${String(lastNumber).padStart(3, '0')}`;
  };
}

const nextReservation = createIdGenerator('R');
console.log(nextReservation(), nextReservation());

// Mezipaměť: pro stejné argumenty spočítá výsledek jen jednou.
function memoize(fn) {
  const cache = new Map();
  return (...args) => {
    const key = JSON.stringify(args);
    if (!cache.has(key)) {
      cache.set(key, fn(...args));
    }
    return cache.get(key);
  };
}

const minutesFromPrague = memoize((station) => travelMinutes('Praha hl. n.', station));
minutesFromPrague('Olomouc hl. n.');
console.log(minutesFromPrague('Olomouc hl. n.'), 'min, výpočtů:', travelCalls);

// Od kolegy: doba jízdy pro tabuli spojení, má se počítat jen jednou.
const travelCache = memoize(travelMinutes);

function cachedTravelMinutes(from, to) {
  return travelCache(from, to);
}

// Debounce: zavolá fn až po delay ms klidu, s argumenty posledního zavolání.
function debounce(fn, delay) {
  let timer;
  return (...args) => {
    clearTimeout(timer);
    timer = setTimeout(() => fn(...args), delay);
  };
}

const logSearch = debounce((query) => console.log('Hledám:', query), 300);
logSearch('P');
logSearch('Pra');
logSearch('Praha');

// Od kolegy: zpožděné hledání pro pole Odkud a Kam.
function createDelayedSearch(search, delay) {
  let searchTimer;
  return (query) => {
    clearTimeout(searchTimer);
    searchTimer = setTimeout(() => search(query), delay);
  };
}

// Throttle: zavolá fn nejvýš jednou za interval ms, zavolání mezitím zahodí.
function throttle(fn, interval) {
  let lastRun = 0;
  return (...args) => {
    const now = Date.now();
    if (now - lastRun >= interval) {
      lastRun = now;
      fn(...args);
    }
  };
}

const refreshLog = throttle(() => console.log('Obnovuji odjezdy'), 1000);
refreshLog();
refreshLog();

// Obal, který fn zavolá nejvýš max-krát; další zavolání vrátí undefined.
function limitCalls(fn, max) {
  let calls = 0;
  return (...args) => {
    if (calls >= max) {
      return undefined;
    }
    calls += 1;
    return fn(...args);
  };
}

const freeSearch = limitCalls(findStations, 2);
console.log(freeSearch(stations, 'Brno'), freeSearch(stations, 'Cheb'), freeSearch(stations, 'Kolín'));

// Našeptávač: stanice podle dotazu, česky seřazené, nejvýš limit výsledků.
// Stejný dotaz se podruhé vezme z mezipaměti.
function createSuggester(names, limit = 5) {
  const sorted = sortStations(names);
  return memoize((query) => {
    const trimmed = query.trim();
    if (trimmed === '') {
      return [];
    }
    return sorted.filter(matchesQuery(trimmed)).slice(0, limit);
  });
}

const suggest = createSuggester(stations);
console.log(suggest('hl'));
```
