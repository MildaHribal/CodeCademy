---
title: Seřaď debounce
kind: parsons
see: js-funkce-hloubka/closures#stav-mimo-tovarnu
---

# --description--

Našeptávač nemá hledat po každém písmenu. Když uživatel píše „Brno", stačí hledat jednou, až dopíše. Na to se používá [[debounce]]: obal, který funkci zavolá až po určité době **klidu**. Každé další zavolání během čekání odpočet zruší a začne znovu.

Průběh pro `debounce(search, 300)`, když uživatel píše rychle:

| čas | co se stane | odpočet |
|---|---|---|
| 0 ms | napíše „B" | začne, `search` by běžel v 300 ms |
| 120 ms | napíše „Br" | starý zruší, nový do 420 ms |
| 250 ms | napíše „Brn" | starý zruší, nový do 550 ms |
| 380 ms | napíše „Brno" | starý zruší, nový do 680 ms |
| 680 ms | nic nepíše | `search` proběhne **jednou** |

Potřebuješ dvě funkce prohlížeče: `setTimeout(fn, delay)` spustí funkci za `delay` ms a vrátí číslo časovače, `clearTimeout(číslo)` naplánovaný časovač zruší. Číslo posledního časovače si obal musí pamatovat mezi voláními — a každý obal svoje.

**Úkol:** Seřaď řádky tak, aby `debounce(fn, delay)` vrátila obal, který `fn` zavolá až po `delay` ms od posledního zavolání obalu. Odsazení nastavíš klávesou Tab, do mezery doplň, který časovač se ruší. Dva řádky jsou navíc.

# --hints--

Tři rychlá zavolání obalu spustí `fn` jen jednou, až po čekání.

```js
let calls = 0;
const delayed = debounce(() => {
  calls += 1;
}, 60);
delayed();
delayed();
delayed();
assert.equal(calls, 0, 'hned po zavolání obalu se fn ještě nemá spustit');
await helpers.wait(300);
assert.equal(calls, 1, 'po třech rychlých zavoláních a chvíli klidu má fn proběhnout právě jednou');
```

Po chvíli klidu obal zase funguje od začátku.

```js
let calls = 0;
const delayed = debounce(() => {
  calls += 1;
}, 60);
delayed();
await helpers.wait(300);
delayed();
await helpers.wait(300);
assert.equal(calls, 2, 'dvě zavolání oddělená chvílí klidu mají spustit fn dvakrát');
```

Dva obaly mají každý svůj časovač.

```js
let first = 0;
let second = 0;
const a = debounce(() => {
  first += 1;
}, 60);
const b = debounce(() => {
  second += 1;
}, 60);
a();
b();
await helpers.wait(300);
assert.equal(first, 1, 'první obal má fn zavolat, i když hned po něm proběhl druhý obal');
assert.equal(second, 1, 'druhý obal má fn zavolat');
```

# --help--

## --tip--

Jde o časovač v prostředí továrny, podobný `createDelay` v části [Stav mimo továrnu](see:js-funkce-hloubka/closures#stav-mimo-tovarnu).

## --tip--

Proměnná s číslem časovače patří do továrny, ne do obalu: v obalu by vznikla při každém zavolání nová a nebylo by co rušit. `setTimeout` dostane funkci, ne výsledek jejího zavolání.

# --seed--

## --file-- script.js

```js
// Stanice pro našeptávač, seřazené podle počtu cestujících.
const stations = [
  'Praha hl. n.',
  'Brno hl. n.',
  'Ostrava hl. n.',
  'Plzeň hl. n.',
  'Olomouc hl. n.',
  'Pardubice hl. n.',
  'Praha-Smíchov',
  'České Budějovice',
  'Hradec Králové hl. n.',
  'Ústí nad Labem hl. n.',
  'Liberec',
  'Zlín střed',
  'Česká Třebová',
  'Kolín',
  'Jihlava',
  'Cheb',
  'Chomutov',
  'Děčín hl. n.',
];

// Pomocné funkce pro pozdější kroky. Počítadla ukazují, kolikrát se opravdu spustily.
let timetableLoads = 0;
let travelCalls = 0;

// Načte jízdní řád (ve skutečné aplikaci by ho stahovala ze serveru).
function loadTimetable() {
  timetableLoads += 1;
  console.log('Načítám jízdní řád…');
  return { updated: '14. 9. 2026', trains: 1840 };
}

// Doba jízdy v minutách, zjednodušeně podle vzdálenosti od Prahy
// (ve skutečné aplikaci pomalý výpočet trasy).
const kmFromPrague = {
  'Praha hl. n.': 0,
  Kolín: 62,
  'Pardubice hl. n.': 104,
  'Česká Třebová': 164,
  'Olomouc hl. n.': 252,
  'Ostrava hl. n.': 356,
  'Plzeň hl. n.': 114,
  Cheb: 220,
};

function travelMinutes(from, to) {
  travelCalls += 1;
  const km = Math.abs(kmFromPrague[from] - kmFromPrague[to]);
  return Math.round((km / 96) * 60);
}

// Cena jízdenky v Kč: 1,60 Kč za kilometr a sleva podle tarifu.
function ticketPrice(tariff, km) {
  const discounts = { adult: 0, student: 0.25, senior: 0.5 };
  return Math.round(km * 1.6 * (1 - discounts[tariff]));
}

// Stanice, které obsahují dotaz, bez ohledu na velikost písmen.
function findStations(names, query) {
  const needle = query.toLowerCase();
  return names.filter((name) => name.toLowerCase().includes(needle));
}

console.log(findStations(stations, 'praha'));

// Nové pole stanic seřazené podle české abecedy.
function sortStations(names) {
  return names.toSorted((a, b) => a.localeCompare(b, 'cs'));
}

console.log(sortStations(findStations(stations, 'hl. n.')));

// Vyrobí funkci, která pozná, jestli název stanice obsahuje dotaz.
function matchesQuery(query) {
  const needle = query.toLowerCase();
  return (name) => name.toLowerCase().includes(needle);
}

console.log(stations.filter(matchesQuery('praha')));

// Počítadlo: každé zavolání přičte jedničku a vrátí nový stav.
function createCounter() {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
}

const countSearches = createCounter();
countSearches();
console.log('Hledání:', countSearches());

// Historie hledání: nejnovější dotaz první, nejvýš limit dotazů.
function createSearchHistory(limit) {
  let queries = [];
  return {
    add(query) {
      queries = [query, ...queries].slice(0, limit);
    },
    list() {
      return [...queries];
    },
  };
}

const recentSearches = createSearchHistory(3);
recentSearches.add('Brno');
recentSearches.add('Cheb');
console.log(recentSearches.list());

// Od kolegy: klávesové zkratky 1, 2, 3… pro oblíbené stanice.
// Každá zkratka je funkce, která vrátí svou stanici.
function createShortcuts(names) {
  const shortcuts = [];
  for (let i = 0; i < names.length; i++) {
    shortcuts.push(() => names[i]);
  }
  return shortcuts;
}

// Obal, který zavolá fn jen poprvé a potom vrací uložený výsledek.
function once(fn) {
  let called = false;
  let result;
  return (...args) => {
    if (!called) {
      called = true;
      result = fn(...args);
    }
    return result;
  };
}

const getTimetable = once(loadTimetable);
getTimetable();
console.log(getTimetable(), 'načteno:', timetableLoads);

// Částečná aplikace: předvyplní první argumenty funkce.
function partial(fn, ...presetArgs) {
  return (...laterArgs) => fn(...presetArgs, ...laterArgs);
}

const studentPrice = partial(ticketPrice, 'student');
console.log('Student Praha–Brno:', studentPrice(255), 'Kč');

// Čísla rezervací: prefix a pořadové číslo doplněné nulami na tři místa.
function createIdGenerator(prefix) {
  let lastNumber = 0;
  return () => {
    lastNumber += 1;
    return `${prefix}-${String(lastNumber).padStart(3, '0')}`;
  };
}

const nextReservation = createIdGenerator('R');
console.log(nextReservation(), nextReservation());

// Mezipaměť: pro stejné argumenty spočítá výsledek jen jednou.
function memoize(fn) {
  const cache = new Map();
  return (...args) => {
    const key = JSON.stringify(args);
    if (!cache.has(key)) {
      cache.set(key, fn(...args));
    }
    return cache.get(key);
  };
}

const minutesFromPrague = memoize((station) => travelMinutes('Praha hl. n.', station));
minutesFromPrague('Olomouc hl. n.');
console.log(minutesFromPrague('Olomouc hl. n.'), 'min, výpočtů:', travelCalls);

// Od kolegy: doba jízdy pro tabuli spojení, má se počítat jen jednou.
const travelCache = memoize(travelMinutes);

function cachedTravelMinutes(from, to) {
  return travelCache(from, to);
}

--edit--
--edit--
```

# --parsons--

```js
function debounce(fn, delay) {
  let timer;
  return () => {
    clearTimeout(__1__);
    timer = setTimeout(fn, delay);
  };
}
```

## --distractors--

```js
timer = setTimeout(fn(), delay);
setTimeout(fn, delay);
```

## --blanks--

```text
1: timer
```
