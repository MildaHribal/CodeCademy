---
title: "Vyber nástroj sám: tabule co nejrychleji"
kind: choose
---

# --description--

Ukázka ti vypsala něco přes 120 ms. Tři servery na sobě nijak nezávisí, a přesto každý čeká, až doběhne ten předchozí. U pomalejších serverů by tabule naskočila za několik sekund.

**Úkol:** Pod `loadAllSequential` napiš `async` funkci `loadAll(stop)`, která vrátí stejný výsledek, ale načte všechny tři dopravce **co nejrychleji**. Když některý server neodpoví, `loadAll` se zamítne jeho chybou. Jak to zařídíš, je na tobě.

V ukázce pak zavolej `loadAll` místo `loadAllSequential` a porovnej čas.

# --hints--

`loadAll('Hlavní nádraží')` se splní všemi osmi odjezdy seřazenými podle času.

```js
const departures = await loadAll('Hlavní nádraží');
assert.ok(Array.isArray(departures), "loadAll('Hlavní nádraží') se má splnit polem odjezdů");
assert.deepEqual(
  departures.map((item) => item.time),
  ['14:02', '14:05', '14:07', '14:09', '14:10', '14:18', '14:25', '14:32'],
  "loadAll('Hlavní nádraží') má vrátit všech 8 odjezdů seřazených podle času",
);
```

Všechny tři požadavky běží naráz.

```js
await networkIdle();
network.maxActive = 0;
await loadAll('Mendlovo náměstí');
assert.equal(network.maxActive, 3, `při loadAll mají běžet všechny tři požadavky naráz, nejvíc jich ale běželo ${network.maxActive}`);
```

Když jeden server neodpoví, `loadAll` se zamítne jeho chybou.

```js
servers.trains.down = true;
let caught = null;
await loadAll('Hlavní nádraží').catch((error) => {
  caught = error;
});
assert.equal(caught?.message, 'Server vlaky neodpovídá', "když server vlaků neodpovídá, loadAll se má zamítnout chybou 'Server vlaky neodpovídá'");
```

# --help--

## --tip--

Požadavek začne běžet ve chvíli, kdy zavoláš funkci, ne na `await`. Připomeň si část [Postupně, nebo souběžně](see:js-async/async-await#postupne-nebo-soubezne).

## --tip--

Nejdřív spusť všechno, pak počkej na všechno najednou. Na „všechno, nebo chyba" je v lekci o Promise jedna z kombinačních metod.

## --tip--

Rozepsané po podcílech:

```text
// 1. spusť tři požadavky hned za sebou, bez čekání
// 2. počkej, až se splní všechny
// 3. spoj výsledky a seřaď je
```

# --explain--

Vysvětli vlastními slovy, proč `loadAllSequential` trvá zhruba součet všech tří odezev a tvoje `loadAll` jen tu nejdelší.

## --model--

V `loadAllSequential` stojí `await` před každým voláním, takže druhý požadavek se spustí až po skončení prvního a třetí po skončení druhého. Časy se proto sečtou. V `loadAll` zavolám všechny tři funkce hned za sebou, požadavky běží naráz a na všechny počkám jednou, třeba přes `Promise.all`. Čekám tak jen na nejpomalejší z nich. Postupně má smysl čekat jen tehdy, když další požadavek potřebuje výsledek předchozího.

## --checklist--

- `await` před každým voláním spustí další požadavek až po dokončení předchozího.
- Požadavek začne běžet zavoláním funkce, ne na `await`.
- Souběžně: nejdřív všechno spustit, pak počkat najednou.
- Souběžné načítání trvá zhruba jako nejpomalejší požadavek.

# --seed--

## --file-- script.js

```js
// ===== Simulace serverů dopravců (neměň) =====

// Jízdní řády dvou brněnských zastávek. Čas odjezdu je text „HH:MM".
const timetable = {
  'Hlavní nádraží': [
    { provider: 'trains', line: 'R 11', destination: 'Praha hl. n.', time: '14:18' },
    { provider: 'trams', line: '4', destination: 'Obřany', time: '14:02' },
    { provider: 'buses', line: '401', destination: 'Mikulov', time: '14:10' },
    { provider: 'trains', line: 'Os 4908', destination: 'Břeclav', time: '14:05' },
    { provider: 'trams', line: '12', destination: 'Komárov', time: '14:09' },
    { provider: 'buses', line: '109', destination: 'Blansko', time: '14:25' },
    { provider: 'trains', line: 'R 17', destination: 'Olomouc', time: '14:32' },
    { provider: 'trams', line: '9', destination: 'Lesná', time: '14:07' },
  ],
  'Mendlovo náměstí': [
    { provider: 'buses', line: '67', destination: 'Líšeň', time: '14:12' },
    { provider: 'trams', line: '1', destination: 'Bystrc', time: '14:06' },
    { provider: 'buses', line: '44', destination: 'Bystrc', time: '14:03' },
    { provider: 'trams', line: '6', destination: 'Starý Lískovec', time: '14:04' },
  ],
};

// Servery dopravců. Testy si je přepínají: delay = odezva v ms,
// failures = kolikrát za sebou server selže, než odpoví, down = neodpovídá vůbec.
const servers = {
  trains: { label: 'vlaky', delay: 60, failures: 0, down: false },
  buses: { label: 'autobusy', delay: 40, failures: 0, down: false },
  trams: { label: 'tramvaje', delay: 20, failures: 0, down: false },
};

// Statistika sítě: kolik požadavků proběhlo a kolik jich běželo naráz.
const network = { calls: 0, active: 0, maxActive: 0 };

// Odjezdy jednoho dopravce ze zastávky. Vrací Promise a umí se zrušit přes signal.
function fetchDepartures(provider, stop, { signal } = {}) {
  const server = servers[provider];
  network.calls++;
  network.active++;
  network.maxActive = Math.max(network.maxActive, network.active);
  return new Promise((resolve, reject) => {
    const finish = () => {
      network.active--;
      signal?.removeEventListener('abort', onAbort);
    };
    const onAbort = () => {
      clearTimeout(timer);
      finish();
      reject(signal.reason);
    };
    const timer = setTimeout(() => {
      finish();
      if (server.down) return reject(new Error(`Server ${server.label} neodpovídá`));
      if (server.failures > 0) {
        server.failures--;
        return reject(new Error(`Server ${server.label} je přetížený`));
      }
      if (!timetable[stop]) return reject(new Error(`Zastávka ${stop} neexistuje`));
      resolve(timetable[stop].filter((item) => item.provider === provider).map((item) => ({ ...item })));
    }, server.delay);
    if (signal?.aborted) return onAbort();
    signal?.addEventListener('abort', onAbort);
  });
}

// Počká, až doběhnou všechny požadavky (používají testy).
function networkIdle() {
  return new Promise((resolve) => {
    const check = () => (network.active === 0 ? resolve() : setTimeout(check, 5));
    check();
  });
}

// Jeden řádek tabule, třeba „14:05  Os 4908  Břeclav".
function formatDeparture(departure) {
  return `${departure.time}  ${departure.line.padEnd(7)} ${departure.destination}`;
}

// Vypíše tabule do konzole.
function showBoard(stop, departures) {
  console.log(`Odjezdy — ${stop}`);
  for (const departure of departures) {
    console.log(formatDeparture(departure));
  }
}

// ===== Tvůj kód =====

// Nový seznam odjezdů seřazený od nejbližšího.
function sortByTime(departures) {
  return departures.toSorted((a, b) => a.time.localeCompare(b.time));
}

// Počká zadaný počet milisekund.
function sleep(ms) {
  return new Promise((resolve) => setTimeout(resolve, ms));
}

// Odjezdy jednoho dopravce seřazené od nejbližšího.
async function loadProvider(provider, stop) {
  const departures = await fetchDepartures(provider, stop);
  return sortByTime(departures);
}

// Odjezdy všech tří dopravců, načtené jeden po druhém.
async function loadAllSequential(stop) {
  const trains = await fetchDepartures('trains', stop);
  const buses = await fetchDepartures('buses', stop);
  const trams = await fetchDepartures('trams', stop);
  return sortByTime([...trains, ...buses, ...trams]);
}

--edit--
// ===== Ukázka: výsledek uvidíš v konzoli =====
async function demo() {
  const started = performance.now();
  const departures = await loadAllSequential('Hlavní nádraží');
  showBoard('Hlavní nádraží', departures);
  console.log(`Načteno za ${Math.round(performance.now() - started)} ms`);
}

demo();
--edit--
```

# --solution--

## --file-- script.js

```js
// ===== Simulace serverů dopravců (neměň) =====

// Jízdní řády dvou brněnských zastávek. Čas odjezdu je text „HH:MM".
const timetable = {
  'Hlavní nádraží': [
    { provider: 'trains', line: 'R 11', destination: 'Praha hl. n.', time: '14:18' },
    { provider: 'trams', line: '4', destination: 'Obřany', time: '14:02' },
    { provider: 'buses', line: '401', destination: 'Mikulov', time: '14:10' },
    { provider: 'trains', line: 'Os 4908', destination: 'Břeclav', time: '14:05' },
    { provider: 'trams', line: '12', destination: 'Komárov', time: '14:09' },
    { provider: 'buses', line: '109', destination: 'Blansko', time: '14:25' },
    { provider: 'trains', line: 'R 17', destination: 'Olomouc', time: '14:32' },
    { provider: 'trams', line: '9', destination: 'Lesná', time: '14:07' },
  ],
  'Mendlovo náměstí': [
    { provider: 'buses', line: '67', destination: 'Líšeň', time: '14:12' },
    { provider: 'trams', line: '1', destination: 'Bystrc', time: '14:06' },
    { provider: 'buses', line: '44', destination: 'Bystrc', time: '14:03' },
    { provider: 'trams', line: '6', destination: 'Starý Lískovec', time: '14:04' },
  ],
};

// Servery dopravců. Testy si je přepínají: delay = odezva v ms,
// failures = kolikrát za sebou server selže, než odpoví, down = neodpovídá vůbec.
const servers = {
  trains: { label: 'vlaky', delay: 60, failures: 0, down: false },
  buses: { label: 'autobusy', delay: 40, failures: 0, down: false },
  trams: { label: 'tramvaje', delay: 20, failures: 0, down: false },
};

// Statistika sítě: kolik požadavků proběhlo a kolik jich běželo naráz.
const network = { calls: 0, active: 0, maxActive: 0 };

// Odjezdy jednoho dopravce ze zastávky. Vrací Promise a umí se zrušit přes signal.
function fetchDepartures(provider, stop, { signal } = {}) {
  const server = servers[provider];
  network.calls++;
  network.active++;
  network.maxActive = Math.max(network.maxActive, network.active);
  return new Promise((resolve, reject) => {
    const finish = () => {
      network.active--;
      signal?.removeEventListener('abort', onAbort);
    };
    const onAbort = () => {
      clearTimeout(timer);
      finish();
      reject(signal.reason);
    };
    const timer = setTimeout(() => {
      finish();
      if (server.down) return reject(new Error(`Server ${server.label} neodpovídá`));
      if (server.failures > 0) {
        server.failures--;
        return reject(new Error(`Server ${server.label} je přetížený`));
      }
      if (!timetable[stop]) return reject(new Error(`Zastávka ${stop} neexistuje`));
      resolve(timetable[stop].filter((item) => item.provider === provider).map((item) => ({ ...item })));
    }, server.delay);
    if (signal?.aborted) return onAbort();
    signal?.addEventListener('abort', onAbort);
  });
}

// Počká, až doběhnou všechny požadavky (používají testy).
function networkIdle() {
  return new Promise((resolve) => {
    const check = () => (network.active === 0 ? resolve() : setTimeout(check, 5));
    check();
  });
}

// Jeden řádek tabule, třeba „14:05  Os 4908  Břeclav".
function formatDeparture(departure) {
  return `${departure.time}  ${departure.line.padEnd(7)} ${departure.destination}`;
}

// Vypíše tabule do konzole.
function showBoard(stop, departures) {
  console.log(`Odjezdy — ${stop}`);
  for (const departure of departures) {
    console.log(formatDeparture(departure));
  }
}

// ===== Tvůj kód =====

// Nový seznam odjezdů seřazený od nejbližšího.
function sortByTime(departures) {
  return departures.toSorted((a, b) => a.time.localeCompare(b.time));
}

// Počká zadaný počet milisekund.
function sleep(ms) {
  return new Promise((resolve) => setTimeout(resolve, ms));
}

// Odjezdy jednoho dopravce seřazené od nejbližšího.
async function loadProvider(provider, stop) {
  const departures = await fetchDepartures(provider, stop);
  return sortByTime(departures);
}

// Odjezdy všech tří dopravců, načtené jeden po druhém.
async function loadAllSequential(stop) {
  const trains = await fetchDepartures('trains', stop);
  const buses = await fetchDepartures('buses', stop);
  const trams = await fetchDepartures('trams', stop);
  return sortByTime([...trains, ...buses, ...trams]);
}

// Odjezdy všech tří dopravců, načtené souběžně.
async function loadAll(stop) {
  const [trains, buses, trams] = await Promise.all([
    fetchDepartures('trains', stop),
    fetchDepartures('buses', stop),
    fetchDepartures('trams', stop),
  ]);
  return sortByTime([...trains, ...buses, ...trams]);
}

// ===== Ukázka: výsledek uvidíš v konzoli =====
async function demo() {
  const started = performance.now();
  const departures = await loadAll('Hlavní nádraží');
  showBoard('Hlavní nádraží', departures);
  console.log(`Načteno za ${Math.round(performance.now() - started)} ms`);
}

demo();
```
