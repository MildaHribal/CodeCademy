---
title: Katalog po dávkách
see: js-async/event-loop#dlouhy-vypocet-zamrazi-stranku
timeoutMs: 9000
---

# --description--

**Úkol:** Uprav obsluhu tlačítka **Zobrazit celý katalog** tak, aby katalog vykreslila po dávkách po 50 kartách. Stránka má během vykreslování dál reagovat a odpočet tikat.

Zkus pak kliknout a hned najet myší na karty nebo kliknout na **Do košíku**.

# --hints--

Po kliknutí se katalog nevykreslí celý v jedné úloze.

```js
await helpers.click(document.querySelector('#show-all'));
const count = document.querySelectorAll('#products li').length;
assert.ok(count < fullCatalog.length, `hned po kliknutí je v seznamu ${count} karet — celý katalog se nemá vykreslit v jedné úloze`);
```

Po chvíli je v seznamu všech 600 karet.

```js
await helpers.click(document.querySelector('#show-all'));
await helpers.waitFor(() => document.querySelectorAll('#products li').length === fullCatalog.length, 6000);
```

# --help--

## --tip--

Použij funkci z minulého kroku, viz [Dlouhý výpočet zamrazí stránku](see:js-async/event-loop#dlouhy-vypocet-zamrazi-stranku).

## --tip--

Mění se jen jedno volání v obsluze kliknutí: jiná funkce a velikost dávky navíc.

## --tip--

Na jiných datech by to bylo `renderRowsInBatches(allOrders, 25)` místo `renderRows(allOrders)`.

# --seed--

## --file-- index.html

```html
<!DOCTYPE html>
<html lang="cs">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bleskový výprodej · Horská výbava</title>
    <link rel="stylesheet" href="styles.css">
  </head>
  <body>
    <header class="sale">
      <p class="sale__label">Bleskový výprodej</p>
      <h1 class="sale__title">Až −40 % na horskou výbavu</h1>
      <p class="sale__countdown">Končí za <time id="countdown">--:--:--</time></p>
      <p class="sale__delivery">Objednej do uzávěrky a doručíme zítra. Zbývá <span id="delivery">--:--:--</span></p>
    </header>
    <main class="shop">
      <div class="shop__bar">
        <p class="cart">V košíku: <strong id="cart-count">0</strong></p>
        <button id="show-all" class="button button--ghost" type="button">Zobrazit celý katalog</button>
      </div>
      <progress id="render-progress" class="progress" max="1" value="0" hidden></progress>
      <ul id="products" class="products"></ul>
    </main>
    <div id="toast" class="toast" role="status" hidden></div>
    <script src="script.js"></script>
  </body>
</html>
```

## --file-- styles.css

```css
:root {
  --forest: #12352b;
  --forest-2: #1f5243;
  --accent: #ff7a3d;
  --cream: #f6f1e7;
  --card: #fffdf9;
  --ink: #1d2a24;
  --muted: #637269;
  --line: #e4dccd;
  --radius: 0.9rem;
  --shadow: 0 12px 30px rgb(18 53 43 / 0.12);
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  background: var(--cream);
  color: var(--ink);
  font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
  line-height: 1.5;
}

.sale {
  padding: 2.5rem 1.5rem 2rem;
  text-align: center;
  color: var(--cream);
  background: radial-gradient(circle at 20% 0%, var(--forest-2), var(--forest) 60%);
}

.sale__label {
  display: inline-block;
  margin: 0;
  padding: 0.2rem 0.8rem;
  border-radius: 99rem;
  background: var(--accent);
  color: var(--forest);
  font-size: 0.8rem;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.sale__title {
  margin: 0.75rem 0 0.25rem;
  font-size: clamp(1.8rem, 4vw, 2.6rem);
}

.sale__countdown {
  margin: 0.5rem 0 0;
  font-size: 1.1rem;
}

#countdown {
  display: inline-block;
  margin-inline-start: 0.4rem;
  font-size: 2.4rem;
  font-weight: 800;
  font-variant-numeric: tabular-nums;
}

#countdown.is-urgent {
  color: var(--accent);
  animation: pulse 1s ease-in-out infinite;
}

@keyframes pulse {
  50% {
    transform: scale(1.06);
  }
}

.sale.is-ended .sale__countdown::after {
  content: " · výprodej skončil";
  color: var(--accent);
  font-weight: 700;
}

.sale__delivery {
  margin: 0.75rem 0 0;
  color: rgb(246 241 231 / 0.75);
  font-size: 0.9rem;
}

#delivery {
  font-variant-numeric: tabular-nums;
}

.shop {
  width: min(100% - 2rem, 64rem);
  margin: 2rem auto 4rem;
}

.shop__bar {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  margin-block-end: 1rem;
}

.cart {
  margin: 0;
}

.progress {
  width: 100%;
  height: 0.5rem;
  margin-block-end: 1rem;
  accent-color: var(--accent);
}

.products {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(14rem, 1fr));
  gap: 1rem;
  margin: 0;
  padding: 0;
  list-style: none;
}

.product {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 0.35rem;
  padding: 1.25rem;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: var(--card);
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.product:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow);
}

.product__badge {
  align-self: flex-start;
  padding: 0.1rem 0.5rem;
  border-radius: 99rem;
  background: var(--forest);
  color: var(--cream);
  font-size: 0.8rem;
  font-weight: 700;
}

.product__name {
  margin: 0.25rem 0 0;
  font-size: 1.05rem;
}

.product__price {
  margin: 0;
  font-size: 1.4rem;
  font-weight: 800;
  color: var(--forest);
}

.product__note {
  margin: 0 0 0.5rem;
  color: var(--muted);
  font-size: 0.8rem;
}

.button {
  margin-block-start: auto;
  padding: 0.6rem 1rem;
  border: 0;
  border-radius: 0.6rem;
  background: var(--accent);
  color: var(--forest);
  font: inherit;
  font-weight: 700;
  cursor: pointer;
  transition: background-color 0.15s ease;
}

.button:hover {
  background: #ff9460;
}

.button:focus-visible {
  outline: 3px solid var(--forest);
  outline-offset: 2px;
}

.button:disabled {
  opacity: 0.55;
  cursor: wait;
}

.button--ghost {
  background: transparent;
  color: var(--forest);
  box-shadow: inset 0 0 0 2px var(--forest);
}

.button--ghost:hover {
  background: rgb(18 53 43 / 0.08);
}

.toast {
  position: fixed;
  inset-inline: 0;
  bottom: 1.5rem;
  width: fit-content;
  max-width: calc(100% - 2rem);
  margin-inline: auto;
  padding: 0.8rem 1.2rem;
  border-radius: 0.7rem;
  background: var(--forest);
  color: var(--cream);
  box-shadow: var(--shadow);
}
```

## --file-- script.js

```js
// Horská výbava v bleskovém výprodeji. Ceny jsou v korunách.
const products = [
  { id: 1, name: 'Stan Hory 2', price: 3490, oldPrice: 4990 },
  { id: 2, name: 'Spacák Sněžka −5 °C', price: 1890, oldPrice: 2690 },
  { id: 3, name: 'Čelovka 400 lm', price: 490, oldPrice: 790 },
  { id: 4, name: 'Batoh Šumava 35 l', price: 1990, oldPrice: 2890 },
  { id: 5, name: 'Trekové hole Jizera', price: 890, oldPrice: 1290 },
  { id: 6, name: 'Karimatka Krkonoše', price: 690, oldPrice: 990 },
  { id: 7, name: 'Vařič Beskydy', price: 1190, oldPrice: 1590 },
  { id: 8, name: 'Termoska 0,75 l', price: 590, oldPrice: 890 },
];

// V ukázce končí výprodej 2 h 15 min po načtení stránky
// a uzávěrka doručení na zítřek je za 47 minut.
const saleEndsAt = Date.now() + (2 * 60 + 15) * 60 * 1000;
const deliveryDeadline = Date.now() + 47 * 60 * 1000;

const priceFormat = new Intl.NumberFormat('cs-CZ', { style: 'currency', currency: 'CZK', maximumFractionDigits: 0 });

// Nejnižší cena za posledních 30 dní, kterou musí e-shop u slevy ukázat.
// Prochází celou historii cen, a proto u jednoho produktu trvá pár milisekund.
function lowestPrice30Days(product) {
  let lowest = product.oldPrice;
  for (let record = 0; record < 60000; record++) {
    const price = product.oldPrice - ((record * 7919) % 11) * 10;
    if (price < lowest && price >= product.price) lowest = price;
  }
  return lowest;
}

// Karta jednoho produktu (prvek li, zatím mimo stránku).
function createProductCard(product) {
  const card = document.createElement('li');
  card.className = 'product';
  card.dataset.id = product.id;

  const badge = document.createElement('span');
  badge.className = 'product__badge';
  badge.textContent = `−${Math.round((1 - product.price / product.oldPrice) * 100)} %`;

  const name = document.createElement('h2');
  name.className = 'product__name';
  name.textContent = product.name;

  const price = document.createElement('p');
  price.className = 'product__price';
  price.textContent = priceFormat.format(product.price);

  const note = document.createElement('p');
  note.className = 'product__note';
  note.textContent = `Nejnižší cena za 30 dní: ${priceFormat.format(lowestPrice30Days(product))}`;

  const button = document.createElement('button');
  button.type = 'button';
  button.className = 'button product__add';
  button.textContent = 'Do košíku';

  card.append(badge, name, price, note, button);
  return card;
}

// Celý katalog: 600 variant velikostí a barev.
const fullCatalog = Array.from({ length: 600 }, (_, index) => {
  const base = products[index % products.length];
  return { ...base, id: 100 + index, name: `${base.name} · varianta ${index + 1}` };
});

// Vykreslí karty produktů a nahradí předchozí obsah seznamu.
function renderProducts(items) {
  const list = document.querySelector('#products');
  list.replaceChildren(...items.map(createProductCard));
}

renderProducts(products);

// Zbývající čas v ms jako hh:mm:ss, záporný čas jako 00:00:00.
function formatRemaining(ms) {
  const totalSeconds = Math.max(0, Math.floor(ms / 1000));
  const hours = Math.floor(totalSeconds / 3600);
  const minutes = Math.floor((totalSeconds % 3600) / 60);
  const seconds = totalSeconds % 60;
  return [hours, minutes, seconds].map((part) => String(part).padStart(2, '0')).join(':');
}

// Přepíše odpočet podle aktuálního času, po konci výprodeje ho zastaví.
function updateCountdown() {
  const remaining = saleEndsAt - Date.now();
  document.querySelector('#countdown').textContent = formatRemaining(remaining);
  if (remaining <= 0) {
    clearInterval(countdownTimer);
    document.querySelector('.sale').classList.add('is-ended');
  }
}

updateCountdown();

const countdownTimer = setInterval(updateCountdown, 1000);

// Od kolegy: odpočet do uzávěrky doručení na zítřek.
function updateDelivery() {
  const remaining = deliveryDeadline - Date.now();
  document.querySelector('#delivery').textContent = formatRemaining(remaining);
}

updateDelivery();
const deliveryTimer = setInterval(updateDelivery, 1000);

let toastTimer;

// Ukáže oznámení a za duration ms ho schová; nové oznámení odloží schování.
function showToast(message, duration = 3000) {
  const toast = document.querySelector('#toast');
  toast.textContent = message;
  toast.hidden = false;
  clearTimeout(toastTimer);
  toastTimer = setTimeout(() => {
    toast.hidden = true;
  }, duration);
}

let cartCount = 0;

// Kliknutí na „Do košíku" v kterékoli kartě (i v kartách přidaných později).
document.querySelector('#products').addEventListener('click', (event) => {
  const button = event.target.closest('.product__add');
  if (!button || button.disabled) return;
  button.disabled = true;
  setTimeout(() => {
    button.disabled = false;
  }, 800);
  cartCount++;
  document.querySelector('#cart-count').textContent = cartCount;
  const name = button.closest('.product').querySelector('.product__name').textContent;
  showToast(`${name} je v košíku`);
});

function renderInBatches(items, batchSize) {
  const list = document.querySelector('#products');
  list.replaceChildren();
  let index = 0;
  function renderBatch() {
    const batch = items.slice(index, index + batchSize);
    list.append(...batch.map(createProductCard));
    index += batchSize;
    if (index < items.length) {
      setTimeout(renderBatch, 0);
    }
  }
  renderBatch();
}

--edit--
document.querySelector('#show-all').addEventListener('click', () => {
  renderProducts(fullCatalog);
});
--edit--
```

# --solution--

## --file-- script.js

```js
// Horská výbava v bleskovém výprodeji. Ceny jsou v korunách.
const products = [
  { id: 1, name: 'Stan Hory 2', price: 3490, oldPrice: 4990 },
  { id: 2, name: 'Spacák Sněžka −5 °C', price: 1890, oldPrice: 2690 },
  { id: 3, name: 'Čelovka 400 lm', price: 490, oldPrice: 790 },
  { id: 4, name: 'Batoh Šumava 35 l', price: 1990, oldPrice: 2890 },
  { id: 5, name: 'Trekové hole Jizera', price: 890, oldPrice: 1290 },
  { id: 6, name: 'Karimatka Krkonoše', price: 690, oldPrice: 990 },
  { id: 7, name: 'Vařič Beskydy', price: 1190, oldPrice: 1590 },
  { id: 8, name: 'Termoska 0,75 l', price: 590, oldPrice: 890 },
];

// V ukázce končí výprodej 2 h 15 min po načtení stránky
// a uzávěrka doručení na zítřek je za 47 minut.
const saleEndsAt = Date.now() + (2 * 60 + 15) * 60 * 1000;
const deliveryDeadline = Date.now() + 47 * 60 * 1000;

const priceFormat = new Intl.NumberFormat('cs-CZ', { style: 'currency', currency: 'CZK', maximumFractionDigits: 0 });

// Nejnižší cena za posledních 30 dní, kterou musí e-shop u slevy ukázat.
// Prochází celou historii cen, a proto u jednoho produktu trvá pár milisekund.
function lowestPrice30Days(product) {
  let lowest = product.oldPrice;
  for (let record = 0; record < 60000; record++) {
    const price = product.oldPrice - ((record * 7919) % 11) * 10;
    if (price < lowest && price >= product.price) lowest = price;
  }
  return lowest;
}

// Karta jednoho produktu (prvek li, zatím mimo stránku).
function createProductCard(product) {
  const card = document.createElement('li');
  card.className = 'product';
  card.dataset.id = product.id;

  const badge = document.createElement('span');
  badge.className = 'product__badge';
  badge.textContent = `−${Math.round((1 - product.price / product.oldPrice) * 100)} %`;

  const name = document.createElement('h2');
  name.className = 'product__name';
  name.textContent = product.name;

  const price = document.createElement('p');
  price.className = 'product__price';
  price.textContent = priceFormat.format(product.price);

  const note = document.createElement('p');
  note.className = 'product__note';
  note.textContent = `Nejnižší cena za 30 dní: ${priceFormat.format(lowestPrice30Days(product))}`;

  const button = document.createElement('button');
  button.type = 'button';
  button.className = 'button product__add';
  button.textContent = 'Do košíku';

  card.append(badge, name, price, note, button);
  return card;
}

// Celý katalog: 600 variant velikostí a barev.
const fullCatalog = Array.from({ length: 600 }, (_, index) => {
  const base = products[index % products.length];
  return { ...base, id: 100 + index, name: `${base.name} · varianta ${index + 1}` };
});

// Vykreslí karty produktů a nahradí předchozí obsah seznamu.
function renderProducts(items) {
  const list = document.querySelector('#products');
  list.replaceChildren(...items.map(createProductCard));
}

renderProducts(products);

// Zbývající čas v ms jako hh:mm:ss, záporný čas jako 00:00:00.
function formatRemaining(ms) {
  const totalSeconds = Math.max(0, Math.floor(ms / 1000));
  const hours = Math.floor(totalSeconds / 3600);
  const minutes = Math.floor((totalSeconds % 3600) / 60);
  const seconds = totalSeconds % 60;
  return [hours, minutes, seconds].map((part) => String(part).padStart(2, '0')).join(':');
}

// Přepíše odpočet podle aktuálního času, po konci výprodeje ho zastaví.
function updateCountdown() {
  const remaining = saleEndsAt - Date.now();
  document.querySelector('#countdown').textContent = formatRemaining(remaining);
  if (remaining <= 0) {
    clearInterval(countdownTimer);
    document.querySelector('.sale').classList.add('is-ended');
  }
}

updateCountdown();

const countdownTimer = setInterval(updateCountdown, 1000);

// Od kolegy: odpočet do uzávěrky doručení na zítřek.
function updateDelivery() {
  const remaining = deliveryDeadline - Date.now();
  document.querySelector('#delivery').textContent = formatRemaining(remaining);
}

updateDelivery();
const deliveryTimer = setInterval(updateDelivery, 1000);

let toastTimer;

// Ukáže oznámení a za duration ms ho schová; nové oznámení odloží schování.
function showToast(message, duration = 3000) {
  const toast = document.querySelector('#toast');
  toast.textContent = message;
  toast.hidden = false;
  clearTimeout(toastTimer);
  toastTimer = setTimeout(() => {
    toast.hidden = true;
  }, duration);
}

let cartCount = 0;

// Kliknutí na „Do košíku" v kterékoli kartě (i v kartách přidaných později).
document.querySelector('#products').addEventListener('click', (event) => {
  const button = event.target.closest('.product__add');
  if (!button || button.disabled) return;
  button.disabled = true;
  setTimeout(() => {
    button.disabled = false;
  }, 800);
  cartCount++;
  document.querySelector('#cart-count').textContent = cartCount;
  const name = button.closest('.product').querySelector('.product__name').textContent;
  showToast(`${name} je v košíku`);
});

function renderInBatches(items, batchSize) {
  const list = document.querySelector('#products');
  list.replaceChildren();
  let index = 0;
  function renderBatch() {
    const batch = items.slice(index, index + batchSize);
    list.append(...batch.map(createProductCard));
    index += batchSize;
    if (index < items.length) {
      setTimeout(renderBatch, 0);
    }
  }
  renderBatch();
}

document.querySelector('#show-all').addEventListener('click', () => {
  renderInBatches(fullCatalog, 50);
});
```
