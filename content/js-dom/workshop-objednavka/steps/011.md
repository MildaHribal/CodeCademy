---
title: "Nefungující validace"
kind: debug
see: html-formulare/validace-v-prohlizeci
maxChange: 1.0
---

# --description--

## Hlášení

Zákazník si stěžuje, že u objednávky svítí chyba "Zkontroluj prosím zvýrazněná pole.", i když má vše vyplněné správně! Problém je v tom, že pole se validují, ale tlačítko `checkValidity` pořád hází chybu.

## Úkol

Najdi v kódu chybu. E-mailové pole z nějakého důvodu nutí uživatele psát jen čísla místo e-mailu. Změň jeho atribut type, aby se to spravilo.

# --hints--

Má e-mailové pole správný typ?
```js
const input = document.querySelector('input[name="email"]');
assert.equal(input.type, 'email', 'E-mail musí mít type email');
```

# --help--

## --tip--

Podívej se v HTML na atribut `type` u pole s e-mailem — hodnota `type` rozhoduje,
co prohlížeč považuje za platný vstup. Přehled typů je v části
[Validace v prohlížeči](see:html-formulare/validace-v-prohlizeci).

## --tip--

Pole s číselným typem přijme jen čísla, takže e-mail v něm nikdy platný nebude.
Změň jedinou hodnotu.

# --seed--
## --file-- index.html
```html
<link rel="stylesheet" href="style.css">
<form id="order" novalidate>
  <div id="error" style="color: red;"></div>
  <input type="text" name="name" required placeholder="Jméno">
  --edit--
  <input type="number" name="email" required placeholder="E-mail">
  --edit--
  
  <label>
    <input type="checkbox" id="is-company"> Nakupuji na firmu
  </label>
  <div id="company-info" style="display: none;">
    <input type="text" name="ico" placeholder="IČO">
  </div>
  
  <button disabled>Objednat</button>
</form>

<div class="summary">
  Vytváříme objednávku pro: <strong id="summary-name"></strong>
</div>
```

## --file-- script.js
```js
const form = document.getElementById('order');
const errorDiv = document.getElementById('error');
const checkbox = document.getElementById('is-company');
const companyInfo = document.getElementById('company-info');
const summaryName = document.getElementById('summary-name');
const btn = document.querySelector('form button');
const icoInput = document.querySelector('input[name="ico"]');
const nameInput = document.querySelector('input[name="name"]');

form.addEventListener('input', () => {
  if (nameInput.value === 'test') {
    nameInput.setCustomValidity('Jméno nesmí být "test"');
  } else {
    nameInput.setCustomValidity('');
  }

  const data = new FormData(form);
  summaryName.textContent = data.get('name');
  btn.disabled = !form.checkValidity();
});

form.addEventListener('submit', (e) => {
  e.preventDefault();
  if (!form.checkValidity()) {
    errorDiv.textContent = 'Zkontroluj prosím zvýrazněná pole.';
  } else {
    errorDiv.textContent = 'Všechno v pořádku.';
  }
});

checkbox.addEventListener('change', () => {
  if (checkbox.checked) {
    companyInfo.style.display = 'block';
    icoInput.required = true;
  } else {
    companyInfo.style.display = 'none';
    icoInput.required = false;
  }
  form.dispatchEvent(new Event('input'));
});
```

## --file-- style.css
```css
input {
  border: 1px solid #ccc;
  padding: 5px;
}
input:user-invalid {
  border-color: red;
}
```

# --solution--

## --file-- index.html
```html
<link rel="stylesheet" href="style.css">
<form id="order" novalidate>
  <div id="error" style="color: red;"></div>
  <input type="text" name="name" required placeholder="Jméno">
  <input type="email" name="email" required placeholder="E-mail">
  
  <label>
    <input type="checkbox" id="is-company"> Nakupuji na firmu
  </label>
  <div id="company-info" style="display: none;">
    <input type="text" name="ico" placeholder="IČO">
  </div>
  
  <button disabled>Objednat</button>
</form>

<div class="summary">
  Vytváříme objednávku pro: <strong id="summary-name"></strong>
</div>
```
