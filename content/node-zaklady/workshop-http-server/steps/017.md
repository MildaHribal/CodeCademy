---
title: Uložení nové knihy
main: server.js
see: node-zaklady/co-je-node#soubory-node-fs-promises
---

# --description--

Server knihu vytvoří a hned zapomene.

**Úkol:** nová kniha z `POST /api/books` se uloží do `books.json`. Objeví se
v `GET /api/books` a zůstane tam i po restartu serveru. Další nová kniha dostane
další id.

Ulož ji **dřív**, než pošleš odpověď `201`. Stav `201` klientovi tvrdí, že kniha
existuje. Kdybys odpověděl a zápis by pak selhal (plný disk, chybějící práva),
klient by si myslel, že je kniha uložená, a ona by nebyla.

Po odeslání knihy v HTTP klientovi otevři soubor `books.json` v editoru — nová kniha
tam je. Na vlastním počítači totéž zkusíš přes `curl`:

```sh
curl -i -X POST http://localhost:3000/api/books \
  -H 'Content-Type: application/json' \
  -d '{"title": "R.U.R.", "author": "Karel Čapek", "year": 1920}'
```

# --hints--

Nová kniha se uloží: je v `books.json` a vrátí ji i `GET /api/books`.

```js
const fs = await import('node:fs/promises');
const booksPath = `${helpers.dir}/books.json`;
const server = await helpers.startServer('server.js');
try {
  const postBook = (body) => fetch(new URL('/api/books', server.url), {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: typeof body === 'string' ? body : JSON.stringify(body),
  });
  const input = { title: 'Povídky z jedné kapsy', author: 'Karel Čapek', year: 1929 };
  await postBook(input);
  const saved = JSON.parse(await fs.readFile(booksPath, 'utf8'));
  assert.equal(saved.length, JSON.parse(files['books.json']).length + 1, 'Po POST /api/books má mít books.json o jednu knihu víc');
  assert.ok(saved.some((book) => book.title === input.title), 'Po POST /api/books má být nová kniha v books.json');
  const list = await (await fetch(new URL('/api/books', server.url))).json();
  assert.ok(list.some((book) => book.title === input.title), 'Po POST /api/books má GET /api/books vrátit i novou knihu');
} finally {
  await fs.writeFile(booksPath, files['books.json']);
}
```

Dvě nové knihy po sobě dostanou různá id a uloží se obě.

```js
const fs = await import('node:fs/promises');
const booksPath = `${helpers.dir}/books.json`;
const server = await helpers.startServer('server.js');
try {
  const postBook = (body) => fetch(new URL('/api/books', server.url), {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: typeof body === 'string' ? body : JSON.stringify(body),
  });
  const first = await (await postBook({ title: 'R.U.R.', author: 'Karel Čapek', year: 1920 })).json();
  const second = await (await postBook({ title: 'Máj', author: 'Karel Hynek Mácha', year: 1836 })).json();
  assert.notEqual(first.id, second.id, `Dvě nové knihy mají mít různá id, obě dostaly ${first.id}`);
  const saved = JSON.parse(await fs.readFile(booksPath, 'utf8'));
  assert.equal(saved.length, JSON.parse(files['books.json']).length + 2, 'Po dvou POST /api/books má mít books.json o dvě knihy víc');
} finally {
  await fs.writeFile(booksPath, files['books.json']);
}
```

Kniha zůstane i po restartu serveru.

```js
const fs = await import('node:fs/promises');
const booksPath = `${helpers.dir}/books.json`;
try {
  let server = await helpers.startServer('server.js');
  const res = await fetch(new URL('/api/books', server.url), {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ title: 'Bílá nemoc', author: 'Karel Čapek', year: 1937 }),
  });
  assert.equal(res.status, 201, 'POST /api/books má vrátit stav 201');
  await server.stop();
  server = await helpers.startServer('server.js');
  const list = await (await fetch(new URL('/api/books', server.url))).json();
  assert.ok(list.some((book) => book.title === 'Bílá nemoc'), 'Po restartu serveru má GET /api/books dál vracet knihu Bílá nemoc');
} finally {
  await fs.writeFile(booksPath, files['books.json']);
}
```

`POST /api/books` dál vrací stav `201` a vytvořenou knihu.

```js
const fs = await import('node:fs/promises');
const booksPath = `${helpers.dir}/books.json`;
const server = await helpers.startServer('server.js');
try {
  const postBook = (body) => fetch(new URL('/api/books', server.url), {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: typeof body === 'string' ? body : JSON.stringify(body),
  });
  const res = await postBook({ title: 'R.U.R.', author: 'Karel Čapek', year: 1920 });
  assert.equal(res.status, 201, 'POST /api/books má dál vracet stav 201');
  const book = await res.json();
  assert.equal(book.title, 'R.U.R.', 'POST /api/books má dál vracet vytvořenou knihu');
} finally {
  await fs.writeFile(booksPath, files['books.json']);
}
```

# --help--

## --tip--

Ukládání do souboru máš hotové v `library.js` — co přesně `writeFile` dělá, připomene
[Soubory: `node:fs/promises`](see:node-zaklady/co-je-node#soubory-node-fs-promises).

## --tip--

Do importu z `./library.js` přidej `saveBooks`. V obsluze `POST` přidej knihu do
načteného pole a celé pole ulož s `await` — až potom `sendJson` se `201`.

## --tip--

Stejné pořadí u komentářů: `await saveComments([...comments, comment]);` a teprve
pak odpověď.

# --seed--

## --file-- server.js

```js
import { createServer } from 'node:http';
import { readBody } from './body.js';
import { findBook, loadBooks } from './library.js';
import { handleStats } from './stats.js';

// Port z prostředí. Proměnné prostředí jsou řetězce, proto Number().
export const port = Number(process.env.PORT ?? 3000);

// Pošle data jako JSON se stavovým kódem a hlavičkou, podle které klient pozná typ.
function sendJson(res, status, data) {
  res.writeHead(status, { 'Content-Type': 'application/json; charset=utf-8' });
  res.end(JSON.stringify(data));
}

const server = createServer(async (req, res) => {
  const { pathname, searchParams } = new URL(req.url, 'http://localhost');

  if (pathname === '/api/books') {
    if (req.method === 'GET') {
      const books = await loadBooks();
      const author = searchParams.get('author');
      if (author === null) {
        return sendJson(res, 200, books);
      }
      return sendJson(res, 200, books.filter((book) => book.author === author));
    }
    if (req.method === 'POST') {
      const input = JSON.parse(await readBody(req));
      const books = await loadBooks();
      const book = {
        id: Math.max(0, ...books.map((item) => item.id)) + 1,
        title: input.title,
        author: input.author,
        year: input.year,
      };
      return sendJson(res, 201, book);
    }
    res.setHeader('Allow', 'GET, POST');
    return sendJson(res, 405, { error: 'Tahle metoda tu není povolená.' });
  }

  if (pathname.startsWith('/api/books/')) {
    const id = Number(pathname.slice('/api/books/'.length));
    const book = findBook(await loadBooks(), id);
    if (!book) {
      return sendJson(res, 404, { error: 'Kniha nenalezena.' });
    }
    return sendJson(res, 200, book);
  }

  if (pathname === '/api/authors') {
    const books = await loadBooks();
    const authors = [...new Set(books.map((book) => book.author))];
    authors.sort((a, b) => a.localeCompare(b, 'cs'));
    return sendJson(res, 200, authors);
  }

  if (pathname === '/api/stats') {
    return handleStats(req, res, await loadBooks());
  }

  sendJson(res, 404, { error: 'Tahle adresa neexistuje.' });
});

server.listen(port, () => {
  console.log(`Server běží na http://localhost:${port}`);
});
```

## --file-- library.js

```js
import { readFile, writeFile } from 'node:fs/promises';

// Funkce pro práci s knihami. Server je bude importovat.

// books.json leží vedle tohoto souboru. Cesta postavená od import.meta.url
// funguje, ať spustíš node z jakékoli složky.
const booksFile = new URL('./books.json', import.meta.url);

export async function loadBooks() {
  const text = await readFile(booksFile, 'utf8');
  return JSON.parse(text);
}

export async function saveBooks(books) {
  await writeFile(booksFile, JSON.stringify(books, null, 2));
}

export function findBook(books, id) {
  return books.find((book) => book.id === id);
}
```

## --file-- stats.js

```js
// Statistiky knihovny pro GET /api/stats — dopsal kolega Tomáš.
// Nepovinný filtr ?author=… spočítá statistiky jen z knih jednoho autora.

function respond(res, code, payload) {
  res.writeHead(code, { 'Content-Type': 'application/json; charset=utf-8' });
  res.end(JSON.stringify(payload));
}

export function handleStats(req, res, allBooks) {
  const params = new URL(req.url, 'http://localhost').searchParams;
  const author = params.get('author');

  const picked = [];
  for (const b of allBooks) {
    if (author === null || b.author === author) {
      picked.push(b);
    }
  }

  if (picked.length === 0) {
    return respond(res, 404, { error: 'Tento autor v knihovně nemá žádnou knihu.' });
  }

  const years = [];
  for (const b of picked) {
    years.push(b.year);
  }

  respond(res, 200, {
    count: picked.length,
    firstYear: Math.min(...years),
    lastYear: Math.max(...years),
  });
}
```

## --file-- body.js

```js
// Přečte celé tělo požadavku jako text. Tělo přichází po kouscích (proud).
export async function readBody(req) {
  const chunks = [];
  for await (const chunk of req) {
    chunks.push(chunk);
  }
  return Buffer.concat(chunks).toString('utf8');
}
```

## --file-- books.json

```json
[
  { "id": 1, "title": "Saturnin", "author": "Zdeněk Jirotka", "year": 1942 },
  { "id": 2, "title": "Krakatit", "author": "Karel Čapek", "year": 1924 },
  { "id": 3, "title": "Válka s mloky", "author": "Karel Čapek", "year": 1936 },
  { "id": 4, "title": "Spalovač mrtvol", "author": "Ladislav Fuks", "year": 1967 },
  { "id": 5, "title": "Babička", "author": "Božena Němcová", "year": 1855 }
]
```

## --file-- package.json

```json
{
  "name": "knihovna",
  "type": "module"
}
```

# --solution--

## --file-- server.js

```js
import { createServer } from 'node:http';
import { readBody } from './body.js';
import { findBook, loadBooks, saveBooks } from './library.js';
import { handleStats } from './stats.js';

// Port z prostředí. Proměnné prostředí jsou řetězce, proto Number().
export const port = Number(process.env.PORT ?? 3000);

// Pošle data jako JSON se stavovým kódem a hlavičkou, podle které klient pozná typ.
function sendJson(res, status, data) {
  res.writeHead(status, { 'Content-Type': 'application/json; charset=utf-8' });
  res.end(JSON.stringify(data));
}

const server = createServer(async (req, res) => {
  const { pathname, searchParams } = new URL(req.url, 'http://localhost');

  if (pathname === '/api/books') {
    if (req.method === 'GET') {
      const books = await loadBooks();
      const author = searchParams.get('author');
      if (author === null) {
        return sendJson(res, 200, books);
      }
      return sendJson(res, 200, books.filter((book) => book.author === author));
    }
    if (req.method === 'POST') {
      const input = JSON.parse(await readBody(req));
      const books = await loadBooks();
      const book = {
        id: Math.max(0, ...books.map((item) => item.id)) + 1,
        title: input.title,
        author: input.author,
        year: input.year,
      };
      books.push(book);
      await saveBooks(books);
      return sendJson(res, 201, book);
    }
    res.setHeader('Allow', 'GET, POST');
    return sendJson(res, 405, { error: 'Tahle metoda tu není povolená.' });
  }

  if (pathname.startsWith('/api/books/')) {
    const id = Number(pathname.slice('/api/books/'.length));
    const book = findBook(await loadBooks(), id);
    if (!book) {
      return sendJson(res, 404, { error: 'Kniha nenalezena.' });
    }
    return sendJson(res, 200, book);
  }

  if (pathname === '/api/authors') {
    const books = await loadBooks();
    const authors = [...new Set(books.map((book) => book.author))];
    authors.sort((a, b) => a.localeCompare(b, 'cs'));
    return sendJson(res, 200, authors);
  }

  if (pathname === '/api/stats') {
    return handleStats(req, res, await loadBooks());
  }

  sendJson(res, 404, { error: 'Tahle adresa neexistuje.' });
});

server.listen(port, () => {
  console.log(`Server běží na http://localhost:${port}`);
});
```
