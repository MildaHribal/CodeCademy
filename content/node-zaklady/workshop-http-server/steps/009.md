---
title: Routování podle metody
---

# --description--

Každý HTTP požadavek má kromě adresy i **metodu**, která říká, co s věcí na adrese
udělat: `GET` přečíst, `POST` vytvořit, `PUT`/`PATCH` změnit, `DELETE` smazat.
Stejná adresa tak může dělat různé věci — `GET /api/books` knihy vypíše,
`POST /api/books` (v příštích krocích) knihu přidá.

Server metodu zatím ignoruje: na `DELETE /api/books` odpoví seznamem knih, jako by
se nic nestalo. Klient, který se pokusil knihy smazat, si pak myslí, že se to
povedlo.

Metoda je v `req.method` jako řetězec velkými písmeny (`'GET'`, `'POST'`…).
Na metodu, kterou adresa neumí, se odpovídá stavem `405 Method Not Allowed`
a hlavičkou `Allow`, ve které je seznam metod, které adresa umí.

U routy `/api/books` v `server.js`:

- na `GET` odpověz jako dosud seznamem knih,
- na jakoukoli jinou metodu odpověz stavem `405`, hlavičkou `Allow: GET`
  a JSON objektem s klíčem `error`.

Hlavičku, kterou `sendJson` nezná, nastavíš předem přes `res.setHeader(jméno,
hodnota)`. `writeHead` ji pak pošle spolu se svými.

Na jiných datech:

```js
if (pathname === '/api/orders') {
  if (req.method === 'GET') {
    return sendJson(res, 200, await loadOrders());
  }
  res.setHeader('Allow', 'GET');
  return sendJson(res, 405, { error: 'Tahle metoda tu není povolená.' });
}
```

Ostatní adresy (`/api/books/:id`, `/api/authors`) zatím nech, jak jsou.

# --hints--

`GET /api/books` dál vrací seznam knih.

```js
const server = await helpers.startServer('server.js');
const res = await fetch(new URL('/api/books', server.url));
assert.equal(res.status, 200);
assert.deepEqual(await res.json(), JSON.parse(files['books.json']));
```

`DELETE /api/books` a `PUT /api/books` vrátí stav `405`.

```js
const server = await helpers.startServer('server.js');
for (const method of ['DELETE', 'PUT']) {
  const res = await fetch(new URL('/api/books', server.url), { method });
  assert.equal(res.status, 405, `${method} /api/books má vrátit 405`);
}
```

Odpověď `405` má hlavičku `Allow` s metodou `GET`.

```js
const server = await helpers.startServer('server.js');
const res = await fetch(new URL('/api/books', server.url), { method: 'DELETE' });
assert.match(res.headers.get('allow') ?? '', /\bGET\b/, `Hlavička Allow má obsahovat GET, přišlo: ${res.headers.get('allow')}`);
```

Odpověď `405` je JSON objekt s klíčem `error`.

```js
const server = await helpers.startServer('server.js');
const res = await fetch(new URL('/api/books', server.url), { method: 'DELETE' });
assert.equal(res.status, 405);
const body = await res.json();
assert.equal(typeof body.error, 'string', 'Tělo 405 má mít klíč error s textem');
```

# --seed--

## --file-- package.json

```json
{
  "name": "knihovna",
  "type": "module"
}
```

## --file-- books.json

```json
[
  { "id": 1, "title": "Saturnin", "author": "Zdeněk Jirotka", "year": 1942 },
  { "id": 2, "title": "Krakatit", "author": "Karel Čapek", "year": 1924 },
  { "id": 3, "title": "Válka s mloky", "author": "Karel Čapek", "year": 1936 },
  { "id": 4, "title": "Spalovač mrtvol", "author": "Ladislav Fuks", "year": 1967 },
  { "id": 5, "title": "Babička", "author": "Božena Němcová", "year": 1855 }
]
```

## --file-- library.js

```js
import { readFile } from 'node:fs/promises';

// books.json leží vedle tohoto souboru. Cesta postavená od import.meta.url
// funguje, ať spustíš node z jakékoli složky.
const booksFile = new URL('./books.json', import.meta.url);

export async function loadBooks() {
  const text = await readFile(booksFile, 'utf8');
  return JSON.parse(text);
}
```

## --file-- server.js

```js
import { createServer } from 'node:http';
import { loadBooks } from './library.js';

// Port z prostředí. Proměnné prostředí jsou řetězce, proto Number().
export const port = Number(process.env.PORT ?? 3000);

// Pošle data jako JSON se stavovým kódem a hlavičkou, podle které klient pozná typ.
function sendJson(res, status, data) {
  res.writeHead(status, { 'Content-Type': 'application/json; charset=utf-8' });
  res.end(JSON.stringify(data));
}

const server = createServer(async (req, res) => {
  const { pathname } = new URL(req.url, 'http://localhost');

  if (pathname === '/api/books') {
--edit--
    return sendJson(res, 200, await loadBooks());
--edit--
  }

  if (pathname.startsWith('/api/books/')) {
    const id = Number(pathname.slice('/api/books/'.length));
    const books = await loadBooks();
    const book = books.find((item) => item.id === id);
    if (!book) {
      return sendJson(res, 404, { error: 'Kniha nenalezena.' });
    }
    return sendJson(res, 200, book);
  }

  if (pathname === '/api/authors') {
    const books = await loadBooks();
    const authors = [...new Set(books.map((book) => book.author))];
    authors.sort((a, b) => a.localeCompare(b, 'cs'));
    return sendJson(res, 200, authors);
  }

  sendJson(res, 404, { error: 'Tahle adresa neexistuje.' });
});

server.listen(port, () => {
  console.log(`Server běží na http://localhost:${port}`);
});
```

# --solution--

## --file-- server.js

```js
import { createServer } from 'node:http';
import { loadBooks } from './library.js';

// Port z prostředí. Proměnné prostředí jsou řetězce, proto Number().
export const port = Number(process.env.PORT ?? 3000);

// Pošle data jako JSON se stavovým kódem a hlavičkou, podle které klient pozná typ.
function sendJson(res, status, data) {
  res.writeHead(status, { 'Content-Type': 'application/json; charset=utf-8' });
  res.end(JSON.stringify(data));
}

const server = createServer(async (req, res) => {
  const { pathname } = new URL(req.url, 'http://localhost');

  if (pathname === '/api/books') {
    if (req.method === 'GET') {
      return sendJson(res, 200, await loadBooks());
    }
    res.setHeader('Allow', 'GET');
    return sendJson(res, 405, { error: 'Tahle metoda tu není povolená.' });
  }

  if (pathname.startsWith('/api/books/')) {
    const id = Number(pathname.slice('/api/books/'.length));
    const books = await loadBooks();
    const book = books.find((item) => item.id === id);
    if (!book) {
      return sendJson(res, 404, { error: 'Kniha nenalezena.' });
    }
    return sendJson(res, 200, book);
  }

  if (pathname === '/api/authors') {
    const books = await loadBooks();
    const authors = [...new Set(books.map((book) => book.author))];
    authors.sort((a, b) => a.localeCompare(b, 'cs'));
    return sendJson(res, 200, authors);
  }

  sendJson(res, 404, { error: 'Tahle adresa neexistuje.' });
});

server.listen(port, () => {
  console.log(`Server běží na http://localhost:${port}`);
});
```
