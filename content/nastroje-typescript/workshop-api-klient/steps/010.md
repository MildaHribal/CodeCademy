---
title: Nové užití pro getUser
timeoutMs: 60000
runtime: node
kind: choose
---

# --description--

Máme generickou funkci `fetchJson`, můžeme s ní snadno stahovat cokoli. Zkus si to.

**Úkol:** Vytvoř schéma `userSchema`, kde bude pouze parametr `name: z.string()`, odvoď z něj `type User`, a přidej funkci `getUser(id: number)`, která přes URL `https://api.example.com/users/{id}` (použij interpolaci) zavolá `fetchJson`.

# --hints--

`index.ts` prochází kontrolou typů bez chyb.

```js
const result = await helpers.run('npx tsc --noEmit', { timeoutMs: 60000 });
assert.equal(result.code, 0, `tsc má projít bez chyb, ale vypsal:\n${result.stdout}`);
```

Máme novou funkci getUser, která bere číslo.

```js
assert.match(files['index.ts'], /async\s+function\s+getUser\(\s*\w+\s*:\s*number\s*\)/, 'Funkce getUser musí brát parameter typu number.');
```

# --help--

## --tip--

Zod schéma `userSchema` bude definováno přes `z.object({ name: z.string() })`.

## --tip-- 2

Zavoláš to jako `return fetchJson(\`https://api.example.com/users/${id}\`, userSchema);`

# --seed--

## --file-- package.json

```json
{
  "name": "workshop-api-klient",
  "type": "module"
}
```

## --file-- tsconfig.json

```json
{
  "compilerOptions": {
    "strict": true,
    "target": "esnext",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "types": ["node"],
    "noEmit": true
  },
  "include": ["*.ts"]
}
```

## --file-- index.ts

```ts
import { z } from 'zod';
type Station = z.infer<typeof stationSchema>;

const example: Station = {
  id: "s1",
  name: "Hlavní nádraží",
  bikesAvailable: 5,
  isRenting: true
};

const stationSchema = z.object({
  id: z.string(),
  name: z.string(),
  bikesAvailable: z.number(),
  isRenting: z.boolean(),
});
const stationsSchema = z.array(stationSchema);

export async function fetchJson<T>(url: string, schema: z.ZodType<T>) {
  const response = await fetch(url);
  if (!response.ok) {
    throw new Error('API selhalo');
  }
  const data: unknown = await response.json();
  return schema.parse(data);
}

--edit--

--edit--

export async function getStations() {
  return fetchJson('https://api.example.com/stations', stationsSchema);
}
```

# --solution--

## --file-- package.json

```json
{
  "name": "workshop-api-klient",
  "type": "module"
}
```

## --file-- tsconfig.json

```json
{
  "compilerOptions": {
    "strict": true,
    "target": "esnext",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "types": ["node"],
    "noEmit": true
  },
  "include": ["*.ts"]
}
```

## --file-- index.ts

```ts
import { z } from 'zod';
type Station = z.infer<typeof stationSchema>;

const example: Station = {
  id: "s1",
  name: "Hlavní nádraží",
  bikesAvailable: 5,
  isRenting: true
};

const stationSchema = z.object({
  id: z.string(),
  name: z.string(),
  bikesAvailable: z.number(),
  isRenting: z.boolean(),
});
const stationsSchema = z.array(stationSchema);

export async function fetchJson<T>(url: string, schema: z.ZodType<T>) {
  const response = await fetch(url);
  if (!response.ok) {
    throw new Error('API selhalo');
  }
  const data: unknown = await response.json();
  return schema.parse(data);
}

const userSchema = z.object({ name: z.string() });
type User = z.infer<typeof userSchema>;

export async function getUser(id: number) {
  return fetchJson(`https://api.example.com/users/${id}`, userSchema);
}

export async function getStations() {
  return fetchJson('https://api.example.com/stations', stationsSchema);
}
```
