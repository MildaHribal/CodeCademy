---
title: Generický fetchJson s typem T
timeoutMs: 60000
runtime: node
kind: parsons
---

# --description--

Nyní můžeme zajistit, aby `fetchJson` provádělo i samotnou validaci, když mu přidáme schéma a generický typ.

**Úkol:** Zkus to složit.

# --hints--

`index.ts` prochází kontrolou typů bez chyb.

```js
const result = await helpers.run('npx tsc --noEmit', { timeoutMs: 60000 });
assert.equal(result.code, 0, `tsc má projít bez chyb, ale vypsal:\n${result.stdout}`);
```

Funkce fetchJson existuje.

```js
assert.match(files['index.ts'], /function\s+fetchJson/, 'Chybí fetchJson');
```


# --seed--

## --file-- package.json

```json
{
  "name": "workshop-api-klient",
  "type": "module"
}
```

## --file-- tsconfig.json

```json
{
  "compilerOptions": {
    "strict": true,
    "target": "esnext",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "types": ["node"],
    "noEmit": true
  },
  "include": ["*.ts"]
}
```

## --file-- index.ts

```ts
import { z } from 'zod';
type Station = z.infer<typeof stationSchema>;

const example: Station = {
  id: "s1",
  name: "Hlavní nádraží",
  bikesAvailable: 5,
  isRenting: true
};

const stationSchema = z.object({
  id: z.string(),
  name: z.string(),
  bikesAvailable: z.number(),
  isRenting: z.boolean(),
});
const stationsSchema = z.array(stationSchema);

--edit--
--edit--


```

# --parsons--

```ts
export async function fetchJson<T>(url: string, schema: __1__) {
  const response = await fetch(url);
  if (!response.ok) {
    throw new Error('API selhalo');
  }
  const data: unknown = await response.json();
  return schema.parse(data);
}


```

## --distractors--

```ts
export async function fetchJson(url: string, schema: T) {
```

## --blanks--

```text
1: z.ZodType<T>
1: z.Schema<T>
```
