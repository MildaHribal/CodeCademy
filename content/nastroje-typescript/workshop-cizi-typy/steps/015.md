---
title: Report agentury
---

# --description--

Modul je opravený i rozšířený. Na závěr z něj uděláš report, který si manažerka
pustí jedním příkazem.

**Úkol:** v `ukazka.ts` vypiš přehled agentury v tomhle pořadí:

- nadpis `Statistiky agentury`,
- celkovou tržbu s jednotkou,
- místa konání oddělená čárkou,
- průměrnou obsazenost v procentech,
- a pak pro každý koncert řádek s kapelou, místem a popisem jeho stavu.

Pak zmáčkni **Spustit**. Porovnej výsledek s tím, co ti modul vypisoval na
začátku workshopu — a co všechno se cestou opravilo.

## Co teď umíš

Dostal jsi cizí modul s deseti řádky chyb a prošel jsi je jednu po druhé:
chybějící typ parametru, překlep v názvu vlastnosti, hodnotu, která nemusí
existovat, text z formuláře v početním výrazu, návratový typ, který lhal,
a zápis do seznamu jen ke čtení. Žádnou z nich jsi neumlčel — všechny opravil.
Pak jsi modul rozšířil o rozlišené sjednocení stavů, vyčerpávající `switch`,
generickou funkci a dva typy odvozené z jednoho původního.

A ještě něco důležitějšího: viděl jsi chybu, kterou typy nenašly. Kontrola typů
hlídá tvary, ne význam. Testy a kontrola typů se nezastupují, doplňují se.

V další lekci půjdeš na hranici aplikace — tam, kde data přicházejí zvenku
a kde ani dokonalé typy nestačí.

## Udělej po svém

Testy tyhle věci nekontrolují:

- Přidej stav `odlozeno` s novým datem a nech kontrolu, ať ti sama ukáže, kde
  všude ho musíš doplnit. Vodítko: začni tím, že variantu přidáš do typu, a pusť
  kontrolu.
- Dopiš funkci, která vrátí koncerty seřazené podle data. Vodítko: datum je text
  ve tvaru `2026-10-03`, takže se řadí stejně jako čísla.
- Přepiš data na něco svého — turnaje, festivaly, kurzy. Kontrola typů ti řekne,
  na co jsi zapomněl.

# --hints--

`ukazka.ts` jde spustit a vypíše nadpis a celkovou tržbu.

```js
const beh = await helpers.run('node ukazka.ts');
assert.equal(beh.code, 0, 'node ukazka.ts má doběhnout bez chyby. Hlásí:\n' + beh.stderr);
assert.match(beh.stdout, /Statistiky agentury/u, 'report má začínat nadpisem „Statistiky agentury". Vypsalo se:\n' + beh.stdout);
assert.match(beh.stdout, /3658100/u, 'report má obsahovat celkovou tržbu 3658100. Vypsalo se:\n' + beh.stdout);
```

Report obsahuje místa konání a průměrnou obsazenost.

```js
const beh = await helpers.run('node ukazka.ts');
assert.match(beh.stdout, /Brno.*Praha.*Ostrava/su, 'report má obsahovat místa konání oddělená čárkou. Vypsalo se:\n' + beh.stdout);
assert.match(beh.stdout, /73/u, 'report má obsahovat průměrnou obsazenost 73. Vypsalo se:\n' + beh.stdout);
```

U každého koncertu je kapela a popis jeho stavu.

```js
const beh = await helpers.run('node ukazka.ts');
assert.match(beh.stdout, /Zrní[^\n]*V prodeji/u, 'u koncertu Zrní má být popis stavu „V prodeji…". Vypsalo se:\n' + beh.stdout);
assert.match(beh.stdout, /Vojtěch Dyk[^\n]*Vyprodáno/u, 'u koncertu Vojtěcha Dyka má být „Vyprodáno". Vypsalo se:\n' + beh.stdout);
assert.match(beh.stdout, /Mydy[^\n]*Zrušeno/u, 'u koncertu Mydy má být „Zrušeno…". Vypsalo se:\n' + beh.stdout);
```

Kontrola typů projde bez chyby.

```js
const r = await helpers.run('npx tsc --noEmit');
assert.equal(r.code, 0, 'npx tsc --noEmit má projít bez chyby. Hlásí:\n' + r.stdout + r.stderr);
```

# --help--

## --tip--

Všechny funkce, které potřebuješ, jsou vyexportované ze `statistiky.ts`. Nic
nepočítej znovu.

## --tip--

Stav koncertu vyrobíš z koncertu jednou funkcí a text z něj druhou — dají se
zavolat do sebe.

## --tip--

Řádek s kapelou a stavem vypadá na jiných datech takhle:
`` console.log(`  ${kurz.nazev} (${kurz.mesto}): ${popisTerminu(termin)}`); ``

# --seed--

## --file-- package.json

```json
{
  "name": "statistiky-koncertu",
  "type": "module"
}
```

## --file-- tsconfig.json

```json
{
  "compilerOptions": {
    "strict": true,
    "target": "esnext",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "types": ["node"],
    "noEmit": true,
    "erasableSyntaxOnly": true,
    "verbatimModuleSyntax": true,
    "allowImportingTsExtensions": true
  },
  "include": ["*.ts"]
}
```

## --file-- koncerty.ts

```ts
// Data koncertní agentury. Tenhle soubor je v pořádku, sahat na něj nemusíš.
export type Koncert = {
  readonly id: number;
  kapela: string;
  misto: string;
  datum: string;
  kapacita: number;
  prodano: number;
  vstupne: number;
  zruseno?: string;
};

export const koncerty: readonly Koncert[] = [
  { id: 1, kapela: 'Zrní', misto: 'Brno', datum: '2026-10-03', kapacita: 800, prodano: 640, vstupne: 590 },
  { id: 2, kapela: 'Vojtěch Dyk', misto: 'Praha', datum: '2026-10-11', kapacita: 2500, prodano: 2500, vstupne: 990 },
  { id: 3, kapela: 'Mydy', misto: 'Ostrava', datum: '2026-11-02', kapacita: 600, prodano: 210, vstupne: 450, zruseno: 'Nemoc kapely' },
  { id: 4, kapela: 'Lenny', misto: 'Praha', datum: '2026-11-20', kapacita: 1200, prodano: 900, vstupne: 790 },
];
```

## --file-- statistiky.ts

```ts
// Statistiky koncertů. Modul psal kolega v JavaScriptu; po přejmenování na .ts
// hlásí kontrola typů deset řádků chyb. Data v koncerty.ts jsou v pořádku.
import { koncerty } from './koncerty.ts';
import type { Koncert } from './koncerty.ts';

// Tržba za všechny koncerty: prodané vstupenky krát cena vstupenky.
export function trzba(seznam: readonly Koncert[]): number {
  return seznam.reduce((soucet, koncert) => soucet + koncert.prodano * koncert.vstupne, 0);
}

// Seznam měst, kde se hraje, každé jednou.
export function mistaKonani(seznam: readonly Koncert[]): string[] {
  return [...new Set(seznam.map((koncert) => koncert.misto))];
}

// Obsazenost koncertu v procentech, zaokrouhlená na celá procenta.
export function obsazenost(id: number): number {
  const koncert = koncerty.find((jeden) => jeden.id === id);
  if (!koncert) return 0;
  return Math.round((koncert.prodano / koncert.kapacita) * 100);
}

// Kolik by koncert vydělal, kdyby vstupné bylo takové, jaké přišlo z formuláře.
export function trzbaPriCene(id: number, hodnota: string): number {
  const koncert = koncerty.find((jeden) => jeden.id === id);
  if (!koncert) return 0;
  const cena = Number(hodnota);
  if (!Number.isFinite(cena) || cena < 0) return 0;
  return koncert.prodano * cena;
}

// První vyprodaný koncert v seznamu.
export function prvniVyprodany(seznam: readonly Koncert[]): Koncert | undefined {
  return seznam.find((koncert) => koncert.prodano === koncert.kapacita);
}

// Průměrná obsazenost všech koncertů v procentech. Dopsal kolega Tomáš.
export function prumernaObsazenost(seznam: readonly Koncert[]): number {
  if (seznam.length === 0) return 0;
  let soucet = 0;
  for (const koncert of seznam) {
    soucet += Math.round((koncert.prodano / koncert.kapacita) * 100);
  }
  return Math.round(soucet / seznam.length);
}

// Přidá koncert do nabídky.
export function pridejKoncert(novy: Koncert): Koncert[] {
  return [...koncerty, novy];
}

// Stav koncertu: každá varianta nese jen svoje data.
export type StavKoncertu =
  | { stav: 'v-prodeji'; zbyva: number }
  | { stav: 'vyprodano' }
  | { stav: 'zruseno'; duvod: string };

export function stavKoncertu(koncert: Koncert): StavKoncertu {
  if (koncert.zruseno) {
    return { stav: 'zruseno', duvod: koncert.zruseno };
  }
  if (koncert.prodano >= koncert.kapacita) {
    return { stav: 'vyprodano' };
  }
  return { stav: 'v-prodeji', zbyva: koncert.kapacita - koncert.prodano };
}

export function popisStavu(stav: StavKoncertu): string {
  switch (stav.stav) {
    case 'v-prodeji':
      return `V prodeji, zbývá ${stav.zbyva} vstupenek`;
    case 'vyprodano':
      return 'Vyprodáno';
    case 'zruseno':
      return `Zrušeno: ${stav.duvod}`;
    default: {
      const nezname: never = stav;
      return nezname;
    }
  }
}

export function podleId<T extends { id: number }>(polozky: readonly T[], id: number): T | undefined {
  return polozky.find((polozka) => polozka.id === id);
}

// Kolik vstupenek zbývá dohromady na koncertech, které se dají koupit.
export function zbyvaCelkem(seznam: readonly Koncert[]): number {
  let zbyva = 0;
  for (const koncert of seznam) {
    const stav = stavKoncertu(koncert);
    if (stav.stav === 'v-prodeji') {
      zbyva += stav.zbyva;
    }
  }
  return zbyva;
}

// Koncert, který ještě nemá id — takový přijde z formuláře.
export type NovyKoncert = Omit<Koncert, 'id'>;

export function vytvorKoncert(novy: NovyKoncert, seznam: readonly Koncert[]): Koncert[] {
  const dalsiId = Math.max(0, ...seznam.map((koncert) => koncert.id)) + 1;
  return [...seznam, { ...novy, id: dalsiId }];
}

export function upravKoncert(seznam: readonly Koncert[], id: number, zmeny: Partial<NovyKoncert>): Koncert[] {
  return seznam.map((koncert) => (koncert.id === id ? { ...koncert, ...zmeny } : koncert));
}
```

## --file-- ukazka.ts

```ts
import { koncerty } from './koncerty.ts';
import { mistaKonani, obsazenost, trzba } from './statistiky.ts';

console.log('Tržba celkem:', trzba(koncerty));
console.log('Místa konání:', mistaKonani(koncerty));
console.log('Obsazenost koncertu 1:', obsazenost(1), '%');
```

# --solution--

## --file-- ukazka.ts

```ts
import { koncerty } from './koncerty.ts';
import { mistaKonani, popisStavu, prumernaObsazenost, stavKoncertu, trzba } from './statistiky.ts';

console.log('Statistiky agentury');
console.log(`Tržba celkem: ${trzba(koncerty)} Kč`);
console.log(`Místa konání: ${mistaKonani(koncerty).join(', ')}`);
console.log(`Průměrná obsazenost: ${prumernaObsazenost(koncerty)} %`);

for (const koncert of koncerty) {
  console.log(`  ${koncert.kapela} (${koncert.misto}): ${popisStavu(stavKoncertu(koncert))}`);
}
```
