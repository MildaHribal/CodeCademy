---
title: Sleva nad dva tisíce
kind: choose
---

# --description--

Obchod vyhlásil akci. Pravidla dostal e-mailem:

- Z částky **nad 2 000 Kč** dostane zákazník slevu 10 %.
- Na 2 000 Kč a míň sleva není.
- Sleva se počítá na celé koruny.
- Nikdy nepřesáhne 500 Kč, i kdyby byl košík za sto tisíc.

**Úkol:** v `kosik.ts` napiš a vyexportuj funkci `sleva(castka)`. Bere číslo,
vrací číslo — kolik se z té částky odečte.

Tenhle krok ti neřekne, čím to spočítat. Nástroj si vyber sám; projde každé
řešení, které dává správná čísla.

# --hints--

Do 2 000 Kč včetně je sleva nula.

```js
const { sleva } = await helpers.importFile('kosik.ts');
assert.equal(sleva(0), 0, 'sleva(0) má vrátit 0');
assert.equal(sleva(1500), 0, 'sleva(1500) má vrátit 0, akce platí až nad 2000 Kč');
assert.equal(sleva(2000), 0, 'sleva(2000) má vrátit 0 — akce platí až nad 2000 Kč, ne od 2000 Kč');
```

Nad 2 000 Kč je sleva desetina částky, zaokrouhlená na celé koruny.

```js
const { sleva } = await helpers.importFile('kosik.ts');
assert.equal(sleva(2500), 250, 'sleva(2500) má vrátit 250');
assert.equal(sleva(2888), 289, 'sleva(2888) má vrátit 289 — desetina je 288,8 a zaokrouhluje se na celé koruny');
assert.equal(sleva(2001), 200, 'sleva(2001) má vrátit 200 — desetina je 200,1');
```

Sleva nikdy nepřesáhne 500 Kč.

```js
const { sleva } = await helpers.importFile('kosik.ts');
assert.equal(sleva(5000), 500, 'sleva(5000) má vrátit 500 — přesně na stropu');
assert.equal(sleva(9000), 500, 'sleva(9000) má vrátit 500, ne 900');
assert.equal(sleva(100000), 500, 'sleva(100000) má vrátit 500');
```

Kontrola typů projde a `sleva` má otypovaný parametr i návratovou hodnotu.

```js
const kod = [
  "import { sleva } from './kosik.ts';",
  "const castka: number = sleva(3000);",
  "// @ts-expect-error sleva bere číslo, ne text",
  "const spatne = sleva('3000');",
  "console.log(castka, spatne);",
].join('\n');
await helpers.run(`cat > .kontrola.ts <<'KONEC'\n${kod}\nKONEC`);
const r = await helpers.run('npx tsc --noEmit --strict --target esnext --module nodenext --moduleResolution nodenext --types node --allowImportingTsExtensions --ignoreConfig .kontrola.ts');
assert.equal(r.code, 0, 'sleva má brát číslo a vracet číslo. Kontrola typů hlásí:\n' + r.stdout + r.stderr);
const projekt = await helpers.run('npx tsc --noEmit');
assert.equal(projekt.code, 0, 'npx tsc --noEmit má projít bez chyby. Hlásí:\n' + projekt.stdout + projekt.stderr);
```

# --help--

## --tip--

Rozděl si to na tři nezávislá rozhodnutí: kdy sleva vůbec je, kolik z částky
dělá, a co se stane, když vyjde moc velká. Každé z nich má v JavaScriptu hotový
nástroj.

## --tip--

Poslední pravidlo („nikdy víc než 500") je zastropování: ze dvou čísel bereš to
menší. Zaokrouhlení na celé koruny řeší `Math`.

## --tip--

Na jiných datech vypadá zastropování takhle:
`const zobrazenychKomentaru = Math.min(komentare.length, 10);`

# --seed--

## --file-- katalog.ts

```ts
// Katalog e-shopu Herna. Kolega ho napsal v JavaScriptu, soubor jen přejmenoval.
export const STAVY = ['skladem', 'na-objednavku', 'vyprodano'] as const;

export type Stav = (typeof STAVY)[number];

export type Hra = {
  readonly id: number;
  nazev: string;
  cena: number;
  stav: Stav;
  podtitul?: string;
};

export const katalog: readonly Hra[] = [
  { id: 1, nazev: 'Azul', cena: 999, stav: 'skladem', podtitul: 'Portugalské kachličky' },
  { id: 2, nazev: 'Osadníci z Katanu', cena: 1290, stav: 'skladem' },
  { id: 3, nazev: 'Dixit', cena: 890, stav: 'na-objednavku', podtitul: 'Příběh v obrázcích' },
  { id: 4, nazev: 'Krycí jména', cena: 490, stav: 'vyprodano' },
  { id: 5, nazev: 'Carcassonne', cena: 790, stav: 'skladem' },
  { id: 6, nazev: 'Čtyřlístek', cena: 390, stav: 'skladem' },
  { id: 7, nazev: 'Duch', cena: 290, stav: 'na-objednavku' },
];

export const POPISY_STAVU = {
  skladem: 'Skladem, odesíláme do 24 hodin',
  'na-objednavku': 'Na objednávku, 7 až 10 dní',
  vyprodano: 'Vyprodáno',
} as const satisfies Record<Stav, string>;
```

## --file-- kosik.ts

```ts
import { katalog } from './katalog.ts';
import type { Hra } from './katalog.ts';

export type Polozka = {
  hra: Hra;
  kusy: number;
};

// Najde hru v katalogu podle id. Když taková není, vrátí undefined.
export function najdiHru(id: number) {
  return katalog.find((hra) => hra.id === id);
}

export function mezisoucet(kosik: Polozka[]): number {
  return kosik.reduce((soucet, polozka) => soucet + polozka.hra.cena * polozka.kusy, 0);
}

export function pridej(kosik: Polozka[], hra: Hra, kusy: number): Polozka[] {
  const uzTam = kosik.some((polozka) => polozka.hra.id === hra.id);
  if (uzTam) {
    return kosik.map((polozka) =>
      polozka.hra.id === hra.id ? { ...polozka, kusy: polozka.kusy + kusy } : polozka,
    );
  }
  return [...kosik, { hra, kusy }];
}

export function nazevHry(hra: Hra): string {
  return hra.podtitul ? `${hra.nazev}: ${hra.podtitul}` : hra.nazev;
}

export type Formatovac = (castka: number) => string;

export const naKoruny: Formatovac = (castka) => `${castka.toLocaleString('cs-CZ')} Kč`;

export function shrnuti(kosik: Polozka[], format: Formatovac): string[] {
  return kosik.map((polozka) => {
    const castka = polozka.hra.cena * polozka.kusy;
    return `${nazevHry(polozka.hra)} ×${polozka.kusy} — ${format(castka)}`;
  });
}

export function filtrujKatalog(hry: readonly Hra[], podminka: (hra: Hra) => boolean): Hra[] {
  return hry.filter(podminka);
}
--edit--

--edit--
```

## --file-- sklad.ts

```ts
import { najdiHru } from './kosik.ts';

// Sklad: co se dá hned koupit. Psal kolega Tomáš.
export function jeSkladem(id: number): boolean {
  const hra = najdiHru(id);
  return hra?.stav === 'skladem';
}

export function kolikSkladem(ids: number[]): number {
  return ids.filter((id) => jeSkladem(id)).length;
}
```

## --file-- ukazka.ts

```ts
import { katalog } from './katalog.ts';
import { mezisoucet, najdiHru, pridej } from './kosik.ts';
import type { Polozka } from './kosik.ts';

console.log(`V katalogu je ${katalog.length} her.`);
console.log('Hra s id 2:', najdiHru(2));

const kosik: Polozka[] = [
  { hra: katalog[0], kusy: 2 },
  { hra: katalog[2], kusy: 1 },
];

console.log(`Mezisoučet košíku: ${mezisoucet(kosik)} Kč`);
console.log(`S jedním Azulem navíc: ${mezisoucet(pridej(kosik, katalog[0], 1))} Kč`);
```

## --file-- tsconfig.json

```json
{
  "compilerOptions": {
    "strict": true,
    "target": "esnext",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "types": ["node"],
    "noEmit": true,
    "erasableSyntaxOnly": true,
    "verbatimModuleSyntax": true,
    "allowImportingTsExtensions": true
  },
  "include": ["*.ts"]
}
```

## --file-- package.json

```json
{
  "name": "kosik-her",
  "type": "module"
}
```

# --solution--

## --file-- kosik.ts

```ts
import { katalog } from './katalog.ts';
import type { Hra } from './katalog.ts';

export type Polozka = {
  hra: Hra;
  kusy: number;
};

// Najde hru v katalogu podle id. Když taková není, vrátí undefined.
export function najdiHru(id: number) {
  return katalog.find((hra) => hra.id === id);
}

export function mezisoucet(kosik: Polozka[]): number {
  return kosik.reduce((soucet, polozka) => soucet + polozka.hra.cena * polozka.kusy, 0);
}

export function pridej(kosik: Polozka[], hra: Hra, kusy: number): Polozka[] {
  const uzTam = kosik.some((polozka) => polozka.hra.id === hra.id);
  if (uzTam) {
    return kosik.map((polozka) =>
      polozka.hra.id === hra.id ? { ...polozka, kusy: polozka.kusy + kusy } : polozka,
    );
  }
  return [...kosik, { hra, kusy }];
}

export function nazevHry(hra: Hra): string {
  return hra.podtitul ? `${hra.nazev}: ${hra.podtitul}` : hra.nazev;
}

export type Formatovac = (castka: number) => string;

export const naKoruny: Formatovac = (castka) => `${castka.toLocaleString('cs-CZ')} Kč`;

export function shrnuti(kosik: Polozka[], format: Formatovac): string[] {
  return kosik.map((polozka) => {
    const castka = polozka.hra.cena * polozka.kusy;
    return `${nazevHry(polozka.hra)} ×${polozka.kusy} — ${format(castka)}`;
  });
}

export function filtrujKatalog(hry: readonly Hra[], podminka: (hra: Hra) => boolean): Hra[] {
  return hry.filter(podminka);
}

export function sleva(castka: number): number {
  if (castka <= 2000) return 0;
  return Math.min(Math.round(castka * 0.1), 500);
}
```
