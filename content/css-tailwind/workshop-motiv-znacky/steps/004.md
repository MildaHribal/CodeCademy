---
libs: tailwind
title: "Vlastní velikost odsazení"
---

# --description--
Máme v designu obrovskou mezeru, která se často opakuje a je přesně `5rem`. Abychom nepoužívali ošklivé hodnoty jako `gap-[5rem]`, můžeme si vytvořit vlastní rozestup na škále.

**Úkol:** Do `@theme` přidej novou vzdálenost `--spacing-huge` a jako hodnotu jí dej `5rem`. Poté kontejneru `<main>` změň `gap-8` na `gap-huge`.

# --hints--
Splněno.
```js
const m = document.querySelector('main'); assert.ok(m.classList.contains('gap-huge'), 'Používá mezeru gap-huge.'); assert.equal(getComputedStyle(m).gap, '80px', 'Velikost odpovídá 5rem (80px).');
```

# --help--
## --tip--
Rozestupy ovládá `--spacing-*`. Vytvoř správnou vlastnost s příslušnou hodnotou a použij to přes `gap-huge`.

# --seed--
## --file-- index.html
```html
<main class="flex flex-col gap-8 p-12 max-w-lg mx-auto">
  <h1 class="text-3xl font-bold font-brand">Přizpůsobený motiv</h1>
  
  <article class="bg-secondary p-6 rounded-lg shadow">
    <h2 class="text-xl font-semibold mb-2">Naše nabídka</h2>
    <p class="text-slate-800">Chtěli bychom, aby tato část byla krásně oranžová.</p>
  </article>

  <button class="bg-primary text-white px-4 py-2 rounded-md">
    Odeslat
  </button>
</main>
```

## --file-- style.css
```css
@theme {
  --color-primary: #8b5cf6;
  --font-brand: Georgia, serif;
  --color-secondary: #fb923c;
}
```

# --solution--
## --file-- index.html
```html
<main class="flex flex-col gap-huge p-12 max-w-lg mx-auto">
  <h1 class="text-3xl font-bold font-brand">Přizpůsobený motiv</h1>
  
  <article class="bg-secondary p-6 rounded-lg shadow">
    <h2 class="text-xl font-semibold mb-2">Naše nabídka</h2>
    <p class="text-slate-800">Chtěli bychom, aby tato část byla krásně oranžová.</p>
  </article>

  <button class="bg-primary text-white px-4 py-2 rounded-md">
    Odeslat
  </button>
</main>
```

## --file-- style.css
```css
@theme {
  --color-primary: #8b5cf6;
  --font-brand: Georgia, serif;
  --color-secondary: #fb923c;
  --spacing-huge: 5rem;
}
```
