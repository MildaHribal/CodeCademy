---
title: Filtr podle typu jídla
kind: choose
---

# --description--

Večer chce člověk vidět, kolik toho snědl k obědu, ne celý den najednou.

**Úkol:** Nad zápis přidej do hlavičky sekce (`div` s třídou `sekce__hlavicka`) panel `div` s třídou `filtr` a v něm pět tlačítek (třída `filtr__tlacitko`): **Vše**, **Snídaně**, **Oběd**, **Večeře** a **Svačina**. Po kliknutí ať jsou v zápisu vidět jen záznamy odpovídajícího typu; **Vše** ukáže všechno. Zvolené tlačítko musí být na první pohled poznat (ve stylech je pro něj třída `filtr__tlacitko--aktivni`, ale klidně to vyřeš jinak).

Jak si zvolený typ zapamatuješ a jak výběr provedeš, je na tobě. Testy se dívají jen na to, co je vidět: která tlačítka jsou, co je v zápisu po kliknutí a že se aktivní tlačítko liší od ostatních.

Součet v hlavičce zůstává za **celý den** — filtr je jen pohled na zápis.

# --hints--

V hlavičce zápisu je pět tlačítek filtru, na začátku je zvolené Vše.

```js
const tlacitka = [...document.querySelectorAll('.sekce__hlavicka .filtr .filtr__tlacitko')];
assert.equal(tlacitka.length, 5, 'filtr má mít pět tlačítek');
assert.deepEqual(tlacitka.map((el) => el.textContent.trim()), ['Vše', 'Snídaně', 'Oběd', 'Večeře', 'Svačina'], 'tlačítka mají být Vše, Snídaně, Oběd, Večeře a Svačina');
const znak = (el) => { const s = getComputedStyle(el); return [el.className, s.backgroundColor, s.color, s.fontWeight, el.getAttribute('aria-pressed')].join('|'); };
assert.notEqual(znak(tlacitka[0]), znak(tlacitka[2]), 'zvolené tlačítko (na začátku Vše) se má lišit od ostatních');
```

Kliknutí na Oběd nechá v zápisu jen obědy.

```js
const najdi = (text) => [...document.querySelectorAll('.volba')].find((el) => el.textContent.includes(text));
await helpers.click(najdi('Guláš'));
await helpers.click(najdi('Jablko'));
await helpers.click(najdi('Salát'));
await helpers.flush();
const filtr = (text) => [...document.querySelectorAll('.filtr__tlacitko')].find((el) => el.textContent.trim() === text);
await helpers.click(filtr('Oběd'));
await helpers.flush();
const videt = [...document.querySelectorAll('.zaznam')].map((el) => el.textContent);
assert.equal(videt.length, 2, 'v zápisu mají zůstat dva obědy');
assert.ok(videt.every((text) => text.includes('Guláš') || text.includes('Salát')), 'v zápisu mají být jen obědy');
assert.match(document.querySelector('.souhrn__cislo').textContent, /1305/, 'součet v hlavičce zůstává za celý den (780 + 95 + 430)');
```

Vše vrátí celý zápis a aktivní tlačítko se přepne.

```js
const najdi = (text) => [...document.querySelectorAll('.volba')].find((el) => el.textContent.includes(text));
await helpers.click(najdi('Guláš'));
await helpers.click(najdi('Jablko'));
await helpers.flush();
const filtr = (text) => [...document.querySelectorAll('.filtr__tlacitko')].find((el) => el.textContent.trim() === text);
const znak = (el) => { const s = getComputedStyle(el); return [el.className, s.backgroundColor, s.color, s.fontWeight, el.getAttribute('aria-pressed')].join('|'); };
await helpers.click(filtr('Svačina'));
await helpers.flush();
assert.equal(document.querySelectorAll('.zaznam').length, 1, 'po volbě Svačina má zůstat jen jablko');
assert.notEqual(znak(filtr('Svačina')), znak(filtr('Oběd')), 'aktivní má být tlačítko Svačina');
await helpers.click(filtr('Vše'));
await helpers.flush();
assert.equal(document.querySelectorAll('.zaznam').length, 2, 'volba Vše má ukázat celý zápis');
```

# --help--

## --tip--

Druhý stav v komponentě funguje stejně jako první — viz [Stav v `useState`](see:react-zaklady/stav-jako-snimek#stav-v-usestate).

## --tip--

Zvolený typ si musí komponenta pamatovat mezi kliknutími. Seznam, který vykresluješ, pak z celého zápisu vybereš podle něj — a zvolené tlačítko se od ostatních liší třídou.

## --tip--

Tlačítka filtru nepiš pětkrát ručně: udělej si nad komponentou pole názvů a vykresli je stejně jako nabídku jídel.

# --seed--

## --file-- data.js

```js
// Denní cíl a nabídka jídel. Data neměň.
export const DENNI_CIL = 2200;

export const potraviny = [
  { id: 'ovesna-kase', nazev: 'Ovesná kaše s banánem', typ: 'Snídaně', kcal: 320 },
  { id: 'tvaroh', nazev: 'Tvaroh s ovocem', typ: 'Snídaně', kcal: 210 },
  { id: 'gulas', nazev: 'Guláš s knedlíkem', typ: 'Oběd', kcal: 780 },
  { id: 'salat', nazev: 'Salát s kuřetem', typ: 'Oběd', kcal: 430 },
  { id: 'rizek', nazev: 'Řízek s bramborem', typ: 'Večeře', kcal: 690 },
  { id: 'omeleta', nazev: 'Omeleta se šunkou', typ: 'Večeře', kcal: 380 },
  { id: 'jablko', nazev: 'Jablko', typ: 'Svačina', kcal: 95 },
  { id: 'kava', nazev: 'Káva s mlékem', typ: 'Svačina', kcal: 60 },
];
```

## --file-- styles.css

```css
:root {
  --pozadi: #f5f7f4;
  --karta: #ffffff;
  --ink: #1b2420;
  --tlumene: #667068;
  --linka: #dde5dc;
  --akcent: #2f7d5d;
  --akcent-svetly: #e6f3ec;
  --varovani: #b4451f;
  --radius: 1rem;
}

*,
*::before,
*::after {
  box-sizing: border-box;
}

body {
  margin: 0;
  background: var(--pozadi);
  color: var(--ink);
  font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
  line-height: 1.55;
}

.stranka {
  width: min(100% - 2rem, 44rem);
  margin-inline: auto;
  padding-block: 2rem 4rem;
  display: grid;
  gap: 1.5rem;
}

.souhrn {
  display: grid;
  gap: 0.5rem;
  padding: 1.5rem;
  background: var(--karta);
  border-radius: var(--radius);
  box-shadow: 0 1px 3px rgb(27 36 32 / 0.1);
}

.souhrn__nadpis {
  margin: 0;
  font-size: 1.1rem;
  color: var(--tlumene);
  font-weight: 600;
}

.souhrn__cislo {
  margin: 0;
  font-size: 2.6rem;
  font-weight: 800;
  line-height: 1;
  letter-spacing: -0.03em;
}

.souhrn__cislo--prekroceno {
  color: var(--varovani);
}

.souhrn__zbyva {
  margin: 0;
  color: var(--tlumene);
}

.ukazatel {
  block-size: 0.6rem;
  border-radius: 999px;
  background: var(--linka);
  overflow: hidden;
}

.ukazatel__cara {
  block-size: 100%;
  border-radius: 999px;
  background: var(--akcent);
}

.ukazatel__cara--prekroceno {
  background: var(--varovani);
}

.sekce {
  display: grid;
  gap: 0.75rem;
}

.sekce__hlavicka {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-between;
  gap: 0.6rem;
}

.sekce__nadpis {
  margin: 0;
  font-size: 1.25rem;
  letter-spacing: -0.02em;
}

.volby {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
}

.volba {
  display: inline-flex;
  align-items: baseline;
  gap: 0.4rem;
  padding: 0.55rem 0.9rem;
  border: 1px solid var(--linka);
  border-radius: 999px;
  background: var(--karta);
  color: inherit;
  font: inherit;
  cursor: pointer;
  transition: border-color 0.15s ease, translate 0.15s ease;
}

.volba:hover {
  border-color: var(--akcent);
  translate: 0 -2px;
}

.volba__kcal {
  color: var(--tlumene);
  font-size: 0.8rem;
}

.filtr {
  display: flex;
  flex-wrap: wrap;
  gap: 0.35rem;
}

.filtr__tlacitko {
  padding: 0.35rem 0.75rem;
  border: 0;
  border-radius: 999px;
  background: transparent;
  color: var(--tlumene);
  font: inherit;
  font-size: 0.9rem;
  cursor: pointer;
}

.filtr__tlacitko--aktivni {
  background: var(--akcent-svetly);
  color: var(--akcent);
  font-weight: 700;
}

.zaznamy {
  display: grid;
  gap: 0.5rem;
  margin: 0;
  padding: 0;
  list-style: none;
}

.zaznam {
  display: grid;
  grid-template-columns: 1fr auto auto auto;
  align-items: center;
  gap: 0.75rem;
  padding: 0.7rem 0.9rem;
  background: var(--karta);
  border: 1px solid var(--linka);
  border-radius: 0.8rem;
}

.zaznam--nejvic {
  border-color: var(--akcent);
  box-shadow: inset 3px 0 0 var(--akcent);
}

.zaznam__nazev {
  margin: 0;
  font-weight: 600;
}

.zaznam__typ {
  color: var(--tlumene);
  font-size: 0.8rem;
  font-weight: 400;
}

.zaznam__porce {
  display: flex;
  align-items: center;
  gap: 0.4rem;
}

.kulate {
  inline-size: 1.9rem;
  block-size: 1.9rem;
  border: 1px solid var(--linka);
  border-radius: 50%;
  background: var(--karta);
  color: inherit;
  font: inherit;
  line-height: 1;
  cursor: pointer;
}

.kulate:hover {
  border-color: var(--akcent);
}

.zaznam__kcal {
  font-variant-numeric: tabular-nums;
  font-weight: 700;
}

.smazat {
  border: 0;
  background: transparent;
  color: var(--tlumene);
  font: inherit;
  cursor: pointer;
}

.smazat:hover {
  color: var(--varovani);
}

.prazdno {
  margin: 0;
  padding: 1.25rem;
  border: 1px dashed var(--linka);
  border-radius: 0.8rem;
  color: var(--tlumene);
  text-align: center;
}

.tise {
  justify-self: start;
  padding: 0.5rem 1rem;
  border: 1px solid var(--linka);
  border-radius: 999px;
  background: var(--karta);
  color: var(--tlumene);
  font: inherit;
  cursor: pointer;
}

.tise:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

button:focus-visible {
  outline: 3px solid var(--akcent);
  outline-offset: 2px;
}
```

## --file-- utils.js

```js
// Kolik kalorií mají všechny zapsané porce dohromady.
export function soucetKalorii(zaznamy) {
  return zaznamy.reduce((celkem, zaznam) => celkem + zaznam.kcal * zaznam.porce, 0);
}
```

## --file-- App.jsx

```jsx
import './styles.css';
import { useState } from 'react';
import { DENNI_CIL, potraviny } from './data.js';
import { soucetKalorii } from './utils.js';

export default function App() {
  const [zaznamy, setZaznamy] = useState([]);

  const snedeno = soucetKalorii(zaznamy);
  const zbyva = DENNI_CIL - snedeno;
  const procenta = Math.min(100, Math.round((snedeno / DENNI_CIL) * 100));

  function pridej(potravina) {
    setZaznamy([...zaznamy, { ...potravina, id: crypto.randomUUID(), porce: 1 }]);
  }

  function zmenPorci(id, zmena) {
    setZaznamy(
      zaznamy.map((zaznam) =>
        zaznam.id === id ? { ...zaznam, porce: Math.max(1, zaznam.porce + zmena) } : zaznam,
      ),
    );
  }

  function smaz(id) {
    setZaznamy(zaznamy.filter((zaznam) => zaznam.id !== id));
  }

  return (
    <div className="stranka">
      <section className="souhrn">
        <h1 className="souhrn__nadpis">Dnes máš snědeno</h1>
        <p className="souhrn__cislo">{snedeno} kcal</p>
        <p className="souhrn__zbyva">{`Do cíle ${DENNI_CIL} kcal zbývá ${zbyva} kcal.`}</p>
        <div className="ukazatel" aria-hidden="true">
          <div className="ukazatel__cara" style={{ inlineSize: `${procenta}%` }} />
        </div>
      </section>

      <section className="sekce">
        <h2 className="sekce__nadpis">Co jsem snědl</h2>
        <div className="volby">
          {potraviny.map((potravina) => (
            <button className="volba" key={potravina.id} onClick={() => pridej(potravina)}>
              {potravina.nazev} <span className="volba__kcal">{potravina.kcal} kcal</span>
            </button>
          ))}
        </div>
      </section>

      <section className="sekce">
        <div className="sekce__hlavicka">
          <h2 className="sekce__nadpis">Dnešní zápis</h2>
        </div>
        {zaznamy.length === 0 && <p className="prazdno">Zatím nic. Klikni na jídlo nahoře.</p>}
        <ul className="zaznamy">
          {zaznamy.map((zaznam) => (
            <li className="zaznam" key={zaznam.id}>
              <p className="zaznam__nazev">
                {zaznam.nazev} <span className="zaznam__typ">{zaznam.typ}</span>
              </p>
              <span className="zaznam__porce">
                <button className="kulate" onClick={() => zmenPorci(zaznam.id, -1)} aria-label="Ubrat porci">−</button>
                {zaznam.porce}×
                <button className="kulate" onClick={() => zmenPorci(zaznam.id, 1)} aria-label="Přidat porci">+</button>
              </span>
              <span className="zaznam__kcal">{zaznam.kcal * zaznam.porce} kcal</span>
              <button className="smazat" onClick={() => smaz(zaznam.id)} aria-label="Smazat záznam">×</button>
            </li>
          ))}
        </ul>
      </section>
    </div>
  );
}
```


# --solution--

## --file-- App.jsx

```jsx
import './styles.css';
import { useState } from 'react';
import { DENNI_CIL, potraviny } from './data.js';
import { soucetKalorii } from './utils.js';

const TYPY = ['Vše', 'Snídaně', 'Oběd', 'Večeře', 'Svačina'];

export default function App() {
  const [zaznamy, setZaznamy] = useState([]);
  const [filtr, setFiltr] = useState('Vše');

  const snedeno = soucetKalorii(zaznamy);
  const zbyva = DENNI_CIL - snedeno;
  const procenta = Math.min(100, Math.round((snedeno / DENNI_CIL) * 100));
  const videt = filtr === 'Vše' ? zaznamy : zaznamy.filter((zaznam) => zaznam.typ === filtr);

  function pridej(potravina) {
    setZaznamy([...zaznamy, { ...potravina, id: crypto.randomUUID(), porce: 1 }]);
  }

  function zmenPorci(id, zmena) {
    setZaznamy(
      zaznamy.map((zaznam) =>
        zaznam.id === id ? { ...zaznam, porce: Math.max(1, zaznam.porce + zmena) } : zaznam,
      ),
    );
  }

  function smaz(id) {
    setZaznamy(zaznamy.filter((zaznam) => zaznam.id !== id));
  }

  return (
    <div className="stranka">
      <section className="souhrn">
        <h1 className="souhrn__nadpis">Dnes máš snědeno</h1>
        <p className="souhrn__cislo">{snedeno} kcal</p>
        <p className="souhrn__zbyva">{`Do cíle ${DENNI_CIL} kcal zbývá ${zbyva} kcal.`}</p>
        <div className="ukazatel" aria-hidden="true">
          <div className="ukazatel__cara" style={{ inlineSize: `${procenta}%` }} />
        </div>
      </section>

      <section className="sekce">
        <h2 className="sekce__nadpis">Co jsem snědl</h2>
        <div className="volby">
          {potraviny.map((potravina) => (
            <button className="volba" key={potravina.id} onClick={() => pridej(potravina)}>
              {potravina.nazev} <span className="volba__kcal">{potravina.kcal} kcal</span>
            </button>
          ))}
        </div>
      </section>

      <section className="sekce">
        <div className="sekce__hlavicka">
          <h2 className="sekce__nadpis">Dnešní zápis</h2>
          <div className="filtr">
            {TYPY.map((typ) => (
              <button
                className={typ === filtr ? 'filtr__tlacitko filtr__tlacitko--aktivni' : 'filtr__tlacitko'}
                key={typ}
                onClick={() => setFiltr(typ)}
              >
                {typ}
              </button>
            ))}
          </div>
        </div>
        {videt.length === 0 && <p className="prazdno">Zatím nic. Klikni na jídlo nahoře.</p>}
        <ul className="zaznamy">
          {videt.map((zaznam) => (
            <li className="zaznam" key={zaznam.id}>
              <p className="zaznam__nazev">
                {zaznam.nazev} <span className="zaznam__typ">{zaznam.typ}</span>
              </p>
              <span className="zaznam__porce">
                <button className="kulate" onClick={() => zmenPorci(zaznam.id, -1)} aria-label="Ubrat porci">−</button>
                {zaznam.porce}×
                <button className="kulate" onClick={() => zmenPorci(zaznam.id, 1)} aria-label="Přidat porci">+</button>
              </span>
              <span className="zaznam__kcal">{zaznam.kcal * zaznam.porce} kcal</span>
              <button className="smazat" onClick={() => smaz(zaznam.id)} aria-label="Smazat záznam">×</button>
            </li>
          ))}
        </ul>
      </section>
    </div>
  );
}
```

