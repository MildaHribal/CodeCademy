---
title: Největší hřích dne
---

# --description--

Poslední detail, kvůli kterému si aplikaci člověk otevře i zítra: ať je na první pohled vidět, co dnešek nejvíc zvedlo.

**Úkol:** Najdi záznam s nejvyšším počtem kalorií za celý řádek (kalorie za porci krát počet porcí) a jeho položce přidej navíc třídu `zaznam--nejvic` (ve stylech má zelený proužek vlevo). Ostatní položky zůstanou beze změny; na prázdný den se nesmí nic rozbít.

Hledej v celém zápisu, ne jen v tom, co je zrovna vidět přes filtr — a pole záznamů přitom nech, jak je.

## Udělej po svém

Zápisník je hotový. Co s ním dál, je na tobě — vyber si aspoň jedno:

- **Rozpis po jídlech.** Ukaž pod souhrnem, kolik kalorií padlo na snídani, oběd, večeři a svačiny.
- **Vizuální nápad.** Dej ukazateli tři pásma (do 80 %, do 100 %, přes) a obarvi ho podle toho, jak daleko člověk je.
- **Poznámka k jídlu.** Přidej u záznamu možnost vybrat velikost porce (malá, velká) a přepočítej podle ní kalorie.
- **Tvoje téma.** Přepiš data na to, co si chceš hlídat ty: útrata dne, tréninkové minuty, vypitá voda.

V příští lekci se naučíš pracovat s formulářovými poli: zápisník pak nebude odkázaný jen na nabídku, uživatel si jídlo bude moct napsat sám a stav se bude zvedat do společného rodiče.

# --hints--

Zvýrazněný je záznam s nejvyšším příspěvkem do součtu.

```js
const najdi = (text) => [...document.querySelectorAll('.volba')].find((el) => el.textContent.includes(text));
await helpers.click(najdi('Jablko'));
await helpers.click(najdi('Guláš'));
await helpers.click(najdi('Káva'));
await helpers.flush();
const zaznamy = [...document.querySelectorAll('.zaznam')];
const zvyraznene = zaznamy.filter((el) => el.classList.contains('zaznam--nejvic'));
assert.equal(zvyraznene.length, 1, 'zvýrazněný má být právě jeden záznam');
assert.ok(zvyraznene[0].textContent.includes('Guláš'), 'nejvíc kalorií má guláš (780)');
```

Rozhodují kalorie za celý řádek, ne za jednu porci.

```js
const najdi = (text) => [...document.querySelectorAll('.volba')].find((el) => el.textContent.includes(text));
await helpers.click(najdi('Guláš'));
await helpers.click(najdi('Jablko'));
await helpers.flush();
const jablko = [...document.querySelectorAll('.zaznam')].find((el) => el.textContent.includes('Jablko'));
const plus = jablko.querySelector('.zaznam__porce button:last-of-type');
for (let i = 0; i < 9; i += 1) await helpers.click(plus);
await helpers.flush();
const zvyraznene = [...document.querySelectorAll('.zaznam')].filter((el) => el.classList.contains('zaznam--nejvic'));
assert.equal(zvyraznene.length, 1, 'zvýrazněný má být právě jeden záznam');
assert.ok(zvyraznene[0].textContent.includes('Jablko'), 'deset jablek (950) je víc než guláš (780)');
```

Prázdný den ani filtr nic nerozbijí a zbytek funguje dál.

```js
const najdi = (text) => [...document.querySelectorAll('.volba')].find((el) => el.textContent.includes(text));
const filtr = (text) => [...document.querySelectorAll('.filtr__tlacitko')].find((el) => el.textContent.trim() === text);
assert.equal(document.querySelectorAll('.zaznam--nejvic').length, 0, 'na prázdný den není co zvýrazňovat');
await helpers.click(najdi('Guláš'));
await helpers.click(najdi('Jablko'));
await helpers.flush();
await helpers.click(filtr('Svačina'));
await helpers.flush();
const videt = [...document.querySelectorAll('.zaznam')];
assert.equal(videt.length, 1, 've svačinách má zůstat jablko');
assert.ok(!videt[0].classList.contains('zaznam--nejvic'), 'nejvíc kalorií má guláš — jablko se zvýrazňovat nemá, i když je zrovna samo vidět');
```

# --help--

## --tip--

Hledání největší položky bez zásahu do pole je v části [Řazení a porovnávací funkce](see:js-pole/metody-pole-do-hloubky#razeni-a-porovnavaci-funkce); stejně dobře poslouží `reduce`.

## --tip--

Vítěze si najdi jednou do proměnné vedle ostatních výpočtů a v položce pak jen porovnej, jestli je to on.

## --tip--

Pozor na prázdný zápis: hledání v prázdném poli nesmí skončit chybou, ale prostě ničím.

# --seed--

## --file-- data.js

```js
// Denní cíl a nabídka jídel. Data neměň.
export const DENNI_CIL = 2200;

export const potraviny = [
  { id: 'ovesna-kase', nazev: 'Ovesná kaše s banánem', typ: 'Snídaně', kcal: 320 },
  { id: 'tvaroh', nazev: 'Tvaroh s ovocem', typ: 'Snídaně', kcal: 210 },
  { id: 'gulas', nazev: 'Guláš s knedlíkem', typ: 'Oběd', kcal: 780 },
  { id: 'salat', nazev: 'Salát s kuřetem', typ: 'Oběd', kcal: 430 },
  { id: 'rizek', nazev: 'Řízek s bramborem', typ: 'Večeře', kcal: 690 },
  { id: 'omeleta', nazev: 'Omeleta se šunkou', typ: 'Večeře', kcal: 380 },
  { id: 'jablko', nazev: 'Jablko', typ: 'Svačina', kcal: 95 },
  { id: 'kava', nazev: 'Káva s mlékem', typ: 'Svačina', kcal: 60 },
];
```

## --file-- styles.css

```css
:root {
  --pozadi: #f5f7f4;
  --karta: #ffffff;
  --ink: #1b2420;
  --tlumene: #667068;
  --linka: #dde5dc;
  --akcent: #2f7d5d;
  --akcent-svetly: #e6f3ec;
  --varovani: #b4451f;
  --radius: 1rem;
}

*,
*::before,
*::after {
  box-sizing: border-box;
}

body {
  margin: 0;
  background: var(--pozadi);
  color: var(--ink);
  font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
  line-height: 1.55;
}

.stranka {
  width: min(100% - 2rem, 44rem);
  margin-inline: auto;
  padding-block: 2rem 4rem;
  display: grid;
  gap: 1.5rem;
}

.souhrn {
  display: grid;
  gap: 0.5rem;
  padding: 1.5rem;
  background: var(--karta);
  border-radius: var(--radius);
  box-shadow: 0 1px 3px rgb(27 36 32 / 0.1);
}

.souhrn__nadpis {
  margin: 0;
  font-size: 1.1rem;
  color: var(--tlumene);
  font-weight: 600;
}

.souhrn__cislo {
  margin: 0;
  font-size: 2.6rem;
  font-weight: 800;
  line-height: 1;
  letter-spacing: -0.03em;
}

.souhrn__cislo--prekroceno {
  color: var(--varovani);
}

.souhrn__zbyva {
  margin: 0;
  color: var(--tlumene);
}

.ukazatel {
  block-size: 0.6rem;
  border-radius: 999px;
  background: var(--linka);
  overflow: hidden;
}

.ukazatel__cara {
  block-size: 100%;
  border-radius: 999px;
  background: var(--akcent);
}

.ukazatel__cara--prekroceno {
  background: var(--varovani);
}

.sekce {
  display: grid;
  gap: 0.75rem;
}

.sekce__hlavicka {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-between;
  gap: 0.6rem;
}

.sekce__nadpis {
  margin: 0;
  font-size: 1.25rem;
  letter-spacing: -0.02em;
}

.volby {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
}

.volba {
  display: inline-flex;
  align-items: baseline;
  gap: 0.4rem;
  padding: 0.55rem 0.9rem;
  border: 1px solid var(--linka);
  border-radius: 999px;
  background: var(--karta);
  color: inherit;
  font: inherit;
  cursor: pointer;
  transition: border-color 0.15s ease, translate 0.15s ease;
}

.volba:hover {
  border-color: var(--akcent);
  translate: 0 -2px;
}

.volba__kcal {
  color: var(--tlumene);
  font-size: 0.8rem;
}

.filtr {
  display: flex;
  flex-wrap: wrap;
  gap: 0.35rem;
}

.filtr__tlacitko {
  padding: 0.35rem 0.75rem;
  border: 0;
  border-radius: 999px;
  background: transparent;
  color: var(--tlumene);
  font: inherit;
  font-size: 0.9rem;
  cursor: pointer;
}

.filtr__tlacitko--aktivni {
  background: var(--akcent-svetly);
  color: var(--akcent);
  font-weight: 700;
}

.zaznamy {
  display: grid;
  gap: 0.5rem;
  margin: 0;
  padding: 0;
  list-style: none;
}

.zaznam {
  display: grid;
  grid-template-columns: 1fr auto auto auto;
  align-items: center;
  gap: 0.75rem;
  padding: 0.7rem 0.9rem;
  background: var(--karta);
  border: 1px solid var(--linka);
  border-radius: 0.8rem;
}

.zaznam--nejvic {
  border-color: var(--akcent);
  box-shadow: inset 3px 0 0 var(--akcent);
}

.zaznam__nazev {
  margin: 0;
  font-weight: 600;
}

.zaznam__typ {
  color: var(--tlumene);
  font-size: 0.8rem;
  font-weight: 400;
}

.zaznam__porce {
  display: flex;
  align-items: center;
  gap: 0.4rem;
}

.kulate {
  inline-size: 1.9rem;
  block-size: 1.9rem;
  border: 1px solid var(--linka);
  border-radius: 50%;
  background: var(--karta);
  color: inherit;
  font: inherit;
  line-height: 1;
  cursor: pointer;
}

.kulate:hover {
  border-color: var(--akcent);
}

.zaznam__kcal {
  font-variant-numeric: tabular-nums;
  font-weight: 700;
}

.smazat {
  border: 0;
  background: transparent;
  color: var(--tlumene);
  font: inherit;
  cursor: pointer;
}

.smazat:hover {
  color: var(--varovani);
}

.prazdno {
  margin: 0;
  padding: 1.25rem;
  border: 1px dashed var(--linka);
  border-radius: 0.8rem;
  color: var(--tlumene);
  text-align: center;
}

.tise {
  justify-self: start;
  padding: 0.5rem 1rem;
  border: 1px solid var(--linka);
  border-radius: 999px;
  background: var(--karta);
  color: var(--tlumene);
  font: inherit;
  cursor: pointer;
}

.tise:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

button:focus-visible {
  outline: 3px solid var(--akcent);
  outline-offset: 2px;
}
```

## --file-- utils.js

```js
// Kolik kalorií mají všechny zapsané porce dohromady.
export function soucetKalorii(zaznamy) {
  return zaznamy.reduce((celkem, zaznam) => celkem + zaznam.kcal * zaznam.porce, 0);
}
```

## --file-- App.jsx

```jsx
import './styles.css';
import { useState } from 'react';
import { DENNI_CIL, potraviny } from './data.js';
import { soucetKalorii } from './utils.js';
import Souhrn from './Souhrn.jsx';

const TYPY = ['Vše', 'Snídaně', 'Oběd', 'Večeře', 'Svačina'];

export default function App() {
  const [zaznamy, setZaznamy] = useState([]);
  const [filtr, setFiltr] = useState('Vše');

  const snedeno = soucetKalorii(zaznamy);
  const zbyva = DENNI_CIL - snedeno;
  const procenta = Math.min(100, Math.round((snedeno / DENNI_CIL) * 100));
  const prekroceno = snedeno > DENNI_CIL;
  const videt = filtr === 'Vše' ? zaznamy : zaznamy.filter((zaznam) => zaznam.typ === filtr);

  function pridej(potravina) {
    setZaznamy([...zaznamy, { ...potravina, id: crypto.randomUUID(), porce: 1 }]);
  }

  function zmenPorci(id, zmena) {
    setZaznamy(
      zaznamy.map((zaznam) =>
        zaznam.id === id ? { ...zaznam, porce: Math.max(1, zaznam.porce + zmena) } : zaznam,
      ),
    );
  }

  function smaz(id) {
    setZaznamy(zaznamy.filter((zaznam) => zaznam.id !== id));
  }

  function vynulujDen() {
    setZaznamy([]);
    setFiltr('Vše');
  }

  return (
    <div className="stranka">
      <Souhrn snedeno={snedeno} zbyva={zbyva} procenta={procenta} prekroceno={prekroceno} />

      <section className="sekce">
        <h2 className="sekce__nadpis">Co jsem snědl</h2>
        <div className="volby">
          {potraviny.map((potravina) => (
            <button className="volba" key={potravina.id} onClick={() => pridej(potravina)}>
              {potravina.nazev} <span className="volba__kcal">{potravina.kcal} kcal</span>
            </button>
          ))}
        </div>
      </section>

      <section className="sekce">
        <div className="sekce__hlavicka">
          <h2 className="sekce__nadpis">Dnešní zápis</h2>
          <div className="filtr">
            {TYPY.map((typ) => (
              <button
                className={typ === filtr ? 'filtr__tlacitko filtr__tlacitko--aktivni' : 'filtr__tlacitko'}
                key={typ}
                onClick={() => setFiltr(typ)}
              >
                {typ}
              </button>
            ))}
          </div>
        </div>
        {videt.length === 0 && (
          <p className="prazdno">{zaznamy.length === 0 ? 'Zatím nic. Klikni na jídlo nahoře.' : 'V téhle kategorii dnes nic nemáš.'}</p>
        )}
        <ul className="zaznamy">
          {videt.map((zaznam) => (
            <li className="zaznam" key={zaznam.id}>
              <p className="zaznam__nazev">
                {zaznam.nazev} <span className="zaznam__typ">{zaznam.typ}</span>
              </p>
              <span className="zaznam__porce">
                <button className="kulate" onClick={() => zmenPorci(zaznam.id, -1)} aria-label="Ubrat porci">−</button>
                {zaznam.porce}×
                <button className="kulate" onClick={() => zmenPorci(zaznam.id, 1)} aria-label="Přidat porci">+</button>
              </span>
              <span className="zaznam__kcal">{zaznam.kcal * zaznam.porce} kcal</span>
              <button className="smazat" onClick={() => smaz(zaznam.id)} aria-label="Smazat záznam">×</button>
            </li>
          ))}
        </ul>
        <button className="tise" disabled={zaznamy.length === 0} onClick={vynulujDen}>
          Vynulovat den
        </button>
      </section>
    </div>
  );
}
```

## --file-- Souhrn.jsx

```jsx
export default function Souhrn({ snedeno, zbyva, procenta, prekroceno }) {
  return (
    <section className="souhrn">
      <h1 className="souhrn__nadpis">Dnes máš snědeno</h1>
      <p className={prekroceno ? 'souhrn__cislo souhrn__cislo--prekroceno' : 'souhrn__cislo'}>{snedeno} kcal</p>
      <p className="souhrn__zbyva">
        {prekroceno ? `Cíl máš překročený o ${-zbyva} kcal.` : `Do cíle zbývá ${zbyva} kcal.`}
      </p>
      <div className="ukazatel" aria-hidden="true">
        <div className={prekroceno ? 'ukazatel__cara ukazatel__cara--prekroceno' : 'ukazatel__cara'} style={{ inlineSize: `${procenta}%` }} />
      </div>
    </section>
  );
}
```


# --solution--

## --file-- App.jsx

```jsx
import './styles.css';
import { useState } from 'react';
import { DENNI_CIL, potraviny } from './data.js';
import { soucetKalorii } from './utils.js';
import Souhrn from './Souhrn.jsx';

const TYPY = ['Vše', 'Snídaně', 'Oběd', 'Večeře', 'Svačina'];

export default function App() {
  const [zaznamy, setZaznamy] = useState([]);
  const [filtr, setFiltr] = useState('Vše');

  const snedeno = soucetKalorii(zaznamy);
  const zbyva = DENNI_CIL - snedeno;
  const procenta = Math.min(100, Math.round((snedeno / DENNI_CIL) * 100));
  const prekroceno = snedeno > DENNI_CIL;
  const videt = filtr === 'Vše' ? zaznamy : zaznamy.filter((zaznam) => zaznam.typ === filtr);
  const nejvic = zaznamy.toSorted((prvni, druhy) => druhy.kcal * druhy.porce - prvni.kcal * prvni.porce)[0];

  function pridej(potravina) {
    setZaznamy([...zaznamy, { ...potravina, id: crypto.randomUUID(), porce: 1 }]);
  }

  function zmenPorci(id, zmena) {
    setZaznamy(
      zaznamy.map((zaznam) =>
        zaznam.id === id ? { ...zaznam, porce: Math.max(1, zaznam.porce + zmena) } : zaznam,
      ),
    );
  }

  function smaz(id) {
    setZaznamy(zaznamy.filter((zaznam) => zaznam.id !== id));
  }

  function vynulujDen() {
    setZaznamy([]);
    setFiltr('Vše');
  }

  return (
    <div className="stranka">
      <Souhrn snedeno={snedeno} zbyva={zbyva} procenta={procenta} prekroceno={prekroceno} />

      <section className="sekce">
        <h2 className="sekce__nadpis">Co jsem snědl</h2>
        <div className="volby">
          {potraviny.map((potravina) => (
            <button className="volba" key={potravina.id} onClick={() => pridej(potravina)}>
              {potravina.nazev} <span className="volba__kcal">{potravina.kcal} kcal</span>
            </button>
          ))}
        </div>
      </section>

      <section className="sekce">
        <div className="sekce__hlavicka">
          <h2 className="sekce__nadpis">Dnešní zápis</h2>
          <div className="filtr">
            {TYPY.map((typ) => (
              <button
                className={typ === filtr ? 'filtr__tlacitko filtr__tlacitko--aktivni' : 'filtr__tlacitko'}
                key={typ}
                onClick={() => setFiltr(typ)}
              >
                {typ}
              </button>
            ))}
          </div>
        </div>
        {videt.length === 0 && (
          <p className="prazdno">{zaznamy.length === 0 ? 'Zatím nic. Klikni na jídlo nahoře.' : 'V téhle kategorii dnes nic nemáš.'}</p>
        )}
        <ul className="zaznamy">
          {videt.map((zaznam) => (
            <li className={zaznam === nejvic ? 'zaznam zaznam--nejvic' : 'zaznam'} key={zaznam.id}>
              <p className="zaznam__nazev">
                {zaznam.nazev} <span className="zaznam__typ">{zaznam.typ}</span>
              </p>
              <span className="zaznam__porce">
                <button className="kulate" onClick={() => zmenPorci(zaznam.id, -1)} aria-label="Ubrat porci">−</button>
                {zaznam.porce}×
                <button className="kulate" onClick={() => zmenPorci(zaznam.id, 1)} aria-label="Přidat porci">+</button>
              </span>
              <span className="zaznam__kcal">{zaznam.kcal * zaznam.porce} kcal</span>
              <button className="smazat" onClick={() => smaz(zaznam.id)} aria-label="Smazat záznam">×</button>
            </li>
          ))}
        </ul>
        <button className="tise" disabled={zaznamy.length === 0} onClick={vynulujDen}>
          Vynulovat den
        </button>
      </section>
    </div>
  );
}
```

