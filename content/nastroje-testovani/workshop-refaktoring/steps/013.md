---
title: Zaveď novou slevu pro seniory
main: ukazka.js
timeoutMs: 30000
see: nastroje-testovani/proc-testovat#testuj-chovani-ne-implementaci
---

# --description--

Rada města schválila, že senioři budou od ledna platit **čtvrtinu** poplatku místo
poloviny. Tohle je poprvé, co se chování má změnit schválně — a přesně proto je pořadí
kroků jiné než dosud.

Charakterizační test drží dnešní stav. Když ho necháš být a změníš kód, zčervená
a ty nebudeš vědět, jestli kvůli nové slevě, nebo kvůli chybě. Proto:

1. **Nejdřív oprav test** na částku, která má nově vyjít, a spusť ho. Musí spadnout —
   a spadnout přesně na tom jednom čísle.
2. **Pak změň pravidlo** v `slevy.js`.
3. Testy zezelenají a ostatní čtenáři zůstanou nedotčení.

Marie dluží na poplatcích 550 Kč. Po nové slevě zaplatí 138 Kč (550 × 0,25 = 137,5
a zaokrouhluje se stejně jako dosud). Jany ani Petra se změna netýká.

**Úkol:** proveď oba kroky v tomhle pořadí. Souhrnný řádek v `ukazka.js` neměň —
má se přepočítat sám.

# --hints--

Senior platí čtvrtinu, ostatní kategorie zůstaly beze změny.

```js
const { slevaProKategorii } = await helpers.importFile('slevy.js');
assert.equal(slevaProKategorii('senior'), 0.25, "slevaProKategorii('senior') má nově vrátit 0.25");
assert.equal(slevaProKategorii('student'), 0.5, "slevaProKategorii('student') má zůstat na 0.5");
assert.equal(slevaProKategorii('dospělý'), 1, "slevaProKategorii('dospělý') má zůstat na 1");
```

Marie platí 138 Kč, Jana a Petr mají upomínku beze změny.

```js
const { vypisUpominky } = await helpers.importFile('poplatky.js');
const ctenari = await helpers.importFile('ctenari.js');
const ocekavano = {
  jana: [
    'Upomínka — Městská knihovna Kutná Hora',
    'Čtenář: Jana Nováková (student)',
    '  Alchymista (kniha): 55 Kč, po termínu 14 dní',
    '  Respekt 12/2026 (časopis): 24 Kč, po termínu 3 dny',
    '  Šifra mistra Leonarda (audiokniha): 0 Kč, po termínu 0 dní',
    'Celkem k zaplacení: 40 Kč',
    'Body do čtenářského klubu: 4',
    '',
  ].join('\n'),
  petr: [
    'Upomínka — Městská knihovna Kutná Hora',
    'Čtenář: Petr Doležal (dospělý)',
    '  Bílá velryba (kniha): 0 Kč, po termínu 2 dny',
    '  Reflex 09/2026 (časopis): 0 Kč, po termínu 0 dní',
    '  Saturnin (audiokniha): 0 Kč, po termínu 1 den',
    'Celkem k zaplacení: 0 Kč',
    'Body do čtenářského klubu: 4',
    '',
  ].join('\n'),
  marie: [
    'Upomínka — Městská knihovna Kutná Hora',
    'Čtenář: Marie Kratochvílová (senior)',
    '  Osadníci z Katanu (desková hra): 250 Kč, po termínu 40 dní',
    '  Kroniky Narnie (kniha): 300 Kč, po termínu 95 dní',
    'Celkem k zaplacení: 138 Kč',
    'Body do čtenářského klubu: 2',
    '',
  ].join('\n'),
};
for (const [klic, text] of Object.entries(ocekavano)) {
  assert.equal(vypisUpominky(ctenari[klic]), text, `Upomínka pro čtenáře ${klic} vypadá jinak než dřív — refaktoring nesmí změnit chování`);
}
```

Tvoje testy projdou a počítají s novou částkou.

```js
assert.match(files['poplatky.test.js'], /138/, 'V testech má být nová částka 138 Kč — charakterizační test se při vědomé změně chování upravuje první');
const beh = await helpers.run('node --test poplatky.test.js');
assert.equal(beh.code, 0, `Tvoje testy z minulých kroků musí projít:\n${beh.stdout}${beh.stderr}`);
const pocet = Number(/^(?:#|\u2139)\s*tests\s+(\d+)/m.exec(beh.stdout)?.[1] ?? 0);
assert.ok(pocet >= 1, `Soubor poplatky.test.js má obsahovat testy, ale spustilo se jich ${pocet}`);
```

Souhrn v ukázce se přepočítal sám.

```js
const beh = await helpers.run('node ukazka.js');
assert.equal(beh.code, 0, `node ukazka.js má doběhnout bez chyby:\n${beh.stdout}${beh.stderr}`);
const stejne = (text) => text.normalize('NFC').replace(/\u00a0/g, ' ').replace(/[ \t]+/g, ' ').trim();
const vystup = stejne(beh.stdout);
assert.ok(vystup.includes(stejne('Celkem k inkasu od 3 čtenářů: 178 Kč')), `Po nové slevě má souhrn ukázat 178 Kč. Vypsalo se:\n${beh.stdout}`);
```

# --help--

## --tip--

Změna chování se od refaktoringu liší jediným: test se mění taky, a mění se **první**. Viz [Testuj chování, ne implementaci](see:nastroje-testovani/proc-testovat#testuj-chovani-ne-implementaci).

## --tip--

V testu Marie se mění jediný řádek — ten s celkovou částkou. Zbytek upomínky zůstává.

## --tip--

Kdyby po změně pravidla spadl i test Jany nebo Petra, sáhl jsi na víc, než bylo potřeba: koeficient se mění u jediné kategorie.

# --seed--

## --file-- poplatky.js

```js
// Upomínky Městské knihovny Kutná Hora.
// První verzi napsal Ivan Sýkora v roce 2019, od té doby sem každý přidal svoje pravidlo.

import { slevaProKategorii } from './slevy.js';

export const SAZEBNIK = {
  kniha: { zaDen: 5, odklad: 3, strop: 300, body: 1 },
  'časopis': { zaDen: 8, odklad: 0, strop: 200, body: 1 },
  audiokniha: { zaDen: 12, odklad: 1, strop: 400, body: 2 },
  'desková hra': { zaDen: 10, odklad: 0, strop: 250, body: 1 },
};

// Co v sazebníku není (mapy, stolní hry), počítá se takhle.
export const VYCHOZI_SAZBA = { zaDen: 10, odklad: 0, strop: 250, body: 1 };

export function spoctiUpominku(ctenar) {
  const polozky = ctenar.vypujcky.map((vypujcka) => ({
    nazev: vypujcka.nazev,
    druh: vypujcka.druh,
    dniPoTerminu: vypujcka.dniPoTerminu,
    poplatek: poplatekZaVypujcku(vypujcka),
    body: bodyZaVypujcku(vypujcka),
  }));
  const poplatky = polozky.reduce((soucet, polozka) => soucet + polozka.poplatek, 0);
  return {
    jmeno: ctenar.jmeno,
    kategorie: ctenar.kategorie,
    polozky,
    celkem: Math.round(poplatky * slevaProKategorii(ctenar.kategorie)),
    body: polozky.reduce((soucet, polozka) => soucet + polozka.body, 0),
  };
}

export function formatujUpominku(souhrn) {
  const radky = [
    'Upomínka — Městská knihovna Kutná Hora',
    `Čtenář: ${souhrn.jmeno} (${souhrn.kategorie})`,
    ...souhrn.polozky.map(
      (polozka) =>
        `  ${polozka.nazev} (${polozka.druh}): ${polozka.poplatek} Kč, po termínu ${tvarDnu(polozka.dniPoTerminu)}`,
    ),
    `Celkem k zaplacení: ${souhrn.celkem} Kč`,
    `Body do čtenářského klubu: ${souhrn.body}`,
    '',
  ];
  return radky.join('\n');
}

export function vypisUpominky(ctenar) {
  return formatujUpominku(spoctiUpominku(ctenar));
}

export function upominkaProWeb(ctenar) {
  const souhrn = spoctiUpominku(ctenar);
  return {
    jmeno: souhrn.jmeno,
    celkemHaleru: souhrn.celkem * 100,
    dluzneVypujcky: souhrn.polozky
      .filter((polozka) => polozka.poplatek > 0)
      .map((polozka) => ({ nazev: polozka.nazev, poplatek: polozka.poplatek })),
  };
}

export function poplatekZaVypujcku(vypujcka) {
  const sazba = SAZEBNIK[vypujcka.druh] ?? VYCHOZI_SAZBA;
  const dniNavic = vypujcka.dniPoTerminu - sazba.odklad;
  if (dniNavic <= 0) {
    return 0;
  }
  return Math.min(dniNavic * sazba.zaDen, sazba.strop);
}

export function bodyZaVypujcku(vypujcka) {
  const sazba = SAZEBNIK[vypujcka.druh] ?? VYCHOZI_SAZBA;
  return sazba.body;
}

function tvarDnu(pocet) {
  if (pocet === 1) {
    return '1 den';
  }
  if (pocet >= 2 && pocet <= 4) {
    return `${pocet} dny`;
  }
  return `${pocet} dní`;
}
```

## --file-- slevy.js

```js
// Slevy čtenářského klubu. Mění se ze všeho nejčastěji, proto mají vlastní soubor.

const SLEVY = {
  student: 0.5,
  senior: 0.5,
};

export function slevaProKategorii(kategorie) {
  return SLEVY[kategorie] ?? 1;
}
```

## --file-- poplatky.test.js

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';

import { vypisUpominky } from './poplatky.js';
import { jana, petr, marie } from './ctenari.js';

test('upomínka pro Janu Novákovou (student) vypadá přesně takhle', () => {
  const ocekavano = [
    'Upomínka — Městská knihovna Kutná Hora',
    'Čtenář: Jana Nováková (student)',
    '  Alchymista (kniha): 55 Kč, po termínu 14 dní',
    '  Respekt 12/2026 (časopis): 24 Kč, po termínu 3 dny',
    '  Šifra mistra Leonarda (audiokniha): 0 Kč, po termínu 0 dní',
    'Celkem k zaplacení: 40 Kč',
    'Body do čtenářského klubu: 4',
    '',
  ].join('\n');

  assert.equal(vypisUpominky(jana), ocekavano, 'Upomínka pro Janu se změnila proti dnešnímu stavu');
});

test('Petr Doležal nic nedluží, ale body dostane', () => {
  const ocekavano = [
    'Upomínka — Městská knihovna Kutná Hora',
    'Čtenář: Petr Doležal (dospělý)',
    '  Bílá velryba (kniha): 0 Kč, po termínu 2 dny',
    '  Reflex 09/2026 (časopis): 0 Kč, po termínu 0 dní',
    '  Saturnin (audiokniha): 0 Kč, po termínu 1 den',
    'Celkem k zaplacení: 0 Kč',
    'Body do čtenářského klubu: 4',
    '',
  ].join('\n');

  assert.equal(vypisUpominky(petr), ocekavano, 'Upomínka pro Petra se změnila proti dnešnímu stavu');
});

test('Marie Kratochvílová (senior) narazí na stropy a slevu', () => {
  const ocekavano = [
    'Upomínka — Městská knihovna Kutná Hora',
    'Čtenář: Marie Kratochvílová (senior)',
    '  Osadníci z Katanu (desková hra): 250 Kč, po termínu 40 dní',
    '  Kroniky Narnie (kniha): 300 Kč, po termínu 95 dní',
    'Celkem k zaplacení: 275 Kč',
    'Body do čtenářského klubu: 2',
    '',
  ].join('\n');

  assert.equal(vypisUpominky(marie), ocekavano, 'Upomínka pro Marii se změnila proti dnešnímu stavu');
});
```

## --file-- ukazka.js

```js
// Vypíše upomínku tak, jak ji dnes uvidí knihovnice u přepážky.
import { vypisUpominky, upominkaProWeb, spoctiUpominku } from './poplatky.js';
import { jana, petr, marie } from './ctenari.js';

console.log(vypisUpominky(jana));
console.log(JSON.stringify(upominkaProWeb(jana), null, 2));

const ctenari = [jana, petr, marie];
const inkaso = ctenari.reduce((soucet, ctenar) => soucet + spoctiUpominku(ctenar).celkem, 0);
console.log(`Celkem k inkasu od ${ctenari.length} čtenářů: ${inkaso} Kč`);
```

## --file-- ctenari.js

```js
// Ukazkova data z vypujcniho systemu. Dny po terminu dopocitava system k dnesnimu dni.

export const jana = {
  jmeno: 'Jana Nováková',
  kategorie: 'student',
  vypujcky: [
    { nazev: 'Alchymista', druh: 'kniha', dniPoTerminu: 14 },
    { nazev: 'Respekt 12/2026', druh: 'časopis', dniPoTerminu: 3 },
    { nazev: 'Šifra mistra Leonarda', druh: 'audiokniha', dniPoTerminu: 0 },
  ],
};

export const petr = {
  jmeno: 'Petr Doležal',
  kategorie: 'dospělý',
  vypujcky: [
    { nazev: 'Bílá velryba', druh: 'kniha', dniPoTerminu: 2 },
    { nazev: 'Reflex 09/2026', druh: 'časopis', dniPoTerminu: 0 },
    { nazev: 'Saturnin', druh: 'audiokniha', dniPoTerminu: 1 },
  ],
};

export const marie = {
  jmeno: 'Marie Kratochvílová',
  kategorie: 'senior',
  vypujcky: [
    { nazev: 'Osadníci z Katanu', druh: 'desková hra', dniPoTerminu: 40 },
    { nazev: 'Kroniky Narnie', druh: 'kniha', dniPoTerminu: 95 },
  ],
};
```

## --file-- package.json

```json
{
  "name": "knihovna-upominky",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test poplatky.test.js"
  }
}
```

# --solution--

## --file-- slevy.js

```js
// Slevy čtenářského klubu. Mění se ze všeho nejčastěji, proto mají vlastní soubor.

const SLEVY = {
  student: 0.5,
  senior: 0.25,
};

export function slevaProKategorii(kategorie) {
  return SLEVY[kategorie] ?? 1;
}
```

## --file-- poplatky.test.js

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';

import { vypisUpominky } from './poplatky.js';
import { jana, petr, marie } from './ctenari.js';

test('upomínka pro Janu Novákovou (student) vypadá přesně takhle', () => {
  const ocekavano = [
    'Upomínka — Městská knihovna Kutná Hora',
    'Čtenář: Jana Nováková (student)',
    '  Alchymista (kniha): 55 Kč, po termínu 14 dní',
    '  Respekt 12/2026 (časopis): 24 Kč, po termínu 3 dny',
    '  Šifra mistra Leonarda (audiokniha): 0 Kč, po termínu 0 dní',
    'Celkem k zaplacení: 40 Kč',
    'Body do čtenářského klubu: 4',
    '',
  ].join('\n');

  assert.equal(vypisUpominky(jana), ocekavano, 'Upomínka pro Janu se změnila proti dnešnímu stavu');
});

test('Petr Doležal nic nedluží, ale body dostane', () => {
  const ocekavano = [
    'Upomínka — Městská knihovna Kutná Hora',
    'Čtenář: Petr Doležal (dospělý)',
    '  Bílá velryba (kniha): 0 Kč, po termínu 2 dny',
    '  Reflex 09/2026 (časopis): 0 Kč, po termínu 0 dní',
    '  Saturnin (audiokniha): 0 Kč, po termínu 1 den',
    'Celkem k zaplacení: 0 Kč',
    'Body do čtenářského klubu: 4',
    '',
  ].join('\n');

  assert.equal(vypisUpominky(petr), ocekavano, 'Upomínka pro Petra se změnila proti dnešnímu stavu');
});

test('Marie Kratochvílová (senior) narazí na stropy a slevu', () => {
  const ocekavano = [
    'Upomínka — Městská knihovna Kutná Hora',
    'Čtenář: Marie Kratochvílová (senior)',
    '  Osadníci z Katanu (desková hra): 250 Kč, po termínu 40 dní',
    '  Kroniky Narnie (kniha): 300 Kč, po termínu 95 dní',
    'Celkem k zaplacení: 138 Kč',
    'Body do čtenářského klubu: 2',
    '',
  ].join('\n');

  assert.equal(vypisUpominky(marie), ocekavano, 'Upomínka pro Marii se změnila proti dnešnímu stavu');
});
```
