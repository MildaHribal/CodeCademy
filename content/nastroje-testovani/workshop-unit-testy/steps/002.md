---
title: První test v node:test
main: ukazka.js
see: nastroje-testovani/proc-testovat#prvni-test-v-node-test
timeoutMs: 30000
---

# --description--

`formatDuration` teď funguje. Aby fungovala i za měsíc, až na ni někdo sáhne,
napíšeš jí první test.

Testy patří do souboru `utils.test.js` vedle `utils.js`. Soubor potřebuje tři
importy: funkci `test` z `node:test`, objekt `assert` z `node:assert/strict`
a testovanou funkci z `./utils.js`. Pak zavoláš `test(jméno, funkce)` a v těle
porovnáš výsledek přes `assert.equal(skutečnost, očekávání)`.

Stejný tvar pro jinou funkci, kterou v deníku nemáš:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { formatDistance } from './format.js';

test('formatDistance píše kilometry s čárkou', () => {
  assert.equal(formatDistance(12.4), '12,4 km');
});
```

**Úkol:** do `utils.test.js` napiš test, který ověří, že `formatDuration(2525)`
vrátí `42:05`. Jméno testu ať říká, co funkce dělá.

Kontrola spustí `node --test utils.test.js`. Pak tvůj test pustí proti
podstrčené verzi `formatDuration`, která zapomíná na nulu před sekundami nebo
místo dvojtečky píše tečku — a test musí selhat.

> [!TIP]
> Na svém počítači pustíš totéž příkazem `node --test` ve složce projektu. Najde
> sám všechny soubory končící `.test.js`.

# --hints--

`node --test utils.test.js` projde a soubor obsahuje aspoň jeden test.

```js
const result = await helpers.run('node --test utils.test.js');
assert.equal(result.code, 0, `node --test utils.test.js má projít bez selhání:\n${result.stdout}${result.stderr}`);
const count = Number(/tests (\d+)/.exec(result.stdout)?.[1] ?? 0);
assert.ok(count >= 1, `utils.test.js má obsahovat aspoň jeden test, node --test jich našel ${count}`);
```

Test selže, když `formatDuration` zapomene doplnit nulu před sekundami (`42:5`).

```js
const fs = await import('node:fs/promises');
const source = files['utils.js'];
const from = ".padStart(2, '0')";
assert.ok(source.includes(from), 'utils.js v tomhle kroku neměň — kontrola do něj podstrkuje chyby');
const original = await helpers.run('node --test utils.test.js');
assert.equal(original.code, 0, 'nejdřív musí tvoje testy projít nad správným utils.js');
await fs.mkdir(`${helpers.dir}/mutant-pad`, { recursive: true });
await fs.writeFile(`${helpers.dir}/mutant-pad/utils.js`, source.replace(from, ''));
await fs.writeFile(`${helpers.dir}/mutant-pad/utils.test.js`, files['utils.test.js']);
const mutant = await helpers.run('node --test mutant-pad/utils.test.js');
assert.notEqual(mutant.code, 0, 'formatDuration(2525) teď vrací „42:5", ale tvoje testy prošly — chybí aserce na výsledek?');
```

Test selže, když `formatDuration` oddělí minuty tečkou (`42.05`).

```js
const fs = await import('node:fs/promises');
const source = files['utils.js'];
const from = 'return `${minutes}:${seconds}`;';
assert.ok(source.includes(from), 'utils.js v tomhle kroku neměň — kontrola do něj podstrkuje chyby');
const original = await helpers.run('node --test utils.test.js');
assert.equal(original.code, 0, 'nejdřív musí tvoje testy projít nad správným utils.js');
await fs.mkdir(`${helpers.dir}/mutant-dot`, { recursive: true });
await fs.writeFile(`${helpers.dir}/mutant-dot/utils.js`, source.replace(from, 'return `${minutes}.${seconds}`;'));
await fs.writeFile(`${helpers.dir}/mutant-dot/utils.test.js`, files['utils.test.js']);
const mutant = await helpers.run('node --test mutant-dot/utils.test.js');
assert.notEqual(mutant.code, 0, 'formatDuration(2525) teď vrací „42.05", ale tvoje testy prošly');
```

# --seed--

## --file-- utils.js

```js
// Pomocné funkce Běžeckého deníku. Napsal je kolega Ondra, testy zatím nemají.

// Délka běhu v sekundách → text pro přehled, třeba 2525 → „42:05".
export function formatDuration(totalSeconds) {
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = String(totalSeconds % 60).padStart(2, '0');
  return `${minutes}:${seconds}`;
}

// Text z formuláře „42:05" nebo „1:05:30" → počet sekund. Neplatný čas vyhodí RangeError.
export function parseDuration(text) {
  const parts = text.split(':').map(Number);
  const [seconds, minutes, hours = 0] = [...parts].reverse();
  const valid =
    parts.length >= 2 &&
    parts.length <= 3 &&
    parts.every((part) => Number.isInteger(part) && part >= 0) &&
    seconds <= 59 &&
    (parts.length === 2 || minutes <= 59);
  if (!valid) {
    throw new RangeError(`Neplatný čas „${text}", čekám m:ss nebo h:mm:ss`);
  }
  return hours * 3600 + minutes * 60 + seconds;
}

// Tempo na kilometr zaokrouhlené na celé sekundy, třeba „5:03 /km". Vzdálenost musí být kladná.
export function pace(totalSeconds, km) {
  if (!(km > 0)) {
    throw new RangeError('Vzdálenost musí být větší než 0 km');
  }
  return `${formatDuration(Math.round(totalSeconds / km))} /km`;
}

// Název běhu → kousek adresy: malá písmena bez diakritiky, slova oddělená jednou pomlčkou.
export function slugify(title) {
  return title
    .normalize('NFD')
    .replace(/\p{Diacritic}/gu, '')
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '');
}

// Souhrn týdne: počet běhů, kilometry zaokrouhlené na desetiny a celkový čas v sekundách.
export function weekSummary(runs) {
  const km = runs.reduce((sum, run) => sum + run.km, 0);
  const seconds = runs.reduce((sum, run) => sum + run.seconds, 0);
  return { count: runs.length, km: Math.round(km * 10) / 10, seconds };
}

// Načte běh z úložiště. Neznámé id skončí chybou „Běh … nenalezen".
export async function loadRun(store, id) {
  const run = store.get(id);
  if (!run) {
    throw new Error(`Běh ${id} nenalezen`);
  }
  return run;
}

// Uloží běh a vrátí jeho nové id. Běh bez kladné vzdálenosti odmítne chybou RangeError.
export async function saveRun(store, run) {
  if (!(run.km > 0)) {
    throw new RangeError('Běh musí mít kladnou vzdálenost');
  }
  const id = store.size + 1;
  store.set(id, { ...run, id });
  return id;
}
```

## --file-- ukazka.js

```js
import { formatDuration, pace, slugify } from './utils.js';

const run = { title: 'Běh kolem Máchova jezera', km: 12.4, seconds: 4020 };

console.log(`${run.title} → /beh/${slugify(run.title)}`);
console.log(`Čas: ${formatDuration(run.seconds)}, tempo: ${pace(run.seconds, run.km)}`);
```

## --file-- package.json

```json
{
  "name": "bezecky-denik",
  "type": "module"
}
```

## --file-- utils.test.js

```js
--edit--

--edit--
```

# --solution--

## --file-- utils.test.js

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { formatDuration } from './utils.js';

test('formatDuration píše minuty a sekundy', () => {
  assert.equal(formatDuration(2525), '42:05');
});
```
