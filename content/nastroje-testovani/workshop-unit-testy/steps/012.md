---
title: Souhrn týdne po svém
kind: choose
main: ukazka.js
see: nastroje-testovani/proc-testovat#okrajove-pripady
timeoutMs: 30000
---

# --description--

`weekSummary` dělá z běhů za týden řádek do přehledu: kolik běhů, kolik kilometrů
a kolik času. Tentokrát nedostaneš vzor — jak test postavíš a čím výsledek
porovnáš, je na tobě.

Na co si dát pozor:

- **Kilometry s desetinami.** Běžecké hodinky měří na desetiny a součet desetinných
  čísel v JavaScriptu nevychází vždycky přesně. Vyber vzdálenosti, u kterých to
  nastane — funkce slibuje zaokrouhlení na desetiny a test to má prověřit.
  Zkus si součet nejdřív pustit v `ukazka.js` přes `console.log`.
- **Týden bez běhu.** I prázdný týden má v přehledu řádek s nulami.

**Úkol:** přidej `weekSummary` do importu a otestuj, že spočítá běhy, kilometry
zaokrouhlené na desetiny a celkový čas — i pro týden, kdy kamarád neběžel.

# --hints--

`node --test utils.test.js` projde celý.

```js
const result = await helpers.run('node --test utils.test.js');
assert.equal(result.code, 0, `všechny testy mají projít:\n${result.stdout}${result.stderr}`);
```

Testy selžou, když `weekSummary` kilometry nezaokrouhlí.

```js
const fs = await import('node:fs/promises');
const source = files['utils.js'];
const from = 'km: Math.round(km * 10) / 10,';
assert.ok(source.includes(from), 'utils.js v tomhle kroku neměň — kontrola do něj podstrkuje chyby');
const original = await helpers.run('node --test utils.test.js');
assert.equal(original.code, 0, 'nejdřív musí tvoje testy projít nad správným utils.js');
await fs.mkdir(`${helpers.dir}/mutant-float`, { recursive: true });
await fs.writeFile(`${helpers.dir}/mutant-float/utils.js`, source.replace(from, 'km,'));
await fs.writeFile(`${helpers.dir}/mutant-float/utils.test.js`, files['utils.test.js']);
const mutant = await helpers.run('node --test mutant-float/utils.test.js');
assert.notEqual(mutant.code, 0, 'weekSummary teď vrací nezaokrouhlený součet kilometrů (třeba 36.400000000000006), ale tvoje testy prošly — vybral jsi vzdálenosti, jejichž součet nevyjde přesně?');
```

Testy selžou, když `weekSummary` zaokrouhlí kilometry na celá čísla.

```js
const fs = await import('node:fs/promises');
const source = files['utils.js'];
const from = 'km: Math.round(km * 10) / 10,';
assert.ok(source.includes(from), 'utils.js v tomhle kroku neměň — kontrola do něj podstrkuje chyby');
const original = await helpers.run('node --test utils.test.js');
assert.equal(original.code, 0, 'nejdřív musí tvoje testy projít nad správným utils.js');
await fs.mkdir(`${helpers.dir}/mutant-whole`, { recursive: true });
await fs.writeFile(`${helpers.dir}/mutant-whole/utils.js`, source.replace(from, 'km: Math.round(km),'));
await fs.writeFile(`${helpers.dir}/mutant-whole/utils.test.js`, files['utils.test.js']);
const mutant = await helpers.run('node --test mutant-whole/utils.test.js');
assert.notEqual(mutant.code, 0, 'weekSummary teď zaokrouhluje na celé kilometry, ale tvoje testy prošly');
```

Testy selžou, když `weekSummary` pro prázdný týden vrátí `null`.

```js
const fs = await import('node:fs/promises');
const source = files['utils.js'];
const from = 'export function weekSummary(runs) {';
assert.ok(source.includes(from), 'utils.js v tomhle kroku neměň — kontrola do něj podstrkuje chyby');
const original = await helpers.run('node --test utils.test.js');
assert.equal(original.code, 0, 'nejdřív musí tvoje testy projít nad správným utils.js');
await fs.mkdir(`${helpers.dir}/mutant-empty`, { recursive: true });
await fs.writeFile(`${helpers.dir}/mutant-empty/utils.js`, source.replace(from, `${from}\n  if (runs.length === 0) return null;`));
await fs.writeFile(`${helpers.dir}/mutant-empty/utils.test.js`, files['utils.test.js']);
const mutant = await helpers.run('node --test mutant-empty/utils.test.js');
assert.notEqual(mutant.code, 0, 'weekSummary([]) teď vrací null, ale tvoje testy prošly — chybí prázdný týden');
```

# --help--

## --tip--

Prázdné pole je typický okrajový případ, viz lekce v části [Okrajové případy](see:nastroje-testovani/proc-testovat#okrajove-pripady).

## --tip--

Výsledek je objekt. Buď ho porovnej celý aserci, která porovnává obsah, nebo zkontroluj každou vlastnost zvlášť. Kilometry v očekávání napiš ručně jako číslo s jedním desetinným místem.

## --tip--

Kterou dvojici čísel JavaScript nesečte přesně, zjistíš za pár vteřin: `console.log(1.1 + 2.2)` vypíše `3.3000000000000003`.

# --seed--

## --file-- utils.js

```js
// Pomocné funkce Běžeckého deníku. Napsal je kolega Ondra, testy zatím nemají.

// Délka běhu v sekundách → text pro přehled, třeba 2525 → „42:05".
export function formatDuration(totalSeconds) {
  const hours = Math.floor(totalSeconds / 3600);
  const minutes = Math.floor((totalSeconds % 3600) / 60);
  const seconds = String(totalSeconds % 60).padStart(2, '0');
  if (hours > 0) {
    return `${hours}:${String(minutes).padStart(2, '0')}:${seconds}`;
  }
  return `${minutes}:${seconds}`;
}

// Text z formuláře „42:05" nebo „1:05:30" → počet sekund. Neplatný čas vyhodí RangeError.
export function parseDuration(text) {
  const parts = text.split(':').map(Number);
  const [seconds, minutes, hours = 0] = [...parts].reverse();
  const valid =
    parts.length >= 2 &&
    parts.length <= 3 &&
    parts.every((part) => Number.isInteger(part) && part >= 0) &&
    seconds <= 59 &&
    (parts.length === 2 || minutes <= 59);
  if (!valid) {
    throw new RangeError(`Neplatný čas „${text}", čekám m:ss nebo h:mm:ss`);
  }
  return hours * 3600 + minutes * 60 + seconds;
}

// Tempo na kilometr zaokrouhlené na celé sekundy, třeba „5:03 /km". Vzdálenost musí být kladná.
export function pace(totalSeconds, km) {
  if (!(km > 0)) {
    throw new RangeError('Vzdálenost musí být větší než 0 km');
  }
  return `${formatDuration(Math.round(totalSeconds / km))} /km`;
}

// Název běhu → kousek adresy: malá písmena bez diakritiky, slova oddělená jednou pomlčkou.
export function slugify(title) {
  return title
    .normalize('NFD')
    .replace(/\p{Diacritic}/gu, '')
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '');
}

// Souhrn týdne: počet běhů, kilometry zaokrouhlené na desetiny a celkový čas v sekundách.
export function weekSummary(runs) {
  const km = runs.reduce((sum, run) => sum + run.km, 0);
  const seconds = runs.reduce((sum, run) => sum + run.seconds, 0);
  return { count: runs.length, km: Math.round(km * 10) / 10, seconds };
}

// Načte běh z úložiště. Neznámé id skončí chybou „Běh … nenalezen".
export async function loadRun(store, id) {
  const run = store.get(id);
  if (!run) {
    throw new Error(`Běh ${id} nenalezen`);
  }
  return run;
}

// Uloží běh a vrátí jeho nové id. Běh bez kladné vzdálenosti odmítne chybou RangeError.
export async function saveRun(store, run) {
  if (!(run.km > 0)) {
    throw new RangeError('Běh musí mít kladnou vzdálenost');
  }
  const id = store.size + 1;
  store.set(id, { ...run, id });
  return id;
}
```

## --file-- ukazka.js

```js
import { formatDuration, pace, slugify } from './utils.js';

const run = { title: 'Běh kolem Máchova jezera', km: 12.4, seconds: 4020 };

console.log(`${run.title} → /beh/${slugify(run.title)}`);
console.log(`Čas: ${formatDuration(run.seconds)}, tempo: ${pace(run.seconds, run.km)}`);
```

## --file-- package.json

```json
{
  "name": "bezecky-denik",
  "type": "module"
}
```

## --file-- utils.test.js

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { formatDuration, parseDuration, slugify, loadRun, saveRun } from './utils.js';

test('formatDuration píše minuty a sekundy', () => {
  assert.equal(formatDuration(2525), '42:05');
});

test('formatDuration píše u dlouhého běhu hodiny', () => {
  assert.equal(formatDuration(3725), '1:02:05');
});

test('formatDuration u běhu pod minutu začíná nulou', () => {
  assert.equal(formatDuration(59), '0:59');
});

test('formatDuration píše přesně hodinu už s hodinami', () => {
  assert.equal(formatDuration(3600), '1:00:00');
});

test('parseDuration odmítne 60 sekund', () => {
  assert.throws(() => parseDuration('7:60'), RangeError);
});

const validTimes = [
  { text: '42:05', seconds: 2525 },
  { text: '0:59', seconds: 59 },
  { text: '1:05:30', seconds: 3930 },
];
for (const { text, seconds } of validTimes) {
  test(`parseDuration('${text}') vrátí ${seconds} s`, () => {
    assert.equal(parseDuration(text), seconds);
  });
}

const slugCases = [
  { title: 'Běh kolem Máchova jezera', slug: 'beh-kolem-machova-jezera' },
  { title: '  Půlmaraton Praha 2026! ', slug: 'pulmaraton-praha-2026' },
  { title: 'Ranní   výklus -- Stromovka', slug: 'ranni-vyklus-stromovka' },
];
for (const { title, slug } of slugCases) {
  test(`slugify('${title}') vrátí ${slug}`, () => {
    assert.equal(slugify(title), slug);
  });
}

test('loadRun odmítne neznámé id', async () => {
  const store = new Map();
  await assert.rejects(loadRun(store, 99), { message: /nenalezen/ });
});

test('saveRun odmítne běh bez vzdálenosti', async () => {
  const store = new Map();
  await assert.rejects(saveRun(store, { title: 'Rozcvička', km: 0, seconds: 300 }), RangeError);
});

test('saveRun uloží běh a loadRun ho vrátí', async () => {
  const store = new Map();
  const run = { title: 'Divoká Šárka', km: 9.8, seconds: 3300 };

  const id = await saveRun(store, run);
  const loaded = await loadRun(store, id);

  assert.equal(id, 1);
  assert.deepEqual(loaded, { ...run, id: 1 });
});
--edit--

--edit--
```

# --solution--

## --file-- utils.test.js

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { formatDuration, parseDuration, slugify, weekSummary, loadRun, saveRun } from './utils.js';

test('formatDuration píše minuty a sekundy', () => {
  assert.equal(formatDuration(2525), '42:05');
});

test('formatDuration píše u dlouhého běhu hodiny', () => {
  assert.equal(formatDuration(3725), '1:02:05');
});

test('formatDuration u běhu pod minutu začíná nulou', () => {
  assert.equal(formatDuration(59), '0:59');
});

test('formatDuration píše přesně hodinu už s hodinami', () => {
  assert.equal(formatDuration(3600), '1:00:00');
});

test('parseDuration odmítne 60 sekund', () => {
  assert.throws(() => parseDuration('7:60'), RangeError);
});

const validTimes = [
  { text: '42:05', seconds: 2525 },
  { text: '0:59', seconds: 59 },
  { text: '1:05:30', seconds: 3930 },
];
for (const { text, seconds } of validTimes) {
  test(`parseDuration('${text}') vrátí ${seconds} s`, () => {
    assert.equal(parseDuration(text), seconds);
  });
}

const slugCases = [
  { title: 'Běh kolem Máchova jezera', slug: 'beh-kolem-machova-jezera' },
  { title: '  Půlmaraton Praha 2026! ', slug: 'pulmaraton-praha-2026' },
  { title: 'Ranní   výklus -- Stromovka', slug: 'ranni-vyklus-stromovka' },
];
for (const { title, slug } of slugCases) {
  test(`slugify('${title}') vrátí ${slug}`, () => {
    assert.equal(slugify(title), slug);
  });
}

test('loadRun odmítne neznámé id', async () => {
  const store = new Map();
  await assert.rejects(loadRun(store, 99), { message: /nenalezen/ });
});

test('saveRun odmítne běh bez vzdálenosti', async () => {
  const store = new Map();
  await assert.rejects(saveRun(store, { title: 'Rozcvička', km: 0, seconds: 300 }), RangeError);
});

test('saveRun uloží běh a loadRun ho vrátí', async () => {
  const store = new Map();
  const run = { title: 'Divoká Šárka', km: 9.8, seconds: 3300 };

  const id = await saveRun(store, run);
  const loaded = await loadRun(store, id);

  assert.equal(id, 1);
  assert.deepEqual(loaded, { ...run, id: 1 });
});

test('weekSummary sečte týden a zaokrouhlí kilometry na desetiny', () => {
  const runs = [
    { title: 'Stromovka', km: 5.2, seconds: 1680 },
    { title: 'Letná', km: 10.1, seconds: 3060 },
    { title: 'Pražský půlmaraton', km: 21.1, seconds: 6900 },
  ];
  assert.deepEqual(weekSummary(runs), { count: 3, km: 36.4, seconds: 11640 });
});

test('weekSummary vrátí pro týden bez běhu nuly', () => {
  assert.deepEqual(weekSummary([]), { count: 0, km: 0, seconds: 0 });
});
```
