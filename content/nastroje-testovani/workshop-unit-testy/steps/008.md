---
title: Tabulka případů pro slugify
main: ukazka.js
see: nastroje-testovani/proc-testovat
timeoutMs: 30000
---

# --description--

Teď stejný vzor napíšeš sám, pro `slugify`. Funkce dělá z názvu běhu kousek
adresy (`/beh/beh-kolem-machova-jezera`), takže když se rozbije, přestanou
fungovat odkazy, které už kamarád sdílel.

Podle komentáře v `utils.js` slibuje tři věci a na každou potřebuješ aspoň jeden
řádek tabulky:

- **diakritika** zmizí a písmena budou malá,
- **mezery a interpunkce na začátku a na konci** v adrese nezůstanou,
- **víc oddělovačů za sebou** (dvě mezery, ` -- `) se sloučí do jedné pomlčky.

Názvy si vymysli sám — třeba podle běhů, které znáš. Očekávaný slug napiš ručně,
nepočítej ho kódem.

**Úkol:** přidej `slugify` do importu a napiš parametrizovaný test: pole případů
a cyklus, který pro každý případ zaregistruje test se vstupem ve jménu.

# --hints--

`node --test utils.test.js` projde celý.

```js
const result = await helpers.run('node --test utils.test.js');
assert.equal(result.code, 0, `všechny testy mají projít:\n${result.stdout}${result.stderr}`);
```

Testy selžou, když `slugify` nechá v adrese diakritiku.

```js
const fs = await import('node:fs/promises');
const source = files['utils.js'];
const from = ".replace(/\\p{Diacritic}/gu, '')";
assert.ok(source.includes(from), 'utils.js v tomhle kroku neměň — kontrola do něj podstrkuje chyby');
const original = await helpers.run('node --test utils.test.js');
assert.equal(original.code, 0, 'nejdřív musí tvoje testy projít nad správným utils.js');
await fs.mkdir(`${helpers.dir}/mutant-diacritic`, { recursive: true });
await fs.writeFile(`${helpers.dir}/mutant-diacritic/utils.js`, source.replace(from, ''));
await fs.writeFile(`${helpers.dir}/mutant-diacritic/utils.test.js`, files['utils.test.js']);
const mutant = await helpers.run('node --test mutant-diacritic/utils.test.js');
assert.notEqual(mutant.code, 0, "slugify('Běh') teď vrací „be-h\", ale tvoje testy prošly — chybí případ s diakritikou");
```

Testy selžou, když `slugify` nechá pomlčky na začátku nebo na konci.

```js
const fs = await import('node:fs/promises');
const source = files['utils.js'];
const from = ".replace(/^-+|-+$/g, '')";
assert.ok(source.includes(from), 'utils.js v tomhle kroku neměň — kontrola do něj podstrkuje chyby');
const original = await helpers.run('node --test utils.test.js');
assert.equal(original.code, 0, 'nejdřív musí tvoje testy projít nad správným utils.js');
await fs.mkdir(`${helpers.dir}/mutant-edges`, { recursive: true });
await fs.writeFile(`${helpers.dir}/mutant-edges/utils.js`, source.replace(from, ''));
await fs.writeFile(`${helpers.dir}/mutant-edges/utils.test.js`, files['utils.test.js']);
const mutant = await helpers.run('node --test mutant-edges/utils.test.js');
assert.notEqual(mutant.code, 0, "slugify(' Běh! ') teď vrací „-beh-\", ale tvoje testy prošly — chybí případ s mezerou nebo interpunkcí na kraji");
```

Testy selžou, když `slugify` z více oddělovačů za sebou udělá víc pomlček.

```js
const fs = await import('node:fs/promises');
const source = files['utils.js'];
const from = '.replace(/[^a-z0-9]+/g, ';
assert.ok(source.includes(from), 'utils.js v tomhle kroku neměň — kontrola do něj podstrkuje chyby');
const original = await helpers.run('node --test utils.test.js');
assert.equal(original.code, 0, 'nejdřív musí tvoje testy projít nad správným utils.js');
await fs.mkdir(`${helpers.dir}/mutant-dashes`, { recursive: true });
await fs.writeFile(`${helpers.dir}/mutant-dashes/utils.js`, source.replace(from, '.replace(/[^a-z0-9]/g, '));
await fs.writeFile(`${helpers.dir}/mutant-dashes/utils.test.js`, files['utils.test.js']);
const mutant = await helpers.run('node --test mutant-dashes/utils.test.js');
assert.notEqual(mutant.code, 0, "slugify('Ranní  běh') teď vrací „ranni--beh\", ale tvoje testy prošly — chybí případ s víc oddělovači za sebou");
```

# --help--

## --tip--

Je to stejný vzor jako v minulém kroku, viz [Tabulka případů](see:nastroje-testovani/proc-testovat). Jeden řádek tabulky může prověřit i dva sliby najednou.

## --tip--

Každý řádek tabulky potřebuje vstupní název a ručně napsaný slug. Jméno testu slož v šabloně z obou hodnot.

## --tip--

Stejná tabulka pro iniciály: `{ name: '  Šárka   Dvořáková ', expected: 'ŠD' }` — mezery na krajích i uprostřed v jednom řádku.

# --seed--

## --file-- utils.js

```js
// Pomocné funkce Běžeckého deníku. Napsal je kolega Ondra, testy zatím nemají.

// Délka běhu v sekundách → text pro přehled, třeba 2525 → „42:05".
export function formatDuration(totalSeconds) {
  const hours = Math.floor(totalSeconds / 3600);
  const minutes = Math.floor((totalSeconds % 3600) / 60);
  const seconds = String(totalSeconds % 60).padStart(2, '0');
  if (hours > 0) {
    return `${hours}:${String(minutes).padStart(2, '0')}:${seconds}`;
  }
  return `${minutes}:${seconds}`;
}

// Text z formuláře „42:05" nebo „1:05:30" → počet sekund. Neplatný čas vyhodí RangeError.
export function parseDuration(text) {
  const parts = text.split(':').map(Number);
  const [seconds, minutes, hours = 0] = [...parts].reverse();
  const valid =
    parts.length >= 2 &&
    parts.length <= 3 &&
    parts.every((part) => Number.isInteger(part) && part >= 0) &&
    seconds <= 59 &&
    (parts.length === 2 || minutes <= 59);
  if (!valid) {
    throw new RangeError(`Neplatný čas „${text}", čekám m:ss nebo h:mm:ss`);
  }
  return hours * 3600 + minutes * 60 + seconds;
}

// Tempo na kilometr zaokrouhlené na celé sekundy, třeba „5:03 /km". Vzdálenost musí být kladná.
export function pace(totalSeconds, km) {
  if (!(km > 0)) {
    throw new RangeError('Vzdálenost musí být větší než 0 km');
  }
  return `${formatDuration(Math.round(totalSeconds / km))} /km`;
}

// Název běhu → kousek adresy: malá písmena bez diakritiky, slova oddělená jednou pomlčkou.
export function slugify(title) {
  return title
    .normalize('NFD')
    .replace(/\p{Diacritic}/gu, '')
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '');
}

// Souhrn týdne: počet běhů, kilometry zaokrouhlené na desetiny a celkový čas v sekundách.
export function weekSummary(runs) {
  const km = runs.reduce((sum, run) => sum + run.km, 0);
  const seconds = runs.reduce((sum, run) => sum + run.seconds, 0);
  return { count: runs.length, km: Math.round(km * 10) / 10, seconds };
}

// Načte běh z úložiště. Neznámé id skončí chybou „Běh … nenalezen".
export async function loadRun(store, id) {
  const run = store.get(id);
  if (!run) {
    throw new Error(`Běh ${id} nenalezen`);
  }
  return run;
}

// Uloží běh a vrátí jeho nové id. Běh bez kladné vzdálenosti odmítne chybou RangeError.
export async function saveRun(store, run) {
  if (!(run.km > 0)) {
    throw new RangeError('Běh musí mít kladnou vzdálenost');
  }
  const id = store.size + 1;
  store.set(id, { ...run, id });
  return id;
}
```

## --file-- ukazka.js

```js
import { formatDuration, pace, slugify } from './utils.js';

const run = { title: 'Běh kolem Máchova jezera', km: 12.4, seconds: 4020 };

console.log(`${run.title} → /beh/${slugify(run.title)}`);
console.log(`Čas: ${formatDuration(run.seconds)}, tempo: ${pace(run.seconds, run.km)}`);
```

## --file-- package.json

```json
{
  "name": "bezecky-denik",
  "type": "module"
}
```

## --file-- utils.test.js

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { formatDuration, parseDuration } from './utils.js';

test('formatDuration píše minuty a sekundy', () => {
  assert.equal(formatDuration(2525), '42:05');
});

test('formatDuration píše u dlouhého běhu hodiny', () => {
  assert.equal(formatDuration(3725), '1:02:05');
});

test('formatDuration u běhu pod minutu začíná nulou', () => {
  assert.equal(formatDuration(59), '0:59');
});

test('formatDuration píše přesně hodinu už s hodinami', () => {
  assert.equal(formatDuration(3600), '1:00:00');
});

test('parseDuration odmítne 60 sekund', () => {
  assert.throws(() => parseDuration('7:60'), RangeError);
});

const validTimes = [
  { text: '42:05', seconds: 2525 },
  { text: '0:59', seconds: 59 },
  { text: '1:05:30', seconds: 3930 },
];
for (const { text, seconds } of validTimes) {
  test(`parseDuration('${text}') vrátí ${seconds} s`, () => {
    assert.equal(parseDuration(text), seconds);
  });
}
--edit--

--edit--
```

# --solution--

## --file-- utils.test.js

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { formatDuration, parseDuration, slugify } from './utils.js';

test('formatDuration píše minuty a sekundy', () => {
  assert.equal(formatDuration(2525), '42:05');
});

test('formatDuration píše u dlouhého běhu hodiny', () => {
  assert.equal(formatDuration(3725), '1:02:05');
});

test('formatDuration u běhu pod minutu začíná nulou', () => {
  assert.equal(formatDuration(59), '0:59');
});

test('formatDuration píše přesně hodinu už s hodinami', () => {
  assert.equal(formatDuration(3600), '1:00:00');
});

test('parseDuration odmítne 60 sekund', () => {
  assert.throws(() => parseDuration('7:60'), RangeError);
});

const validTimes = [
  { text: '42:05', seconds: 2525 },
  { text: '0:59', seconds: 59 },
  { text: '1:05:30', seconds: 3930 },
];
for (const { text, seconds } of validTimes) {
  test(`parseDuration('${text}') vrátí ${seconds} s`, () => {
    assert.equal(parseDuration(text), seconds);
  });
}

const slugCases = [
  { title: 'Běh kolem Máchova jezera', slug: 'beh-kolem-machova-jezera' },
  { title: '  Půlmaraton Praha 2026! ', slug: 'pulmaraton-praha-2026' },
  { title: 'Ranní   výklus -- Stromovka', slug: 'ranni-vyklus-stromovka' },
];
for (const { title, slug } of slugCases) {
  test(`slugify('${title}') vrátí ${slug}`, () => {
    assert.equal(slugify(title), slug);
  });
}
```
