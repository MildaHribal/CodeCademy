---
title: Očekávaná chyba přes assert.throws
main: ukazka.js
see: nastroje-testovani/proc-testovat#ocekavana-chyba-throws-a-rejects
timeoutMs: 30000
---

# --description--

Druhá funkce deníku, `parseDuration`, čte čas z formuláře. Když kamarád napíše
`7:60`, nemá z toho vzniknout 480 sekund, ale chyba `RangeError`. Že funkce
chybu **vyhodí**, je její chování jako každé jiné — a dá se otestovat.

`assert.throws` dostane **funkci**, kterou sám zavolá, a jako druhý argument
třídu chyby, kterou čeká. Takhle by vypadal test pro jinou funkci:

```js
test('parseWeight odmítne záporné kilogramy', () => {
  assert.throws(() => parseWeight('-2 kg'), RangeError);
});
```

**Úkol:**

1. Do importu z `./utils.js` přidej `parseDuration`.
2. Napiš test, že `parseDuration('7:60')` vyhodí `RangeError`.

> [!PITFALL]
> `assert.throws(parseDuration('7:60'), RangeError)` bez `() =>` zavolá funkci dřív,
> než ji `assert.throws` dostane. Chyba vyletí mimo aserci a test spadne s
> `RangeError: Neplatný čas „7:60"` — přesně s tou chybou, kterou chtěl hlídat.

# --hints--

`node --test utils.test.js` projde celý.

```js
const result = await helpers.run('node --test utils.test.js');
assert.equal(result.code, 0, `všechny testy mají projít:\n${result.stdout}${result.stderr}`);
```

Testy selžou, když `parseDuration` přestane hlídat sekundy nad 59.

```js
const fs = await import('node:fs/promises');
const source = files['utils.js'];
const from = 'seconds <= 59 &&';
assert.ok(source.includes(from), 'utils.js v tomhle kroku neměň — kontrola do něj podstrkuje chyby');
const original = await helpers.run('node --test utils.test.js');
assert.equal(original.code, 0, 'nejdřív musí tvoje testy projít nad správným utils.js');
await fs.mkdir(`${helpers.dir}/mutant-seconds`, { recursive: true });
await fs.writeFile(`${helpers.dir}/mutant-seconds/utils.js`, source.replace(from, ''));
await fs.writeFile(`${helpers.dir}/mutant-seconds/utils.test.js`, files['utils.test.js']);
const mutant = await helpers.run('node --test mutant-seconds/utils.test.js');
assert.notEqual(mutant.code, 0, "parseDuration('7:60') teď vrací 480 bez chyby, ale tvoje testy prošly");
```

Testy selžou, když `parseDuration` vyhodí obecný `Error` místo `RangeError`.

```js
const fs = await import('node:fs/promises');
const source = files['utils.js'];
const from = 'throw new RangeError(`Neplatný čas';
assert.ok(source.includes(from), 'utils.js v tomhle kroku neměň — kontrola do něj podstrkuje chyby');
const original = await helpers.run('node --test utils.test.js');
assert.equal(original.code, 0, 'nejdřív musí tvoje testy projít nad správným utils.js');
await fs.mkdir(`${helpers.dir}/mutant-error`, { recursive: true });
await fs.writeFile(`${helpers.dir}/mutant-error/utils.js`, source.replace(from, 'throw new Error(`Neplatný čas'));
await fs.writeFile(`${helpers.dir}/mutant-error/utils.test.js`, files['utils.test.js']);
const mutant = await helpers.run('node --test mutant-error/utils.test.js');
assert.notEqual(mutant.code, 0, 'parseDuration teď vyhazuje Error místo RangeError, ale tvoje testy prošly — předáváš assert.throws i třídu chyby?');
```

# --explain--

Vysvětli vlastními slovy, proč `assert.throws` potřebuje dostat funkci `() => parseDuration('7:60')`, a ne rovnou výsledek volání.

## --model--

Argumenty se vyhodnotí dřív, než se zavolá funkce, které je předávám. Kdybych
napsal `parseDuration('7:60')` přímo, chyba by vyletěla ještě před `assert.throws`
a test by spadl. Když předám funkci, zavolá ji až `assert.throws` uvnitř sebe,
chybu zachytí a porovná s očekávanou třídou.

## --checklist--

- Argumenty funkce se vyhodnotí před jejím zavoláním.
- Bez obalení do funkce vyletí chyba mimo `assert.throws` a test spadne.
- `assert.throws` funkci zavolá sám a chybu zachytí.
- Druhý argument určí, jakou chybu test čeká.

# --seed--

## --file-- utils.js

```js
// Pomocné funkce Běžeckého deníku. Napsal je kolega Ondra, testy zatím nemají.

// Délka běhu v sekundách → text pro přehled, třeba 2525 → „42:05".
export function formatDuration(totalSeconds) {
  const hours = Math.floor(totalSeconds / 3600);
  const minutes = Math.floor((totalSeconds % 3600) / 60);
  const seconds = String(totalSeconds % 60).padStart(2, '0');
  if (hours > 0) {
    return `${hours}:${String(minutes).padStart(2, '0')}:${seconds}`;
  }
  return `${minutes}:${seconds}`;
}

// Text z formuláře „42:05" nebo „1:05:30" → počet sekund. Neplatný čas vyhodí RangeError.
export function parseDuration(text) {
  const parts = text.split(':').map(Number);
  const [seconds, minutes, hours = 0] = [...parts].reverse();
  const valid =
    parts.length >= 2 &&
    parts.length <= 3 &&
    parts.every((part) => Number.isInteger(part) && part >= 0) &&
    seconds <= 59 &&
    (parts.length === 2 || minutes <= 59);
  if (!valid) {
    throw new RangeError(`Neplatný čas „${text}", čekám m:ss nebo h:mm:ss`);
  }
  return hours * 3600 + minutes * 60 + seconds;
}

// Tempo na kilometr zaokrouhlené na celé sekundy, třeba „5:03 /km". Vzdálenost musí být kladná.
export function pace(totalSeconds, km) {
  if (!(km > 0)) {
    throw new RangeError('Vzdálenost musí být větší než 0 km');
  }
  return `${formatDuration(Math.round(totalSeconds / km))} /km`;
}

// Název běhu → kousek adresy: malá písmena bez diakritiky, slova oddělená jednou pomlčkou.
export function slugify(title) {
  return title
    .normalize('NFD')
    .replace(/\p{Diacritic}/gu, '')
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '');
}

// Souhrn týdne: počet běhů, kilometry zaokrouhlené na desetiny a celkový čas v sekundách.
export function weekSummary(runs) {
  const km = runs.reduce((sum, run) => sum + run.km, 0);
  const seconds = runs.reduce((sum, run) => sum + run.seconds, 0);
  return { count: runs.length, km: Math.round(km * 10) / 10, seconds };
}

// Načte běh z úložiště. Neznámé id skončí chybou „Běh … nenalezen".
export async function loadRun(store, id) {
  const run = store.get(id);
  if (!run) {
    throw new Error(`Běh ${id} nenalezen`);
  }
  return run;
}

// Uloží běh a vrátí jeho nové id. Běh bez kladné vzdálenosti odmítne chybou RangeError.
export async function saveRun(store, run) {
  if (!(run.km > 0)) {
    throw new RangeError('Běh musí mít kladnou vzdálenost');
  }
  const id = store.size + 1;
  store.set(id, { ...run, id });
  return id;
}
```

## --file-- ukazka.js

```js
import { formatDuration, pace, slugify } from './utils.js';

const run = { title: 'Běh kolem Máchova jezera', km: 12.4, seconds: 4020 };

console.log(`${run.title} → /beh/${slugify(run.title)}`);
console.log(`Čas: ${formatDuration(run.seconds)}, tempo: ${pace(run.seconds, run.km)}`);
```

## --file-- package.json

```json
{
  "name": "bezecky-denik",
  "type": "module"
}
```

## --file-- utils.test.js

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { formatDuration } from './utils.js';

test('formatDuration píše minuty a sekundy', () => {
  assert.equal(formatDuration(2525), '42:05');
});

test('formatDuration píše u dlouhého běhu hodiny', () => {
  assert.equal(formatDuration(3725), '1:02:05');
});

test('formatDuration u běhu pod minutu začíná nulou', () => {
  assert.equal(formatDuration(59), '0:59');
});

test('formatDuration píše přesně hodinu už s hodinami', () => {
  assert.equal(formatDuration(3600), '1:00:00');
});
--edit--

--edit--
```

# --solution--

## --file-- utils.test.js

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { formatDuration, parseDuration } from './utils.js';

test('formatDuration píše minuty a sekundy', () => {
  assert.equal(formatDuration(2525), '42:05');
});

test('formatDuration píše u dlouhého běhu hodiny', () => {
  assert.equal(formatDuration(3725), '1:02:05');
});

test('formatDuration u běhu pod minutu začíná nulou', () => {
  assert.equal(formatDuration(59), '0:59');
});

test('formatDuration píše přesně hodinu už s hodinami', () => {
  assert.equal(formatDuration(3600), '1:00:00');
});

test('parseDuration odmítne 60 sekund', () => {
  assert.throws(() => parseDuration('7:60'), RangeError);
});
```
