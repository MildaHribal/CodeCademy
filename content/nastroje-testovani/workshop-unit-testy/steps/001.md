---
title: Opakování: čas běhu jako text
kind: recall
main: ukazka.js
see: js-retezce-cisla/retezce#upravy-trim-velikost-pismen-padstart-replaceall
---

# --description--

Kamarád si píše webový Běžecký deník: zapisuje běhy, počítá tempo a týdenní
souhrny. Pomocné funkce mu napsal kolega Ondra v `utils.js` — a žádná z nich nemá
jediný test. Za celý workshop to napravíš. Tvoje testy nebudou jen „procházet":
kontrola ti do `utils.js` podstrčí rozbité verze funkcí a tvoje testy je musí
chytit.

Projekt má tři soubory:

- `utils.js` — pomocné funkce deníku. Nad každou je komentář, co má dělat.
- `ukazka.js` — vypíše jeden běh. Spustíš ho tlačítkem **Spustit**.
- `package.json` — kvůli `"type": "module"`.

První krok je z JavaScriptu, který už umíš, a napíšeš ho bez návodu.

**Úkol:** v `utils.js` doplň tělo funkce `formatDuration(totalSeconds)`. Dostane
délku běhu v celých sekundách a vrátí text `minuty:sekundy`, kde sekundy mají
vždy dvě číslice: `2525` → `42:05`, `59` → `0:59`, `600` → `10:00`. Hodiny zatím
neřeš.

# --hints--

`formatDuration(2525)` vrátí `42:05`.

```js
const { formatDuration } = await helpers.importFile('utils.js');
assert.equal(formatDuration(2525), '42:05', 'formatDuration(2525) má vrátit 42:05 — 42 minut a 5 sekund');
```

Sekundy mají vždy dvě číslice.

```js
const { formatDuration } = await helpers.importFile('utils.js');
assert.equal(formatDuration(600), '10:00', 'formatDuration(600) má vrátit 10:00, ne 10:0');
assert.equal(formatDuration(61), '1:01', 'formatDuration(61) má vrátit 1:01');
```

Běh kratší než minuta začíná nulou.

```js
const { formatDuration } = await helpers.importFile('utils.js');
assert.equal(formatDuration(59), '0:59', 'formatDuration(59) má vrátit 0:59');
```

# --seed--

## --file-- utils.js

```js
// Pomocné funkce Běžeckého deníku. Napsal je kolega Ondra, testy zatím nemají.

// Délka běhu v sekundách → text pro přehled, třeba 2525 → „42:05".
export function formatDuration(totalSeconds) {
--edit--

--edit--
}

// Text z formuláře „42:05" nebo „1:05:30" → počet sekund. Neplatný čas vyhodí RangeError.
export function parseDuration(text) {
  const parts = text.split(':').map(Number);
  const [seconds, minutes, hours = 0] = [...parts].reverse();
  const valid =
    parts.length >= 2 &&
    parts.length <= 3 &&
    parts.every((part) => Number.isInteger(part) && part >= 0) &&
    seconds <= 59 &&
    (parts.length === 2 || minutes <= 59);
  if (!valid) {
    throw new RangeError(`Neplatný čas „${text}", čekám m:ss nebo h:mm:ss`);
  }
  return hours * 3600 + minutes * 60 + seconds;
}

// Tempo na kilometr zaokrouhlené na celé sekundy, třeba „5:03 /km". Vzdálenost musí být kladná.
export function pace(totalSeconds, km) {
  if (!(km > 0)) {
    throw new RangeError('Vzdálenost musí být větší než 0 km');
  }
  return `${formatDuration(Math.round(totalSeconds / km))} /km`;
}

// Název běhu → kousek adresy: malá písmena bez diakritiky, slova oddělená jednou pomlčkou.
export function slugify(title) {
  return title
    .normalize('NFD')
    .replace(/\p{Diacritic}/gu, '')
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '');
}

// Souhrn týdne: počet běhů, kilometry zaokrouhlené na desetiny a celkový čas v sekundách.
export function weekSummary(runs) {
  const km = runs.reduce((sum, run) => sum + run.km, 0);
  const seconds = runs.reduce((sum, run) => sum + run.seconds, 0);
  return { count: runs.length, km: Math.round(km * 10) / 10, seconds };
}

// Načte běh z úložiště. Neznámé id skončí chybou „Běh … nenalezen".
export async function loadRun(store, id) {
  const run = store.get(id);
  if (!run) {
    throw new Error(`Běh ${id} nenalezen`);
  }
  return run;
}

// Uloží běh a vrátí jeho nové id. Běh bez kladné vzdálenosti odmítne chybou RangeError.
export async function saveRun(store, run) {
  if (!(run.km > 0)) {
    throw new RangeError('Běh musí mít kladnou vzdálenost');
  }
  const id = store.size + 1;
  store.set(id, { ...run, id });
  return id;
}
```

## --file-- ukazka.js

```js
import { formatDuration, pace, slugify } from './utils.js';

const run = { title: 'Běh kolem Máchova jezera', km: 12.4, seconds: 4020 };

console.log(`${run.title} → /beh/${slugify(run.title)}`);
console.log(`Čas: ${formatDuration(run.seconds)}, tempo: ${pace(run.seconds, run.km)}`);
```

## --file-- package.json

```json
{
  "name": "bezecky-denik",
  "type": "module"
}
```

# --solution--

## --file-- utils.js

```js
// Pomocné funkce Běžeckého deníku. Napsal je kolega Ondra, testy zatím nemají.

// Délka běhu v sekundách → text pro přehled, třeba 2525 → „42:05".
export function formatDuration(totalSeconds) {
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = String(totalSeconds % 60).padStart(2, '0');
  return `${minutes}:${seconds}`;
}

// Text z formuláře „42:05" nebo „1:05:30" → počet sekund. Neplatný čas vyhodí RangeError.
export function parseDuration(text) {
  const parts = text.split(':').map(Number);
  const [seconds, minutes, hours = 0] = [...parts].reverse();
  const valid =
    parts.length >= 2 &&
    parts.length <= 3 &&
    parts.every((part) => Number.isInteger(part) && part >= 0) &&
    seconds <= 59 &&
    (parts.length === 2 || minutes <= 59);
  if (!valid) {
    throw new RangeError(`Neplatný čas „${text}", čekám m:ss nebo h:mm:ss`);
  }
  return hours * 3600 + minutes * 60 + seconds;
}

// Tempo na kilometr zaokrouhlené na celé sekundy, třeba „5:03 /km". Vzdálenost musí být kladná.
export function pace(totalSeconds, km) {
  if (!(km > 0)) {
    throw new RangeError('Vzdálenost musí být větší než 0 km');
  }
  return `${formatDuration(Math.round(totalSeconds / km))} /km`;
}

// Název běhu → kousek adresy: malá písmena bez diakritiky, slova oddělená jednou pomlčkou.
export function slugify(title) {
  return title
    .normalize('NFD')
    .replace(/\p{Diacritic}/gu, '')
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '');
}

// Souhrn týdne: počet běhů, kilometry zaokrouhlené na desetiny a celkový čas v sekundách.
export function weekSummary(runs) {
  const km = runs.reduce((sum, run) => sum + run.km, 0);
  const seconds = runs.reduce((sum, run) => sum + run.seconds, 0);
  return { count: runs.length, km: Math.round(km * 10) / 10, seconds };
}

// Načte běh z úložiště. Neznámé id skončí chybou „Běh … nenalezen".
export async function loadRun(store, id) {
  const run = store.get(id);
  if (!run) {
    throw new Error(`Běh ${id} nenalezen`);
  }
  return run;
}

// Uloží běh a vrátí jeho nové id. Běh bez kladné vzdálenosti odmítne chybou RangeError.
export async function saveRun(store, run) {
  if (!(run.km > 0)) {
    throw new RangeError('Běh musí mít kladnou vzdálenost');
  }
  const id = store.size + 1;
  store.set(id, { ...run, id });
  return id;
}
```
