---
title: "Opakování: způsob doručení"
kind: recall
---

# --description--

Třetí krok je doručení. Data jsou v `menu.js` v poli `zpusobyDoruceni`: každý způsob má `nazev`, `popis` a `cena` (vyzvednutí je zdarma).

Tohle je stejný vzor, jaký jsi psal u pizzy i u velikosti — tentokrát bez návodu.

**Úkol:** Přidej akci pro změnu způsobu doručení a v `KrokTri` vypiš oba způsoby stejným vzorem jako předchozí kroky. Místo ceny se u dopravy zdarma píše slovo `zdarma`, u placené `+59 Kč`. Cena v liště se musí změnit, jakmile uživatel vybere kurýra.

# --help--

## --tip--

Nový klíč ve stavu už existuje — akce ho jen přepíše, stejně jako u velikosti.

## --tip--

Rozhodnutí mezi „zdarma" a cenou s plusem je jeden ternární operátor přímo v JSX.

## --tip--

Na jiných datech: `<p>{doplnek.cena === 0 ? 'v ceně' : `+${doplnek.cena} Kč`}</p>`.

# --hints--

Třetí krok nabízí dva způsoby doručení a vyzvednutí je vybrané od začátku.

```js
{
  const cil = [...document.querySelectorAll('button')].find((t) => t.textContent.includes("Diavola"));
  assert.ok(cil, 'Na stránce čekám tlačítko s textem ' + "Diavola");
  await helpers.click(cil);
  await helpers.flush();
}

{
  const cil = [...document.querySelectorAll('button')].find((t) => t.textContent.includes("Pokračovat"));
  assert.ok(cil, 'Na stránce čekám tlačítko s textem ' + "Pokračovat");
  await helpers.click(cil);
  await helpers.flush();
}

{
  const cil = [...document.querySelectorAll('button')].find((t) => t.textContent.includes("Pokračovat"));
  assert.ok(cil, 'Na stránce čekám tlačítko s textem ' + "Pokračovat");
  await helpers.click(cil);
  await helpers.flush();
}

const nazvy = [...document.querySelectorAll('.polozka__nazev')].map((n) => n.textContent.trim());
assert.deepEqual(nazvy, ['Vyzvednu si sám', 'Kurýr až domů'], 'Na třetím kroku čekám dva způsoby doručení');
const vybrane = [...document.querySelectorAll('.polozka')].filter((t) => t.getAttribute('aria-pressed') === 'true');
assert.match(vybrane[0].textContent, /Vyzvednu si sám/, 'Výchozí způsob doručení ve stavu je vyzvednuti');
```

Kurýr se dá vybrat a promítne se do ceny.

```js
{
  const cil = [...document.querySelectorAll('button')].find((t) => t.textContent.includes("Diavola"));
  assert.ok(cil, 'Na stránce čekám tlačítko s textem ' + "Diavola");
  await helpers.click(cil);
  await helpers.flush();
}

{
  const cil = [...document.querySelectorAll('button')].find((t) => t.textContent.includes("Pokračovat"));
  assert.ok(cil, 'Na stránce čekám tlačítko s textem ' + "Pokračovat");
  await helpers.click(cil);
  await helpers.flush();
}

{
  const cil = [...document.querySelectorAll('button')].find((t) => t.textContent.includes("Pokračovat"));
  assert.ok(cil, 'Na stránce čekám tlačítko s textem ' + "Pokračovat");
  await helpers.click(cil);
  await helpers.flush();
}

{
  const cil = [...document.querySelectorAll('button')].find((t) => t.textContent.includes("Kurýr až domů"));
  assert.ok(cil, 'Na stránce čekám tlačítko s textem ' + "Kurýr až domů");
  await helpers.click(cil);
  await helpers.flush();
}

const vybrane = [...document.querySelectorAll('.polozka')].filter((t) => t.getAttribute('aria-pressed') === 'true');
assert.equal(vybrane.length, 1, 'Vybraný má zůstat právě jeden způsob doručení');
assert.match(document.querySelector('.lista__cena').textContent, /278\s*Kč/, 'Diavola (219) ve střední velikosti s kurýrem (59) stojí 278 Kč');
```

Doprava zdarma je v nabídce označená slovem, ne nulou.

```js
{
  const cil = [...document.querySelectorAll('button')].find((t) => t.textContent.includes("Diavola"));
  assert.ok(cil, 'Na stránce čekám tlačítko s textem ' + "Diavola");
  await helpers.click(cil);
  await helpers.flush();
}

{
  const cil = [...document.querySelectorAll('button')].find((t) => t.textContent.includes("Pokračovat"));
  assert.ok(cil, 'Na stránce čekám tlačítko s textem ' + "Pokračovat");
  await helpers.click(cil);
  await helpers.flush();
}

{
  const cil = [...document.querySelectorAll('button')].find((t) => t.textContent.includes("Pokračovat"));
  assert.ok(cil, 'Na stránce čekám tlačítko s textem ' + "Pokračovat");
  await helpers.click(cil);
  await helpers.flush();
}

const vyzvednuti = [...document.querySelectorAll('.polozka')].find((t) => /Vyzvednu si sám/.test(t.textContent));
assert.match(vyzvednuti.textContent, /zdarma/, 'U vyzvednutí (cena 0) má být napsané „zdarma"');
```

# --seed--

## --file-- App.jsx

```jsx
import { useReducer } from 'react';
import { pizzy, prilohy, velikosti } from './menu';
import { spocitejCenu } from './cena';

const VYCHOZI = {
  krok: 1,
  pizza: null,
  velikost: 'stredni',
  prilohy: [],
  doruceni: 'vyzvednuti',
};

function objednavkaReducer(stav, akce) {
  switch (akce.typ) {
    case 'pizza':
      return { ...stav, pizza: akce.id };
    case 'dalsi':
      return { ...stav, krok: stav.krok + 1 };
    case 'zpet':
      return { ...stav, krok: stav.krok - 1 };
    case 'velikost':
      return { ...stav, velikost: akce.id };
    case 'reset':
      return VYCHOZI;
    case 'priloha': {
      const uz = stav.prilohy.includes(akce.id);
      return {
        ...stav,
        prilohy: uz ? stav.prilohy.filter((id) => id !== akce.id) : [...stav.prilohy, akce.id],
      };
    }
    default:
      throw new Error('Neznámá akce: ' + akce.typ);
  }
}

function KrokJedna({ stav, poslat }) {
  return (
    <section className="sekce">
      <h2 className="sekce__nadpis">Vyber si pizzu</h2>
      <ul className="nabidka">
        {pizzy.map((pizza) => (
          <li key={pizza.id}>
            <button
              type="button"
              className="polozka"
              aria-pressed={stav.pizza === pizza.id}
              onClick={() => poslat({ typ: 'pizza', id: pizza.id })}
            >
              {pizza.stitek && <span className="polozka__stitek">{pizza.stitek}</span>}
              <p className="polozka__nazev">{pizza.nazev}</p>
              <p className="polozka__popis">{pizza.popis}</p>
              <p className="polozka__cena">{pizza.cena} Kč</p>
            </button>
          </li>
        ))}
      </ul>
    </section>
  );
}

function KrokDva({ stav, poslat }) {
  return (
    <section className="sekce">
      <h2 className="sekce__nadpis">Velikost a přílohy</h2>
      <ul className="nabidka">
        {velikosti.map((velikost) => (
          <li key={velikost.id}>
            <button
              type="button"
              className="polozka"
              aria-pressed={stav.velikost === velikost.id}
              onClick={() => poslat({ typ: 'velikost', id: velikost.id })}
            >
              <p className="polozka__nazev">{velikost.nazev}</p>
              <p className="polozka__popis">{velikost.prumer}</p>
            </button>
          </li>
        ))}
      </ul>
      <h3 className="sekce__nadpis">Přílohy navíc</h3>
      <ul className="nabidka">
        {prilohy.map((priloha) => (
          <li key={priloha.id}>
            <button
              type="button"
              className="polozka"
              aria-pressed={stav.prilohy.includes(priloha.id)}
              onClick={() => poslat({ typ: 'priloha', id: priloha.id })}
            >
              <p className="polozka__nazev">{priloha.nazev}</p>
              <p className="polozka__cena">+{priloha.cena} Kč</p>
            </button>
          </li>
        ))}
      </ul>
    </section>
  );
}

function KrokTri() {
  return (
    <section className="sekce">
      <h2 className="sekce__nadpis">Doručení</h2>
    </section>
  );
}

function Lista({ stav, poslat }) {
  return (
    <div className="lista">
      <div className="lista__obsah">
        <p className="lista__cena">
          {stav.pizza ? `Celkem ${spocitejCenu(stav)} Kč` : 'Zatím nic nevybráno'}
        </p>
        {stav.krok > 1 && (
          <button type="button" className="tlacitko tlacitko--tichy" onClick={() => poslat({ typ: 'zpet' })}>
            Zpět
          </button>
        )}
        {stav.krok < 3 && (
          <button type="button" className="tlacitko" onClick={() => poslat({ typ: 'dalsi' })}>
            Pokračovat
          </button>
        )}
        <button type="button" className="tlacitko tlacitko--tichy" onClick={() => poslat({ typ: 'reset' })}>
          Začít znovu
        </button>
      </div>
    </div>
  );
}

export default function App() {
  const [stav, poslat] = useReducer(objednavkaReducer, VYCHOZI);

  return (
    <div className="objednavka">
      <h1 className="objednavka__nadpis">Pizzeria Na Rohu</h1>
      <p className="objednavka__popis">Objednávka ve třech krocích: pizza, velikost a doručení.</p>

      {stav.krok === 1 && <KrokJedna stav={stav} poslat={poslat} />}
      {stav.krok === 2 && <KrokDva stav={stav} poslat={poslat} />}
      {stav.krok === 3 && <KrokTri stav={stav} poslat={poslat} />}

      <Lista stav={stav} poslat={poslat} />
    </div>
  );
}
```

## --file-- cena.js

```js
import { pizzy, prilohy, velikosti, zpusobyDoruceni } from './menu';

/**
 * Spočítá celkovou cenu objednávky: pizzu přepočtenou na vybranou velikost,
 * přílohy a dopravu. Dokud není vybraná pizza, vrací nulu.
 */
export function spocitejCenu(stav) {
const pizza = pizzy.find((p) => p.id === stav.pizza);
if (!pizza) return 0;
const velikost = velikosti.find((v) => v.id === stav.velikost);
const doprava = zpusobyDoruceni.find((z) => z.id === stav.doruceni);
const priplatky = stav.prilohy.reduce((soucet, id) => soucet + prilohy.find((p) => p.id === id).cena, 0);
return Math.round(pizza.cena * velikost.koeficient) + priplatky + doprava.cena;
}
```

## --file-- menu.js

```js
// Nabídka pizzerie. Ceny jsou v korunách za střední velikost.
export const pizzy = [
  { id: 'margherita', nazev: 'Margherita', popis: 'Rajčatový základ, mozzarella, čerstvá bazalka', cena: 179, stitek: 'vegetariánská' },
  { id: 'diavola', nazev: 'Diavola', popis: 'Pikantní salám, chilli, mozzarella', cena: 219, stitek: 'pálivá' },
  { id: 'formaggi', nazev: 'Quattro formaggi', popis: 'Mozzarella, gorgonzola, parmazán, niva', cena: 239, stitek: 'sýrová' },
  { id: 'prosciutto', nazev: 'Prosciutto e funghi', popis: 'Šunka, žampiony, mozzarella', cena: 209, stitek: null },
];

export const velikosti = [
  { id: 'mala', nazev: 'Malá', prumer: '26 cm', koeficient: 0.8 },
  { id: 'stredni', nazev: 'Střední', prumer: '32 cm', koeficient: 1 },
  { id: 'velka', nazev: 'Velká', prumer: '40 cm', koeficient: 1.35 },
];

export const prilohy = [
  { id: 'olivy', nazev: 'Olivy', cena: 25 },
  { id: 'rukola', nazev: 'Rukola', cena: 29 },
  { id: 'niva', nazev: 'Niva navíc', cena: 35 },
  { id: 'vejce', nazev: 'Vejce', cena: 19 },
];

export const zpusobyDoruceni = [
  { id: 'vyzvednuti', nazev: 'Vyzvednu si sám', popis: 'Moravské náměstí, do 25 minut', cena: 0 },
  { id: 'kuryr', nazev: 'Kurýr až domů', popis: 'Brno-střed, do 45 minut', cena: 59 },
];
```

## --file-- styles.css

```css
:root {
  --plocha: #faf7f2;
  --papir: #ffffff;
  --text: #1f1a16;
  --text-tlumeny: #6b625b;
  --linka: #e8e1d8;
  --akce: #c2410c;
  --akce-tmava: #9a3412;
  --uspech: #15803d;
  --radius: 0.85rem;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  padding: 2rem 1.25rem 7rem;
  min-height: 100vh;
  background: var(--plocha);
  color: var(--text);
  font-family: system-ui, "Segoe UI", sans-serif;
  line-height: 1.5;
}

.objednavka {
  margin: 0 auto;
  max-width: 46rem;
}

.objednavka__nadpis {
  margin: 0 0 0.25rem;
  font-size: 1.7rem;
  letter-spacing: -0.01em;
}

.objednavka__popis {
  margin: 0 0 1.5rem;
  color: var(--text-tlumeny);
}

.kroky {
  display: flex;
  gap: 0.5rem;
  margin: 0 0 1.5rem;
  padding: 0;
  list-style: none;
}

.kroky__krok {
  flex: 1;
  padding: 0.5rem 0.75rem;
  border-radius: 999px;
  background: var(--papir);
  color: var(--text-tlumeny);
  font-size: 0.9rem;
  text-align: center;
}

.kroky__krok--aktivni {
  background: var(--akce);
  color: #fff;
  font-weight: 650;
}

.nabidka {
  display: grid;
  gap: 0.85rem;
  grid-template-columns: repeat(auto-fill, minmax(14rem, 1fr));
  margin: 0;
  padding: 0;
  list-style: none;
}

.polozka {
  display: flex;
  flex-direction: column;
  gap: 0.3rem;
  padding: 1rem;
  width: 100%;
  height: 100%;
  border: 2px solid transparent;
  border-radius: var(--radius);
  background: var(--papir);
  color: inherit;
  font: inherit;
  text-align: left;
  cursor: pointer;
  box-shadow: 0 1px 2px rgb(31 26 22 / 0.1);
  transition: border-color 0.15s ease, transform 0.15s ease;
}

.polozka:hover {
  transform: translateY(-2px);
}

.polozka:focus-visible {
  outline: 3px solid var(--akce);
  outline-offset: 2px;
}

.polozka[aria-pressed='true'],
.polozka[aria-checked='true'] {
  border-color: var(--akce);
  background: #fff7ed;
}

.polozka__nazev {
  margin: 0;
  font-size: 1.05rem;
  font-weight: 650;
}

.polozka__popis {
  margin: 0;
  color: var(--text-tlumeny);
  font-size: 0.9rem;
}

.polozka__cena {
  margin-top: auto;
  padding-top: 0.5rem;
  font-variant-numeric: tabular-nums;
  font-weight: 650;
}

.polozka__stitek {
  align-self: flex-start;
  padding: 0.1rem 0.5rem;
  border-radius: 999px;
  background: #fef3c7;
  color: #92400e;
  font-size: 0.75rem;
  letter-spacing: 0.04em;
  text-transform: uppercase;
}

.sekce {
  margin: 0 0 1.5rem;
}

.sekce__nadpis {
  margin: 0 0 0.75rem;
  font-size: 1.15rem;
}

.rekapitulace {
  padding: 1.25rem;
  border-radius: var(--radius);
  background: var(--papir);
  box-shadow: 0 1px 2px rgb(31 26 22 / 0.1);
}

.rekapitulace__radek {
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  margin: 0.35rem 0;
  color: var(--text-tlumeny);
}

.rekapitulace__radek--soucet {
  margin-top: 0.75rem;
  padding-top: 0.75rem;
  border-top: 1px solid var(--linka);
  color: var(--text);
  font-size: 1.1rem;
  font-weight: 650;
}

.lista {
  display: flex;
  position: fixed;
  right: 0;
  bottom: 0;
  left: 0;
  align-items: center;
  gap: 1rem;
  padding: 0.85rem 1.25rem;
  border-top: 1px solid var(--linka);
  background: var(--papir);
}

.lista__obsah {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin: 0 auto;
  width: 100%;
  max-width: 46rem;
}

.lista__cena {
  margin: 0;
  margin-right: auto;
  font-size: 1.2rem;
  font-weight: 650;
  font-variant-numeric: tabular-nums;
}

.tlacitko {
  padding: 0.6rem 1.1rem;
  border: 1px solid transparent;
  border-radius: 0.55rem;
  background: var(--akce);
  color: #fff;
  font: inherit;
  cursor: pointer;
  transition: background 0.15s ease;
}

.tlacitko:hover:not(:disabled) {
  background: var(--akce-tmava);
}

.tlacitko:disabled {
  opacity: 0.45;
  cursor: not-allowed;
}

.tlacitko:focus-visible {
  outline: 3px solid #f59e0b;
  outline-offset: 2px;
}

.tlacitko--tichy {
  border-color: var(--linka);
  background: var(--papir);
  color: var(--text);
}

.tlacitko--tichy:hover:not(:disabled) {
  background: var(--plocha);
}

.hotovo {
  padding: 2.5rem 1.5rem;
  border-radius: var(--radius);
  background: var(--papir);
  text-align: center;
  box-shadow: 0 1px 2px rgb(31 26 22 / 0.1);
}

.hotovo__nadpis {
  margin: 0 0 0.5rem;
  color: var(--uspech);
  font-size: 1.5rem;
}

@media (prefers-reduced-motion: reduce) {
  .polozka {
    transition: none;
  }

  .polozka:hover {
    transform: none;
  }
}
```

# --solution--

## --file-- App.jsx

```jsx
import { useReducer } from 'react';
import { pizzy, prilohy, velikosti, zpusobyDoruceni } from './menu';
import { spocitejCenu } from './cena';

const VYCHOZI = {
  krok: 1,
  pizza: null,
  velikost: 'stredni',
  prilohy: [],
  doruceni: 'vyzvednuti',
};

function objednavkaReducer(stav, akce) {
  switch (akce.typ) {
    case 'pizza':
      return { ...stav, pizza: akce.id };
    case 'dalsi':
      return { ...stav, krok: stav.krok + 1 };
    case 'zpet':
      return { ...stav, krok: stav.krok - 1 };
    case 'velikost':
      return { ...stav, velikost: akce.id };
    case 'doruceni':
      return { ...stav, doruceni: akce.id };
    case 'reset':
      return VYCHOZI;
    case 'priloha': {
      const uz = stav.prilohy.includes(akce.id);
      return {
        ...stav,
        prilohy: uz ? stav.prilohy.filter((id) => id !== akce.id) : [...stav.prilohy, akce.id],
      };
    }
    default:
      throw new Error('Neznámá akce: ' + akce.typ);
  }
}

function KrokJedna({ stav, poslat }) {
  return (
    <section className="sekce">
      <h2 className="sekce__nadpis">Vyber si pizzu</h2>
      <ul className="nabidka">
        {pizzy.map((pizza) => (
          <li key={pizza.id}>
            <button
              type="button"
              className="polozka"
              aria-pressed={stav.pizza === pizza.id}
              onClick={() => poslat({ typ: 'pizza', id: pizza.id })}
            >
              {pizza.stitek && <span className="polozka__stitek">{pizza.stitek}</span>}
              <p className="polozka__nazev">{pizza.nazev}</p>
              <p className="polozka__popis">{pizza.popis}</p>
              <p className="polozka__cena">{pizza.cena} Kč</p>
            </button>
          </li>
        ))}
      </ul>
    </section>
  );
}

function KrokDva({ stav, poslat }) {
  return (
    <section className="sekce">
      <h2 className="sekce__nadpis">Velikost a přílohy</h2>
      <ul className="nabidka">
        {velikosti.map((velikost) => (
          <li key={velikost.id}>
            <button
              type="button"
              className="polozka"
              aria-pressed={stav.velikost === velikost.id}
              onClick={() => poslat({ typ: 'velikost', id: velikost.id })}
            >
              <p className="polozka__nazev">{velikost.nazev}</p>
              <p className="polozka__popis">{velikost.prumer}</p>
            </button>
          </li>
        ))}
      </ul>
      <h3 className="sekce__nadpis">Přílohy navíc</h3>
      <ul className="nabidka">
        {prilohy.map((priloha) => (
          <li key={priloha.id}>
            <button
              type="button"
              className="polozka"
              aria-pressed={stav.prilohy.includes(priloha.id)}
              onClick={() => poslat({ typ: 'priloha', id: priloha.id })}
            >
              <p className="polozka__nazev">{priloha.nazev}</p>
              <p className="polozka__cena">+{priloha.cena} Kč</p>
            </button>
          </li>
        ))}
      </ul>
    </section>
  );
}

function KrokTri({ stav, poslat }) {
  return (
    <section className="sekce">
      <h2 className="sekce__nadpis">Doručení</h2>
      <ul className="nabidka">
        {zpusobyDoruceni.map((zpusob) => (
          <li key={zpusob.id}>
            <button
              type="button"
              className="polozka"
              aria-pressed={stav.doruceni === zpusob.id}
              onClick={() => poslat({ typ: 'doruceni', id: zpusob.id })}
            >
              <p className="polozka__nazev">{zpusob.nazev}</p>
              <p className="polozka__popis">{zpusob.popis}</p>
              <p className="polozka__cena">{zpusob.cena === 0 ? 'zdarma' : `+${zpusob.cena} Kč`}</p>
            </button>
          </li>
        ))}
      </ul>
    </section>
  );
}

function Lista({ stav, poslat }) {
  return (
    <div className="lista">
      <div className="lista__obsah">
        <p className="lista__cena">
          {stav.pizza ? `Celkem ${spocitejCenu(stav)} Kč` : 'Zatím nic nevybráno'}
        </p>
        {stav.krok > 1 && (
          <button type="button" className="tlacitko tlacitko--tichy" onClick={() => poslat({ typ: 'zpet' })}>
            Zpět
          </button>
        )}
        {stav.krok < 3 && (
          <button type="button" className="tlacitko" onClick={() => poslat({ typ: 'dalsi' })}>
            Pokračovat
          </button>
        )}
        <button type="button" className="tlacitko tlacitko--tichy" onClick={() => poslat({ typ: 'reset' })}>
          Začít znovu
        </button>
      </div>
    </div>
  );
}

export default function App() {
  const [stav, poslat] = useReducer(objednavkaReducer, VYCHOZI);

  return (
    <div className="objednavka">
      <h1 className="objednavka__nadpis">Pizzeria Na Rohu</h1>
      <p className="objednavka__popis">Objednávka ve třech krocích: pizza, velikost a doručení.</p>

      {stav.krok === 1 && <KrokJedna stav={stav} poslat={poslat} />}
      {stav.krok === 2 && <KrokDva stav={stav} poslat={poslat} />}
      {stav.krok === 3 && <KrokTri stav={stav} poslat={poslat} />}

      <Lista stav={stav} poslat={poslat} />
    </div>
  );
}
```

