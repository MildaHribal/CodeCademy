---
title: Velikosti
---

# --description--

**Úkol:** Přidej do `buttonVariants` druhou variantu `size` s hodnotami `sm` a `md` a doplň ji do
výchozích variant (`md`). Třídy, které se liší podle velikosti (výška, odsazení, velikost písma),
musí odejít ze základních tříd do varianty — jinak by je `cva` nemělo co přebít.

- `sm`: `h-8 px-3 text-xs`
- `md`: `h-10 px-4 text-sm`

Pak v `App.tsx` vykresli volné termíny jako malá tichá tlačítka (`<Button variant="ticha" size="sm">`)
s třídou `border border-slate-200`, ať jsou vidět. Termíny se tím z popisků stanou ovladatelnými
prvky — v náhledu si na ně zkus najet myší.

# --hints--

Termíny u Jany jsou tlačítka vysoká 32 px s malým písmem.

```js
const karta = document.querySelector('[data-testid="karta-jana"]');
const terminy = [...karta.querySelectorAll('button')].filter((el) => /^\d{1,2}:\d{2}$/.test(el.textContent.trim()));
assert.equal(terminy.length, 3, 'Karta Jany má mít tři tlačítka s termíny (9:30, 11:00, 14:00)');
const styl = getComputedStyle(terminy[0]);
await helpers.waitFor(() => styl.height === '32px', 3000).catch(() => {});
assert.equal(styl.height, '32px', 'Termín má být malé tlačítko (size sm, h-8)');
assert.equal(styl.fontSize, '12px', 'Termín má mít text-xs, tedy 12 px');
assert.equal(styl.paddingLeft, '12px', 'Termín má mít odsazení px-3, tedy 12 px');
```

Tlačítka bez velikosti zůstala velká.

```js
const karta = document.querySelector('[data-testid="karta-jana"]');
const rezervovat = [...karta.querySelectorAll('button')].find((el) => el.textContent.trim() === 'Rezervovat');
const styl = getComputedStyle(rezervovat);
await helpers.waitFor(() => styl.height === '40px', 3000).catch(() => {});
assert.equal(styl.height, '40px', 'Bez props size má platit výchozí velikost md (40 px)');
assert.equal(styl.fontSize, '14px', 'Výchozí velikost má mít text-sm, tedy 14 px');
```

Velikost a varianta se kombinují nezávisle.

```js
const { buttonVariants } = await helpers.importFile('components/ui/button.tsx');
const male = buttonVariants({ variant: 'ticha', size: 'sm' });
assert.match(male, /h-8/, "buttonVariants({ variant: 'ticha', size: 'sm' }) má obsahovat h-8");
assert.match(male, /bg-transparent/, 'malé tiché tlačítko má mít i třídy tiché varianty');
assert.doesNotMatch(buttonVariants({ size: 'sm' }), /h-10/, 'malé tlačítko už nesmí nést výšku h-10 ze základních tříd');
```

# --help--

## --tip--

Varianty jsou nezávislé osy vzhledu: tvar a barva zvlášť, velikost zvlášť. V `cva` je to druhý klíč v objektu `variants`.

## --tip--

Co se mezi velikostmi liší, nesmí zůstat v základním řetězci tříd. Projdi základní třídy a odstěhuj do variant ty, které mění výšku, odsazení a písmo.

## --tip--

Každá nová osa patří i do výchozích variant a do typu props — jinak komponenta o velikosti neví.

# --seed--

## --file-- styles.css

```css
@import "tailwindcss";

@theme {
  --color-znacka: #0f766e;
  --color-znacka-tmava: #115e59;
  --color-znacka-svetla: #ccfbf1;
  --color-nebezpeci: #be123c;
  --color-plocha: #f6f7f9;
  --color-tlumena: #e2e8f0;
  --color-popisek: #475569;
  --radius-karta: 1rem;
}
```

## --file-- data.ts

```ts
export type Kadernice = {
  id: string;
  jmeno: string;
  iniciely: string;
  specializace: string;
  cenaOd: number;
  volneTerminy: string[];
  fotka: string;
};

export const kadernice: Kadernice[] = [
  {
    id: 'jana',
    jmeno: 'Jana Procházková',
    iniciely: 'JP',
    specializace: 'Střih a barvení',
    cenaOd: 890,
    volneTerminy: ['9:30', '11:00', '14:00'],
    fotka: '/fotky/jana.jpg',
  },
  {
    id: 'marek',
    jmeno: 'Marek Dvořák',
    iniciely: 'MD',
    specializace: 'Pánské střihy a úprava vousů',
    cenaOd: 590,
    volneTerminy: ['10:00', '15:30'],
    fotka: '/fotky/marek.jpg',
  },
  {
    id: 'tereza',
    jmeno: 'Tereza Kučerová',
    iniciely: 'TK',
    specializace: 'Svatební a společenské účesy',
    cenaOd: 1490,
    volneTerminy: [],
    fotka: '/fotky/tereza.jpg',
  },
];
```

## --file-- App.tsx

```tsx
import { useState, type FormEvent } from 'react';
import { Badge } from './components/ui/badge';
import { Button } from './components/ui/button';
import { kadernice } from './data';
import './styles.css';

export default function App() {
  const [jmeno, setJmeno] = useState('');
  const [chyba, setChyba] = useState('');

  function odeslat(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    setChyba(jmeno.trim() === '' ? 'Vyplň jméno, ať víme, komu se ozvat.' : '');
  }

  return (
    <main className="min-h-screen bg-plocha px-6 py-10 text-slate-900">
      <div className="mx-auto max-w-3xl">
        <header className="mb-6">
          <h1 className="text-2xl font-semibold">Střih &amp; Styl</h1>
          <p className="mt-1 text-slate-600">Rezervace na dnešek, Ostrava-Poruba.</p>
        </header>

        <div className="mb-6 flex items-center justify-between gap-4 rounded-karta border border-slate-200 bg-white px-6 py-4">
          <p className="text-sm">Máš rezervaci na 16:00 u Marka Dvořáka.</p>
          <Button>Zrušit rezervaci</Button>
        </div>

        <ul className="flex flex-col gap-4">
          {kadernice.map((osoba) => (
            <li
              key={osoba.id}
              data-testid={'karta-' + osoba.id}
              className="rounded-karta border border-slate-200 bg-white p-6"
            >
              <div className="flex items-start justify-between gap-4">
                <div>
                  <h2 className="text-lg font-semibold">{osoba.jmeno}</h2>
                  <p className="text-sm text-slate-600">{osoba.specializace}</p>
                </div>
                {osoba.volneTerminy.length > 0 ? (
                  <Badge title={'Nejbližší termín ' + osoba.volneTerminy[0]}>Volno dnes</Badge>
                ) : (
                  <Badge className="bg-tlumena text-popisek" title="Dnes už má plno">
                    Dnes obsazeno
                  </Badge>
                )}
              </div>

              <div className="mt-4">
                <p className="text-sm text-slate-600">Cena od {osoba.cenaOd} Kč</p>
                <div className="mt-3 flex flex-wrap gap-2">
                  {osoba.volneTerminy.map((cas) => (
                    <span key={cas} className="rounded-lg border border-slate-200 px-3 py-1 text-sm">
                      {cas}
                    </span>
                  ))}
                </div>
              </div>

              <div className="mt-6 flex flex-wrap gap-3">
                <Button>Rezervovat</Button>
                <Button variant="ticha">Zobrazit profil</Button>
              </div>
            </li>
          ))}
        </ul>

        <form onSubmit={odeslat} data-testid="zadost" className="mt-8 rounded-karta border border-slate-200 bg-white p-6">
          <h2 className="text-lg font-semibold">Nevyhovuje ti žádný termín?</h2>
          <p className="mt-1 text-sm text-slate-600">Nech nám jméno a ozveme se s náhradním termínem.</p>
          <div className="mt-4 flex items-start gap-3">
            <input
              data-testid="pole-jmeno"
              value={jmeno}
              onChange={(event) => setJmeno(event.target.value)}
              placeholder="Jméno a příjmení"
              className="h-10 w-full rounded-lg border border-slate-300 px-3 text-sm"
            />
            <Button>Odeslat</Button>
          </div>
          {chyba !== '' && <p className="mt-2 text-sm text-nebezpeci">{chyba}</p>}
        </form>
      </div>
    </main>
  );
}
```

## --file-- lib/utils.ts

```ts
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

## --file-- components/ui/badge.tsx

```tsx
import type { ComponentProps } from 'react';
import { cn } from '../../lib/utils';

export function Badge({ className, ...props }: ComponentProps<'span'>) {
  return (
    <span
      className={cn(
        'inline-flex items-center rounded-full bg-znacka-svetla px-3 py-1 text-xs font-medium text-znacka',
        className,
      )}
      {...props}
    />
  );
}
```

## --file-- components/ui/button.tsx

```tsx
import type { ComponentProps } from 'react';
import { cn } from '../../lib/utils';

import { cva } from 'class-variance-authority';
export const buttonVariants = cva('inline-flex h-10 items-center justify-center rounded-lg px-4 text-sm font-medium transition-colors', {
  variants: {
    variant: {
      plna: 'bg-znacka text-white hover:bg-znacka-tmava',
      ticha: 'bg-transparent text-slate-700 hover:bg-slate-100',
    },
  },
  defaultVariants: { variant: 'plna' },
});

type ButtonProps = ComponentProps<'button'> & { variant?: 'plna' | 'ticha' };

export function Button({ className, variant, ...props }: ButtonProps) {
  return <button className={cn(buttonVariants({ variant }), className)} {...props} />;
}
```

# --solution--

## --file-- components/ui/button.tsx

```tsx
import type { ComponentProps } from 'react';
import { cn } from '../../lib/utils';

import { cva } from 'class-variance-authority';
export const buttonVariants = cva('inline-flex items-center justify-center rounded-lg font-medium transition-colors', {
  variants: {
    variant: {
      plna: 'bg-znacka text-white hover:bg-znacka-tmava',
      ticha: 'bg-transparent text-slate-700 hover:bg-slate-100',
    },
    size: {
      sm: 'h-8 px-3 text-xs',
      md: 'h-10 px-4 text-sm',
    },
  },
  defaultVariants: { variant: 'plna', size: 'md' },
});

type ButtonProps = ComponentProps<'button'> & { variant?: 'plna' | 'ticha'; size?: 'sm' | 'md' };

export function Button({ className, variant, size, ...props }: ButtonProps) {
  return <button className={cn(buttonVariants({ variant, size }), className)} {...props} />;
}
```

## --file-- App.tsx

```tsx
import { useState, type FormEvent } from 'react';
import { Badge } from './components/ui/badge';
import { Button } from './components/ui/button';
import { kadernice } from './data';
import './styles.css';

export default function App() {
  const [jmeno, setJmeno] = useState('');
  const [chyba, setChyba] = useState('');

  function odeslat(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    setChyba(jmeno.trim() === '' ? 'Vyplň jméno, ať víme, komu se ozvat.' : '');
  }

  return (
    <main className="min-h-screen bg-plocha px-6 py-10 text-slate-900">
      <div className="mx-auto max-w-3xl">
        <header className="mb-6">
          <h1 className="text-2xl font-semibold">Střih &amp; Styl</h1>
          <p className="mt-1 text-slate-600">Rezervace na dnešek, Ostrava-Poruba.</p>
        </header>

        <div className="mb-6 flex items-center justify-between gap-4 rounded-karta border border-slate-200 bg-white px-6 py-4">
          <p className="text-sm">Máš rezervaci na 16:00 u Marka Dvořáka.</p>
          <Button>Zrušit rezervaci</Button>
        </div>

        <ul className="flex flex-col gap-4">
          {kadernice.map((osoba) => (
            <li
              key={osoba.id}
              data-testid={'karta-' + osoba.id}
              className="rounded-karta border border-slate-200 bg-white p-6"
            >
              <div className="flex items-start justify-between gap-4">
                <div>
                  <h2 className="text-lg font-semibold">{osoba.jmeno}</h2>
                  <p className="text-sm text-slate-600">{osoba.specializace}</p>
                </div>
                {osoba.volneTerminy.length > 0 ? (
                  <Badge title={'Nejbližší termín ' + osoba.volneTerminy[0]}>Volno dnes</Badge>
                ) : (
                  <Badge className="bg-tlumena text-popisek" title="Dnes už má plno">
                    Dnes obsazeno
                  </Badge>
                )}
              </div>

              <div className="mt-4">
                <p className="text-sm text-slate-600">Cena od {osoba.cenaOd} Kč</p>
                <div className="mt-3 flex flex-wrap gap-2">
                  {osoba.volneTerminy.map((cas) => (
                    <Button key={cas} variant="ticha" size="sm" className="border border-slate-200">
                      {cas}
                    </Button>
                  ))}
                </div>
              </div>

              <div className="mt-6 flex flex-wrap gap-3">
                <Button>Rezervovat</Button>
                <Button variant="ticha">Zobrazit profil</Button>
              </div>
            </li>
          ))}
        </ul>

        <form onSubmit={odeslat} data-testid="zadost" className="mt-8 rounded-karta border border-slate-200 bg-white p-6">
          <h2 className="text-lg font-semibold">Nevyhovuje ti žádný termín?</h2>
          <p className="mt-1 text-sm text-slate-600">Nech nám jméno a ozveme se s náhradním termínem.</p>
          <div className="mt-4 flex items-start gap-3">
            <input
              data-testid="pole-jmeno"
              value={jmeno}
              onChange={(event) => setJmeno(event.target.value)}
              placeholder="Jméno a příjmení"
              className="h-10 w-full rounded-lg border border-slate-300 px-3 text-sm"
            />
            <Button>Odeslat</Button>
          </div>
          {chyba !== '' && <p className="mt-2 text-sm text-nebezpeci">{chyba}</p>}
        </form>
      </div>
    </main>
  );
}
```
