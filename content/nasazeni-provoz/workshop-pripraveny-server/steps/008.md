---
title: Server loguje přes logger
main: server.js
see: nasazeni-provoz/logovani-a-chyby#urovne-logu
---

# --description--

Logger je hotový, teď ho zapoj do `server.js`. Po startu dnes server vypíše
`Půjčovna kol běží na portu 8080`. Nástroj na logy z téhle věty port nevytáhne
a řádek nemá úroveň ani čas.

**Úkol:** v `server.js` vytvoř jeden logger s minimální úrovní z konfigurace
a hlášku po startu zapiš přes něj: úroveň `info`, stálá zpráva (třeba
`Server poslouchá`) a port jako samostatné číselné pole `port`. `console.log` ze
`server.js` zmizí.

Kandidáti: `createLogger` z `./logger.js`, klíč `logLevel` z `loadConfig`, metoda
`logger.info(zpráva, pole)`.

Po **Spustit** uvidíš ve výstupu místo věty řádek JSON. Zkus v terminálu
`LOG_LEVEL=warn node server.js`: řádek po startu zmizí, protože `info` je pod `warn`.

# --hints--

Po startu server zapíše řádek JSON s úrovní `info` a číselným polem `port` s portem, na kterém poslouchá.

```js
const server = await helpers.startServer('server.js', { timeoutMs: 4000 });
const parse = (line) => { try { return JSON.parse(line); } catch { return null; } };
const entry = await helpers.waitFor(
  () => server.output().split('\n').map(parse).find((item) => item && item.port === server.port),
  2000,
).catch(() => assert.fail(`Po startu má server zapsat řádek JSON s polem port: ${server.port}. Výstup serveru:\n${server.output()}`));
assert.equal(entry.level, 'info', 'Řádek po startu má mít level "info"');
assert.equal(typeof entry.msg, 'string', 'Řádek po startu má mít zprávu msg');
```

Výstup serveru neobsahuje žádný řádek, který není JSON.

```js
const server = await helpers.startServer('server.js', { timeoutMs: 4000 });
await helpers.waitFor(() => server.output().trim() !== '', 2000).catch(() => {});
await fetch(new URL('/api/bikes', server.url));
const other = server.output().split('\n').filter((line) => line.trim() !== '').filter((line) => {
  try { JSON.parse(line); return false; } catch { return true; }
});
assert.deepEqual(other, [], 'Každý řádek výstupu serveru má být JSON — zbyl tam console.log?');
```

S `LOG_LEVEL=warn` se řádek po startu nezapíše.

```js
const server = await helpers.startServer('server.js', { env: { LOG_LEVEL: 'warn' }, timeoutMs: 4000 });
await fetch(new URL('/api/bikes', server.url));
await helpers.wait(300);
assert.doesNotMatch(server.output(), new RegExp(`"port":${server.port}`), 'S LOG_LEVEL=warn se řádek info po startu nemá zapsat — dostává createLogger level z konfigurace?');
```

# --help--

## --tip--

Minimální úroveň patří do konfigurace, aby šla změnit bez nové verze — viz
[Úrovně logů](see:nasazeni-provoz/logovani-a-chyby#urovne-logu).

## --tip--

Logger vytvoř jednou nahoře vedle `config` a `db`. `createLogger` dostane objekt
s klíčem `level`. Volání patří do callbacku `server.listen`.

## --tip--

Stejný vzor v jiné službě: `const log = createLogger({ level: settings.logLevel });`
a pak `log.info('Fronta připravena', { workers: settings.workers });`.

# --seed--

## --file-- server.js

```js
import { createServer } from 'node:http';
import { loadConfig } from './config.js';
import { openDatabase } from './db.js';
import { createApp } from './app.js';

const config = loadConfig(process.env);
const db = openDatabase(config.databaseFile);
const server = createServer(createApp({ db }));

server.listen(config.port, () => {
  console.log(`Půjčovna kol běží na portu ${config.port}`);
});
```

## --file-- logger.js

```js
// Strukturovaný logger: jedna událost = jeden řádek JSON na standardní výstup.

const LEVELS = ['debug', 'info', 'warn', 'error'];
const writeToStdout = (line) => process.stdout.write(`${line}\n`);

export function createLogger({ level = 'info', write = writeToStdout } = {}) {
  const minIndex = LEVELS.indexOf(level);

  function log(entryLevel, msg, fields = {}) {
    if (LEVELS.indexOf(entryLevel) < minIndex) return;
    const entry = { time: new Date().toISOString(), level: entryLevel, msg, ...fields };
    write(JSON.stringify(entry));
  }

  return {
    debug: (msg, fields) => log('debug', msg, fields),
    info: (msg, fields) => log('info', msg, fields),
    warn: (msg, fields) => log('warn', msg, fields),
    error: (msg, fields) => log('error', msg, fields),
  };
}
```

## --file-- config.js

```js
// Veškeré nastavení aplikace se čte tady, na jednom místě, při startu.

export function loadConfig(env) {
  if (env.NODE_ENV === 'production' && !env.DATABASE_FILE) {
    throw new Error('Chybí proměnná prostředí DATABASE_FILE.');
  }
  return {
    port: Number(env.PORT ?? 3000),
    databaseFile: env.DATABASE_FILE ?? 'pujcovna-dev.db',
    logLevel: env.LOG_LEVEL ?? 'info',
    shutdownTimeoutMs: Number(env.SHUTDOWN_TIMEOUT_MS ?? 8000),
  };
}
```

## --file-- app.js

```js
// HTTP API půjčovny kol. Server (server.js) sem posílá každý požadavek.

async function readJson(req) {
  let body = '';
  for await (const chunk of req) body += chunk;
  return JSON.parse(body);
}

export function createApp({ db }) {
  function sendJson(res, status, data) {
    res.writeHead(status, { 'Content-Type': 'application/json; charset=utf-8' });
    res.end(JSON.stringify(data));
  }

  async function handle(req, res) {
    const { pathname } = new URL(req.url, 'http://localhost');

    if (req.method === 'GET' && pathname === '/health') {
      try {
        db.prepare('SELECT 1').get();
        return sendJson(res, 200, { status: 'ok' });
      } catch {
        return sendJson(res, 503, { status: 'error' });
      }
    }

    if (req.method === 'GET' && pathname === '/api/bikes') {
      const bikes = db
        .prepare('SELECT id, name, type, station, price_per_day AS pricePerDay FROM bikes ORDER BY id')
        .all();
      return sendJson(res, 200, bikes);
    }

    if (req.method === 'POST' && pathname === '/api/rentals') {
      let input;
      try {
        input = await readJson(req);
      } catch {
        return sendJson(res, 400, { error: 'Tělo požadavku není platný JSON.' });
      }
      const bikeId = Number(input?.bikeId);
      const days = Number(input?.days);
      const customerName = String(input?.customerName ?? '').trim();
      if (!Number.isInteger(bikeId) || !Number.isInteger(days) || days < 1 || customerName === '') {
        return sendJson(res, 400, { error: 'Vyplň kolo, jméno a počet dní (aspoň 1).' });
      }
      const bike = db.prepare('SELECT id, price_per_day AS pricePerDay FROM bikes WHERE id = ?').get(bikeId);
      if (!bike) {
        return sendJson(res, 404, { error: 'Takové kolo v půjčovně nemáme.' });
      }
      const result = db
        .prepare('INSERT INTO rentals (bike_id, customer_name, days, created_at) VALUES (?, ?, ?, ?)')
        .run(bike.id, customerName, days, new Date().toISOString());
      return sendJson(res, 201, {
        id: Number(result.lastInsertRowid),
        bikeId: bike.id,
        days,
        totalPrice: bike.pricePerDay * days,
      });
    }

    sendJson(res, 404, { error: 'Tahle adresa neexistuje.' });
  }

  return (req, res) => {
    handle(req, res);
  };
}
```

## --file-- db.js

```js
import { DatabaseSync } from 'node:sqlite';

const BIKES = [
  ['Superior XC 879', 'horské', 'Brno – Výstaviště', 450],
  ['Author Mission', 'městské', 'Brno – Mendlovo náměstí', 290],
  ['Kellys Tyke 24', 'dětské', 'Brno – Výstaviště', 190],
  ['Specialized Turbo Vado', 'elektrokolo', 'Brno – Lužánky', 890],
  ['Cannondale Topstone', 'gravel', 'Brno – Lužánky', 520],
];

// Otevře databázi půjčovny. Při prvním spuštění založí tabulky a vloží kola.
export function openDatabase(file) {
  const db = new DatabaseSync(file);
  db.exec(`
    CREATE TABLE IF NOT EXISTS bikes (
      id INTEGER PRIMARY KEY,
      name TEXT NOT NULL,
      type TEXT NOT NULL,
      station TEXT NOT NULL,
      price_per_day INTEGER NOT NULL
    );
    CREATE TABLE IF NOT EXISTS rentals (
      id INTEGER PRIMARY KEY,
      bike_id INTEGER NOT NULL REFERENCES bikes (id),
      customer_name TEXT NOT NULL,
      days INTEGER NOT NULL,
      created_at TEXT NOT NULL
    );
  `);
  const { count } = db.prepare('SELECT COUNT(*) AS count FROM bikes').get();
  if (count === 0) {
    const insert = db.prepare('INSERT INTO bikes (name, type, station, price_per_day) VALUES (?, ?, ?, ?)');
    for (const bike of BIKES) insert.run(...bike);
  }
  return db;
}
```

## --file-- package.json

```json
{
  "name": "pujcovna-kol",
  "type": "module"
}
```

# --solution--

## --file-- server.js

```js
import { createServer } from 'node:http';
import { loadConfig } from './config.js';
import { createLogger } from './logger.js';
import { openDatabase } from './db.js';
import { createApp } from './app.js';

const config = loadConfig(process.env);
const logger = createLogger({ level: config.logLevel });
const db = openDatabase(config.databaseFile);
const server = createServer(createApp({ db }));

server.listen(config.port, () => {
  logger.info('Server poslouchá', { port: config.port });
});
```

