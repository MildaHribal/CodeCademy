---
title: Opakování: port z prostředí
kind: recall
main: server.js
see: node-zaklady/co-je-node#process-program-a-svet-kolem-nej
---

# --description--

Brněnská půjčovna kol má malé API: seznam kol (`GET /api/bikes`) a rezervaci
(`POST /api/rentals`). Na notebooku běží bez potíží. V tomhle workshopu z něj uděláš
službu, kterou jde pustit na hosting nebo do Dockeru: nastavení z prostředí, health
check, strukturované logy s request id a vypnutí, které neutne rozpracovanou rezervaci.

Projekt má čtyři soubory:

- `server.js` — spustí server. Tady budeš pracovat nejčastěji.
- `app.js` — obsluha požadavků. Funkce `createApp({ db })` vrátí handler pro `createServer`.
- `db.js` — otevře SQLite databázi přes vestavěný modul `node:sqlite` a při prvním
  spuštění do ní vloží pět kol.
- `package.json` — kvůli řádku `"type": "module"`.

Server zkus pustit tlačítkem **Spustit** a v HTTP klientovi zavolej `GET /api/bikes`.

První krok znáš ze sekce o Node, proto bez návodu.

**Úkol:** server v `server.js` poslouchá natvrdo na portu 3000. Hosting ale port
přiděluje v proměnné prostředí `PORT`. Uprav `server.js`, aby poslouchal na portu
z `PORT` a bez ní na 3000. Výpis po startu ať ukazuje skutečný port.

# --hints--

Server poslouchá na portu, který dostane v proměnné prostředí `PORT`.

```js
let server;
try {
  server = await helpers.startServer('server.js', { timeoutMs: 4000 });
} catch (error) {
  assert.fail(`Server nezačal poslouchat na portu z proměnné PORT. Čte ji server.js? ${error.message}`);
}
const res = await fetch(new URL('/api/bikes', server.url));
assert.equal(res.status, 200, 'GET /api/bikes má na portu z PORT vrátit stav 200');
```

Výpis po startu obsahuje skutečný port z `PORT`, ne 3000.

```js
const server = await helpers.startServer('server.js', { timeoutMs: 4000 }).catch((error) =>
  assert.fail(`Server nezačal poslouchat na portu z proměnné PORT: ${error.message}`));
await helpers.waitFor(() => server.output().trim() !== '', 2000).catch(() => {});
assert.match(server.output(), new RegExp(String(server.port)), `Výpis po startu má obsahovat port ${server.port} z proměnné PORT`);
```

# --seed--

## --file-- server.js

```js
import { createServer } from 'node:http';
import { openDatabase } from './db.js';
import { createApp } from './app.js';

const db = openDatabase('pujcovna-dev.db');
const server = createServer(createApp({ db }));
--edit--

server.listen(3000, () => {
  console.log('Půjčovna kol běží na portu 3000');
});
--edit--
```

## --file-- app.js

```js
// HTTP API půjčovny kol. Server (server.js) sem posílá každý požadavek.

async function readJson(req) {
  let body = '';
  for await (const chunk of req) body += chunk;
  return JSON.parse(body);
}

export function createApp({ db }) {
  function sendJson(res, status, data) {
    res.writeHead(status, { 'Content-Type': 'application/json; charset=utf-8' });
    res.end(JSON.stringify(data));
  }

  async function handle(req, res) {
    const { pathname } = new URL(req.url, 'http://localhost');

    if (req.method === 'GET' && pathname === '/api/bikes') {
      const bikes = db
        .prepare('SELECT id, name, type, station, price_per_day AS pricePerDay FROM bikes ORDER BY id')
        .all();
      return sendJson(res, 200, bikes);
    }

    if (req.method === 'POST' && pathname === '/api/rentals') {
      let input;
      try {
        input = await readJson(req);
      } catch {
        return sendJson(res, 400, { error: 'Tělo požadavku není platný JSON.' });
      }
      const bikeId = Number(input?.bikeId);
      const days = Number(input?.days);
      const customerName = String(input?.customerName ?? '').trim();
      if (!Number.isInteger(bikeId) || !Number.isInteger(days) || days < 1 || customerName === '') {
        return sendJson(res, 400, { error: 'Vyplň kolo, jméno a počet dní (aspoň 1).' });
      }
      const bike = db.prepare('SELECT id, price_per_day AS pricePerDay FROM bikes WHERE id = ?').get(bikeId);
      if (!bike) {
        return sendJson(res, 404, { error: 'Takové kolo v půjčovně nemáme.' });
      }
      const result = db
        .prepare('INSERT INTO rentals (bike_id, customer_name, days, created_at) VALUES (?, ?, ?, ?)')
        .run(bike.id, customerName, days, new Date().toISOString());
      return sendJson(res, 201, {
        id: Number(result.lastInsertRowid),
        bikeId: bike.id,
        days,
        totalPrice: bike.pricePerDay * days,
      });
    }

    sendJson(res, 404, { error: 'Tahle adresa neexistuje.' });
  }

  return (req, res) => {
    handle(req, res);
  };
}
```

## --file-- db.js

```js
import { DatabaseSync } from 'node:sqlite';

const BIKES = [
  ['Superior XC 879', 'horské', 'Brno – Výstaviště', 450],
  ['Author Mission', 'městské', 'Brno – Mendlovo náměstí', 290],
  ['Kellys Tyke 24', 'dětské', 'Brno – Výstaviště', 190],
  ['Specialized Turbo Vado', 'elektrokolo', 'Brno – Lužánky', 890],
  ['Cannondale Topstone', 'gravel', 'Brno – Lužánky', 520],
];

// Otevře databázi půjčovny. Při prvním spuštění založí tabulky a vloží kola.
export function openDatabase(file) {
  const db = new DatabaseSync(file);
  db.exec(`
    CREATE TABLE IF NOT EXISTS bikes (
      id INTEGER PRIMARY KEY,
      name TEXT NOT NULL,
      type TEXT NOT NULL,
      station TEXT NOT NULL,
      price_per_day INTEGER NOT NULL
    );
    CREATE TABLE IF NOT EXISTS rentals (
      id INTEGER PRIMARY KEY,
      bike_id INTEGER NOT NULL REFERENCES bikes (id),
      customer_name TEXT NOT NULL,
      days INTEGER NOT NULL,
      created_at TEXT NOT NULL
    );
  `);
  const { count } = db.prepare('SELECT COUNT(*) AS count FROM bikes').get();
  if (count === 0) {
    const insert = db.prepare('INSERT INTO bikes (name, type, station, price_per_day) VALUES (?, ?, ?, ?)');
    for (const bike of BIKES) insert.run(...bike);
  }
  return db;
}
```

## --file-- package.json

```json
{
  "name": "pujcovna-kol",
  "type": "module"
}
```

# --solution--

## --file-- server.js

```js
import { createServer } from 'node:http';
import { openDatabase } from './db.js';
import { createApp } from './app.js';

const db = openDatabase('pujcovna-dev.db');
const server = createServer(createApp({ db }));

const port = Number(process.env.PORT ?? 3000);
server.listen(port, () => {
  console.log(`Půjčovna kol běží na portu ${port}`);
});
```
