---
title: Výpis seznamu do stránky
runtime: dom
---

# --description--

Funkce jsou hotové, teď je ukážeš. V kroku přibyly soubory `index.html` a `styles.css` — stránka s prázdným seznamem `<ul id="shopping-list">`. Náhled teď ukazuje stránku místo konzole.

Napiš funkci `renderList(items)`, která do `#shopping-list` vypíše jednu položku seznamu `<li>` za každou položku pole. Text `li` je popis z `describeItem`. Koupené položky dostanou třídu `bought` (styly už ji přeškrtnou). Na konci souboru funkci zavolej: `renderList(groceries);`.

Ke stránce se z JavaScriptu dostaneš přes objekt `document`. Práci se stránkou (DOM) podrobně probírá sekce DOM, události a prohlížeč. Na tenhle krok stačí čtyři věci:

- `document.querySelector('#id')` najde prvek na stránce,
- `document.createElement('li')` vytvoří nový prvek (zatím mimo stránku),
- `element.textContent = 'text'` nastaví jeho text,
- `element.classList.toggle('highlight', isActive)` třídu přidá, když je druhý argument pravdivý, jinak ji odebere.

A pátá, kde se potkáš s poli: `list.replaceChildren(...elements)` nahradí celý obsah prvku zadanými prvky. Díky tomu jde `renderList` zavolat kdykoli znovu a seznam se nezdvojí. Pole prvků se do argumentů „vysype" spreadem, stejně jako v `addItem`.

Na jiných datech — výpis knih:

```js
function renderBooks(books) {
  const shelf = document.querySelector('#shelf');
  const rows = books.map((book) => {
    const row = document.createElement('li');
    row.textContent = book.title;
    row.classList.toggle('long', book.pages > 300);
    return row;
  });
  shelf.replaceChildren(...rows);
}
```

Tady se vyplatí, že máš `describeItem` napsanou jako samostatnou funkci — text položky už nemusíš skládat znovu.

# --hints--

Po načtení stránky je v `#shopping-list` jedna položka `li` za každou položku v `groceries`.

```js
const rows = document.querySelectorAll('#shopping-list li');
assert.equal(rows.length, groceries.length, 'počet li má odpovídat počtu položek v groceries');
```

Text každé položky `li` je popis z `describeItem`.

```js
const rows = [...document.querySelectorAll('#shopping-list li')];
assert.equal(rows.length, groceries.length, 'v seznamu chybí položky');
rows.forEach((row, index) => {
  assert.equal(row.textContent.trim(), describeItem(groceries[index]));
});
```

Koupené položky mají třídu `bought`, nekoupené ne.

```js
const rows = [...document.querySelectorAll('#shopping-list li')];
assert.equal(rows.length, groceries.length, 'v seznamu chybí položky');
rows.forEach((row, index) => {
  assert.equal(row.classList.contains('bought'), groceries[index].bought, `položka ${groceries[index].name} má špatnou třídu`);
});
```

Když `renderList` zavoláš znovu s jiným polem, seznam obsahuje jen nové položky.

```js
renderList([{ id: 99, name: 'Káva', category: 'nápoje', price: 150, quantity: 2, bought: true }]);
const rows = document.querySelectorAll('#shopping-list li');
assert.equal(rows.length, 1, 'po druhém zavolání má být v seznamu jen jedna položka');
assert.equal(rows[0].textContent.trim(), 'Káva — 2 ks × 150 Kč');
assert.ok(rows[0].classList.contains('bought'));
```

# --seed--

## --file-- script.js

```js
// Nákupní seznam. Každá položka je objekt se stejnými klíči,
// cena (price) je za jeden kus v korunách.
const groceries = [
  { id: 1, name: 'Chleba', category: 'pečivo', price: 45, quantity: 1, bought: false },
  { id: 2, name: 'Mléko', category: 'mléčné výrobky', price: 24, quantity: 2, bought: true },
  { id: 3, name: 'Jablka', category: 'ovoce a zelenina', price: 39, quantity: 1, bought: false },
  { id: 4, name: 'Máslo', category: 'mléčné výrobky', price: 59, quantity: 1, bought: false },
  { id: 5, name: 'Rohlíky', category: 'pečivo', price: 5, quantity: 6, bought: true },
  { id: 6, name: 'Zubní pasta', category: 'drogerie', price: 89, quantity: 1, bought: false },
];

const categories = ['pečivo', 'mléčné výrobky', 'ovoce a zelenina', 'drogerie'];

// Vrátí první položku seznamu, u prázdného seznamu undefined.
function firstItem(items) {
  return items[0];
}

// Vrátí poslední položku seznamu, u prázdného seznamu undefined.
function lastItem(items) {
  return items.at(-1);
}

// Vrátí nový seznam s položkou přidanou na konec.
function addItem(items, item) {
  return [...items, item];
}

// Vrátí nový seznam bez položky na daném indexu.
function removeAt(items, index) {
  return items.toSpliced(index, 1);
}

// Kolik kusů je na seznamu celkem.
function totalQuantity(items) {
  let total = 0;
  for (const item of items) {
    total += item.quantity;
  }
  return total;
}

// Názvy položek ve stejném pořadí.
function itemNames(items) {
  return items.map((item) => item.name);
}

// Pro každou položku název a cenu za všechny kusy.
function lineTotals(items) {
  return items.map((item) => ({ name: item.name, total: item.price * item.quantity }));
}

// Popisky ve tvaru „Rohlíky (6 ks)".
function itemLabels(items) {
  return items.map((item) => `${item.name} (${item.quantity} ks)`);
}

// Položky, které ještě nejsou v košíku.
function toBuy(items) {
  return items.filter((item) => !item.bought);
}

// Vrátí nový seznam bez položky s daným id.
function removeById(items, id) {
  return items.filter((item) => item.id !== id);
}

// Položka s daným id, nebo undefined.
function findItem(items, id) {
  return items.find((item) => item.id === id);
}

// Index položky s daným id, nebo -1.
function indexById(items, id) {
  return items.findIndex((item) => item.id === id);
}

// Je v seznamu aspoň jedna nekoupená položka?
function hasUnbought(items) {
  return items.some((item) => !item.bought);
}

// Jsou koupené všechny položky? U prázdného seznamu ano.
function allBought(items) {
  return items.every((item) => item.bought);
}

// Zná obchod tuhle kategorii?
function isKnownCategory(category) {
  return categories.includes(category);
}

// Názvy položek, které ještě nejsou v košíku.
function namesToBuy(items) {
  return itemNames(toBuy(items));
}

// Cena celého nákupu v korunách.
function totalPrice(items) {
  return items.reduce((sum, item) => sum + item.price * item.quantity, 0);
}

// Kolik položek je v které kategorii.
function countByCategory(items) {
  return items.reduce((counts, item) => {
    counts[item.category] = (counts[item.category] ?? 0) + 1;
    return counts;
  }, {});
}

// Nový seznam seřazený od nejlevnější položky.
function sortedByPrice(items) {
  return items.toSorted((a, b) => a.price - b.price);
}

// Nový seznam seřazený podle názvu podle české abecedy.
function sortedByName(items) {
  return items.toSorted((a, b) => a.name.localeCompare(b.name, 'cs'));
}

// Popis položky, např. „Mléko — 2 ks × 24 Kč".
function describeItem({ name, quantity, price }) {
  return `${name} — ${quantity} ks × ${price} Kč`;
}

// Nový seznam, ve kterém má položka s daným id nový počet kusů.
function updateQuantity(items, id, quantity) {
  return items.map((item) => (item.id === id ? { ...item, quantity } : item));
}

// Položky seskupené podle kategorie.
function groupByCategory(items) {
  return Object.groupBy(items, (item) => item.category);
}

// Nový seznam, ve kterém je položka s daným id odškrtnutá (nebo naopak).
function toggleBought(items, id) {
  return items.map((item) => (item.id === id ? { ...item, bought: !item.bought } : item));
}

--edit--

--edit--
```

## --file-- index.html

```html
<!DOCTYPE html>
<html lang="cs">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Nákupní seznam</title>
    <link rel="stylesheet" href="styles.css">
  </head>
  <body>
    <main class="card">
      <h1>Nákupní seznam</h1>
      <dl class="summary">
        <div>
          <dt>Položek ke koupi</dt>
          <dd id="remaining">–</dd>
        </div>
        <div>
          <dt>Zbývá utratit</dt>
          <dd id="total">–</dd>
        </div>
      </dl>
      <ul id="shopping-list" class="list"></ul>
    </main>
    <script src="script.js"></script>
  </body>
</html>
```

## --file-- styles.css

```css
:root {
  --bg: #f3efe6;
  --card: #fffdf8;
  --ink: #2b2a26;
  --muted: #7a756a;
  --accent: #3d7a5a;
  --line: #e6dfd1;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  min-height: 100vh;
  display: grid;
  place-items: start center;
  padding: 3rem 1rem;
  background: var(--bg);
  color: var(--ink);
  font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
}

.card {
  width: min(100%, 28rem);
  padding: 2rem;
  background: var(--card);
  border-radius: 1rem;
  box-shadow: 0 10px 30px rgb(43 42 38 / 0.08);
}

h1 {
  margin: 0 0 1.5rem;
  font-size: 1.6rem;
}

.summary {
  display: flex;
  gap: 1rem;
  margin: 0 0 1.5rem;
}

.summary div {
  flex: 1;
  padding: 0.75rem 1rem;
  background: var(--bg);
  border-radius: 0.6rem;
}

.summary dt {
  font-size: 0.8rem;
  color: var(--muted);
}

.summary dd {
  margin: 0.2rem 0 0;
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--accent);
}

.list {
  list-style: none;
  margin: 0;
  padding: 0;
}

.list li {
  padding: 0.75rem 0 0.75rem 2rem;
  border-bottom: 1px solid var(--line);
  position: relative;
}

.list li::before {
  content: "";
  position: absolute;
  left: 0;
  top: 50%;
  width: 1rem;
  height: 1rem;
  translate: 0 -50%;
  border: 2px solid var(--accent);
  border-radius: 0.3rem;
}

.list li.bought {
  color: var(--muted);
  text-decoration: line-through;
}

.list li.bought::before {
  background: var(--accent);
}
```

# --solution--

## --file-- script.js

```js
// Nákupní seznam. Každá položka je objekt se stejnými klíči,
// cena (price) je za jeden kus v korunách.
const groceries = [
  { id: 1, name: 'Chleba', category: 'pečivo', price: 45, quantity: 1, bought: false },
  { id: 2, name: 'Mléko', category: 'mléčné výrobky', price: 24, quantity: 2, bought: true },
  { id: 3, name: 'Jablka', category: 'ovoce a zelenina', price: 39, quantity: 1, bought: false },
  { id: 4, name: 'Máslo', category: 'mléčné výrobky', price: 59, quantity: 1, bought: false },
  { id: 5, name: 'Rohlíky', category: 'pečivo', price: 5, quantity: 6, bought: true },
  { id: 6, name: 'Zubní pasta', category: 'drogerie', price: 89, quantity: 1, bought: false },
];

const categories = ['pečivo', 'mléčné výrobky', 'ovoce a zelenina', 'drogerie'];

// Vrátí první položku seznamu, u prázdného seznamu undefined.
function firstItem(items) {
  return items[0];
}

// Vrátí poslední položku seznamu, u prázdného seznamu undefined.
function lastItem(items) {
  return items.at(-1);
}

// Vrátí nový seznam s položkou přidanou na konec.
function addItem(items, item) {
  return [...items, item];
}

// Vrátí nový seznam bez položky na daném indexu.
function removeAt(items, index) {
  return items.toSpliced(index, 1);
}

// Kolik kusů je na seznamu celkem.
function totalQuantity(items) {
  let total = 0;
  for (const item of items) {
    total += item.quantity;
  }
  return total;
}

// Názvy položek ve stejném pořadí.
function itemNames(items) {
  return items.map((item) => item.name);
}

// Pro každou položku název a cenu za všechny kusy.
function lineTotals(items) {
  return items.map((item) => ({ name: item.name, total: item.price * item.quantity }));
}

// Popisky ve tvaru „Rohlíky (6 ks)".
function itemLabels(items) {
  return items.map((item) => `${item.name} (${item.quantity} ks)`);
}

// Položky, které ještě nejsou v košíku.
function toBuy(items) {
  return items.filter((item) => !item.bought);
}

// Vrátí nový seznam bez položky s daným id.
function removeById(items, id) {
  return items.filter((item) => item.id !== id);
}

// Položka s daným id, nebo undefined.
function findItem(items, id) {
  return items.find((item) => item.id === id);
}

// Index položky s daným id, nebo -1.
function indexById(items, id) {
  return items.findIndex((item) => item.id === id);
}

// Je v seznamu aspoň jedna nekoupená položka?
function hasUnbought(items) {
  return items.some((item) => !item.bought);
}

// Jsou koupené všechny položky? U prázdného seznamu ano.
function allBought(items) {
  return items.every((item) => item.bought);
}

// Zná obchod tuhle kategorii?
function isKnownCategory(category) {
  return categories.includes(category);
}

// Názvy položek, které ještě nejsou v košíku.
function namesToBuy(items) {
  return itemNames(toBuy(items));
}

// Cena celého nákupu v korunách.
function totalPrice(items) {
  return items.reduce((sum, item) => sum + item.price * item.quantity, 0);
}

// Kolik položek je v které kategorii.
function countByCategory(items) {
  return items.reduce((counts, item) => {
    counts[item.category] = (counts[item.category] ?? 0) + 1;
    return counts;
  }, {});
}

// Nový seznam seřazený od nejlevnější položky.
function sortedByPrice(items) {
  return items.toSorted((a, b) => a.price - b.price);
}

// Nový seznam seřazený podle názvu podle české abecedy.
function sortedByName(items) {
  return items.toSorted((a, b) => a.name.localeCompare(b.name, 'cs'));
}

// Popis položky, např. „Mléko — 2 ks × 24 Kč".
function describeItem({ name, quantity, price }) {
  return `${name} — ${quantity} ks × ${price} Kč`;
}

// Nový seznam, ve kterém má položka s daným id nový počet kusů.
function updateQuantity(items, id, quantity) {
  return items.map((item) => (item.id === id ? { ...item, quantity } : item));
}

// Položky seskupené podle kategorie.
function groupByCategory(items) {
  return Object.groupBy(items, (item) => item.category);
}

// Nový seznam, ve kterém je položka s daným id odškrtnutá (nebo naopak).
function toggleBought(items, id) {
  return items.map((item) => (item.id === id ? { ...item, bought: !item.bought } : item));
}

// Vypíše položky do seznamu na stránce. Jde volat opakovaně.
function renderList(items) {
  const list = document.querySelector('#shopping-list');
  const rows = items.map((item) => {
    const row = document.createElement('li');
    row.textContent = describeItem(item);
    row.classList.toggle('bought', item.bought);
    return row;
  });
  list.replaceChildren(...rows);
}

renderList(groceries);
```
