---
title: Výběr přes filter
---

# --description--

Napiš funkci `toBuy(items)`, která vrátí jen položky, které ještě nejsou koupené (`bought` je `false`). Použij metodu **`filter`**.

`filter` se zapisuje stejně jako `map` — dostane callback, který se zavolá pro každou položku. Rozdíl je v tom, co callback vrací a co z toho vznikne:

- callback u `map` vrací **novou položku** → výsledek má stejnou délku,
- callback u `filter` vrací **`true` nebo `false`** → položka se do výsledku buď dostane (celá a beze změny), nebo vypadne. Výsledek je stejně dlouhý nebo kratší.

Na jiných datech — jen dlouhé knihy:

```js
const longBooks = books.filter((book) => book.pages > 300);
```

Pořadí položek zůstává, původní pole se nemění. Když podmínku nesplní nic, vyjde prázdné pole.

Protože `bought` už je `true`/`false`, nemusíš ho s ničím porovnávat — stačí ho znegovat.

# --hints--

`toBuy` vrátí jen nekoupené položky ve stejném pořadí.

```js
const coffee = { id: 7, name: 'Káva', bought: false };
const tea = { id: 8, name: 'Čaj', bought: true };
const rice = { id: 9, name: 'Rýže', bought: false };
const result = toBuy([coffee, tea, rice]);
assert.ok(Array.isArray(result), 'toBuy má vrátit pole');
assert.equal(result.length, 2);
assert.equal(result[0], coffee);
assert.equal(result[1], rice);
```

Když je všechno koupené, vrátí `toBuy` prázdné pole.

```js
const result = toBuy([{ id: 7, name: 'Káva', bought: true }]);
assert.ok(Array.isArray(result), 'toBuy má vrátit pole');
assert.equal(result.length, 0);
```

`toBuy` používá metodu `filter`.

```js
const source = helpers.stripComments(toBuy.toString(), 'js');
assert.match(source, /\.filter\s*\(/, 'v toBuy chybí volání .filter(…)');
```

# --seed--

## --file-- script.js

```js
// Nákupní seznam. Každá položka je objekt se stejnými klíči,
// cena (price) je za jeden kus v korunách.
const groceries = [
  { id: 1, name: 'Chleba', category: 'pečivo', price: 45, quantity: 1, bought: false },
  { id: 2, name: 'Mléko', category: 'mléčné výrobky', price: 24, quantity: 2, bought: true },
  { id: 3, name: 'Jablka', category: 'ovoce a zelenina', price: 39, quantity: 1, bought: false },
  { id: 4, name: 'Máslo', category: 'mléčné výrobky', price: 59, quantity: 1, bought: false },
  { id: 5, name: 'Rohlíky', category: 'pečivo', price: 5, quantity: 6, bought: true },
  { id: 6, name: 'Zubní pasta', category: 'drogerie', price: 89, quantity: 1, bought: false },
];

const categories = ['pečivo', 'mléčné výrobky', 'ovoce a zelenina', 'drogerie'];

// Vrátí první položku seznamu, u prázdného seznamu undefined.
function firstItem(items) {
  return items[0];
}

// Vrátí poslední položku seznamu, u prázdného seznamu undefined.
function lastItem(items) {
  return items.at(-1);
}

// Vrátí nový seznam s položkou přidanou na konec.
function addItem(items, item) {
  return [...items, item];
}

// Vrátí nový seznam bez položky na daném indexu.
function removeAt(items, index) {
  return items.toSpliced(index, 1);
}

// Kolik kusů je na seznamu celkem.
function totalQuantity(items) {
  let total = 0;
  for (const item of items) {
    total += item.quantity;
  }
  return total;
}

// Názvy položek ve stejném pořadí.
function itemNames(items) {
  return items.map((item) => item.name);
}

// Pro každou položku název a cenu za všechny kusy.
function lineTotals(items) {
  return items.map((item) => ({ name: item.name, total: item.price * item.quantity }));
}

// Popisky ve tvaru „Rohlíky (6 ks)".
function itemLabels(items) {
  return items.map((item) => `${item.name} (${item.quantity} ks)`);
}

--edit--

--edit--
```

# --solution--

## --file-- script.js

```js
// Nákupní seznam. Každá položka je objekt se stejnými klíči,
// cena (price) je za jeden kus v korunách.
const groceries = [
  { id: 1, name: 'Chleba', category: 'pečivo', price: 45, quantity: 1, bought: false },
  { id: 2, name: 'Mléko', category: 'mléčné výrobky', price: 24, quantity: 2, bought: true },
  { id: 3, name: 'Jablka', category: 'ovoce a zelenina', price: 39, quantity: 1, bought: false },
  { id: 4, name: 'Máslo', category: 'mléčné výrobky', price: 59, quantity: 1, bought: false },
  { id: 5, name: 'Rohlíky', category: 'pečivo', price: 5, quantity: 6, bought: true },
  { id: 6, name: 'Zubní pasta', category: 'drogerie', price: 89, quantity: 1, bought: false },
];

const categories = ['pečivo', 'mléčné výrobky', 'ovoce a zelenina', 'drogerie'];

// Vrátí první položku seznamu, u prázdného seznamu undefined.
function firstItem(items) {
  return items[0];
}

// Vrátí poslední položku seznamu, u prázdného seznamu undefined.
function lastItem(items) {
  return items.at(-1);
}

// Vrátí nový seznam s položkou přidanou na konec.
function addItem(items, item) {
  return [...items, item];
}

// Vrátí nový seznam bez položky na daném indexu.
function removeAt(items, index) {
  return items.toSpliced(index, 1);
}

// Kolik kusů je na seznamu celkem.
function totalQuantity(items) {
  let total = 0;
  for (const item of items) {
    total += item.quantity;
  }
  return total;
}

// Názvy položek ve stejném pořadí.
function itemNames(items) {
  return items.map((item) => item.name);
}

// Pro každou položku název a cenu za všechny kusy.
function lineTotals(items) {
  return items.map((item) => ({ name: item.name, total: item.price * item.quantity }));
}

// Popisky ve tvaru „Rohlíky (6 ks)".
function itemLabels(items) {
  return items.map((item) => `${item.name} (${item.quantity} ks)`);
}

// Položky, které ještě nejsou v košíku.
function toBuy(items) {
  return items.filter((item) => !item.bought);
}
```
