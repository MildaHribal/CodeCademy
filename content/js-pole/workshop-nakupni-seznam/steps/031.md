---
title: Seskupení přes Object.groupBy
see: js-pole/metody-pole-do-hloubky#akumulator-nemusi-byt-cislo
---

# --description--

**Úkol:** Napiš funkci `groupByCategory(items)`, která vrátí objekt, kde klíčem je kategorie a hodnotou **pole položek** z té kategorie, v pořadí, v jakém jsou v seznamu. Prázdný seznam vrátí objekt bez klíčů. Použij **`Object.groupBy`**.

Seskupení jsi vlastně psal před pár kroky: `countByCategory` počítala položky v kategoriích. Kdyby místo počtu přidávala položky do pole, bylo by to seskupení. Je to tak častá úloha, že má JavaScript od roku 2024 hotovou funkci `Object.groupBy(pole, callback)`: callback vrátí **klíč skupiny** a položka se přidá do pole pod tím klíčem.

Není to metoda pole — `items.groupBy` neexistuje. Je to funkce na `Object`, které pole předáš jako první argument. Položky ve skupinách jsou tytéž objekty jako v původním poli, nic se nekopíruje ani nemění.

# --hints--

`groupByCategory` vrátí pro každou kategorii pole jejích položek ve stejném pořadí.

```js
const coffee = { id: 7, name: 'Káva', category: 'nápoje' };
const rice = { id: 8, name: 'Rýže', category: 'přílohy' };
const tea = { id: 9, name: 'Čaj', category: 'nápoje' };
const result = groupByCategory([coffee, rice, tea]);
assert.deepEqual(Object.keys(result).sort(), ['nápoje', 'přílohy'], 'groupByCategory([Káva, Rýže, Čaj]) má mít klíče nápoje a přílohy');
assert.deepEqual(result['nápoje'], [coffee, tea], 'groupByCategory([Káva (nápoje), Rýže (přílohy), Čaj (nápoje)]) má pod nápoje dát [Káva, Čaj]');
assert.deepEqual(result['přílohy'], [rice], 'groupByCategory([Káva, Rýže (přílohy), Čaj]) má pod přílohy dát [Rýže]');
assert.equal(result['nápoje'][0], coffee, 've skupině mají být tytéž objekty položek, ne kopie');
```

Pro prázdné pole vrátí `groupByCategory` objekt bez klíčů.

```js
const result = groupByCategory([]);
assert.equal(typeof result, 'object', 'groupByCategory([]) má vrátit objekt');
assert.ok(result !== null, 'groupByCategory([]) nemá vrátit null');
assert.deepEqual(Object.keys(result), [], 'groupByCategory([]) má vrátit objekt bez klíčů');
```

`groupByCategory` používá `Object.groupBy`.

```js
const source = helpers.stripComments(groupByCategory.toString(), 'js');
assert.match(source, /Object\s*\.\s*groupBy\s*\(/, 'v groupByCategory chybí volání Object.groupBy(…)');
```

# --help--

## --tip--

Jde o seskupení, hotovou variantu `reduce` do objektu — viz konec části [Akumulátor nemusí být číslo](see:js-pole/metody-pole-do-hloubky#akumulator-nemusi-byt-cislo).

## --tip--

Vrať výsledek `Object.groupBy`. První argument je pole položek, druhý callback, který pro položku vrátí její kategorii.

## --tip--

Na jiných datech: `Object.groupBy(books, (book) => book.author)` rozdělí knihy podle autora.

# --seed--

## --file-- script.js

```js
// Nákupní seznam. Každá položka je objekt se stejnými klíči,
// cena (price) je za jeden kus v korunách.
const groceries = [
  { id: 1, name: 'Chleba', category: 'pečivo', price: 45, quantity: 1, bought: false },
  { id: 2, name: 'Mléko', category: 'mléčné výrobky', price: 24, quantity: 2, bought: true },
  { id: 3, name: 'Jablka', category: 'ovoce a zelenina', price: 39, quantity: 1, bought: false },
  { id: 4, name: 'Máslo', category: 'mléčné výrobky', price: 59, quantity: 1, bought: false },
  { id: 5, name: 'Rohlíky', category: 'pečivo', price: 5, quantity: 6, bought: true },
  { id: 6, name: 'Zubní pasta', category: 'drogerie', price: 89, quantity: 1, bought: false },
];

// Vytvoří novou položku seznamu: jeden kus, zatím nekoupená.
function createItem(id, name, category, price) {
  return { id, name, category, price, quantity: 1, bought: false };
}

const categories = ['pečivo', 'mléčné výrobky', 'ovoce a zelenina', 'drogerie'];

// Vrátí první položku seznamu, u prázdného seznamu undefined.
function firstItem(items) {
  return items[0];
}

// Vrátí poslední položku seznamu, u prázdného seznamu undefined.
function lastItem(items) {
  return items.at(-1);
}

// Vrátí nový seznam s položkou přidanou na konec.
function addItem(items, item) {
  return [...items, item];
}

// Vrátí nový seznam bez položky na daném indexu.
function removeAt(items, index) {
  return items.toSpliced(index, 1);
}

// Kolik kusů je na seznamu celkem.
function totalQuantity(items) {
  let total = 0;
  for (const item of items) {
    total += item.quantity;
  }
  return total;
}

// Názvy položek ve stejném pořadí.
function itemNames(items) {
  return items.map((item) => item.name);
}

// Pro každou položku název a cenu za všechny kusy.
function lineTotals(items) {
  return items.map((item) => ({ name: item.name, total: item.price * item.quantity }));
}

// Popisky ve tvaru „Rohlíky (6 ks)".
function itemLabels(items) {
  return items.map((item) => `${item.name} (${item.quantity} ks)`);
}

// Položky, které ještě nejsou v košíku.
function toBuy(items) {
  return items.filter((item) => !item.bought);
}

// Vrátí nový seznam bez položky s daným id.
function removeById(items, id) {
  return items.filter((item) => item.id !== id);
}

// Položka s daným id, nebo undefined.
function findItem(items, id) {
  return items.find((item) => item.id === id);
}

// Index položky s daným id, nebo -1.
function indexById(items, id) {
  return items.findIndex((item) => item.id === id);
}

// Od kolegy: je položka s tímhle id už v seznamu?
function hasItem(items, id) {
  const position = items.findIndex((item) => item.id === id);
  if (position !== -1) {
    return true;
  }
  return false;
}

// Je v seznamu aspoň jedna nekoupená položka?
function hasUnbought(items) {
  return items.some((item) => !item.bought);
}

// Jsou koupené všechny položky? U prázdného seznamu ano.
function allBought(items) {
  return items.every((item) => item.bought);
}

// Zná obchod tuhle kategorii?
function isKnownCategory(category) {
  return categories.includes(category);
}

// Cena celého nákupu v korunách.
function totalPrice(items) {
  return items.reduce((sum, item) => sum + item.price * item.quantity, 0);
}

// Nový seznam seřazený od nejlevnější položky.
function sortedByPrice(items) {
  return items.toSorted((a, b) => a.price - b.price);
}

// Nový seznam seřazený podle názvu podle české abecedy.
function sortedByName(items) {
  return items.toSorted((a, b) => a.name.localeCompare(b.name, 'cs'));
}

// Od kolegy: jednotkové ceny všech položek od nejnižší.
function pricesAscending(items) {
  return items.map((item) => item.price).sort((a, b) => a - b);
}

// Položka, za kterou v nákupu utratíš nejvíc; při shodě ta dřívější.
function priciestItem(items) {
  let priciest;
  for (const item of items) {
    if (priciest === undefined || item.price * item.quantity > priciest.price * priciest.quantity) {
      priciest = item;
    }
  }
  return priciest;
}

function countByCategory(items) {
  return items.reduce((counts, item) => {
    counts[item.category] = (counts[item.category] ?? 0) + 1;
    return counts;
  }, {});
}

// Kolik kusů je v které kategorii.
function quantityByCategory(items) {
  return items.reduce((quantities, item) => {
    quantities[item.category] = (quantities[item.category] ?? 0) + item.quantity;
    return quantities;
  }, {});
}

// Názvy položek, které ještě nejsou v košíku.
function namesToBuy(items) {
  return itemNames(toBuy(items));
}

// Nový seznam, ve kterém má položka s daným id nový počet kusů.
function updateQuantity(items, id, quantity) {
  return items.map((item) => (item.id === id ? { ...item, quantity } : item));
}

// Od kolegy: vrátí seznam, ve kterém je všechno koupené.
function markAllBought(items) {
  return items.map((item) => {
    return { ...item, bought: true };
  });
}

// Nový seznam, ve kterém je položka s daným id odškrtnutá (nebo naopak).
function toggleBought(items, id) {
  return items.map((item) => (item.id === id ? { ...item, bought: !item.bought } : item));
}

// Popis položky, např. „Mléko — 2 ks × 24 Kč".
function describeItem({ name, quantity, price }) {
  return `${name} — ${quantity} ks × ${price} Kč`;
}

--edit--

--edit--
```

# --solution--

## --file-- script.js

```js
// Nákupní seznam. Každá položka je objekt se stejnými klíči,
// cena (price) je za jeden kus v korunách.
const groceries = [
  { id: 1, name: 'Chleba', category: 'pečivo', price: 45, quantity: 1, bought: false },
  { id: 2, name: 'Mléko', category: 'mléčné výrobky', price: 24, quantity: 2, bought: true },
  { id: 3, name: 'Jablka', category: 'ovoce a zelenina', price: 39, quantity: 1, bought: false },
  { id: 4, name: 'Máslo', category: 'mléčné výrobky', price: 59, quantity: 1, bought: false },
  { id: 5, name: 'Rohlíky', category: 'pečivo', price: 5, quantity: 6, bought: true },
  { id: 6, name: 'Zubní pasta', category: 'drogerie', price: 89, quantity: 1, bought: false },
];

// Vytvoří novou položku seznamu: jeden kus, zatím nekoupená.
function createItem(id, name, category, price) {
  return { id, name, category, price, quantity: 1, bought: false };
}

const categories = ['pečivo', 'mléčné výrobky', 'ovoce a zelenina', 'drogerie'];

// Vrátí první položku seznamu, u prázdného seznamu undefined.
function firstItem(items) {
  return items[0];
}

// Vrátí poslední položku seznamu, u prázdného seznamu undefined.
function lastItem(items) {
  return items.at(-1);
}

// Vrátí nový seznam s položkou přidanou na konec.
function addItem(items, item) {
  return [...items, item];
}

// Vrátí nový seznam bez položky na daném indexu.
function removeAt(items, index) {
  return items.toSpliced(index, 1);
}

// Kolik kusů je na seznamu celkem.
function totalQuantity(items) {
  let total = 0;
  for (const item of items) {
    total += item.quantity;
  }
  return total;
}

// Názvy položek ve stejném pořadí.
function itemNames(items) {
  return items.map((item) => item.name);
}

// Pro každou položku název a cenu za všechny kusy.
function lineTotals(items) {
  return items.map((item) => ({ name: item.name, total: item.price * item.quantity }));
}

// Popisky ve tvaru „Rohlíky (6 ks)".
function itemLabels(items) {
  return items.map((item) => `${item.name} (${item.quantity} ks)`);
}

// Položky, které ještě nejsou v košíku.
function toBuy(items) {
  return items.filter((item) => !item.bought);
}

// Vrátí nový seznam bez položky s daným id.
function removeById(items, id) {
  return items.filter((item) => item.id !== id);
}

// Položka s daným id, nebo undefined.
function findItem(items, id) {
  return items.find((item) => item.id === id);
}

// Index položky s daným id, nebo -1.
function indexById(items, id) {
  return items.findIndex((item) => item.id === id);
}

// Od kolegy: je položka s tímhle id už v seznamu?
function hasItem(items, id) {
  const position = items.findIndex((item) => item.id === id);
  if (position !== -1) {
    return true;
  }
  return false;
}

// Je v seznamu aspoň jedna nekoupená položka?
function hasUnbought(items) {
  return items.some((item) => !item.bought);
}

// Jsou koupené všechny položky? U prázdného seznamu ano.
function allBought(items) {
  return items.every((item) => item.bought);
}

// Zná obchod tuhle kategorii?
function isKnownCategory(category) {
  return categories.includes(category);
}

// Cena celého nákupu v korunách.
function totalPrice(items) {
  return items.reduce((sum, item) => sum + item.price * item.quantity, 0);
}

// Nový seznam seřazený od nejlevnější položky.
function sortedByPrice(items) {
  return items.toSorted((a, b) => a.price - b.price);
}

// Nový seznam seřazený podle názvu podle české abecedy.
function sortedByName(items) {
  return items.toSorted((a, b) => a.name.localeCompare(b.name, 'cs'));
}

// Od kolegy: jednotkové ceny všech položek od nejnižší.
function pricesAscending(items) {
  return items.map((item) => item.price).sort((a, b) => a - b);
}

// Položka, za kterou v nákupu utratíš nejvíc; při shodě ta dřívější.
function priciestItem(items) {
  let priciest;
  for (const item of items) {
    if (priciest === undefined || item.price * item.quantity > priciest.price * priciest.quantity) {
      priciest = item;
    }
  }
  return priciest;
}

function countByCategory(items) {
  return items.reduce((counts, item) => {
    counts[item.category] = (counts[item.category] ?? 0) + 1;
    return counts;
  }, {});
}

// Kolik kusů je v které kategorii.
function quantityByCategory(items) {
  return items.reduce((quantities, item) => {
    quantities[item.category] = (quantities[item.category] ?? 0) + item.quantity;
    return quantities;
  }, {});
}

// Názvy položek, které ještě nejsou v košíku.
function namesToBuy(items) {
  return itemNames(toBuy(items));
}

// Nový seznam, ve kterém má položka s daným id nový počet kusů.
function updateQuantity(items, id, quantity) {
  return items.map((item) => (item.id === id ? { ...item, quantity } : item));
}

// Od kolegy: vrátí seznam, ve kterém je všechno koupené.
function markAllBought(items) {
  return items.map((item) => {
    return { ...item, bought: true };
  });
}

// Nový seznam, ve kterém je položka s daným id odškrtnutá (nebo naopak).
function toggleBought(items, id) {
  return items.map((item) => (item.id === id ? { ...item, bought: !item.bought } : item));
}

// Popis položky, např. „Mléko — 2 ks × 24 Kč".
function describeItem({ name, quantity, price }) {
  return `${name} — ${quantity} ks × ${price} Kč`;
}

// Položky seskupené podle kategorie.
function groupByCategory(items) {
  return Object.groupBy(items, (item) => item.category);
}
```
