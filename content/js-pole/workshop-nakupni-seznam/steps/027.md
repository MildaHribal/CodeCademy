---
title: Souhrn nákupu
runtime: dom
---

# --description--

Poslední krok. Nad seznamem jsou dvě políčka se souhrnem a zatím v nich svítí pomlčka.

Napiš funkci `renderSummary(items)`, která do `#remaining` napíše, **kolik položek zbývá koupit** (jen číslo, třeba `4`), a do `#total` **cenu nekoupených položek** ve tvaru `232 Kč`. Na konci souboru ji zavolej s `groceries`.

Nový kód skoro psát nemusíš — skládáš funkce, které už máš. Nekoupené položky ti vrátí `toBuy`, jejich cenu spočítá `totalPrice`. Text nastavíš přes `textContent` jako minule.

Tím je workshop hotový. Za sebou máš:

- indexy, `length` a `at`,
- přidání a odebrání bez mutace přes spread a `toSpliced`,
- `for…of` a metody `map`, `filter`, `find`, `findIndex`, `some`, `every`, `includes` a `reduce`,
- řazení přes `toSorted` s porovnávací funkcí a `localeCompare`,
- destrukturalizaci, spread objektu a `Object.groupBy`,
- výpis pole do stránky.

Stejné funkce potkáš v každé aplikaci, která ukazuje seznam: košík v e-shopu, úkoly v to-do aplikaci, příspěvky ve feedu. Mění se jen jména klíčů. Pravidlo, které si odnes: **funkce, která dostane pole, ho vrací nové a původní nechá být.**

# --hints--

Po načtení stránky je v `#remaining` počet nekoupených položek z `groceries`.

```js
const expected = groceries.filter((item) => !item.bought).length;
assert.equal(document.querySelector('#remaining').textContent.trim(), String(expected));
```

Po načtení stránky je v `#total` cena nekoupených položek ve tvaru `232 Kč`.

```js
const expected = groceries
  .filter((item) => !item.bought)
  .reduce((sum, item) => sum + item.price * item.quantity, 0);
assert.equal(document.querySelector('#total').textContent.trim(), `${expected} Kč`);
```

Když `renderSummary` zavoláš s jiným polem, přepíše obě hodnoty.

```js
renderSummary([
  { id: 7, name: 'Káva', category: 'nápoje', price: 150, quantity: 2, bought: false },
  { id: 8, name: 'Čaj', category: 'nápoje', price: 60, quantity: 1, bought: true },
]);
assert.equal(document.querySelector('#remaining').textContent.trim(), '1');
assert.equal(document.querySelector('#total').textContent.trim(), '300 Kč');
```

# --seed--

## --file-- script.js

```js
// Nákupní seznam. Každá položka je objekt se stejnými klíči,
// cena (price) je za jeden kus v korunách.
const groceries = [
  { id: 1, name: 'Chleba', category: 'pečivo', price: 45, quantity: 1, bought: false },
  { id: 2, name: 'Mléko', category: 'mléčné výrobky', price: 24, quantity: 2, bought: true },
  { id: 3, name: 'Jablka', category: 'ovoce a zelenina', price: 39, quantity: 1, bought: false },
  { id: 4, name: 'Máslo', category: 'mléčné výrobky', price: 59, quantity: 1, bought: false },
  { id: 5, name: 'Rohlíky', category: 'pečivo', price: 5, quantity: 6, bought: true },
  { id: 6, name: 'Zubní pasta', category: 'drogerie', price: 89, quantity: 1, bought: false },
];

const categories = ['pečivo', 'mléčné výrobky', 'ovoce a zelenina', 'drogerie'];

// Vrátí první položku seznamu, u prázdného seznamu undefined.
function firstItem(items) {
  return items[0];
}

// Vrátí poslední položku seznamu, u prázdného seznamu undefined.
function lastItem(items) {
  return items.at(-1);
}

// Vrátí nový seznam s položkou přidanou na konec.
function addItem(items, item) {
  return [...items, item];
}

// Vrátí nový seznam bez položky na daném indexu.
function removeAt(items, index) {
  return items.toSpliced(index, 1);
}

// Kolik kusů je na seznamu celkem.
function totalQuantity(items) {
  let total = 0;
  for (const item of items) {
    total += item.quantity;
  }
  return total;
}

// Názvy položek ve stejném pořadí.
function itemNames(items) {
  return items.map((item) => item.name);
}

// Pro každou položku název a cenu za všechny kusy.
function lineTotals(items) {
  return items.map((item) => ({ name: item.name, total: item.price * item.quantity }));
}

// Popisky ve tvaru „Rohlíky (6 ks)".
function itemLabels(items) {
  return items.map((item) => `${item.name} (${item.quantity} ks)`);
}

// Položky, které ještě nejsou v košíku.
function toBuy(items) {
  return items.filter((item) => !item.bought);
}

// Vrátí nový seznam bez položky s daným id.
function removeById(items, id) {
  return items.filter((item) => item.id !== id);
}

// Položka s daným id, nebo undefined.
function findItem(items, id) {
  return items.find((item) => item.id === id);
}

// Index položky s daným id, nebo -1.
function indexById(items, id) {
  return items.findIndex((item) => item.id === id);
}

// Je v seznamu aspoň jedna nekoupená položka?
function hasUnbought(items) {
  return items.some((item) => !item.bought);
}

// Jsou koupené všechny položky? U prázdného seznamu ano.
function allBought(items) {
  return items.every((item) => item.bought);
}

// Zná obchod tuhle kategorii?
function isKnownCategory(category) {
  return categories.includes(category);
}

// Názvy položek, které ještě nejsou v košíku.
function namesToBuy(items) {
  return itemNames(toBuy(items));
}

// Cena celého nákupu v korunách.
function totalPrice(items) {
  return items.reduce((sum, item) => sum + item.price * item.quantity, 0);
}

// Kolik položek je v které kategorii.
function countByCategory(items) {
  return items.reduce((counts, item) => {
    counts[item.category] = (counts[item.category] ?? 0) + 1;
    return counts;
  }, {});
}

// Nový seznam seřazený od nejlevnější položky.
function sortedByPrice(items) {
  return items.toSorted((a, b) => a.price - b.price);
}

// Nový seznam seřazený podle názvu podle české abecedy.
function sortedByName(items) {
  return items.toSorted((a, b) => a.name.localeCompare(b.name, 'cs'));
}

// Popis položky, např. „Mléko — 2 ks × 24 Kč".
function describeItem({ name, quantity, price }) {
  return `${name} — ${quantity} ks × ${price} Kč`;
}

// Nový seznam, ve kterém má položka s daným id nový počet kusů.
function updateQuantity(items, id, quantity) {
  return items.map((item) => (item.id === id ? { ...item, quantity } : item));
}

// Položky seskupené podle kategorie.
function groupByCategory(items) {
  return Object.groupBy(items, (item) => item.category);
}

// Nový seznam, ve kterém je položka s daným id odškrtnutá (nebo naopak).
function toggleBought(items, id) {
  return items.map((item) => (item.id === id ? { ...item, bought: !item.bought } : item));
}

// Vypíše položky do seznamu na stránce. Jde volat opakovaně.
function renderList(items) {
  const list = document.querySelector('#shopping-list');
  const rows = items.map((item) => {
    const row = document.createElement('li');
    row.textContent = describeItem(item);
    row.classList.toggle('bought', item.bought);
    return row;
  });
  list.replaceChildren(...rows);
}

renderList(groceries);

--edit--

--edit--
```

## --file-- index.html

```html
<!DOCTYPE html>
<html lang="cs">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Nákupní seznam</title>
    <link rel="stylesheet" href="styles.css">
  </head>
  <body>
    <main class="card">
      <h1>Nákupní seznam</h1>
      <dl class="summary">
        <div>
          <dt>Položek ke koupi</dt>
          <dd id="remaining">–</dd>
        </div>
        <div>
          <dt>Zbývá utratit</dt>
          <dd id="total">–</dd>
        </div>
      </dl>
      <ul id="shopping-list" class="list"></ul>
    </main>
    <script src="script.js"></script>
  </body>
</html>
```

## --file-- styles.css

```css
:root {
  --bg: #f3efe6;
  --card: #fffdf8;
  --ink: #2b2a26;
  --muted: #7a756a;
  --accent: #3d7a5a;
  --line: #e6dfd1;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  min-height: 100vh;
  display: grid;
  place-items: start center;
  padding: 3rem 1rem;
  background: var(--bg);
  color: var(--ink);
  font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
}

.card {
  width: min(100%, 28rem);
  padding: 2rem;
  background: var(--card);
  border-radius: 1rem;
  box-shadow: 0 10px 30px rgb(43 42 38 / 0.08);
}

h1 {
  margin: 0 0 1.5rem;
  font-size: 1.6rem;
}

.summary {
  display: flex;
  gap: 1rem;
  margin: 0 0 1.5rem;
}

.summary div {
  flex: 1;
  padding: 0.75rem 1rem;
  background: var(--bg);
  border-radius: 0.6rem;
}

.summary dt {
  font-size: 0.8rem;
  color: var(--muted);
}

.summary dd {
  margin: 0.2rem 0 0;
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--accent);
}

.list {
  list-style: none;
  margin: 0;
  padding: 0;
}

.list li {
  padding: 0.75rem 0 0.75rem 2rem;
  border-bottom: 1px solid var(--line);
  position: relative;
}

.list li::before {
  content: "";
  position: absolute;
  left: 0;
  top: 50%;
  width: 1rem;
  height: 1rem;
  translate: 0 -50%;
  border: 2px solid var(--accent);
  border-radius: 0.3rem;
}

.list li.bought {
  color: var(--muted);
  text-decoration: line-through;
}

.list li.bought::before {
  background: var(--accent);
}
```

# --solution--

## --file-- script.js

```js
// Nákupní seznam. Každá položka je objekt se stejnými klíči,
// cena (price) je za jeden kus v korunách.
const groceries = [
  { id: 1, name: 'Chleba', category: 'pečivo', price: 45, quantity: 1, bought: false },
  { id: 2, name: 'Mléko', category: 'mléčné výrobky', price: 24, quantity: 2, bought: true },
  { id: 3, name: 'Jablka', category: 'ovoce a zelenina', price: 39, quantity: 1, bought: false },
  { id: 4, name: 'Máslo', category: 'mléčné výrobky', price: 59, quantity: 1, bought: false },
  { id: 5, name: 'Rohlíky', category: 'pečivo', price: 5, quantity: 6, bought: true },
  { id: 6, name: 'Zubní pasta', category: 'drogerie', price: 89, quantity: 1, bought: false },
];

const categories = ['pečivo', 'mléčné výrobky', 'ovoce a zelenina', 'drogerie'];

// Vrátí první položku seznamu, u prázdného seznamu undefined.
function firstItem(items) {
  return items[0];
}

// Vrátí poslední položku seznamu, u prázdného seznamu undefined.
function lastItem(items) {
  return items.at(-1);
}

// Vrátí nový seznam s položkou přidanou na konec.
function addItem(items, item) {
  return [...items, item];
}

// Vrátí nový seznam bez položky na daném indexu.
function removeAt(items, index) {
  return items.toSpliced(index, 1);
}

// Kolik kusů je na seznamu celkem.
function totalQuantity(items) {
  let total = 0;
  for (const item of items) {
    total += item.quantity;
  }
  return total;
}

// Názvy položek ve stejném pořadí.
function itemNames(items) {
  return items.map((item) => item.name);
}

// Pro každou položku název a cenu za všechny kusy.
function lineTotals(items) {
  return items.map((item) => ({ name: item.name, total: item.price * item.quantity }));
}

// Popisky ve tvaru „Rohlíky (6 ks)".
function itemLabels(items) {
  return items.map((item) => `${item.name} (${item.quantity} ks)`);
}

// Položky, které ještě nejsou v košíku.
function toBuy(items) {
  return items.filter((item) => !item.bought);
}

// Vrátí nový seznam bez položky s daným id.
function removeById(items, id) {
  return items.filter((item) => item.id !== id);
}

// Položka s daným id, nebo undefined.
function findItem(items, id) {
  return items.find((item) => item.id === id);
}

// Index položky s daným id, nebo -1.
function indexById(items, id) {
  return items.findIndex((item) => item.id === id);
}

// Je v seznamu aspoň jedna nekoupená položka?
function hasUnbought(items) {
  return items.some((item) => !item.bought);
}

// Jsou koupené všechny položky? U prázdného seznamu ano.
function allBought(items) {
  return items.every((item) => item.bought);
}

// Zná obchod tuhle kategorii?
function isKnownCategory(category) {
  return categories.includes(category);
}

// Názvy položek, které ještě nejsou v košíku.
function namesToBuy(items) {
  return itemNames(toBuy(items));
}

// Cena celého nákupu v korunách.
function totalPrice(items) {
  return items.reduce((sum, item) => sum + item.price * item.quantity, 0);
}

// Kolik položek je v které kategorii.
function countByCategory(items) {
  return items.reduce((counts, item) => {
    counts[item.category] = (counts[item.category] ?? 0) + 1;
    return counts;
  }, {});
}

// Nový seznam seřazený od nejlevnější položky.
function sortedByPrice(items) {
  return items.toSorted((a, b) => a.price - b.price);
}

// Nový seznam seřazený podle názvu podle české abecedy.
function sortedByName(items) {
  return items.toSorted((a, b) => a.name.localeCompare(b.name, 'cs'));
}

// Popis položky, např. „Mléko — 2 ks × 24 Kč".
function describeItem({ name, quantity, price }) {
  return `${name} — ${quantity} ks × ${price} Kč`;
}

// Nový seznam, ve kterém má položka s daným id nový počet kusů.
function updateQuantity(items, id, quantity) {
  return items.map((item) => (item.id === id ? { ...item, quantity } : item));
}

// Položky seskupené podle kategorie.
function groupByCategory(items) {
  return Object.groupBy(items, (item) => item.category);
}

// Nový seznam, ve kterém je položka s daným id odškrtnutá (nebo naopak).
function toggleBought(items, id) {
  return items.map((item) => (item.id === id ? { ...item, bought: !item.bought } : item));
}

// Vypíše položky do seznamu na stránce. Jde volat opakovaně.
function renderList(items) {
  const list = document.querySelector('#shopping-list');
  const rows = items.map((item) => {
    const row = document.createElement('li');
    row.textContent = describeItem(item);
    row.classList.toggle('bought', item.bought);
    return row;
  });
  list.replaceChildren(...rows);
}

renderList(groceries);

// Vypíše, kolik položek zbývá koupit a za kolik.
function renderSummary(items) {
  const remaining = toBuy(items);
  document.querySelector('#remaining').textContent = remaining.length;
  document.querySelector('#total').textContent = `${totalPrice(remaining)} Kč`;
}

renderSummary(groceries);
```
