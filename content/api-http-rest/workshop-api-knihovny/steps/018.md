---
title: Automatický test výpůjček
main: server.js
timeoutMs: 30000
see: api-http-rest/http-do-hloubky#automaticky-test-api
---

# --description--

API knihovny je hotové. Než ho předáš frontendu, pojistíš jeho nejdůležitější pravidlo
testem: jedna kniha nesmí mít dvě výpůjčky. Kdo za měsíc upraví `createLoan`, spustí testy
a hned uvidí, jestli pravidlo nerozbil.

V projektu přibyl soubor `loans.test.js` s importy `node:test` a `node:assert` a s adresou API
z proměnné prostředí `API_URL`. Testy se spouštějí proti běžícímu serveru:
`API_URL=http://localhost:3000 node --test loans.test.js`.

**Úkol:** napiš do `loans.test.js` aspoň dva testy:

- výpůjčka volné knihy vrátí `201` a hlavičku `Location` s adresou právě vytvořené výpůjčky,
- výpůjčka knihy, která je už vypůjčená (v datech je to kniha 9), vrátí `409`.

Kontrola tvoje testy spustí proti tvému serveru, kde musí projít, a pak proti třem
rozbitým serverům, kde musí selhat: jeden vždy vrátí `201`, druhý vždy `409` a třetí
zapomíná posílat `Location`. Test, který rozbitý server nepozná, nic nehlídá.

## Co umíš

- Router s [[parametr cesty|parametry cesty]], `405` s hlavičkou `Allow` a `404` pro neznámé adresy.
- Filtr, řazení přes seznam povolených hodnot a stránkování s metadaty v query.
- Správné kódy: `201` s `Location`, `204` bez těla, `400` a `422` pro vadný vstup, `409` pro střet se stavem.
- `PATCH`, který mění jen jedno pole, a odpověď, která je reprezentací zdroje, ne kopií dat.
- Automatický test, který API hlídá při každé změně.

Tohle API je vzor pro každý backend, který budeš psát. V další lekci zjistíš, jak ruční
kontroly vstupu nahradit schématem, a ve workshopu API ve frameworku ho přepíšeš do Expressu.

## Udělej po svém

- **Přehled pro knihovníka:** postav k API stránku se seznamem knih, štítky „Volná" a
  „Vypůjčená" a tlačítky stránkování podle `meta`.
- **Upomínky:** přidej `GET /api/loans?overdue=true`, který vrátí výpůjčky po termínu
  vrácení. Porovnání dat v `RRRR-MM-DD` už znáš.
- **Víc testů:** otestuj stránkování na hranicích (`limit=50`, `limit=51`, stránka za koncem).
- **Vlastní téma:** přestav API na něco, co tě zajímá, třeba půjčovnu kol, rezervace
  tréninků nebo sbírku vinylů. Pravidlo „jedna věc, jeden výpůjčitel" zůstane.

# --hints--

Testy v `loans.test.js` projdou proti tvému serveru a jsou aspoň dva.

```js
const server = await helpers.startServer('server.js');
const result = await helpers.run(`API_URL=${server.url} node --test loans.test.js`, { timeoutMs: 20000 });
const output = result.stdout + result.stderr;
assert.equal(result.code, 0, `node --test loans.test.js má proti tvému serveru projít, výstup:\n${output.slice(-800)}`);
const count = Number(output.match(/(?:ℹ|#) tests (\d+)/)?.[1] ?? 0);
assert.ok(count >= 2, `loans.test.js má obsahovat aspoň dva testy test(…), node --test hlásí tests ${count}`);
```

Testy selžou proti serveru, který na výpůjčku vždy odpoví `201`.

```js
const fs = await import('node:fs/promises');
const fake = `import { createServer } from 'node:http';
const loans = new Map();
let nextId = 3;
createServer(async (req, res) => {
  let raw = '';
  for await (const chunk of req) raw += chunk;
  const send = (status, body, headers = {}) => {
    res.writeHead(status, { 'Content-Type': 'application/json; charset=utf-8', ...headers });
    res.end(JSON.stringify(body));
  };
  if (req.method === 'POST' && req.url === '/api/loans') {
    let input = {};
    try { input = JSON.parse(raw) ?? {}; } catch {}
    const loan = { id: nextId++, bookId: input.bookId, reader: input.reader, dueDate: '2026-10-14' };
    loans.set(loan.id, loan);
    return send(201, loan, { Location: '/api/loans/' + loan.id });
  }
  const match = req.url.match(/^\\/api\\/loans\\/(\\d+)$/);
  if (req.method === 'GET' && match && loans.has(Number(match[1]))) return send(200, loans.get(Number(match[1])));
  send(404, { error: 'Nenalezeno.' });
}).listen(process.env.PORT);
`;
await fs.writeFile(`${helpers.dir}/fake-api.mjs`, fake);
const server = await helpers.startServer('fake-api.mjs');
const result = await helpers.run(`API_URL=${server.url} node --test loans.test.js`, { timeoutMs: 20000 });
assert.notEqual(result.code, 0, 'proti serveru, který výpůjčku vypůjčené knihy vždy přijme (201), mají testy selhat — ověřuješ 409?');
```

Testy selžou proti serveru, který na výpůjčku vždy odpoví `409`.

```js
const fs = await import('node:fs/promises');
const fake = `import { createServer } from 'node:http';
createServer(async (req, res) => {
  for await (const chunk of req) void chunk;
  res.writeHead(req.method === 'POST' ? 409 : 404, { 'Content-Type': 'application/json; charset=utf-8' });
  res.end(JSON.stringify({ error: 'Kniha je právě vypůjčená.' }));
}).listen(process.env.PORT);
`;
await fs.writeFile(`${helpers.dir}/fake-api.mjs`, fake);
const server = await helpers.startServer('fake-api.mjs');
const result = await helpers.run(`API_URL=${server.url} node --test loans.test.js`, { timeoutMs: 20000 });
assert.notEqual(result.code, 0, 'proti serveru, který každou výpůjčku odmítne (409), mají testy selhat — ověřuješ 201 u volné knihy?');
```

Testy selžou proti serveru, který po vytvoření výpůjčky neposílá `Location`.

```js
const fs = await import('node:fs/promises');
const fake = `import { createServer } from 'node:http';
let nextId = 3;
const loans = new Map();
createServer(async (req, res) => {
  let raw = '';
  for await (const chunk of req) raw += chunk;
  const send = (status, body) => {
    res.writeHead(status, { 'Content-Type': 'application/json; charset=utf-8' });
    res.end(JSON.stringify(body));
  };
  if (req.method === 'POST' && req.url === '/api/loans') {
    let input = {};
    try { input = JSON.parse(raw) ?? {}; } catch {}
    if (input.bookId === 9 || [...loans.values()].some((loan) => loan.bookId === input.bookId)) {
      return send(409, { error: 'Kniha je právě vypůjčená.' });
    }
    const loan = { id: nextId++, bookId: input.bookId, reader: input.reader, dueDate: '2026-10-14' };
    loans.set(loan.id, loan);
    return send(201, loan);
  }
  const match = req.url.match(/^\\/api\\/loans\\/(\\d+)$/);
  if (req.method === 'GET' && match && loans.has(Number(match[1]))) return send(200, loans.get(Number(match[1])));
  send(404, { error: 'Nenalezeno.' });
}).listen(process.env.PORT);
`;
await fs.writeFile(`${helpers.dir}/fake-api.mjs`, fake);
const server = await helpers.startServer('fake-api.mjs');
const result = await helpers.run(`API_URL=${server.url} node --test loans.test.js`, { timeoutMs: 20000 });
assert.notEqual(result.code, 0, 'proti serveru, který u 201 neposílá hlavičku Location, mají testy selhat — ověřuješ Location?');
```

# --help--

## --tip--

Jak vypadá test API v `node:test`, ukazuje [Automatický test API](see:api-http-rest/http-do-hloubky#automaticky-test-api).

## --tip--

Každý požadavek je jeden `test('popis', async () => { … })`. V těle pošli `fetch` na
`baseUrl` s metodou, hlavičkou a tělem jako frontend a výsledek ověř přes `assert.equal`.
Adresu v `Location` porovnej s id výpůjčky, které přijde v těle odpovědi.

## --tip--

Stejná kostra pro test rezervace stolu:

```text
test('obsazený stůl vrátí 409')
  1. pošli POST /api/reservations na stůl, který je v datech obsazený
  2. ověř, že stav odpovědi je 409
```

# --seed--

## --file-- package.json

```json
{
  "name": "api-knihovny",
  "type": "module"
}
```

## --file-- data.js

```js
// Data knihovny v paměti. Po restartu serveru se vrátí do původního stavu.
export const books = [
  { id: 1, title: 'Krakatit', author: 'Karel Čapek', genre: 'sci-fi', year: 1924 },
  { id: 2, title: 'Saturnin', author: 'Zdeněk Jirotka', genre: 'humor', year: 1942 },
  { id: 3, title: 'Babička', author: 'Božena Němcová', genre: 'klasika', year: 1855 },
  { id: 4, title: 'Válka s mloky', author: 'Karel Čapek', genre: 'sci-fi', year: 1936 },
  { id: 5, title: 'Černí baroni', author: 'Miloslav Švandrlík', genre: 'humor', year: 1969 },
  { id: 6, title: 'Cirkus Humberto', author: 'Eduard Bass', genre: 'klasika', year: 1941 },
  { id: 7, title: 'Chata v Jezerní kotlině', author: 'Jaroslav Foglar', genre: 'dobrodružství', year: 1938 },
  { id: 8, title: 'Hordubal', author: 'Karel Čapek', genre: 'klasika', year: 1933 },
  { id: 9, title: 'Osudy dobrého vojáka Švejka', author: 'Jaroslav Hašek', genre: 'humor', year: 1921 },
  { id: 10, title: 'Povídky malostranské', author: 'Jan Neruda', genre: 'klasika', year: 1878 },
  { id: 11, title: 'Klapzubova jedenáctka', author: 'Eduard Bass', genre: 'humor', year: 1922 },
  { id: 12, title: 'Továrna na absolutno', author: 'Karel Čapek', genre: 'sci-fi', year: 1922 },
];

// Aktivní výpůjčky. Kniha je vypůjčená, dokud k ní tu existuje výpůjčka.
export const loans = [
  { id: 1, bookId: 4, reader: 'Jana Dvořáková', dueDate: '2026-10-14' },
  { id: 2, bookId: 9, reader: 'Petr Horák', dueDate: '2026-10-02' },
];
```

## --file-- http.js

```js
// Pomocné funkce pro odpovědi. Každá odpověď serveru prochází přes sendJson.

export function sendJson(res, status, data) {
  res.writeHead(status, { 'Content-Type': 'application/json; charset=utf-8' });
  res.end(JSON.stringify(data));
}

export function sendError(res, status, message) {
  sendJson(res, status, { error: message });
}

// Převede text z query na celé číslo v rozsahu. Když to nejde, vrátí null.
export function parseIntInRange(text, min, max) {
  const number = Number(text);
  if (text.trim() === '' || !Number.isInteger(number) || number < min || number > max) {
    return null;
  }
  return number;
}

// Přečte tělo požadavku a převede ho z JSON. Když tělo není platný JSON, vrátí undefined.
export async function readJson(req) {
  const chunks = [];
  for await (const chunk of req) {
    chunks.push(chunk);
  }
  try {
    return JSON.parse(Buffer.concat(chunks).toString('utf8'));
  } catch {
    return undefined;
  }
}
```

## --file-- router.js

```js
// Porovná cestu se vzorem routy. Vrátí objekt s parametry, nebo null.
export function matchPath(pattern, pathname) {
  const patternParts = pattern.split('/');
  const pathParts = pathname.split('/');
  if (patternParts.length !== pathParts.length) return null;
  const params = {};
  for (let i = 0; i < patternParts.length; i++) {
    if (patternParts[i].startsWith(':')) {
      params[patternParts[i].slice(1)] = pathParts[i];
    } else if (patternParts[i] !== pathParts[i]) {
      return null;
    }
  }
  return params;
}
```

## --file-- routes.js

```js
import { listBooks, getBook } from './books.js';
import { createLoan, deleteLoan, getLoan, listLoans, updateLoan } from './loans.js';

// Tabulka rout: metoda, cesta a funkce, která požadavek obslouží.
export const routes = [
  { method: 'GET', path: '/api/books', handler: listBooks },
  { method: 'GET', path: '/api/books/:id', handler: getBook },
  { method: 'GET', path: '/api/loans', handler: listLoans },
  { method: 'GET', path: '/api/loans/:id', handler: getLoan },
  { method: 'POST', path: '/api/loans', handler: createLoan },
  { method: 'PATCH', path: '/api/loans/:id', handler: updateLoan },
  { method: 'DELETE', path: '/api/loans/:id', handler: deleteLoan },
];
```

## --file-- books.js

```js
import { books, loans } from './data.js';
import { parseIntInRange, sendError, sendJson } from './http.js';

// Kniha tak, jak ji vidí klient: data z katalogu a dostupnost spočítaná z výpůjček.
function toBookResponse(book) {
  return { ...book, available: !loans.some((loan) => loan.bookId === book.id) };
}

// Povolená řazení. Klient smí poslat jen jméno, které tu je.
const sorters = {
  title: (a, b) => a.title.localeCompare(b.title, 'cs'),
  author: (a, b) => a.author.localeCompare(b.author, 'cs'),
  year: (a, b) => a.year - b.year,
};

export function listBooks(req, res, { query }) {
  const genre = query.get('genre');
  let result = genre === null ? books : books.filter((book) => book.genre === genre);
  const sort = query.get('sort');
  if (sort !== null) {
    if (!Object.hasOwn(sorters, sort)) {
      return sendError(res, 400, 'Řadit jde jen podle title, author nebo year.');
    }
    result = result.toSorted(sorters[sort]);
  }

  const page = parseIntInRange(query.get('page') ?? '1', 1, Infinity);
  const limit = parseIntInRange(query.get('limit') ?? '5', 1, 50);
  if (page === null || limit === null) {
    return sendError(res, 400, 'page musí být celé číslo od 1 a limit celé číslo od 1 do 50.');
  }
  const start = (page - 1) * limit;
  sendJson(res, 200, {
    data: result.slice(start, start + limit).map(toBookResponse),
    meta: { page, limit, total: result.length, totalPages: Math.ceil(result.length / limit) },
  });
}

export function getBook(req, res, { params }) {
  const book = books.find((item) => item.id === Number(params.id));
  if (!book) {
    return sendError(res, 404, 'Kniha nenalezena.');
  }
  sendJson(res, 200, toBookResponse(book));
}
```

## --file-- server.js

```js
import { createServer } from 'node:http';
import { sendError } from './http.js';
import { matchPath } from './router.js';
import { routes } from './routes.js';

const port = Number(process.env.PORT ?? 3000);

async function handleRequest(req, res) {
  const { pathname, searchParams } = new URL(req.url, 'http://localhost');

  const allowedMethods = [];
  for (const route of routes) {
    const params = matchPath(route.path, pathname);
    if (params === null) {
      continue;
    }
    if (route.method === req.method) {
      return route.handler(req, res, { params, query: searchParams });
    }
    allowedMethods.push(route.method);
  }

  if (allowedMethods.length > 0) {
    res.setHeader('Allow', allowedMethods.join(', '));
    return sendError(res, 405, 'Tahle metoda tu není povolená.');
  }
  sendError(res, 404, 'Tahle adresa neexistuje.');
}

createServer(async (req, res) => {
  try {
    await handleRequest(req, res);
  } catch (error) {
    console.error(error);
    sendError(res, 500, 'Na serveru se něco pokazilo.');
  }
}).listen(port, () => {
  console.log(`API knihovny běží na http://localhost:${port}`);
});
```

## --file-- loans.js

```js
import { books, loans } from './data.js';
import { parseIntInRange, readJson, sendError, sendJson } from './http.js';

// Datum vrácení: dnes + 28 dní, ve tvaru RRRR-MM-DD.
function dueDateFromToday() {
  const date = new Date();
  date.setDate(date.getDate() + 28);
  return date.toISOString().slice(0, 10);
}

export function listLoans(req, res, { query }) {
  const page = parseIntInRange(query.get('page') ?? '1', 1, Infinity);
  const limit = parseIntInRange(query.get('limit') ?? '5', 1, 50);
  if (page === null || limit === null) {
    return sendError(res, 400, 'page musí být celé číslo od 1 a limit celé číslo od 1 do 50.');
  }
  const start = (page - 1) * limit;
  sendJson(res, 200, {
    data: loans.slice(start, start + limit),
    meta: { page, limit, total: loans.length, totalPages: Math.ceil(loans.length / limit) },
  });
}

export function getLoan(req, res, { params }) {
  const loan = loans.find((item) => item.id === Number(params.id));
  if (!loan) {
    return sendError(res, 404, 'Výpůjčka nenalezena.');
  }
  sendJson(res, 200, loan);
}

export async function createLoan(req, res) {
  const input = await readJson(req);
  if (typeof input !== 'object' || input === null) {
    return sendError(res, 400, 'Tělo požadavku musí být JSON objekt.');
  }
  if (!Number.isInteger(input.bookId) || !books.some((book) => book.id === input.bookId)) {
    return sendError(res, 422, 'bookId musí být id existující knihy.');
  }
  if (typeof input.reader !== 'string' || input.reader.trim() === '') {
    return sendError(res, 422, 'reader musí být jméno čtenáře.');
  }
  if (loans.some((loan) => loan.bookId === input.bookId)) {
    return sendError(res, 409, 'Kniha je právě vypůjčená.');
  }

  const loan = {
    id: Math.max(0, ...loans.map((item) => item.id)) + 1,
    bookId: input.bookId,
    reader: input.reader,
    dueDate: dueDateFromToday(),
  };
  loans.push(loan);
  res.setHeader('Location', `/api/loans/${loan.id}`);
  sendJson(res, 201, loan);
}

export async function updateLoan(req, res, { params }) {
  const loan = loans.find((item) => item.id === Number(params.id));
  if (!loan) {
    return sendError(res, 404, 'Výpůjčka nenalezena.');
  }
  const input = await readJson(req);
  if (typeof input !== 'object' || input === null) {
    return sendError(res, 400, 'Tělo požadavku musí být JSON objekt.');
  }
  const { dueDate } = input;
  if (typeof dueDate !== 'string' || !/^\d{4}-\d{2}-\d{2}$/.test(dueDate) || dueDate <= loan.dueDate) {
    return sendError(res, 422, 'dueDate musí být datum RRRR-MM-DD pozdější než současné datum vrácení.');
  }

  loan.dueDate = dueDate;
  sendJson(res, 200, loan);
}

export function deleteLoan(req, res, { params }) {
  const index = loans.findIndex((item) => item.id === Number(params.id));
  if (index === -1) {
    return sendError(res, 404, 'Výpůjčka nenalezena.');
  }
  loans.splice(index, 1);
  res.writeHead(204);
  res.end();
}
```

## --file-- loans.test.js

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';

// Adresu běžícího API dostane test z proměnné prostředí:
// API_URL=http://localhost:3000 node --test loans.test.js
const baseUrl = process.env.API_URL;
--edit--

--edit--
```

# --solution--

## --file-- loans.test.js

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';

// Adresu běžícího API dostane test z proměnné prostředí:
// API_URL=http://localhost:3000 node --test loans.test.js
const baseUrl = process.env.API_URL;

function borrow(bookId, reader) {
  return fetch(`${baseUrl}/api/loans`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ bookId, reader }),
  });
}

test('výpůjčka volné knihy vrátí 201 a Location nové výpůjčky', async () => {
  const res = await borrow(3, 'Eva Malá');
  assert.equal(res.status, 201);
  const loan = await res.json();
  assert.equal(res.headers.get('location'), `/api/loans/${loan.id}`);
});

test('výpůjčka už vypůjčené knihy vrátí 409', async () => {
  const res = await borrow(9, 'Ondřej Novák');
  assert.equal(res.status, 409);
});
```

