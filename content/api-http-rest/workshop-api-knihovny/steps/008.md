---
title: Stránkování s metadaty
main: server.js
see: api-http-rest/navrh-rest#strankovani-offset-a-kurzor
---

# --description--

Knihovna má dnes 12 knih, za rok jich bude 12 000. Seznam, který posílá všechno
najednou, pak zdržuje server, síť i prohlížeč. Katalog proto chce stránky:
`GET /api/books?page=2&limit=5` = druhá pětice knih.

Samotné knihy frontendu nestačí. Aby vykreslil „Strana 2 z 3" a vypnul tlačítko
Další na poslední straně, potřebuje vědět, kolik knih a stránek je celkem. Seznam
proto zabalíš do objektu s daty a metadaty:

```json
{
  "data": [{ "id": 6, "title": "Cirkus Humberto" }],
  "meta": { "page": 2, "limit": 5, "total": 12, "totalPages": 3 }
}
```

**Úkol:** uprav `listBooks` v `books.js`:

- `page` (výchozí `1`) a `limit` (výchozí `5`) přečti z query,
- vrať jen knihy dané stránky, v objektu `{ data, meta }` s klíči `page`, `limit`,
  `total` (počet knih **po filtru**, před stránkováním) a `totalPages`,
- filtr a řazení se použijí **před** stránkováním, jinak by `?sort=title&page=2`
  seřadil jen pět knih z druhé stránky.

Neplatné hodnoty (`page=0`, `limit=abc`) teď neřeš, přijdou na řadu v dalším kroku.

Budeš potřebovat převést text z query na číslo, spočítat, kde stránka začíná, a vyříznout
kus pole. Kandidáti: `Number()`, `slice()`, `splice()`, `Math.ceil()`, `Math.round()`.
Dva z nich se sem nehodí — `splice` mění pole na místě.

# --hints--

`GET /api/books` vrátí první pětici knih a metadata `{ page: 1, limit: 5, total: 12, totalPages: 3 }`.

```js
const server = await helpers.startServer('server.js');
const res = await fetch(`${server.url}/api/books`, { signal: AbortSignal.timeout(3000) })
  .catch(() => assert.fail('GET /api/books: server do 3 sekund neodpověděl'));
assert.equal(res.status, 200, 'GET /api/books má vrátit stav 200');
const body = await res.json();
assert.equal(Array.isArray(body.data), true, `GET /api/books má vrátit objekt s polem data, přišlo: ${JSON.stringify(body).slice(0, 80)}`);
assert.deepEqual(body.data.map((book) => book.id), [1, 2, 3, 4, 5], 'GET /api/books má v data vrátit knihy 1 až 5');
assert.deepEqual(body.meta, { page: 1, limit: 5, total: 12, totalPages: 3 }, 'GET /api/books má vrátit meta { page: 1, limit: 5, total: 12, totalPages: 3 } — čísla, ne texty');
```

`?page=3` vrátí poslední dvě knihy a `?page=2&limit=4` knihy 5 až 8.

```js
const server = await helpers.startServer('server.js');
const get = (path) => fetch(`${server.url}${path}`, { signal: AbortSignal.timeout(3000) })
  .then((res) => res.json())
  .catch(() => assert.fail(`GET ${path}: server do 3 sekund neodpověděl`));
const third = await get('/api/books?page=3');
assert.deepEqual(third.data.map((book) => book.id), [11, 12], 'GET /api/books?page=3 má vrátit knihy 11 a 12');
assert.deepEqual(third.meta, { page: 3, limit: 5, total: 12, totalPages: 3 }, 'GET /api/books?page=3 má vrátit meta { page: 3, limit: 5, total: 12, totalPages: 3 }');
const custom = await get('/api/books?page=2&limit=4');
assert.deepEqual(custom.data.map((book) => book.id), [5, 6, 7, 8], 'GET /api/books?page=2&limit=4 má vrátit knihy 5 až 8');
assert.deepEqual(custom.meta, { page: 2, limit: 4, total: 12, totalPages: 3 }, 'GET /api/books?page=2&limit=4 má vrátit meta { page: 2, limit: 4, total: 12, totalPages: 3 }');
```

Filtr a řazení se použijí před stránkováním a `total` počítá knihy po filtru.

```js
const server = await helpers.startServer('server.js');
const get = (path) => fetch(`${server.url}${path}`, { signal: AbortSignal.timeout(3000) })
  .then((res) => res.json())
  .catch(() => assert.fail(`GET ${path}: server do 3 sekund neodpověděl`));
const sorted = await get('/api/books?sort=title&page=2&limit=3');
assert.deepEqual(sorted.data.map((book) => book.title), ['Hordubal', 'Chata v Jezerní kotlině', 'Klapzubova jedenáctka'],
  'GET /api/books?sort=title&page=2&limit=3 má vrátit 4. až 6. knihu podle abecedy — řadíš před stránkováním?');
const filtered = await get('/api/books?genre=humor&limit=3&page=2');
assert.deepEqual(filtered.data.map((book) => book.id), [11], 'GET /api/books?genre=humor&limit=3&page=2 má vrátit jen knihu 11');
assert.deepEqual(filtered.meta, { page: 2, limit: 3, total: 4, totalPages: 2 }, 'GET /api/books?genre=humor&limit=3&page=2 má mít total 4 (humor má 4 knihy) a totalPages 2');
```

Neplatné řazení vrací dál `400`.

```js
const server = await helpers.startServer('server.js');
const res = await fetch(`${server.url}/api/books?sort=heslo`, { signal: AbortSignal.timeout(3000) })
  .catch(() => assert.fail('GET /api/books?sort=heslo: server do 3 sekund neodpověděl'));
assert.equal(res.status, 400, 'GET /api/books?sort=heslo má dál vracet stav 400');
```

# --help--

## --tip--

Stránkování přes číslo stránky a počet položek vysvětluje
[Stránkování: offset a kurzor](see:api-http-rest/navrh-rest#strankovani-offset-a-kurzor).

## --tip--

Stránka `page` začíná na indexu `(page - 1) * limit` a má `limit` položek. `slice(od, do)`
bere prvky od indexu `od` po index `do` (bez něj). `totalPages` je počet knih dělený
`limit`, zaokrouhlený nahoru.

## --tip--

Stejný výpočet pro fotky v galerii, 20 na stránku, stránka 4:

```text
start = (4 - 1) * 20 = 60     → photos.slice(60, 80)
pages = Math.ceil(135 / 20) = 7
```

# --seed--

## --file-- package.json

```json
{
  "name": "api-knihovny",
  "type": "module"
}
```

## --file-- data.js

```js
// Data knihovny v paměti. Po restartu serveru se vrátí do původního stavu.
export const books = [
  { id: 1, title: 'Krakatit', author: 'Karel Čapek', genre: 'sci-fi', year: 1924 },
  { id: 2, title: 'Saturnin', author: 'Zdeněk Jirotka', genre: 'humor', year: 1942 },
  { id: 3, title: 'Babička', author: 'Božena Němcová', genre: 'klasika', year: 1855 },
  { id: 4, title: 'Válka s mloky', author: 'Karel Čapek', genre: 'sci-fi', year: 1936 },
  { id: 5, title: 'Černí baroni', author: 'Miloslav Švandrlík', genre: 'humor', year: 1969 },
  { id: 6, title: 'Cirkus Humberto', author: 'Eduard Bass', genre: 'klasika', year: 1941 },
  { id: 7, title: 'Chata v Jezerní kotlině', author: 'Jaroslav Foglar', genre: 'dobrodružství', year: 1938 },
  { id: 8, title: 'Hordubal', author: 'Karel Čapek', genre: 'klasika', year: 1933 },
  { id: 9, title: 'Osudy dobrého vojáka Švejka', author: 'Jaroslav Hašek', genre: 'humor', year: 1921 },
  { id: 10, title: 'Povídky malostranské', author: 'Jan Neruda', genre: 'klasika', year: 1878 },
  { id: 11, title: 'Klapzubova jedenáctka', author: 'Eduard Bass', genre: 'humor', year: 1922 },
  { id: 12, title: 'Továrna na absolutno', author: 'Karel Čapek', genre: 'sci-fi', year: 1922 },
];

// Aktivní výpůjčky. Kniha je vypůjčená, dokud k ní tu existuje výpůjčka.
export const loans = [
  { id: 1, bookId: 4, reader: 'Jana Dvořáková', dueDate: '2026-10-14' },
  { id: 2, bookId: 9, reader: 'Petr Horák', dueDate: '2026-10-02' },
];
```

## --file-- http.js

```js
// Pomocné funkce pro odpovědi. Každá odpověď serveru prochází přes sendJson.

export function sendJson(res, status, data) {
  res.writeHead(status, { 'Content-Type': 'application/json; charset=utf-8' });
  res.end(JSON.stringify(data));
}

export function sendError(res, status, message) {
  sendJson(res, status, { error: message });
}

// Přečte tělo požadavku a převede ho z JSON. Když tělo není platný JSON, vrátí undefined.
export async function readJson(req) {
  const chunks = [];
  for await (const chunk of req) {
    chunks.push(chunk);
  }
  try {
    return JSON.parse(Buffer.concat(chunks).toString('utf8'));
  } catch {
    return undefined;
  }
}
```

## --file-- router.js

```js
// Porovná cestu se vzorem routy. Vrátí objekt s parametry, nebo null.
export function matchPath(pattern, pathname) {
  const patternParts = pattern.split('/');
  const pathParts = pathname.split('/');
  if (patternParts.length !== pathParts.length) return null;
  const params = {};
  for (let i = 0; i < patternParts.length; i++) {
    if (patternParts[i].startsWith(':')) {
      params[patternParts[i].slice(1)] = pathParts[i];
    } else if (patternParts[i] !== pathParts[i]) {
      return null;
    }
  }
  return params;
}
```

## --file-- routes.js

```js
import { listBooks, getBook } from './books.js';

// Tabulka rout: metoda, cesta a funkce, která požadavek obslouží.
export const routes = [
  { method: 'GET', path: '/api/books', handler: listBooks },
  { method: 'GET', path: '/api/books/:id', handler: getBook },
];
```

## --file-- books.js

```js
import { books } from './data.js';
import { sendError, sendJson } from './http.js';

// Povolená řazení. Klient smí poslat jen jméno, které tu je.
const sorters = {
  title: (a, b) => a.title.localeCompare(b.title, 'cs'),
  author: (a, b) => a.author.localeCompare(b.author, 'cs'),
  year: (a, b) => a.year - b.year,
};

export function listBooks(req, res, { query }) {
  const genre = query.get('genre');
  let result = genre === null ? books : books.filter((book) => book.genre === genre);
  const sort = query.get('sort');
--edit--
  if (sort !== null) {
    if (!Object.hasOwn(sorters, sort)) {
      return sendError(res, 400, 'Řadit jde jen podle title, author nebo year.');
    }
    result = result.toSorted(sorters[sort]);
  }
  sendJson(res, 200, result);
--edit--
}

export function getBook(req, res, { params }) {
  const book = books.find((item) => item.id === Number(params.id));
  if (!book) {
    return sendError(res, 404, 'Kniha nenalezena.');
  }
  sendJson(res, 200, book);
}
```

## --file-- server.js

```js
import { createServer } from 'node:http';
import { sendError } from './http.js';
import { matchPath } from './router.js';
import { routes } from './routes.js';

const port = Number(process.env.PORT ?? 3000);

async function handleRequest(req, res) {
  const { pathname, searchParams } = new URL(req.url, 'http://localhost');

  const allowedMethods = [];
  for (const route of routes) {
    const params = matchPath(route.path, pathname);
    if (params === null) {
      continue;
    }
    if (route.method === req.method) {
      return route.handler(req, res, { params, query: searchParams });
    }
    allowedMethods.push(route.method);
  }

  if (allowedMethods.length > 0) {
    res.setHeader('Allow', allowedMethods.join(', '));
    return sendError(res, 405, 'Tahle metoda tu není povolená.');
  }
  sendError(res, 404, 'Tahle adresa neexistuje.');
}

createServer(async (req, res) => {
  try {
    await handleRequest(req, res);
  } catch (error) {
    console.error(error);
    sendError(res, 500, 'Na serveru se něco pokazilo.');
  }
}).listen(port, () => {
  console.log(`API knihovny běží na http://localhost:${port}`);
});
```

# --solution--

## --file-- books.js

```js
import { books } from './data.js';
import { sendError, sendJson } from './http.js';

// Povolená řazení. Klient smí poslat jen jméno, které tu je.
const sorters = {
  title: (a, b) => a.title.localeCompare(b.title, 'cs'),
  author: (a, b) => a.author.localeCompare(b.author, 'cs'),
  year: (a, b) => a.year - b.year,
};

export function listBooks(req, res, { query }) {
  const genre = query.get('genre');
  let result = genre === null ? books : books.filter((book) => book.genre === genre);
  const sort = query.get('sort');
  if (sort !== null) {
    if (!Object.hasOwn(sorters, sort)) {
      return sendError(res, 400, 'Řadit jde jen podle title, author nebo year.');
    }
    result = result.toSorted(sorters[sort]);
  }

  const page = Number(query.get('page') ?? 1);
  const limit = Number(query.get('limit') ?? 5);
  const start = (page - 1) * limit;
  sendJson(res, 200, {
    data: result.slice(start, start + limit),
    meta: { page, limit, total: result.length, totalPages: Math.ceil(result.length / limit) },
  });
}

export function getBook(req, res, { params }) {
  const book = books.find((item) => item.id === Number(params.id));
  if (!book) {
    return sendError(res, 404, 'Kniha nenalezena.');
  }
  sendJson(res, 200, book);
}
```
