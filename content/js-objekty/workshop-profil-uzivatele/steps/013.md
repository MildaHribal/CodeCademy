---
title: Veřejný profil bez soukromých údajů
see: js-objekty/objekty#destrukturalizace-vlastnosti-rovnou-do-promennych
---

# --description--

Firmy vidí veřejnou verzi profilu. E-mail a nastavení soukromí do ní nepatří.

**Úkol:** Napiš funkci `publicProfile(profile)`, která vrátí nový objekt se všemi vlastnostmi profilu **kromě** `email` a `privacy`. Původní profil se nezmění.

Kandidáti: [[zbytek vlastností]] (*rest*) v destrukturalizaci, nebo kopie a v ní smazání dvou vlastností. Rest vytvoří nový objekt sám a vystačíš s jedním řádkem. Tvar najdeš v lekci o objektech u destrukturalizace.

# --hints--

`publicProfile` vrátí profil bez `email` a `privacy`.

```js
const result = publicProfile(savedProfile);
assert.equal(Object.hasOwn(result, 'email'), false, 'publicProfile(savedProfile) nemá obsahovat email');
assert.equal(Object.hasOwn(result, 'privacy'), false, 'publicProfile(savedProfile) nemá obsahovat privacy');
assert.equal(result.fullName, 'Jana Dvořáková', 'publicProfile má zachovat fullName');
assert.deepEqual(result.skills, ['HTML', 'CSS', 'JavaScript'], 'publicProfile má zachovat skills');
```

Uložený profil se nezmění.

```js
publicProfile(savedProfile);
assert.equal(savedProfile.email, 'jana.dvorakova@example.cz', 'publicProfile nesmí smazat e-mail z uloženého profilu');
assert.equal(Object.hasOwn(savedProfile, 'privacy'), true, 'publicProfile nesmí smazat privacy z uloženého profilu');
```

# --help--

## --tip--

Zbytek vlastností je v lekci v části [Destrukturalizace](see:js-objekty/objekty#destrukturalizace-vlastnosti-rovnou-do-promennych).

## --tip--

Ve složených závorkách destrukturalizace vyjmenuj `email` a `privacy` a na konec napiš tři tečky se jménem nové proměnné. Tu proměnnou vrať.

## --tip--

Na jiných datech: `const { password, ...safeUser } = user;` dá do `safeUser` všechno kromě hesla.

# --seed--

## --file-- script.js

```js
// Uložený profil vývojářky na pracovním portálu, jak ho vrací API.
const savedProfile = {
  id: 481,
  username: 'jana-dev',
  fullName: 'Jana Dvořáková',
  headline: 'Frontend vývojářka',
  email: 'jana.dvorakova@example.cz',
  location: { city: 'Brno', remote: true },
  links: { github: 'github.com/jana-dev', linkedin: null, website: 'jana-dev.example.cz' },
  privacy: { showEmail: false, showCity: true },
  skills: ['HTML', 'CSS', 'JavaScript'],
};

// Od kolegy: profil s novým nastavením soukromí, třeba updatePrivacy(savedProfile, { showEmail: true }).
function updatePrivacy(profile, changes) {
  const privacy = { ...profile.privacy };
  for (const [key, value] of Object.entries(changes)) {
    privacy[key] = value;
  }
  return { ...profile, privacy };
}

// Od kolegy: profil pro náhled firmám, bez odkazů, které nejsou vyplněné (delete smaže vlastnost).
function withoutEmptyLinks(profile) {
  const copy = { ...profile };
  for (const [network, url] of Object.entries(copy.links)) {
    if (url === null) {
      delete copy.links[network];
    }
  }
  return copy;
}

// Jméno na profilu, bez celého jména přezdívka se zavináčem.
function displayName(profile) {
  return profile.fullName ?? `@${profile.username}`;
}

// Profil z tvaru, který posílá starší API (snake_case), do tvaru aplikace.
function fromApi(apiUser) {
  const { user_name: username, full_name: fullName = null, job_title: headline = 'Vývojář' } = apiUser;
  return { username, fullName, headline };
}

// Nový profil s jiným popisem, původní zůstane beze změny.
function withHeadline(profile, headline) {
  return { ...profile, headline };
}

// Profil se změnami z formuláře; id a username změnit nejde.
function updateProfile(profile, changes) {
  return { ...profile, ...changes, id: profile.id, username: profile.username };
}

function createProfile({
  username,
  fullName = null,
  headline = 'Vývojář',
  city = null,
  remote = false,
} = {}) {
  return { username, fullName, headline, location: { city, remote }, skills: [] };
}

// Tři odkazy profilu, chybějící mají null.
function normalizeLinks({ github = null, linkedin = null, website = null } = {}) {
  return { github, linkedin, website };
}

// Nový profil s jiným městem, původní i jeho location zůstanou.
function withCity(profile, city) {
  return { ...profile, location: { ...profile.location, city } };
}

// Nový profil s odkazem pro zadanou síť.
function setLink(profile, network, url) {
  return { ...profile, links: { ...profile.links, [network]: url } };
}

// Nový profil s dovedností navíc; dovednost, která už v profilu je, se nepřidá.
function addSkill(profile, skill) {
  if (profile.skills.includes(skill)) {
    return profile;
  }
  return { ...profile, skills: [...profile.skills, skill] };
}

--edit--

--edit--
```

# --solution--

## --file-- script.js

```js
// Uložený profil vývojářky na pracovním portálu, jak ho vrací API.
const savedProfile = {
  id: 481,
  username: 'jana-dev',
  fullName: 'Jana Dvořáková',
  headline: 'Frontend vývojářka',
  email: 'jana.dvorakova@example.cz',
  location: { city: 'Brno', remote: true },
  links: { github: 'github.com/jana-dev', linkedin: null, website: 'jana-dev.example.cz' },
  privacy: { showEmail: false, showCity: true },
  skills: ['HTML', 'CSS', 'JavaScript'],
};

// Od kolegy: profil s novým nastavením soukromí, třeba updatePrivacy(savedProfile, { showEmail: true }).
function updatePrivacy(profile, changes) {
  const privacy = { ...profile.privacy };
  for (const [key, value] of Object.entries(changes)) {
    privacy[key] = value;
  }
  return { ...profile, privacy };
}

// Od kolegy: profil pro náhled firmám, bez odkazů, které nejsou vyplněné (delete smaže vlastnost).
function withoutEmptyLinks(profile) {
  const copy = { ...profile };
  for (const [network, url] of Object.entries(copy.links)) {
    if (url === null) {
      delete copy.links[network];
    }
  }
  return copy;
}

// Jméno na profilu, bez celého jména přezdívka se zavináčem.
function displayName(profile) {
  return profile.fullName ?? `@${profile.username}`;
}

// Profil z tvaru, který posílá starší API (snake_case), do tvaru aplikace.
function fromApi(apiUser) {
  const { user_name: username, full_name: fullName = null, job_title: headline = 'Vývojář' } = apiUser;
  return { username, fullName, headline };
}

// Nový profil s jiným popisem, původní zůstane beze změny.
function withHeadline(profile, headline) {
  return { ...profile, headline };
}

// Profil se změnami z formuláře; id a username změnit nejde.
function updateProfile(profile, changes) {
  return { ...profile, ...changes, id: profile.id, username: profile.username };
}

function createProfile({
  username,
  fullName = null,
  headline = 'Vývojář',
  city = null,
  remote = false,
} = {}) {
  return { username, fullName, headline, location: { city, remote }, skills: [] };
}

// Tři odkazy profilu, chybějící mají null.
function normalizeLinks({ github = null, linkedin = null, website = null } = {}) {
  return { github, linkedin, website };
}

// Nový profil s jiným městem, původní i jeho location zůstanou.
function withCity(profile, city) {
  return { ...profile, location: { ...profile.location, city } };
}

// Nový profil s odkazem pro zadanou síť.
function setLink(profile, network, url) {
  return { ...profile, links: { ...profile.links, [network]: url } };
}

// Nový profil s dovedností navíc; dovednost, která už v profilu je, se nepřidá.
function addSkill(profile, skill) {
  if (profile.skills.includes(skill)) {
    return profile;
  }
  return { ...profile, skills: [...profile.skills, skill] };
}

// Veřejný profil bez e-mailu a nastavení soukromí.
function publicProfile(profile) {
  const { email, privacy, ...visible } = profile;
  return visible;
}
```
