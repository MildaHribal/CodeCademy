---
title: "Opakování: přepínač soukromí"
kind: recall
see: js-objekty/objekty#vypocitany-klic-v-literalu
---

# --description--

Na stránce **Soukromí** jsou dva přepínače. Každé kliknutí otočí jedno nastavení: z `true` na `false` a naopak.

**Úkol:** Napiš funkci `togglePrivacy(profile, setting)`, která vrátí nový profil, ve kterém má `privacy[setting]` opačnou hodnotu. Druhé nastavení zůstane a uložený profil ani jeho `privacy` se nezmění.

Tentokrát bez návodu.

# --hints--

`togglePrivacy` otočí zadané nastavení a druhé nechá.

```js
assert.deepEqual(togglePrivacy(savedProfile, 'showEmail').privacy, { showEmail: true, showCity: true }, "togglePrivacy(savedProfile, 'showEmail').privacy má být { showEmail: true, showCity: true }");
assert.deepEqual(togglePrivacy(savedProfile, 'showCity').privacy, { showEmail: false, showCity: false }, "togglePrivacy(savedProfile, 'showCity').privacy má být { showEmail: false, showCity: false }");
```

Dvě otočení za sebou vrátí původní hodnotu a uložený profil se nezmění.

```js
const twice = togglePrivacy(togglePrivacy(savedProfile, 'showEmail'), 'showEmail');
assert.equal(twice.privacy.showEmail, false, 'dvojí togglePrivacy(…, showEmail) má vrátit showEmail: false');
assert.deepEqual(savedProfile.privacy, { showEmail: false, showCity: true }, 'togglePrivacy nesmí změnit savedProfile.privacy');
assert.notEqual(twice.privacy, savedProfile.privacy, 'výsledek togglePrivacy má mít nový objekt privacy');
```

# --help--

## --tip--

Je to stejný tvar jako `setLink` — viz [vypočítaný klíč](see:js-objekty/objekty#vypocitany-klic-v-literalu).

## --tip--

Nový profil, v něm nový objekt `privacy`, a v něm klíč z proměnné `setting` s opačnou hodnotou toho, co je v původním `privacy`.

## --tip--

Podcíle na jiných datech — přepínač notifikací:

```js
// 1. nový účet ze starého
// 2. nový objekt notifications ze starých notifikací
// 3. klíč z proměnné channel s opakem původní hodnoty
```

# --seed--

## --file-- script.js

```js
// Uložený profil vývojářky na pracovním portálu, jak ho vrací API.
const savedProfile = {
  id: 481,
  username: 'jana-dev',
  fullName: 'Jana Dvořáková',
  headline: 'Frontend vývojářka',
  email: 'jana.dvorakova@example.cz',
  location: { city: 'Brno', remote: true },
  links: { github: 'github.com/jana-dev', linkedin: null, website: 'jana-dev.example.cz' },
  privacy: { showEmail: false, showCity: true },
  skills: ['HTML', 'CSS', 'JavaScript'],
};

// Od kolegy: profil s novým nastavením soukromí, třeba updatePrivacy(savedProfile, { showEmail: true }).
function updatePrivacy(profile, changes) {
  const privacy = { ...profile.privacy };
  for (const [key, value] of Object.entries(changes)) {
    privacy[key] = value;
  }
  return { ...profile, privacy };
}

// Od kolegy: profil pro náhled firmám, bez odkazů, které nejsou vyplněné (delete smaže vlastnost).
function withoutEmptyLinks(profile) {
  const copy = { ...profile, links: { ...profile.links } };
  for (const [network, url] of Object.entries(copy.links)) {
    if (url === null) {
      delete copy.links[network];
    }
  }
  return copy;
}

// Jméno na profilu, bez celého jména přezdívka se zavináčem.
function displayName(profile) {
  return profile.fullName ?? `@${profile.username}`;
}

// Profil z tvaru, který posílá starší API (snake_case), do tvaru aplikace.
function fromApi(apiUser) {
  const { user_name: username, full_name: fullName = null, job_title: headline = 'Vývojář' } = apiUser;
  return { username, fullName, headline };
}

// Nový profil s jiným popisem, původní zůstane beze změny.
function withHeadline(profile, headline) {
  return { ...profile, headline };
}

// Profil se změnami z formuláře; id a username změnit nejde.
function updateProfile(profile, changes) {
  return { ...profile, ...changes, id: profile.id, username: profile.username };
}

function createProfile({
  username,
  fullName = null,
  headline = 'Vývojář',
  city = null,
  remote = false,
} = {}) {
  return { username, fullName, headline, location: { city, remote }, skills: [] };
}

// Tři odkazy profilu, chybějící mají null.
function normalizeLinks({ github = null, linkedin = null, website = null } = {}) {
  return { github, linkedin, website };
}

// Nový profil s jiným městem, původní i jeho location zůstanou.
function withCity(profile, city) {
  return { ...profile, location: { ...profile.location, city } };
}

// Nový profil s odkazem pro zadanou síť.
function setLink(profile, network, url) {
  return { ...profile, links: { ...profile.links, [network]: url } };
}

// Nový profil s dovedností navíc; dovednost, která už v profilu je, se nepřidá.
function addSkill(profile, skill) {
  if (profile.skills.includes(skill)) {
    return profile;
  }
  return { ...profile, skills: [...profile.skills, skill] };
}

// Veřejný profil podle nastavení soukromí.
function publicProfile(profile) {
  const { email, privacy, ...visible } = profile;
  let result = visible;
  if (privacy.showEmail) {
    result = { ...result, email };
  }
  if (!privacy.showCity) {
    result = { ...result, location: { ...result.location, city: null } };
  }
  return result;
}

// Jména vlastností prvního patra, které se v konceptu liší od uloženého profilu.
function changedFields(saved, draft) {
  const changed = [];
  for (const key of Object.keys(saved)) {
    if (saved[key] !== draft[key]) {
      changed.push(key);
    }
  }
  return changed;
}

// Údaj je vyplněný, když není null, undefined ani prázdný text.
function isFilled(value) {
  return value !== null && value !== undefined && value !== '';
}

// Procento vyplněných údajů profilu (6 údajů), zaokrouhlené.
function completeness(profile) {
  const values = [
    profile.fullName,
    profile.headline,
    profile.location?.city,
    profile.links?.github,
    profile.links?.linkedin,
  ];
  let filled = 0;
  for (const value of values) {
    if (isFilled(value)) {
      filled += 1;
    }
  }
  if ((profile.skills?.length ?? 0) > 0) {
    filled += 1;
  }
  return Math.round((filled / 6) * 100);
}

--edit--

--edit--
```

# --solution--

## --file-- script.js

```js
// Uložený profil vývojářky na pracovním portálu, jak ho vrací API.
const savedProfile = {
  id: 481,
  username: 'jana-dev',
  fullName: 'Jana Dvořáková',
  headline: 'Frontend vývojářka',
  email: 'jana.dvorakova@example.cz',
  location: { city: 'Brno', remote: true },
  links: { github: 'github.com/jana-dev', linkedin: null, website: 'jana-dev.example.cz' },
  privacy: { showEmail: false, showCity: true },
  skills: ['HTML', 'CSS', 'JavaScript'],
};

// Od kolegy: profil s novým nastavením soukromí, třeba updatePrivacy(savedProfile, { showEmail: true }).
function updatePrivacy(profile, changes) {
  const privacy = { ...profile.privacy };
  for (const [key, value] of Object.entries(changes)) {
    privacy[key] = value;
  }
  return { ...profile, privacy };
}

// Od kolegy: profil pro náhled firmám, bez odkazů, které nejsou vyplněné (delete smaže vlastnost).
function withoutEmptyLinks(profile) {
  const copy = { ...profile, links: { ...profile.links } };
  for (const [network, url] of Object.entries(copy.links)) {
    if (url === null) {
      delete copy.links[network];
    }
  }
  return copy;
}

// Jméno na profilu, bez celého jména přezdívka se zavináčem.
function displayName(profile) {
  return profile.fullName ?? `@${profile.username}`;
}

// Profil z tvaru, který posílá starší API (snake_case), do tvaru aplikace.
function fromApi(apiUser) {
  const { user_name: username, full_name: fullName = null, job_title: headline = 'Vývojář' } = apiUser;
  return { username, fullName, headline };
}

// Nový profil s jiným popisem, původní zůstane beze změny.
function withHeadline(profile, headline) {
  return { ...profile, headline };
}

// Profil se změnami z formuláře; id a username změnit nejde.
function updateProfile(profile, changes) {
  return { ...profile, ...changes, id: profile.id, username: profile.username };
}

function createProfile({
  username,
  fullName = null,
  headline = 'Vývojář',
  city = null,
  remote = false,
} = {}) {
  return { username, fullName, headline, location: { city, remote }, skills: [] };
}

// Tři odkazy profilu, chybějící mají null.
function normalizeLinks({ github = null, linkedin = null, website = null } = {}) {
  return { github, linkedin, website };
}

// Nový profil s jiným městem, původní i jeho location zůstanou.
function withCity(profile, city) {
  return { ...profile, location: { ...profile.location, city } };
}

// Nový profil s odkazem pro zadanou síť.
function setLink(profile, network, url) {
  return { ...profile, links: { ...profile.links, [network]: url } };
}

// Nový profil s dovedností navíc; dovednost, která už v profilu je, se nepřidá.
function addSkill(profile, skill) {
  if (profile.skills.includes(skill)) {
    return profile;
  }
  return { ...profile, skills: [...profile.skills, skill] };
}

// Veřejný profil podle nastavení soukromí.
function publicProfile(profile) {
  const { email, privacy, ...visible } = profile;
  let result = visible;
  if (privacy.showEmail) {
    result = { ...result, email };
  }
  if (!privacy.showCity) {
    result = { ...result, location: { ...result.location, city: null } };
  }
  return result;
}

// Jména vlastností prvního patra, které se v konceptu liší od uloženého profilu.
function changedFields(saved, draft) {
  const changed = [];
  for (const key of Object.keys(saved)) {
    if (saved[key] !== draft[key]) {
      changed.push(key);
    }
  }
  return changed;
}

// Údaj je vyplněný, když není null, undefined ani prázdný text.
function isFilled(value) {
  return value !== null && value !== undefined && value !== '';
}

// Procento vyplněných údajů profilu (6 údajů), zaokrouhlené.
function completeness(profile) {
  const values = [
    profile.fullName,
    profile.headline,
    profile.location?.city,
    profile.links?.github,
    profile.links?.linkedin,
  ];
  let filled = 0;
  for (const value of values) {
    if (isFilled(value)) {
      filled += 1;
    }
  }
  if ((profile.skills?.length ?? 0) > 0) {
    filled += 1;
  }
  return Math.round((filled / 6) * 100);
}

// Nový profil s otočeným nastavením soukromí.
function togglePrivacy(profile, setting) {
  return { ...profile, privacy: { ...profile.privacy, [setting]: !profile.privacy[setting] } };
}
```
