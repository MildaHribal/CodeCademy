---
title: Seřaď bezpečné načtení
kind: parsons
see: js-objekty/kopie-a-json#bezpecne-nacteni-json
---

# --description--

Při startu aplikace se uložený text přečte zpátky. Text ale nemusí existovat (první spuštění) a nemusí to být platný JSON (uživatel ho upravil, zápis se přerušil). Aplikace kvůli tomu nesmí spadnout.

**Úkol:** Seřaď řádky tak, aby `readSaved()` vrátila uložené nastavení z `localStorage`, a když tam nic není nebo text nejde přečíst, prázdný objekt. Tvar znáš z lekce, píšeš ho poprvé, proto ho tentokrát skládáš; v kroku s importem ho napíšeš sám.

Odsazení nastavíš klávesou Tab, do mezery doplň hodnotu, kterou funkce vrátí při chybě. Dva řádky jsou navíc.

# --hints--

Bez uloženého textu vrátí `readSaved` prázdný objekt.

```js
assert.deepEqual(readSaved(), {}, 'readSaved() bez uloženého nastavení má vrátit {}');
```

S platným JSON vrátí `readSaved` uložená data.

```js
localStorage.setItem(STORAGE_KEY, '{"theme":"dark","playback":{"speed":1.5}}');
assert.deepEqual(readSaved(), { theme: 'dark', playback: { speed: 1.5 } }, 'readSaved() má vrátit uložené { theme: dark, playback: { speed: 1.5 } }');
```

S poškozeným textem vrátí `readSaved` prázdný objekt a nespadne.

```js
localStorage.setItem(STORAGE_KEY, '{"theme": "da');
assert.deepEqual(readSaved(), {}, 'readSaved() s poškozeným textem má vrátit {}, ne spadnout na SyntaxError');
```

# --help--

## --tip--

Postup je v lekci v části [Bezpečné načtení JSON](see:js-objekty/kopie-a-json#bezpecne-nacteni-json).

## --tip--

`localStorage.getItem` vrátí `null`, když pod klíčem nic není. `JSON.parse` patří do `try` a náhradní hodnota do `catch`.

# --parsons--

```js
function readSaved() {
  const text = localStorage.getItem(STORAGE_KEY);
  if (text === null) {
    return {};
  }
  try {
    return JSON.parse(text);
  } catch {
    return __1__;
  }
}
```

## --distractors--

```js
return JSON.stringify(text);
if (text === '') {
```

## --blanks--

```text
1: {}
```

# --seed--

## --file-- script.js

```js
// Výchozí nastavení přehrávače podcastů. Uživatel si mění jen část, zbytek se bere odsud.
const DEFAULT_SETTINGS = {
  theme: 'system',
  lastSynced: null,
  playback: { speed: 1, skipSeconds: 15, autoplay: true },
  downloads: { wifiOnly: true, quality: 'vysoká', keepEpisodes: 5 },
  notifications: { newEpisodes: true, weeklySummary: false },
};

// Klíč, pod kterým se nastavení ukládá do localStorage.
const STORAGE_KEY = 'poslech:settings';

// Od kolegy: předvolby, které jde zapnout jedním tlačítkem.
const PRESETS = {
  dataSaver: { downloads: { wifiOnly: true, quality: 'nízká', keepEpisodes: 2 } },
  nightOwl: { theme: 'dark', notifications: { newEpisodes: false, weeklySummary: false } },
};

// Od kolegy: nastavení se zapnutou předvolbou, třeba applyPreset('dataSaver').
function applyPreset(presetName) {
  return Object.assign(DEFAULT_SETTINGS, PRESETS[presetName]);
}

// Od kolegy: text do patičky panelu, třeba 'Synchronizováno 14. 9. 2026'.
function syncedLabel(settings) {
  if (settings.lastSynced === null) {
    return 'Zatím nesynchronizováno';
  }
  return `Synchronizováno ${settings.lastSynced.toLocaleDateString('cs-CZ')}`;
}

// Nové nastavení s jiným tématem.
function withTheme(settings, theme) {
  return { ...settings, theme };
}

// Úplné nastavení: uložené hodnoty přes výchozí, každá skupina sloučená zvlášť.
function mergeSettings(defaults, saved) {
  return {
    ...defaults,
    ...saved,
    playback: { ...defaults.playback, ...saved.playback },
    downloads: { ...defaults.downloads, ...saved.downloads },
    notifications: { ...defaults.notifications, ...saved.notifications },
  };
}

// Uloží nastavení do localStorage jako JSON.
function saveSettings(settings) {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(settings));
}

--edit--
--edit--
```
