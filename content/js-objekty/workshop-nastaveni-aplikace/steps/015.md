---
title: Jen to, co se změnilo
see: js-objekty/kopie-a-json#kopie-po-patrech
---

# --description--

Do úložiště stačí zapsat jen to, čím se nastavení liší od výchozího. Text je kratší, a hlavně: když příští verze aplikace změní výchozí hodnotu, kterou uživatel nikdy neměnil, dostane ji taky.

**Úkol:** Napiš funkci `changedSettings(defaults, settings)`, která vrátí nový objekt jen s rozdíly:

- vlastnost prvního patra, která není skupina (`theme`, `lastSynced`), je ve výsledku, když se její hodnota liší od výchozí (`!==`),
- skupina je ve výsledku, jen když se v ní liší aspoň jedna hodnota, a obsahuje jen ty lišící se hodnoty,
- skupinu poznáš podle výchozí hodnoty: je to objekt, který není `null`,
- když se nic neliší, vrátí prázdný objekt.

# --hints--

`changedSettings` vrátí změněné téma a ve skupině jen změněnou rychlost.

```js
const settings = mergeSettings(DEFAULT_SETTINGS, { theme: 'dark', playback: { speed: 1.5 } });
assert.deepEqual(changedSettings(DEFAULT_SETTINGS, settings), { theme: 'dark', playback: { speed: 1.5 } }, "changedSettings má vrátit { theme: 'dark', playback: { speed: 1.5 } }");
```

Beze změn vrátí `changedSettings` prázdný objekt.

```js
assert.deepEqual(changedSettings(DEFAULT_SETTINGS, loadSettings()), {}, 'changedSettings(DEFAULT_SETTINGS, loadSettings()) bez uložených změn má vrátit {}');
```

Datum synchronizace se ve výsledku objeví jako datum.

```js
const now = new Date('2026-09-14T10:00:00Z');
const result = changedSettings(DEFAULT_SETTINGS, markSynced(loadSettings(), now));
assert.equal(result.lastSynced, now, 'changedSettings má vrátit lastSynced, které se liší od výchozího null — pozor, typeof null je object');
assert.deepEqual(Object.keys(result), ['lastSynced'], 'changedSettings má pro nastavení jen s datem synchronizace vrátit jen lastSynced');
```

# --help--

## --tip--

Postup po patrech znáš z [Kopie po patrech](see:js-objekty/kopie-a-json#kopie-po-patrech); tady ho jen obracíš.

## --tip--

Projdi dvojice výchozího nastavení. U skupiny projdi i její dvojice a lišící se hodnoty sbírej do nového objektu, který přidáš, jen když má nějaký klíč. Skupinu hlídej přes `typeof … === 'object'` **a** zvlášť `!== null`, jinak spadneš na `lastSynced`.

## --tip--

Podcíle na jiných datech — změny v předvolbách tisku:

```js
// 1. prázdný výsledek
// 2. pro každou výchozí dvojici: skupina? → porovnej její hodnoty a přidej jen lišící se
// 3. jinak porovnej hodnotu a přidej, když se liší
```

# --seed--

## --file-- script.js

```js
// Výchozí nastavení přehrávače podcastů. Uživatel si mění jen část, zbytek se bere odsud.
const DEFAULT_SETTINGS = {
  theme: 'system',
  lastSynced: null,
  playback: { speed: 1, skipSeconds: 15, autoplay: true },
  downloads: { wifiOnly: true, quality: 'vysoká', keepEpisodes: 5 },
  notifications: { newEpisodes: true, weeklySummary: false },
};

// Klíč, pod kterým se nastavení ukládá do localStorage.
const STORAGE_KEY = 'poslech:settings';

// Od kolegy: předvolby, které jde zapnout jedním tlačítkem.
const PRESETS = {
  dataSaver: { downloads: { wifiOnly: true, quality: 'nízká', keepEpisodes: 2 } },
  nightOwl: { theme: 'dark', notifications: { newEpisodes: false, weeklySummary: false } },
};

// Od kolegy: nastavení se zapnutou předvolbou, třeba applyPreset('dataSaver').
function applyPreset(presetName) {
  return Object.assign({}, DEFAULT_SETTINGS, PRESETS[presetName]);
}

// Od kolegy: text do patičky panelu, třeba 'Synchronizováno 14. 9. 2026'.
function syncedLabel(settings) {
  if (settings.lastSynced === null) {
    return 'Zatím nesynchronizováno';
  }
  return `Synchronizováno ${new Date(settings.lastSynced).toLocaleDateString('cs-CZ')}`;
}

// Nové nastavení s jiným tématem.
function withTheme(settings, theme) {
  return { ...settings, theme };
}

// Úplné nastavení: uložené hodnoty přes výchozí, každá skupina sloučená zvlášť.
function mergeSettings(defaults, saved) {
  return {
    ...defaults,
    ...saved,
    playback: { ...defaults.playback, ...saved.playback },
    downloads: { ...defaults.downloads, ...saved.downloads },
    notifications: { ...defaults.notifications, ...saved.notifications },
  };
}

// Uloží nastavení do localStorage jako JSON.
function saveSettings(settings) {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(settings));
}

function readSaved() {
  const text = localStorage.getItem(STORAGE_KEY);
  if (text === null) {
    return {};
  }
  try {
    return JSON.parse(text);
  } catch {
    return {};
  }
}

// Úplné nastavení při startu: uložené hodnoty doplněné výchozími.
function loadSettings() {
  return mergeSettings(DEFAULT_SETTINGS, readSaved());
}

// Nastavení z vloženého textu, nebo null, když to nastavení není.
function importSettings(text) {
  let parsed;
  try {
    parsed = JSON.parse(text);
  } catch {
    return null;
  }
  if (typeof parsed !== 'object' || parsed === null) {
    return null;
  }
  return mergeSettings(DEFAULT_SETTINGS, parsed);
}

// JSON k exportu: bez data synchronizace, odsazený pro čtení.
function exportSettings(settings) {
  const { lastSynced, ...exportable } = settings;
  return JSON.stringify(exportable, null, 2);
}

// Nastavení ze starého plochého formátu (verze 1) do formátu se skupinami.
function migrateV1(saved) {
  const { speed, wifiOnly, ...rest } = saved;
  const playback = {};
  if (speed !== undefined) {
    playback.speed = speed;
  }
  const downloads = {};
  if (wifiOnly !== undefined) {
    downloads.wifiOnly = wifiOnly;
  }
  return { ...rest, playback, downloads };
}

// Nové nastavení s datem synchronizace, bez data aktuální čas.
function markSynced(settings, now = new Date()) {
  return { ...settings, lastSynced: now };
}

// Koncept nastavení: hluboká kopie, do které jde zapisovat.
function draftFrom(settings) {
  return structuredClone(settings);
}

// Krátký JSON ke sdílení: jen téma a přehrávání.
function shareableSettings(settings) {
  return JSON.stringify({ theme: settings.theme, playback: settings.playback });
}

// Liší se koncept od uloženého nastavení? Pořadí klíčů nerozhoduje.
function hasUnsavedChanges(saved, draft) {
  const savedText = JSON.stringify(mergeSettings(DEFAULT_SETTINGS, saved));
  const draftText = JSON.stringify(mergeSettings(DEFAULT_SETTINGS, draft));
  return savedText !== draftText;
}

--edit--

--edit--
```

# --solution--

## --file-- script.js

```js
// Výchozí nastavení přehrávače podcastů. Uživatel si mění jen část, zbytek se bere odsud.
const DEFAULT_SETTINGS = {
  theme: 'system',
  lastSynced: null,
  playback: { speed: 1, skipSeconds: 15, autoplay: true },
  downloads: { wifiOnly: true, quality: 'vysoká', keepEpisodes: 5 },
  notifications: { newEpisodes: true, weeklySummary: false },
};

// Klíč, pod kterým se nastavení ukládá do localStorage.
const STORAGE_KEY = 'poslech:settings';

// Od kolegy: předvolby, které jde zapnout jedním tlačítkem.
const PRESETS = {
  dataSaver: { downloads: { wifiOnly: true, quality: 'nízká', keepEpisodes: 2 } },
  nightOwl: { theme: 'dark', notifications: { newEpisodes: false, weeklySummary: false } },
};

// Od kolegy: nastavení se zapnutou předvolbou, třeba applyPreset('dataSaver').
function applyPreset(presetName) {
  return Object.assign({}, DEFAULT_SETTINGS, PRESETS[presetName]);
}

// Od kolegy: text do patičky panelu, třeba 'Synchronizováno 14. 9. 2026'.
function syncedLabel(settings) {
  if (settings.lastSynced === null) {
    return 'Zatím nesynchronizováno';
  }
  return `Synchronizováno ${new Date(settings.lastSynced).toLocaleDateString('cs-CZ')}`;
}

// Nové nastavení s jiným tématem.
function withTheme(settings, theme) {
  return { ...settings, theme };
}

// Úplné nastavení: uložené hodnoty přes výchozí, každá skupina sloučená zvlášť.
function mergeSettings(defaults, saved) {
  return {
    ...defaults,
    ...saved,
    playback: { ...defaults.playback, ...saved.playback },
    downloads: { ...defaults.downloads, ...saved.downloads },
    notifications: { ...defaults.notifications, ...saved.notifications },
  };
}

// Uloží nastavení do localStorage jako JSON.
function saveSettings(settings) {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(settings));
}

function readSaved() {
  const text = localStorage.getItem(STORAGE_KEY);
  if (text === null) {
    return {};
  }
  try {
    return JSON.parse(text);
  } catch {
    return {};
  }
}

// Úplné nastavení při startu: uložené hodnoty doplněné výchozími.
function loadSettings() {
  return mergeSettings(DEFAULT_SETTINGS, readSaved());
}

// Nastavení z vloženého textu, nebo null, když to nastavení není.
function importSettings(text) {
  let parsed;
  try {
    parsed = JSON.parse(text);
  } catch {
    return null;
  }
  if (typeof parsed !== 'object' || parsed === null) {
    return null;
  }
  return mergeSettings(DEFAULT_SETTINGS, parsed);
}

// JSON k exportu: bez data synchronizace, odsazený pro čtení.
function exportSettings(settings) {
  const { lastSynced, ...exportable } = settings;
  return JSON.stringify(exportable, null, 2);
}

// Nastavení ze starého plochého formátu (verze 1) do formátu se skupinami.
function migrateV1(saved) {
  const { speed, wifiOnly, ...rest } = saved;
  const playback = {};
  if (speed !== undefined) {
    playback.speed = speed;
  }
  const downloads = {};
  if (wifiOnly !== undefined) {
    downloads.wifiOnly = wifiOnly;
  }
  return { ...rest, playback, downloads };
}

// Nové nastavení s datem synchronizace, bez data aktuální čas.
function markSynced(settings, now = new Date()) {
  return { ...settings, lastSynced: now };
}

// Koncept nastavení: hluboká kopie, do které jde zapisovat.
function draftFrom(settings) {
  return structuredClone(settings);
}

// Krátký JSON ke sdílení: jen téma a přehrávání.
function shareableSettings(settings) {
  return JSON.stringify({ theme: settings.theme, playback: settings.playback });
}

// Liší se koncept od uloženého nastavení? Pořadí klíčů nerozhoduje.
function hasUnsavedChanges(saved, draft) {
  const savedText = JSON.stringify(mergeSettings(DEFAULT_SETTINGS, saved));
  const draftText = JSON.stringify(mergeSettings(DEFAULT_SETTINGS, draft));
  return savedText !== draftText;
}

// Jen hodnoty, kterými se nastavení liší od výchozího.
function changedSettings(defaults, settings) {
  const changes = {};
  for (const [key, defaultValue] of Object.entries(defaults)) {
    const value = settings[key];
    if (typeof defaultValue === 'object' && defaultValue !== null) {
      const groupChanges = {};
      for (const [name, defaultGroupValue] of Object.entries(defaultValue)) {
        if (value[name] !== defaultGroupValue) {
          groupChanges[name] = value[name];
        }
      }
      if (Object.keys(groupChanges).length > 0) {
        changes[key] = groupChanges;
      }
    } else if (value !== defaultValue) {
      changes[key] = value;
    }
  }
  return changes;
}
```
