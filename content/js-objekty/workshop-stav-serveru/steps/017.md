---
title: "Oprava: kolegův motdBanner"
kind: debug
see: js-objekty/objekty#destrukturalizace-z-undefined
---

# --description--

## Hlášení

Kolega přidal na konec souboru funkci `motdBanner`, která píše do pruhu nad kartou zprávu dne. S daty serveru funguje: `motdBanner(kostkov)` vrátí `'Zpráva dne: Nová mapa od pátku!'`.

Než ale monitorovací služba data pošle, zavolá stránka `motdBanner()` bez argumentu, aby pruh ukázal výchozí text `Dnes bez zprávy`. Místo toho stránka spadne a v konzoli je:

```text
TypeError: Cannot read properties of undefined (reading 'motd')
```

## Úkol

Oprav `motdBanner` tak, aby bez argumentu vrátila `'Zpráva dne: Dnes bez zprávy'` a se serverem fungovala jako dřív. Změň co nejmenší kus kódu.

# --hints--

`motdBanner()` bez argumentu vrátí výchozí zprávu.

```js
assert.equal(motdBanner(), 'Zpráva dne: Dnes bez zprávy', "motdBanner() bez argumentu má vrátit 'Zpráva dne: Dnes bez zprávy', ne spadnout");
```

Se serverem vrací `motdBanner` jeho zprávu, bez zprávy tu výchozí.

```js
assert.equal(motdBanner(kostkov), 'Zpráva dne: Nová mapa od pátku!', "motdBanner(kostkov) má dál vracet 'Zpráva dne: Nová mapa od pátku!'");
assert.equal(motdBanner({ name: 'Lobby' }), 'Zpráva dne: Dnes bez zprávy', "motdBanner({ name: 'Lobby' }) má vrátit výchozí zprávu — Lobby motd nemá");
```

Ostatní funkce fungují dál.

```js
assert.equal(joinAddress(kostkov), 'mc.kostkov.cz:25565', "joinAddress(kostkov) má dál vracet 'mc.kostkov.cz:25565'");
assert.equal(ownerName(kostkov), 'bez majitele', "ownerName(kostkov) má dál vracet 'bez majitele'");
```

# --help--

## --tip--

Chyba odpovídá pasti [Destrukturalizace z `undefined`](see:js-objekty/objekty#destrukturalizace-z-undefined).

## --tip--

Co dostane parametr, když funkci zavoláš bez argumentu? Z té hodnoty se pak destrukturalizace pokouší číst `motd`. Výchozí hodnotu může mít i celý parametr, nejen vlastnost uvnitř.

## --tip--

Na jiných datech: `function badge({ color = 'šedá' } = {})` dá celému parametru výchozí prázdný objekt, takže `badge()` nespadne.

# --seed--

## --file-- script.js

```js
// Stav herního serveru, jak ho vrací monitorovací služba.
// ping = odezva v milisekundách podle regionu, ze kterého se hráč připojuje.
const kostkov = {
  name: 'Kostkov Survival',
  address: 'mc.kostkov.cz',
  version: '26.1',
  online: true,
  motd: 'Nová mapa od pátku!',
  players: { online: 37, max: 100 },
  ping: { eu: 24, us: 131, asia: 248 },
  rules: { pvp: false, whitelist: true, difficulty: 'normal' },
  owner: null,
};

// Od kolegy: hodnota jednoho pravidla, třeba ruleValue(kostkov, 'difficulty').
function ruleValue(server, ruleName) {
  return server.rules[ruleName];
}

// Text s počtem hráčů, třeba '37 z 100 hráčů'.
function playersText(online, max) {
  return `${online} z ${max} hráčů`;
}

// Název a verze, třeba 'Kostkov Survival (26.1)'.
function serverTitle(server) {
  return `${server.name} (${server.version})`;
}

// Text o hráčích serveru, třeba '37 z 100 hráčů'.
function playersLabel(server) {
  return playersText(server.players.online, server.players.max);
}

// Odezva v ms pro zadaný region, třeba pingFor(kostkov, 'us').
function pingFor(server, region) {
  return server.ping[region];
}

// Jedno měření odezvy jako objekt, třeba { asia: 231 }.
function pingEntry(region, ms) {
  return { [region]: ms };
}

// Od kolegy: jedno měření pro graf odezvy, třeba measurement('eu', 24).
const measurement = (region, ms) => ({ region, ms });

// Hranice odezvy a jejich slovní hodnocení.
const pingQuality = {
  goodLimit: 60,
  okLimit: 150,
  label(ms) {
    if (ms <= pingQuality.goodLimit) {
      return 'výborný';
    }
    if (ms <= pingQuality.okLimit) {
      return 'dobrý';
    }
    return 'slabý';
  },
};

// Jméno majitele, nebo 'bez majitele'.
function ownerName(server) {
  return server.owner?.name ?? 'bez majitele';
}

// Uvádí server pravidlo se zadaným jménem (i s hodnotou false)?
function hasRule(server, rule) {
  return Object.hasOwn(server.rules, rule);
}

// Z kolika regionů se měří odezva.
function regionCount(server) {
  return Object.keys(server.ping).length;
}

// Průměrná odezva zaokrouhlená na celé ms, bez měření null.
function averagePing(server) {
  const values = Object.values(server.ping);
  if (values.length === 0) {
    return null;
  }
  let total = 0;
  for (const ms of values) {
    total += ms;
  }
  return Math.round(total / values.length);
}

// Popisky odezvy s jednotkou, třeba { eu: '24 ms' }.
function pingLabels(server) {
  const pairs = [];
  for (const [region, ms] of Object.entries(server.ping)) {
    pairs.push([region, `${ms} ms`]);
  }
  return Object.fromEntries(pairs);
}

// Slovní hodnocení odezvy pro každý region.
function qualityByRegion(server) {
  const pairs = [];
  for (const [region, ms] of Object.entries(server.ping)) {
    pairs.push([region, pingQuality.label(ms)]);
  }
  return Object.fromEntries(pairs);
}

// Region s nejnižší odezvou; při shodě ten dřívější, bez měření undefined.
function bestRegion(server) {
  let best;
  let bestPing;
  for (const [region, ms] of Object.entries(server.ping)) {
    if (best === undefined || ms < bestPing) {
      best = region;
      bestPing = ms;
    }
  }
  return best;
}

// Adresa pro připojení i s portem, bez portu výchozí 25565.
function joinAddress({ address, port = 25565 }) {
  return `${address}:${port}`;
}

--edit--
// Od kolegy: pruh se zprávou dne nad kartou, třeba motdBanner(kostkov).
function motdBanner({ motd = 'Dnes bez zprávy' }) {
  return `Zpráva dne: ${motd}`;
}
--edit--
```

# --solution--

## --file-- script.js

```js
// Stav herního serveru, jak ho vrací monitorovací služba.
// ping = odezva v milisekundách podle regionu, ze kterého se hráč připojuje.
const kostkov = {
  name: 'Kostkov Survival',
  address: 'mc.kostkov.cz',
  version: '26.1',
  online: true,
  motd: 'Nová mapa od pátku!',
  players: { online: 37, max: 100 },
  ping: { eu: 24, us: 131, asia: 248 },
  rules: { pvp: false, whitelist: true, difficulty: 'normal' },
  owner: null,
};

// Od kolegy: hodnota jednoho pravidla, třeba ruleValue(kostkov, 'difficulty').
function ruleValue(server, ruleName) {
  return server.rules[ruleName];
}

// Text s počtem hráčů, třeba '37 z 100 hráčů'.
function playersText(online, max) {
  return `${online} z ${max} hráčů`;
}

// Název a verze, třeba 'Kostkov Survival (26.1)'.
function serverTitle(server) {
  return `${server.name} (${server.version})`;
}

// Text o hráčích serveru, třeba '37 z 100 hráčů'.
function playersLabel(server) {
  return playersText(server.players.online, server.players.max);
}

// Odezva v ms pro zadaný region, třeba pingFor(kostkov, 'us').
function pingFor(server, region) {
  return server.ping[region];
}

// Jedno měření odezvy jako objekt, třeba { asia: 231 }.
function pingEntry(region, ms) {
  return { [region]: ms };
}

// Od kolegy: jedno měření pro graf odezvy, třeba measurement('eu', 24).
const measurement = (region, ms) => ({ region, ms });

// Hranice odezvy a jejich slovní hodnocení.
const pingQuality = {
  goodLimit: 60,
  okLimit: 150,
  label(ms) {
    if (ms <= pingQuality.goodLimit) {
      return 'výborný';
    }
    if (ms <= pingQuality.okLimit) {
      return 'dobrý';
    }
    return 'slabý';
  },
};

// Jméno majitele, nebo 'bez majitele'.
function ownerName(server) {
  return server.owner?.name ?? 'bez majitele';
}

// Uvádí server pravidlo se zadaným jménem (i s hodnotou false)?
function hasRule(server, rule) {
  return Object.hasOwn(server.rules, rule);
}

// Z kolika regionů se měří odezva.
function regionCount(server) {
  return Object.keys(server.ping).length;
}

// Průměrná odezva zaokrouhlená na celé ms, bez měření null.
function averagePing(server) {
  const values = Object.values(server.ping);
  if (values.length === 0) {
    return null;
  }
  let total = 0;
  for (const ms of values) {
    total += ms;
  }
  return Math.round(total / values.length);
}

// Popisky odezvy s jednotkou, třeba { eu: '24 ms' }.
function pingLabels(server) {
  const pairs = [];
  for (const [region, ms] of Object.entries(server.ping)) {
    pairs.push([region, `${ms} ms`]);
  }
  return Object.fromEntries(pairs);
}

// Slovní hodnocení odezvy pro každý region.
function qualityByRegion(server) {
  const pairs = [];
  for (const [region, ms] of Object.entries(server.ping)) {
    pairs.push([region, pingQuality.label(ms)]);
  }
  return Object.fromEntries(pairs);
}

// Region s nejnižší odezvou; při shodě ten dřívější, bez měření undefined.
function bestRegion(server) {
  let best;
  let bestPing;
  for (const [region, ms] of Object.entries(server.ping)) {
    if (best === undefined || ms < bestPing) {
      best = region;
      bestPing = ms;
    }
  }
  return best;
}

// Adresa pro připojení i s portem, bez portu výchozí 25565.
function joinAddress({ address, port = 25565 }) {
  return `${address}:${port}`;
}

// Od kolegy: pruh se zprávou dne nad kartou, třeba motdBanner(kostkov).
function motdBanner({ motd = 'Dnes bez zprávy' } = {}) {
  return `Zpráva dne: ${motd}`;
}
```
