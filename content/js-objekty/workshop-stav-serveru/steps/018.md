---
title: "Opakování: řádek stavu s destrukturalizací"
kind: recall
see: js-objekty/objekty#destrukturalizace-vlastnosti-rovnou-do-promennych
---

# --description--

Do záhlaví stránky se hodí jeden řádek se stavem serveru, třeba `Kostkov Survival · online · 37 z 100 hráčů`. Když server neběží, řádek hráče nezajímá: `Kostkov Survival · offline`.

**Úkol:** Napiš funkci `statusLine(server)`, která vrátí řádek stavu v jednom z těch dvou tvarů (oddělovač je mezera, tečka `·` a mezera). Vlastnosti `name`, `online` a `players` si vytáhni **destrukturalizací přímo v parametru**, počet hráčů vlož přes `playersText`.

Tohle znáš z lekce, tentokrát bez návodu.

# --hints--

`statusLine(kostkov)` vrátí `'Kostkov Survival · online · 37 z 100 hráčů'`.

```js
assert.equal(statusLine(kostkov), 'Kostkov Survival · online · 37 z 100 hráčů', "statusLine(kostkov) má vrátit 'Kostkov Survival · online · 37 z 100 hráčů'");
```

Pro vypnutý server vrátí `statusLine` jen název a `offline`.

```js
const lobby = { name: 'Lobby', online: false, players: { online: 0, max: 20 } };
assert.equal(statusLine(lobby), 'Lobby · offline', "statusLine({ name: 'Lobby', online: false, … }) má vrátit 'Lobby · offline'");
```

`statusLine` bere vlastnosti destrukturalizací v parametru.

```js
const source = helpers.stripComments(statusLine.toString(), 'js');
assert.match(source, /^\s*(?:function\s*[\w$]*\s*)?\(\s*\{/, 'statusLine má mít v parametru destrukturalizaci, třeba ({ name, online, players })');
```

# --help--

## --tip--

Destrukturalizace v parametru je v lekci v části [Destrukturalizace](see:js-objekty/objekty#destrukturalizace-vlastnosti-rovnou-do-promennych).

## --tip--

Místo jména parametru napiš složené závorky se jmény vlastností, které funkce potřebuje. V těle pak podle `online` vrať jeden ze dvou textů.

## --tip--

Podcíle na jiných datech — řádek s objednávkou:

```js
// 1. function orderLine({ id, paid, total })
// 2. nezaplacená objednávka: jen číslo a 'čeká na platbu'
// 3. zaplacená: číslo, 'zaplaceno' a částka
```

# --seed--

## --file-- script.js

```js
// Stav herního serveru, jak ho vrací monitorovací služba.
// ping = odezva v milisekundách podle regionu, ze kterého se hráč připojuje.
const kostkov = {
  name: 'Kostkov Survival',
  address: 'mc.kostkov.cz',
  version: '26.1',
  online: true,
  motd: 'Nová mapa od pátku!',
  players: { online: 37, max: 100 },
  ping: { eu: 24, us: 131, asia: 248 },
  rules: { pvp: false, whitelist: true, difficulty: 'normal' },
  owner: null,
};

// Od kolegy: hodnota jednoho pravidla, třeba ruleValue(kostkov, 'difficulty').
function ruleValue(server, ruleName) {
  return server.rules[ruleName];
}

// Text s počtem hráčů, třeba '37 z 100 hráčů'.
function playersText(online, max) {
  return `${online} z ${max} hráčů`;
}

// Název a verze, třeba 'Kostkov Survival (26.1)'.
function serverTitle(server) {
  return `${server.name} (${server.version})`;
}

// Text o hráčích serveru, třeba '37 z 100 hráčů'.
function playersLabel(server) {
  return playersText(server.players.online, server.players.max);
}

// Odezva v ms pro zadaný region, třeba pingFor(kostkov, 'us').
function pingFor(server, region) {
  return server.ping[region];
}

// Jedno měření odezvy jako objekt, třeba { asia: 231 }.
function pingEntry(region, ms) {
  return { [region]: ms };
}

// Od kolegy: jedno měření pro graf odezvy, třeba measurement('eu', 24).
const measurement = (region, ms) => ({ region, ms });

// Hranice odezvy a jejich slovní hodnocení.
const pingQuality = {
  goodLimit: 60,
  okLimit: 150,
  label(ms) {
    if (ms <= pingQuality.goodLimit) {
      return 'výborný';
    }
    if (ms <= pingQuality.okLimit) {
      return 'dobrý';
    }
    return 'slabý';
  },
};

// Jméno majitele, nebo 'bez majitele'.
function ownerName(server) {
  return server.owner?.name ?? 'bez majitele';
}

// Uvádí server pravidlo se zadaným jménem (i s hodnotou false)?
function hasRule(server, rule) {
  return Object.hasOwn(server.rules, rule);
}

// Z kolika regionů se měří odezva.
function regionCount(server) {
  return Object.keys(server.ping).length;
}

// Průměrná odezva zaokrouhlená na celé ms, bez měření null.
function averagePing(server) {
  const values = Object.values(server.ping);
  if (values.length === 0) {
    return null;
  }
  let total = 0;
  for (const ms of values) {
    total += ms;
  }
  return Math.round(total / values.length);
}

function pingLabels(server) {
  const pairs = [];
  for (const [region, ms] of Object.entries(server.ping)) {
    pairs.push([region, `${ms} ms`]);
  }
  return Object.fromEntries(pairs);
}

// Slovní hodnocení odezvy pro každý region.
function qualityByRegion(server) {
  const pairs = [];
  for (const [region, ms] of Object.entries(server.ping)) {
    pairs.push([region, pingQuality.label(ms)]);
  }
  return Object.fromEntries(pairs);
}

// Region s nejnižší odezvou; při shodě ten dřívější, bez měření undefined.
function bestRegion(server) {
  let best;
  let bestPing;
  for (const [region, ms] of Object.entries(server.ping)) {
    if (best === undefined || ms < bestPing) {
      best = region;
      bestPing = ms;
    }
  }
  return best;
}

// Adresa pro připojení i s portem, bez portu výchozí 25565.
function joinAddress({ address, port = 25565 }) {
  return `${address}:${port}`;
}

// Od kolegy: pruh se zprávou dne nad kartou, třeba motdBanner(kostkov).
function motdBanner({ motd = 'Dnes bez zprávy' } = {}) {
  return `Zpráva dne: ${motd}`;
}

--edit--

--edit--
```

# --solution--

## --file-- script.js

```js
// Stav herního serveru, jak ho vrací monitorovací služba.
// ping = odezva v milisekundách podle regionu, ze kterého se hráč připojuje.
const kostkov = {
  name: 'Kostkov Survival',
  address: 'mc.kostkov.cz',
  version: '26.1',
  online: true,
  motd: 'Nová mapa od pátku!',
  players: { online: 37, max: 100 },
  ping: { eu: 24, us: 131, asia: 248 },
  rules: { pvp: false, whitelist: true, difficulty: 'normal' },
  owner: null,
};

// Od kolegy: hodnota jednoho pravidla, třeba ruleValue(kostkov, 'difficulty').
function ruleValue(server, ruleName) {
  return server.rules[ruleName];
}

// Text s počtem hráčů, třeba '37 z 100 hráčů'.
function playersText(online, max) {
  return `${online} z ${max} hráčů`;
}

// Název a verze, třeba 'Kostkov Survival (26.1)'.
function serverTitle(server) {
  return `${server.name} (${server.version})`;
}

// Text o hráčích serveru, třeba '37 z 100 hráčů'.
function playersLabel(server) {
  return playersText(server.players.online, server.players.max);
}

// Odezva v ms pro zadaný region, třeba pingFor(kostkov, 'us').
function pingFor(server, region) {
  return server.ping[region];
}

// Jedno měření odezvy jako objekt, třeba { asia: 231 }.
function pingEntry(region, ms) {
  return { [region]: ms };
}

// Od kolegy: jedno měření pro graf odezvy, třeba measurement('eu', 24).
const measurement = (region, ms) => ({ region, ms });

// Hranice odezvy a jejich slovní hodnocení.
const pingQuality = {
  goodLimit: 60,
  okLimit: 150,
  label(ms) {
    if (ms <= pingQuality.goodLimit) {
      return 'výborný';
    }
    if (ms <= pingQuality.okLimit) {
      return 'dobrý';
    }
    return 'slabý';
  },
};

// Jméno majitele, nebo 'bez majitele'.
function ownerName(server) {
  return server.owner?.name ?? 'bez majitele';
}

// Uvádí server pravidlo se zadaným jménem (i s hodnotou false)?
function hasRule(server, rule) {
  return Object.hasOwn(server.rules, rule);
}

// Z kolika regionů se měří odezva.
function regionCount(server) {
  return Object.keys(server.ping).length;
}

// Průměrná odezva zaokrouhlená na celé ms, bez měření null.
function averagePing(server) {
  const values = Object.values(server.ping);
  if (values.length === 0) {
    return null;
  }
  let total = 0;
  for (const ms of values) {
    total += ms;
  }
  return Math.round(total / values.length);
}

function pingLabels(server) {
  const pairs = [];
  for (const [region, ms] of Object.entries(server.ping)) {
    pairs.push([region, `${ms} ms`]);
  }
  return Object.fromEntries(pairs);
}

// Slovní hodnocení odezvy pro každý region.
function qualityByRegion(server) {
  const pairs = [];
  for (const [region, ms] of Object.entries(server.ping)) {
    pairs.push([region, pingQuality.label(ms)]);
  }
  return Object.fromEntries(pairs);
}

// Region s nejnižší odezvou; při shodě ten dřívější, bez měření undefined.
function bestRegion(server) {
  let best;
  let bestPing;
  for (const [region, ms] of Object.entries(server.ping)) {
    if (best === undefined || ms < bestPing) {
      best = region;
      bestPing = ms;
    }
  }
  return best;
}

// Adresa pro připojení i s portem, bez portu výchozí 25565.
function joinAddress({ address, port = 25565 }) {
  return `${address}:${port}`;
}

// Od kolegy: pruh se zprávou dne nad kartou, třeba motdBanner(kostkov).
function motdBanner({ motd = 'Dnes bez zprávy' } = {}) {
  return `Zpráva dne: ${motd}`;
}

// Řádek stavu do záhlaví, třeba 'Kostkov Survival · online · 37 z 100 hráčů'.
function statusLine({ name, online, players }) {
  if (!online) {
    return `${name} · offline`;
  }
  return `${name} · online · ${playersText(players.online, players.max)}`;
}
```
