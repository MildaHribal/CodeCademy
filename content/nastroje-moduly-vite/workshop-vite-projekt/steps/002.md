---
title: package.json a první sestavení
main: dev.mjs
timeoutMs: 30000
see: nastroje-moduly-vite/vite#zalozeni-projektu
---

# --description--

Projekt zatím nemá `package.json`, takže nikdo neví, jaké nástroje potřebuje a jak se
spouští. Doplníš ho tak, jak by ho vyrobila šablona `npm create vite`.

**Úkol:** v `package.json` vyplň:

1. `"name": "stehovani"`, `"private": true` a `"type": "module"`,
2. `"scripts"` se třemi skripty: `"dev": "vite"`, `"build": "vite build"`
   a `"preview": "vite preview"`,
3. `"devDependencies"` s Vite ve verzi `"^8.3.0"`.

Vite je nástroj pro vývoj a sestavení, do stránky se sám nedostane, a proto patří do
[[vývojová závislost|vývojových závislostí]]. Stejná struktura v jiném projektu:

```json
{
  "name": "rezervace-kurtu",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "devDependencies": {
    "typescript": "^7.0.0"
  }
}
```

Balíčky jsou v Akademii nainstalované předem, `npm install` pouštět nemusíš. Po
kontrole uvidíš, že `node --run build` vyrobil složku `dist/` s hotovým webem.

# --hints--

`package.json` je platný JSON se jménem `stehovani`, `"private": true` a `"type": "module"`.

```js
let pkg;
try {
  pkg = JSON.parse(files['package.json']);
} catch (error) {
  assert.fail(`package.json není platný JSON: ${error.message}`);
}
assert.equal(pkg.name, 'stehovani', 'package.json má mít "name": "stehovani"');
assert.equal(pkg.private, true, 'package.json má mít "private": true');
assert.equal(pkg.type, 'module', 'package.json má mít "type": "module"');
```

Skripty `dev`, `build` a `preview` pouštějí `vite`, `vite build` a `vite preview`.

```js
let pkg = {};
try { pkg = JSON.parse(files['package.json']); } catch { assert.fail('package.json není platný JSON'); }
const scripts = pkg.scripts ?? {};
assert.equal(scripts.dev?.trim(), 'vite', 'skript dev má být "vite"');
assert.equal(scripts.build?.trim(), 'vite build', 'skript build má být "vite build"');
assert.equal(scripts.preview?.trim(), 'vite preview', 'skript preview má být "vite preview"');
```

Vite je v `devDependencies` ve verzi `^8.3.0`.

```js
let pkg = {};
try { pkg = JSON.parse(files['package.json']); } catch { assert.fail('package.json není platný JSON'); }
assert.equal(pkg.devDependencies?.vite, '^8.3.0', 'devDependencies mají obsahovat "vite": "^8.3.0"');
assert.equal(pkg.dependencies?.vite, undefined, 'vite nepatří do dependencies, jen do devDependencies');
```

`node --run build` projde a vyrobí `dist/index.html` a JavaScript ve `dist/assets/`.

```js
const fs = await import('node:fs');
await helpers.run('rm -rf dist');
const build = await helpers.run('node --run build', { timeoutMs: 25000 });
assert.equal(build.code, 0, `node --run build má projít:\n${build.stdout}${build.stderr}`);
assert.ok(fs.existsSync(`${helpers.dir}/dist/index.html`), 'Po sestavení má existovat dist/index.html');
const assets = fs.existsSync(`${helpers.dir}/dist/assets`) ? fs.readdirSync(`${helpers.dir}/dist/assets`) : [];
assert.ok(assets.some((name) => name.endsWith('.js')), `Ve dist/assets má být sestavený JavaScript, je tam: ${assets.join(', ') || 'nic'}`);
```

# --help--

## --tip--

Co které pole v `package.json` znamená, najdeš v části
[`package.json`: popis projektu](see:nastroje-moduly-vite/npm-a-pnpm#package-json-popis-projektu).

## --tip--

`scripts` i `devDependencies` jsou vnořené objekty. Pozor na čárky mezi položkami
a na to, že za poslední položkou v JSON čárka být nesmí.

# --seed--

## --file-- index.html

```html
<!doctype html>
<html lang="cs">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Stěhování</title>
    <link rel="stylesheet" href="styles.css" />
    <script type="module" src="script.js"></script>
  </head>
  <body>
    <main class="board">
      <header class="board__header">
        <h1 id="move-title">Stěhování</h1>
        <p class="board__counts">
          Zbývá <strong id="open-count">0</strong> z <span id="total-count">0</span> úkolů
        </p>
        <div class="progress" aria-hidden="true">
          <div id="progress-bar" class="progress__bar"></div>
        </div>
      </header>

      <form id="add-form" class="add-form">
        <label for="task-title">Nový úkol</label>
        <input
          id="task-title"
          name="title"
          autocomplete="off"
          placeholder="Třeba: Přepsat adresu na poště"
        />
        <button type="submit">Přidat</button>
      </form>

      <ul id="task-list" class="tasks"></ul>

      <div id="empty-state" class="empty" hidden>
        <img id="empty-image" alt="" width="96" height="96" />
        <p>Všechno je hotové. Můžeš se stěhovat.</p>
      </div>

      <button id="clear-done" type="button" class="clear-done">Smazat hotové</button>
    </main>

    <template id="task-template">
      <li class="task">
        <input class="task__toggle" type="checkbox" />
        <span class="task__title"></span>
        <span class="task__room"></span>
        <button class="task__delete" type="button">×</button>
      </li>
    </template>
  </body>
</html>

```

## --file-- styles.css

```css
:root {
  --color-bg: #f4f1ea;
  --color-surface: #ffffff;
  --color-text: #2b2620;
  --color-muted: #7a7064;
  --color-accent: #c2592b;
  --color-accent-dark: #9e4520;
  --radius: 0.75rem;
  font-family: system-ui, sans-serif;
  color: var(--color-text);
  background: var(--color-bg);
}

body {
  margin: 0;
  padding: 2rem 1rem;
}

.board {
  max-width: 36rem;
  margin: 0 auto;
  padding: 1.5rem;
  background: var(--color-surface);
  border-radius: var(--radius);
  box-shadow: 0 10px 30px rgb(43 38 32 / 0.12);
}

.board__counts {
  color: var(--color-muted);
}

.progress {
  height: 0.5rem;
  background: #ece6dc;
  border-radius: 999px;
  overflow: hidden;
}

.progress__bar {
  width: var(--progress, 0%);
  height: 100%;
  background: var(--color-accent);
  transition: width 0.4s ease;
}

.add-form {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-block: 1.5rem;
}

.add-form label {
  flex-basis: 100%;
  font-weight: 600;
}

.add-form input {
  flex: 1;
  padding: 0.6rem 0.75rem;
  border: 1px solid #d8cfc2;
  border-radius: 0.5rem;
  font: inherit;
}

button {
  font: inherit;
  cursor: pointer;
}

.add-form button,
.clear-done {
  padding: 0.6rem 1rem;
  border: 0;
  border-radius: 0.5rem;
  color: #ffffff;
  background: var(--color-accent);
  transition: background-color 0.2s ease;
}

.add-form button:hover,
.clear-done:hover:not(:disabled) {
  background: var(--color-accent-dark);
}

button:focus-visible,
input:focus-visible {
  outline: 3px solid var(--color-accent);
  outline-offset: 2px;
}

.clear-done:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.tasks {
  display: grid;
  gap: 0.5rem;
  padding: 0;
  list-style: none;
}

.task {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.6rem 0.75rem;
  border: 1px solid #ece6dc;
  border-radius: 0.5rem;
}

.task.is-done .task__title {
  color: var(--color-muted);
  text-decoration: line-through;
}

.task__room {
  margin-left: auto;
  font-size: 0.85rem;
  color: var(--color-muted);
}

.task__delete {
  border: 0;
  background: none;
  font-size: 1.25rem;
  color: var(--color-muted);
}

.empty {
  text-align: center;
  color: var(--color-muted);
}

```

## --file-- script.js

```js
// Seznam úkolů na stěhování. Pole tasks je jediný zdroj pravdy.
import { countOpen, progressPercent } from './tasks.js';

const STORAGE_KEY = 'moving-tasks';

let tasks = [
  { id: 1, title: 'Zabalit knihy', room: 'obývák', done: true },
  { id: 2, title: 'Odhlásit elektřinu', room: 'úřady', done: false },
  { id: 3, title: 'Objednat stěhovací auto', room: 'úřady', done: false },
  { id: 4, title: 'Rozebrat postel', room: 'ložnice', done: false },
  { id: 5, title: 'Vyklidit lednici', room: 'kuchyň', done: false },
];

let nextId = 6;

function createTask(title) {
  const task = { id: nextId, title, room: 'ostatní', done: false, room: 'nezařazeno' };
  nextId += 1;
  return task;
}

function createTaskElement(task) {
  const template = document.querySelector('#task-template');
  const li = template.content.firstElementChild.cloneNode(true);
  li.querySelector('.task__title').textContent = task.title;
  li.querySelector('.task__room').textContent = task.room;
  li.classList.toggle('is-done', task.done);
  li.querySelector('.task__toggle').checked = task.done;
  li.querySelector('.task__delete').setAttribute('aria-label', `Smazat úkol ${task.title}`);
  li.querySelector('.task__toggle').addEventListener('change', () => toggleTask(task.id));
  li.querySelector('.task__delete').addEventListener('click', () => deleteTask(task.id));
  return li;
}

function render() {
  const items = tasks.map((task) => createTaskElement(task));
  document.querySelector('#task-list').replaceChildren(...items);
  document.querySelector('#open-count').textContent = countOpen(tasks);
  document.querySelector('#total-count').textContent = tasks.length;
  document.querySelector('#empty-state').hidden = tasks.length > 0;
  document.querySelector('#clear-done').disabled = !tasks.some((task) => task.done);
  document
    .querySelector('#progress-bar')
    .style.setProperty('--progress', `${progressPercent(tasks)}%`);
}

function toggleTask(id) {
  tasks = tasks.map((task) => (task.id === id ? { ...task, done: !task.done } : task));
  render();
}

function deleteTask(id) {
  tasks = tasks.filter((task) => task.id !== id);
  render();
}

render();

document.querySelector('#add-form').addEventListener('submit', (event) => {
  event.preventDefault();
  const title = document.querySelector('#task-title').value.trim();
  if (title === '') {
    return;
  }
  tasks = [...tasks, createTask(title)];
  render();
  event.target.reset();
});

document.querySelector('#clear-done').addEventListener('click', () => {
  tasks = tasks.filter((task) => !task.done);
  render();
});

```

## --file-- tasks.js

```js
// Čisté funkce nad seznamem úkolů. Nesahají na stránku.

export function countOpen(items) {
  return items.filter((task) => !task.done).length;
}

export function progressPercent(items) {
  if (items.length === 0) {
    return 0;
  }
  const doneCount = items.length - countOpen(items);
  return Math.round((doneCount / items.length) * 100);
}

```

## --file-- package.json

```json
{
--edit--

--edit--
}

```

## --file-- dev.mjs

```js
/* global process */
// Pomocník Akademie: tlačítko Spustit pustí vývojový server Vite.
// Ve vlastním projektu místo toho napíšeš do terminálu npm run dev.
import { createServer } from 'vite';

const server = await createServer({
  cacheDir: '.vite',
  server: { port: Number(process.env.PORT) || 5173, strictPort: true },
});
await server.listen();
server.printUrls();

```

# --solution--

## --file-- package.json

```json
{
  "name": "stehovani",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}

```
