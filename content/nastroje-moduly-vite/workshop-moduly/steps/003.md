---
title: Ceník, který zůstane uvnitř
main: app.js
see: nastroje-moduly-vite/es-moduly#modul-ma-vlastni-rozsah-platnosti
---

# --description--

Druhý modul bude ceník. Zbytek pokladny ale nepotřebuje sahat přímo do pole `menu` —
stačí mu umět najít položku podle `id`. Pole proto zůstane **uvnitř modulu** a ven
půjde jen funkce, která v něm hledá. Kdyby se ceník jednou načítal ze souboru nebo
z databáze, změníš jen `menu.js` a nikdo jiný si toho nevšimne.

**Úkol:**

1. Přesuň pole `menu` a funkci `findItem` z `app.js` do `menu.js`.
2. Exportuj jen `findItem`. Pole `menu` nech bez `export`.
3. V `app.js` přidej pod import z `money.js` import `findItem` z `./menu.js`.

Stejný vzor na jiných datech — seznam poboček je soukromý, ven jde jen hledání:

```js
// branches.js
const branches = [{ city: 'Brno', phone: '541 000 111' }];

export function findBranch(city) {
  return branches.find((branch) => branch.city === city);
}
```

Proměnná na nejvyšší úrovni modulu bez `export` je pro ostatní soubory neviditelná,
ale funkce ze stejného modulu ji normálně používá.

# --hints--

`menu.js` exportuje `findItem`: `findItem('espresso')` vrátí espresso za 55 Kč.

```js
const menuModule = await helpers.importFile('menu.js');
assert.equal(typeof menuModule.findItem, 'function', 'menu.js má exportovat funkci findItem');
const espresso = menuModule.findItem('espresso');
assert.equal(espresso?.name, 'Espresso', "findItem('espresso') má vrátit položku s name 'Espresso'");
assert.equal(espresso?.price, 55, "findItem('espresso') má vrátit položku s cenou 55");
```

Pole `menu` zůstane uvnitř `menu.js` — modul ho neexportuje.

```js
const menuModule = await helpers.importFile('menu.js');
assert.equal(typeof menuModule.findItem, 'function', 'menu.js má exportovat funkci findItem');
assert.equal('menu' in menuModule, false, 'menu.js nemá exportovat pole menu — ven patří jen findItem');
```

`app.js` si `findItem` importuje z `./menu.js` a pole ani funkci už sám nemá.

```js
const app = helpers.stripComments(files['app.js'], 'js');
assert.match(app, /import\s*\{[^}]*\bfindItem\b[^}]*\}\s*from\s*['"]\.\/menu\.js['"]/, 'app.js má obsahovat import { findItem } from \'./menu.js\'');
assert.doesNotMatch(app, /\b(const|let|var)\s+menu\b/, 'app.js už nemá obsahovat pole menu — patří do menu.js');
assert.doesNotMatch(app, /function\s+findItem\b|findItem\s*=/, 'app.js už nemá definovat vlastní findItem');
```

`node app.js` vytiskne obě účtenky jako dřív.

```js
const sameLine = (a) => a.normalize('NFC').replace(/ /g, ' ').replace(/\s+/g, ' ').trim();
const result = await helpers.run('node app.js');
assert.equal(result.code, 0, `node app.js má doběhnout bez chyby:\n${result.stderr}`);
const lines = result.stdout.split('\n').map(sameLine);
assert.ok(lines.includes('3× Domácí limonáda 207 Kč'), `Účtenka stolu 5 má mít řádek „3× Domácí limonáda 207 Kč". Vypsalo se:\n${result.stdout}`);
assert.ok(lines.includes('Celkem 215 Kč'), `Účtenka stolu 3 má končit „Celkem 215 Kč". Vypsalo se:\n${result.stdout}`);
```

# --seed--

## --file-- package.json

```json
{
  "name": "pokladna-kavarny",
  "type": "module"
}
```

## --file-- app.js

```js
// Pokladna kavárny Na Kopečku. Zatím je všechno v jednom souboru.
import { formatPrice } from './money.js';

const menu = [
  { id: 'flat-white', name: 'Flat white', price: 75 },
  { id: 'espresso', name: 'Espresso', price: 55 },
  { id: 'croissant', name: 'Croissant', price: 65 },
  { id: 'cheesecake', name: 'Cheesecake', price: 89 },
  { id: 'lemonade', name: 'Domácí limonáda', price: 69 },
];

function findItem(id) {
  return menu.find((item) => item.id === id);
}

function createOrder(table) {
  return { table, lines: [] };
}

function addItem(order, id, quantity = 1) {
  const item = findItem(id);
  const line = { name: item.name, price: item.price, quantity };
  return { ...order, lines: [...order.lines, line] };
}

function lineTotal(line) {
  return line.price * line.quantity;
}

function orderTotal(order) {
  return order.lines.reduce((sum, line) => sum + lineTotal(line), 0);
}

function padLine(left, right, width = 32) {
  return left.padEnd(width - right.length) + right;
}

function renderReceipt(order) {
  const rows = order.lines.map((line) =>
    padLine(`${line.quantity}× ${line.name}`, formatPrice(lineTotal(line))),
  );
  return [
    `Kavárna Na Kopečku · stůl ${order.table}`,
    ...rows,
    padLine('Celkem', formatPrice(orderTotal(order))),
  ].join('\n');
}

let tableThree = createOrder(3);
tableThree = addItem(tableThree, 'flat-white', 2);
tableThree = addItem(tableThree, 'croissant');

let tableFive = createOrder(5);
tableFive = addItem(tableFive, 'lemonade', 3);
tableFive = addItem(tableFive, 'cheesecake', 2);

console.log(renderReceipt(tableThree));
console.log('');
console.log(renderReceipt(tableFive));
```

## --file-- money.js

```js
// Modul pro práci s penězi.

export function formatPrice(amount) {
  return `${amount} Kč`;
}
```

## --file-- menu.js

```js
// Ceník kavárny.
--edit--

--edit--
```

# --solution--

## --file-- app.js

```js
// Pokladna kavárny Na Kopečku. Zatím je všechno v jednom souboru.
import { formatPrice } from './money.js';
import { findItem } from './menu.js';

function createOrder(table) {
  return { table, lines: [] };
}

function addItem(order, id, quantity = 1) {
  const item = findItem(id);
  const line = { name: item.name, price: item.price, quantity };
  return { ...order, lines: [...order.lines, line] };
}

function lineTotal(line) {
  return line.price * line.quantity;
}

function orderTotal(order) {
  return order.lines.reduce((sum, line) => sum + lineTotal(line), 0);
}

function padLine(left, right, width = 32) {
  return left.padEnd(width - right.length) + right;
}

function renderReceipt(order) {
  const rows = order.lines.map((line) =>
    padLine(`${line.quantity}× ${line.name}`, formatPrice(lineTotal(line))),
  );
  return [
    `Kavárna Na Kopečku · stůl ${order.table}`,
    ...rows,
    padLine('Celkem', formatPrice(orderTotal(order))),
  ].join('\n');
}

let tableThree = createOrder(3);
tableThree = addItem(tableThree, 'flat-white', 2);
tableThree = addItem(tableThree, 'croissant');

let tableFive = createOrder(5);
tableFive = addItem(tableFive, 'lemonade', 3);
tableFive = addItem(tableFive, 'cheesecake', 2);

console.log(renderReceipt(tableThree));
console.log('');
console.log(renderReceipt(tableFive));
```

## --file-- menu.js

```js
// Ceník kavárny.

const menu = [
  { id: 'flat-white', name: 'Flat white', price: 75 },
  { id: 'espresso', name: 'Espresso', price: 55 },
  { id: 'croissant', name: 'Croissant', price: 65 },
  { id: 'cheesecake', name: 'Cheesecake', price: 89 },
  { id: 'lemonade', name: 'Domácí limonáda', price: 69 },
];

export function findItem(id) {
  return menu.find((item) => item.id === id);
}
```
