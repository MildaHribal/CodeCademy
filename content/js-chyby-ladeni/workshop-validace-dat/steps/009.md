---
title: "Vyber nástroj sám: způsob dopravy"
kind: choose
see: js-chyby-ladeni/vlastni-chyby#validace-na-hranici
---

# --description--

Formulář posílá způsob dopravy jako `id` (`'zasilkovna'`, `'ppl'`, `'osobne'`). Kdokoli ale může poslat i něco, co v nabídce není — třeba starou hodnotu z uložené stránky.

**Úkol:** Napiš funkci `validateShipping(id)`, která vrátí **celý objekt** způsobu dopravy ze `shippingOptions` s daným `id`. Když takový způsob v nabídce není, vyhodí `ValidationError` s políčkem `'shipping'` a zprávou `Vyber způsob dopravy`. Pod ní vypiš výsledek pro `formData.shipping`.

Jak způsob v poli najdeš, vybíráš sám.

# --hints--

`validateShipping` vrátí objekt způsobu dopravy s cenou.

```js
assert.deepEqual(validateShipping('ppl'), { id: 'ppl', label: 'PPL na adresu', price: 129 }, "validateShipping('ppl') má vrátit { id: 'ppl', label: 'PPL na adresu', price: 129 }");
assert.equal(validateShipping('osobne').price, 0, "validateShipping('osobne').price má být 0");
```

Neznámý nebo chybějící způsob vyhodí `ValidationError` s políčkem `shipping`.

```js
for (const value of ['dron', '', undefined]) {
  let error;
  try {
    validateShipping(value);
  } catch (caught) {
    error = caught;
  }
  assert.ok(error instanceof ValidationError, `validateShipping(${JSON.stringify(value)}) má vyhodit ValidationError`);
  assert.equal(error.field, 'shipping', `chyba z validateShipping(${JSON.stringify(value)}) má mít field 'shipping'`);
  assert.equal(error.message, 'Vyber způsob dopravy', `chyba z validateShipping(${JSON.stringify(value)}) má mít zprávu 'Vyber způsob dopravy'`);
}
```

`validateShipping` najde i způsob, který přibude do nabídky.

```js
shippingOptions.push({ id: 'balikovna', label: 'Balíkovna', price: 69 });
assert.equal(validateShipping('balikovna')?.price, 69, "po přidání Balíkovny do shippingOptions má validateShipping('balikovna') vrátit její objekt");
```

# --help--

## --tip--

Hledáš jednu položku pole podle podmínky. Když ji nenajdeš, je to chyba políčka — viz [Validace na hranici](see:js-chyby-ladeni/vlastni-chyby#validace-na-hranici).

## --tip--

Hodí se metoda pole, která vrátí první vyhovující položku nebo `undefined`, případně cyklus `for…of` s `return` uvnitř.

## --tip--

Kostra: `// 1. najdi způsob podle id`, `// 2. nenašel se → vyhoď chybu políčka`, `// 3. vrať ho`.

# --seed--

## --file-- script.js

```js
// Pražírna Letná — e-shop s výběrovou kávou.
// Katalog: kód produktu → název a cena za balení v Kč.
const catalog = {
  'ETH-250': { name: 'Etiopie Yirgacheffe 250 g', price: 289 },
  'COL-250': { name: 'Kolumbie Huila 250 g', price: 259 },
  'KEN-250': { name: 'Keňa Nyeri 250 g', price: 319 },
  'BRA-1000': { name: 'Brazílie Santos 1 kg', price: 749 },
};

// Způsoby dopravy a jejich cena v Kč.
const shippingOptions = [
  { id: 'zasilkovna', label: 'Zásilkovna', price: 79 },
  { id: 'ppl', label: 'PPL na adresu', price: 129 },
  { id: 'osobne', label: 'Osobní odběr v pražírně', price: 0 },
];

// Slevové kódy: kód → sleva v procentech.
const coupons = { KAVA10: 10, LETNA15: 15 };

// Pořadí políček ve formuláři a jejich popisky (použiješ na konci workshopu).
const fieldOrder = ['name', 'email', 'street', 'city', 'zip', 'shipping', 'coupon', 'items'];
const fieldLabels = {
  name: 'Jméno',
  email: 'E-mail',
  street: 'Ulice',
  city: 'Město',
  zip: 'PSČ',
  shipping: 'Doprava',
  coupon: 'Slevový kód',
  items: 'Košík',
  form: 'Objednávka',
};

// Data z formuláře objednávky tak, jak je poslal prohlížeč: všechno jsou texty.
const formData = {
  name: '  Jana   Nováková ',
  email: ' Jana.Novakova@Example.cz',
  street: 'Korunní 12',
  city: 'Praha',
  zip: '120 00',
  shipping: 'zasilkovna',
  coupon: '',
  items: [
    { sku: 'ETH-250', quantity: '2' },
    { sku: 'KEN-250', quantity: '1' },
  ],
};

// ----- Od kolegy -----

// Spustí kontrolu jednoho políčka. Když kontrola vyhodí chybu políčka,
// zapíše její zprávu do fieldErrors pod jméno políčka a vrátí undefined.
function checkField(fieldErrors, validate) {
  try {
    return validate();
  } catch (error) {
    if (error.field) {
      fieldErrors[error.field] = error.message;
    }
  }
}

// Sleva v procentech podle slevového kódu. Prázdný kód znamená žádnou slevu.
function validateCoupon(code) {
  if (code === undefined || code.trim() === '') {
    return 0;
  }
  const discount = coupons[code.trim().toUppercase()];
  if (discount === undefined) {
    throw new ValidationError('coupon', 'Tenhle slevový kód neznáme');
  }
  return discount;
}

// ----- Tvoje funkce -----

// Ořízne mezery na okrajích a víc mezer uvnitř nahradí jednou.
function normalizeSpaces(text) {
  return text.trim().replace(/\s+/g, ' ');
}

console.log(normalizeSpaces(formData.name));

class ValidationError extends Error {
  constructor(field, message, options) {
    super(message, options);
    this.name = 'ValidationError';
    this.field = field;
  }
}

// Produkt, který v katalogu není.
class NotFoundError extends Error {
  constructor(sku, options) {
    super(`Produkt ${sku} v katalogu není`, options);
    this.name = 'NotFoundError';
    this.sku = sku;
  }
}

console.log(`${new NotFoundError('GUA-250')}`);

// Povinné textové políčko: vrátí text bez zbytečných mezer, prázdné vyhodí ValidationError.
function requireText(value, field) {
  if (typeof value !== 'string' || value.trim() === '') {
    throw new ValidationError(field, 'Vyplň toto políčko');
  }
  return normalizeSpaces(value);
}

console.log(requireText(formData.street, 'street'));

// E-mail: povinný, ve tvaru jana@example.cz; vrátí ho malými písmeny.
function validateEmail(value) {
  const email = requireText(value, 'email').toLowerCase();
  if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
    throw new ValidationError('email', 'Zadej e-mail ve tvaru jana@example.cz');
  }
  return email;
}

console.log(validateEmail(formData.email));

// PSČ: pět číslic, mezery jsou dovolené; vrátí ho ve tvaru „120 00".
function validateZip(value) {
  const digits = requireText(value, 'zip').replaceAll(' ', '');
  if (!/^\d{5}$/.test(digits)) {
    throw new ValidationError('zip', 'PSČ má pět číslic, třeba 120 00');
  }
  return `${digits.slice(0, 3)} ${digits.slice(3)}`;
}

console.log(validateZip('12000'));

// Počet kusů: celé číslo 1–20. Z formuláře přijde text, z mobilní aplikace číslo.
function validateQuantity(value, field) {
  const quantity = Number(value);
  if (!Number.isInteger(quantity) || quantity < 1 || quantity > 20) {
    throw new ValidationError(field, 'Zadej počet kusů od 1 do 20');
  }
  return quantity;
}

console.log(validateQuantity(formData.items[0].quantity, 'items.0.quantity'));

// Produkt z katalogu; neznámý kód vyhodí NotFoundError.
function findProduct(sku) {
  if (!Object.hasOwn(catalog, sku)) {
    throw new NotFoundError(sku);
  }
  return catalog[sku];
}

console.log(findProduct('KEN-250'));

--edit--

--edit--
```

# --solution--

## --file-- script.js

```js
// Pražírna Letná — e-shop s výběrovou kávou.
// Katalog: kód produktu → název a cena za balení v Kč.
const catalog = {
  'ETH-250': { name: 'Etiopie Yirgacheffe 250 g', price: 289 },
  'COL-250': { name: 'Kolumbie Huila 250 g', price: 259 },
  'KEN-250': { name: 'Keňa Nyeri 250 g', price: 319 },
  'BRA-1000': { name: 'Brazílie Santos 1 kg', price: 749 },
};

// Způsoby dopravy a jejich cena v Kč.
const shippingOptions = [
  { id: 'zasilkovna', label: 'Zásilkovna', price: 79 },
  { id: 'ppl', label: 'PPL na adresu', price: 129 },
  { id: 'osobne', label: 'Osobní odběr v pražírně', price: 0 },
];

// Slevové kódy: kód → sleva v procentech.
const coupons = { KAVA10: 10, LETNA15: 15 };

// Pořadí políček ve formuláři a jejich popisky (použiješ na konci workshopu).
const fieldOrder = ['name', 'email', 'street', 'city', 'zip', 'shipping', 'coupon', 'items'];
const fieldLabels = {
  name: 'Jméno',
  email: 'E-mail',
  street: 'Ulice',
  city: 'Město',
  zip: 'PSČ',
  shipping: 'Doprava',
  coupon: 'Slevový kód',
  items: 'Košík',
  form: 'Objednávka',
};

// Data z formuláře objednávky tak, jak je poslal prohlížeč: všechno jsou texty.
const formData = {
  name: '  Jana   Nováková ',
  email: ' Jana.Novakova@Example.cz',
  street: 'Korunní 12',
  city: 'Praha',
  zip: '120 00',
  shipping: 'zasilkovna',
  coupon: '',
  items: [
    { sku: 'ETH-250', quantity: '2' },
    { sku: 'KEN-250', quantity: '1' },
  ],
};

// ----- Od kolegy -----

// Spustí kontrolu jednoho políčka. Když kontrola vyhodí chybu políčka,
// zapíše její zprávu do fieldErrors pod jméno políčka a vrátí undefined.
function checkField(fieldErrors, validate) {
  try {
    return validate();
  } catch (error) {
    if (error.field) {
      fieldErrors[error.field] = error.message;
    }
  }
}

// Sleva v procentech podle slevového kódu. Prázdný kód znamená žádnou slevu.
function validateCoupon(code) {
  if (code === undefined || code.trim() === '') {
    return 0;
  }
  const discount = coupons[code.trim().toUppercase()];
  if (discount === undefined) {
    throw new ValidationError('coupon', 'Tenhle slevový kód neznáme');
  }
  return discount;
}

// ----- Tvoje funkce -----

// Ořízne mezery na okrajích a víc mezer uvnitř nahradí jednou.
function normalizeSpaces(text) {
  return text.trim().replace(/\s+/g, ' ');
}

console.log(normalizeSpaces(formData.name));

class ValidationError extends Error {
  constructor(field, message, options) {
    super(message, options);
    this.name = 'ValidationError';
    this.field = field;
  }
}

// Produkt, který v katalogu není.
class NotFoundError extends Error {
  constructor(sku, options) {
    super(`Produkt ${sku} v katalogu není`, options);
    this.name = 'NotFoundError';
    this.sku = sku;
  }
}

console.log(`${new NotFoundError('GUA-250')}`);

// Povinné textové políčko: vrátí text bez zbytečných mezer, prázdné vyhodí ValidationError.
function requireText(value, field) {
  if (typeof value !== 'string' || value.trim() === '') {
    throw new ValidationError(field, 'Vyplň toto políčko');
  }
  return normalizeSpaces(value);
}

console.log(requireText(formData.street, 'street'));

// E-mail: povinný, ve tvaru jana@example.cz; vrátí ho malými písmeny.
function validateEmail(value) {
  const email = requireText(value, 'email').toLowerCase();
  if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
    throw new ValidationError('email', 'Zadej e-mail ve tvaru jana@example.cz');
  }
  return email;
}

console.log(validateEmail(formData.email));

// PSČ: pět číslic, mezery jsou dovolené; vrátí ho ve tvaru „120 00".
function validateZip(value) {
  const digits = requireText(value, 'zip').replaceAll(' ', '');
  if (!/^\d{5}$/.test(digits)) {
    throw new ValidationError('zip', 'PSČ má pět číslic, třeba 120 00');
  }
  return `${digits.slice(0, 3)} ${digits.slice(3)}`;
}

console.log(validateZip('12000'));

// Počet kusů: celé číslo 1–20. Z formuláře přijde text, z mobilní aplikace číslo.
function validateQuantity(value, field) {
  const quantity = Number(value);
  if (!Number.isInteger(quantity) || quantity < 1 || quantity > 20) {
    throw new ValidationError(field, 'Zadej počet kusů od 1 do 20');
  }
  return quantity;
}

console.log(validateQuantity(formData.items[0].quantity, 'items.0.quantity'));

// Produkt z katalogu; neznámý kód vyhodí NotFoundError.
function findProduct(sku) {
  if (!Object.hasOwn(catalog, sku)) {
    throw new NotFoundError(sku);
  }
  return catalog[sku];
}

console.log(findProduct('KEN-250'));

// Způsob dopravy z nabídky; neznámý vyhodí ValidationError.
function validateShipping(id) {
  const option = shippingOptions.find((option) => option.id === id);
  if (option === undefined) {
    throw new ValidationError('shipping', 'Vyber způsob dopravy');
  }
  return option;
}

console.log(validateShipping(formData.shipping));
```
