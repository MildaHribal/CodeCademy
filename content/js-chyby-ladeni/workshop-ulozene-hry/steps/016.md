---
title: "Oprava: kolegův backupSlot"
kind: debug
see: js-chyby-ladeni/vyjimky#return-ve-finally
---

# --description--

## Hlášení

Tlačítko **Zálohovat** u slotu volá kolegovu funkci `backupSlot(slotId)` a podle výsledku ukáže „Záloha uložena" nebo „Zálohu nejde vytvořit". Hráči hlásí:

- U poškozené pozice `slot-2` hra ukáže „Záloha uložena", jenže `savedGames['slot-2-backup']` nevznikne.
- `backupSlot('slot-2')` vrací `true`, `backupSlot('slot-9')` taky `true`.

V konzoli žádná chyba není.

## Úkol

Oprav `backupSlot` tak, aby vracela `true` jen tehdy, když se záloha opravdu uložila, jinak `false`. Změň co nejmenší kus kódu.

# --hints--

`backupSlot` vrátí `false` pro poškozenou pozici a zálohu nevytvoří.

```js
assert.equal(backupSlot('slot-2'), false, "backupSlot('slot-2') má vrátit false — pozici nejde načíst");
assert.equal(Object.hasOwn(savedGames, 'slot-2-backup'), false, "po backupSlot('slot-2') nemá vzniknout slot-2-backup");
```

`backupSlot` vrátí `false` i pro slot, který neexistuje.

```js
assert.equal(backupSlot('slot-9'), false, "backupSlot('slot-9') má vrátit false");
```

`backupSlot` dál zálohuje platnou pozici a vrátí `true`.

```js
assert.equal(backupSlot('slot-1'), true, "backupSlot('slot-1') má vrátit true");
assert.deepEqual(JSON.parse(savedGames['slot-1-backup']), JSON.parse(savedGames['slot-1']), "po backupSlot('slot-1') má slot-1-backup obsahovat stejnou pozici jako slot-1");
```

# --help--

## --tip--

Past je v bloku, který se spustí vždycky — viz [`return` ve `finally`](see:js-chyby-ladeni/vyjimky#return-ve-finally).

## --tip--

Co se stane s hodnotou, kterou připraví `return false` v `catch`, když se po něm spustí `finally` s vlastním `return`? Je v `finally` vůbec nějaký úklid?

## --tip--

Na jiných datech: u funkce `sendScore` s `try { …; return 'odesláno'; } catch { return 'chyba'; }` nesmí žádný `return` stát ve `finally`.

# --explain--

Vysvětli vlastními slovy, proč `backupSlot` vracela `true`, i když záloha selhala.

## --model--

Když načtení selhalo, běh skočil do `catch` a ten si připravil výsledek `false`. Než ale funkce skončí, spustí se vždycky `finally` — a jeho `return true` připravený výsledek přepsal. Stejně by přepsal i výjimku. Do `finally` proto patří jen úklid, nikdy `return`. Tady žádný úklid nebyl, takže celý blok `finally` jde pryč.

## --checklist--

- `catch` připravil výsledek `false`.
- `finally` se spustí vždycky, i po `return`.
- `return` ve `finally` přepíše výsledek z `try` i z `catch`.
- Do `finally` patří jen úklid.

# --seed--

## --file-- script.js

```js
// Uložené pozice hry Farma Lísková. Klíč je slot, hodnota text ve formátu JSON —
// přesně tak, jak hra pozice ukládá do úložiště prohlížeče.
const savedGames = {
  'slot-1': '{"player":"Eliška","level":12,"coins":340,"savedAt":"2026-09-12T18:40:00"}',
  'slot-2': '{"player":"Tonda","level":3,"coins":',
  'slot-3': '{"player":"Mája","level":0,"coins":25,"savedAt":"2026-09-01T09:15:00"}',
  'slot-4': '{"player":"Radek","level":7,"coins":-20,"savedAt":"2026-09-11T16:30:00"}',
  autosave: '{"player":"Eliška","level":12,"coins":355,"savedAt":"2026-09-13T21:05:00"}',
};

// Když je loader.busy true, hra ukazuje točící se ikonu nahrávání.
const loader = { busy: false };

// ----- Od kolegy -----

// Zkontroluje počet mincí v uložené hře a vrátí ho.
function checkCoins(coins) {
  if (!Number.isInteger(coins) || coins < 0) {
    throw new RangeError(`Počet mincí musí být nezáporné celé číslo, ne ${coins}`);
  }
  return coins;
}

--edit--
// Zkopíruje pozici do zálohy (slot-1 → slot-1-backup).
// Vrátí true, když se záloha povedla, jinak false.
function backupSlot(slotId) {
  try {
    const save = validateSave(readSave(savedGames[slotId]));
    savedGames[`${slotId}-backup`] = JSON.stringify(save);
    return true;
  } catch {
    return false;
  } finally {
    // ať funkce vždycky něco vrátí
    return true;
  }
}
--edit--

// ----- Tvoje funkce -----

// Z textu uložené pozice vrátí objekt hry.
function readSave(text) {
  return JSON.parse(text);
}

console.log(readSave(savedGames['slot-1']));

// Zkontroluje úroveň hráče (celé číslo 1–50) a vrátí ji.
function checkLevel(level) {
  if (!Number.isInteger(level) || level < 1 || level > 50) {
    throw new RangeError(`Úroveň musí být celé číslo 1–50, ne ${level}`);
  }
  return level;
}

// Zkontroluje celou uloženou hru a vrátí ji.
function validateSave(save) {
  if (typeof save.player !== 'string' || save.player.trim() === '') {
    throw new TypeError('Uložená hra nemá jméno hráče');
  }
  checkLevel(save.level);
  checkCoins(save.coins);
  return save;
}

// Vrátí uloženou hru, nebo null, když ji nejde načíst.
function tryReadSave(text) {
  try {
    return validateSave(readSave(text));
  } catch {
    return null;
  }
}

console.log('slot-2:', tryReadSave(savedGames['slot-2']));

// Popisek pozice do menu, nebo důvod, proč ji nejde načíst.
function slotLabel(slotId) {
  try {
    const save = validateSave(readSave(savedGames[slotId]));
    return `${save.player} · úroveň ${save.level} · ${save.coins} mincí`;
  } catch (error) {
    return `Nejde načíst: ${error.message}`;
  }
}

console.log(slotLabel('slot-1'));
console.log(slotLabel('slot-3'));

function loadSlot(slotId) {
  loader.busy = true;
  try {
    return validateSave(readSave(savedGames[slotId]));
  } finally {
    loader.busy = false;
  }
}

// Uloží hru do slotu. Neplatnou hru neuloží a vyhodí chybu.
function saveSlot(slotId, save) {
  loader.busy = true;
  try {
    validateSave(save);
    savedGames[slotId] = JSON.stringify(save);
  } finally {
    loader.busy = false;
  }
}

// Načte hru ze slotu. Když to nejde, vyhodí chybu s původní chybou v cause.
function loadGame(slotId) {
  if (!Object.hasOwn(savedGames, slotId)) {
    throw new Error(`Pozice ${slotId} neexistuje`);
  }
  try {
    return loadSlot(slotId);
  } catch (error) {
    throw new Error(`Pozici ${slotId} nejde načíst`, { cause: error });
  }
}

try {
  loadGame('slot-2');
} catch (error) {
  console.log(error.message, '←', error.cause.message);
}

// Pozice vložená ze schránky. Prázdný nebo poškozený text vrátí null,
// jiná chyba (třeba volání s číslem místo textu) vyletí dál.
function readSaveOrNull(text) {
  try {
    return JSON.parse(text.trim());
  } catch (error) {
    if (error instanceof SyntaxError) {
      return null;
    }
    throw error;
  }
}

// Sloty, jejichž pozici nejde načíst.
function brokenSlots() {
  return Object.keys(savedGames).filter((slotId) => tryReadSave(savedGames[slotId]) === null);
}

console.log('Poškozené pozice:', brokenSlots());

// Načte všechny zadané sloty. Když některé nejdou, vyhodí AggregateError se všemi chybami.
function loadAll(slotIds) {
  const saves = [];
  const errors = [];
  for (const slotId of slotIds) {
    try {
      saves.push(loadGame(slotId));
    } catch (error) {
      errors.push(error);
    }
  }
  if (errors.length > 0) {
    throw new AggregateError(errors, `Nejde načíst ${errors.length} z ${slotIds.length} uložených her`);
  }
  return saves;
}

try {
  loadAll(Object.keys(savedGames));
} catch (error) {
  console.log(error.message);
}

// Naposledy uložená hra ze všech, které jdou načíst.
function newestSave() {
  let newest;
  for (const text of Object.values(savedGames)) {
    const save = tryReadSave(text);
    if (save !== null && (newest === undefined || save.savedAt > newest.savedAt)) {
      newest = save;
    }
  }
  return newest;
}

console.log('Pokračovat ve hře:', newestSave());

// Tlačítko Hrát: buď obrazovka farmy s hrou, nebo menu s hláškou.
function startGame(slotId) {
  try {
    return { screen: 'farm', save: loadGame(slotId) };
  } catch (error) {
    const reason = error.cause ? ` (${error.cause.message})` : '';
    return { screen: 'menu', notice: `${error.message}${reason}` };
  }
}

console.log(startGame('slot-3'));
```

# --solution--

## --file-- script.js

```js
// Uložené pozice hry Farma Lísková. Klíč je slot, hodnota text ve formátu JSON —
// přesně tak, jak hra pozice ukládá do úložiště prohlížeče.
const savedGames = {
  'slot-1': '{"player":"Eliška","level":12,"coins":340,"savedAt":"2026-09-12T18:40:00"}',
  'slot-2': '{"player":"Tonda","level":3,"coins":',
  'slot-3': '{"player":"Mája","level":0,"coins":25,"savedAt":"2026-09-01T09:15:00"}',
  'slot-4': '{"player":"Radek","level":7,"coins":-20,"savedAt":"2026-09-11T16:30:00"}',
  autosave: '{"player":"Eliška","level":12,"coins":355,"savedAt":"2026-09-13T21:05:00"}',
};

// Když je loader.busy true, hra ukazuje točící se ikonu nahrávání.
const loader = { busy: false };

// ----- Od kolegy -----

// Zkontroluje počet mincí v uložené hře a vrátí ho.
function checkCoins(coins) {
  if (!Number.isInteger(coins) || coins < 0) {
    throw new RangeError(`Počet mincí musí být nezáporné celé číslo, ne ${coins}`);
  }
  return coins;
}

// Zkopíruje pozici do zálohy (slot-1 → slot-1-backup).
// Vrátí true, když se záloha povedla, jinak false.
function backupSlot(slotId) {
  try {
    const save = validateSave(readSave(savedGames[slotId]));
    savedGames[`${slotId}-backup`] = JSON.stringify(save);
    return true;
  } catch {
    return false;
  }
}

// ----- Tvoje funkce -----

// Z textu uložené pozice vrátí objekt hry.
function readSave(text) {
  return JSON.parse(text);
}

console.log(readSave(savedGames['slot-1']));

// Zkontroluje úroveň hráče (celé číslo 1–50) a vrátí ji.
function checkLevel(level) {
  if (!Number.isInteger(level) || level < 1 || level > 50) {
    throw new RangeError(`Úroveň musí být celé číslo 1–50, ne ${level}`);
  }
  return level;
}

// Zkontroluje celou uloženou hru a vrátí ji.
function validateSave(save) {
  if (typeof save.player !== 'string' || save.player.trim() === '') {
    throw new TypeError('Uložená hra nemá jméno hráče');
  }
  checkLevel(save.level);
  checkCoins(save.coins);
  return save;
}

// Vrátí uloženou hru, nebo null, když ji nejde načíst.
function tryReadSave(text) {
  try {
    return validateSave(readSave(text));
  } catch {
    return null;
  }
}

console.log('slot-2:', tryReadSave(savedGames['slot-2']));

// Popisek pozice do menu, nebo důvod, proč ji nejde načíst.
function slotLabel(slotId) {
  try {
    const save = validateSave(readSave(savedGames[slotId]));
    return `${save.player} · úroveň ${save.level} · ${save.coins} mincí`;
  } catch (error) {
    return `Nejde načíst: ${error.message}`;
  }
}

console.log(slotLabel('slot-1'));
console.log(slotLabel('slot-3'));

function loadSlot(slotId) {
  loader.busy = true;
  try {
    return validateSave(readSave(savedGames[slotId]));
  } finally {
    loader.busy = false;
  }
}

// Uloží hru do slotu. Neplatnou hru neuloží a vyhodí chybu.
function saveSlot(slotId, save) {
  loader.busy = true;
  try {
    validateSave(save);
    savedGames[slotId] = JSON.stringify(save);
  } finally {
    loader.busy = false;
  }
}

// Načte hru ze slotu. Když to nejde, vyhodí chybu s původní chybou v cause.
function loadGame(slotId) {
  if (!Object.hasOwn(savedGames, slotId)) {
    throw new Error(`Pozice ${slotId} neexistuje`);
  }
  try {
    return loadSlot(slotId);
  } catch (error) {
    throw new Error(`Pozici ${slotId} nejde načíst`, { cause: error });
  }
}

try {
  loadGame('slot-2');
} catch (error) {
  console.log(error.message, '←', error.cause.message);
}

// Pozice vložená ze schránky. Prázdný nebo poškozený text vrátí null,
// jiná chyba (třeba volání s číslem místo textu) vyletí dál.
function readSaveOrNull(text) {
  try {
    return JSON.parse(text.trim());
  } catch (error) {
    if (error instanceof SyntaxError) {
      return null;
    }
    throw error;
  }
}

// Sloty, jejichž pozici nejde načíst.
function brokenSlots() {
  return Object.keys(savedGames).filter((slotId) => tryReadSave(savedGames[slotId]) === null);
}

console.log('Poškozené pozice:', brokenSlots());

// Načte všechny zadané sloty. Když některé nejdou, vyhodí AggregateError se všemi chybami.
function loadAll(slotIds) {
  const saves = [];
  const errors = [];
  for (const slotId of slotIds) {
    try {
      saves.push(loadGame(slotId));
    } catch (error) {
      errors.push(error);
    }
  }
  if (errors.length > 0) {
    throw new AggregateError(errors, `Nejde načíst ${errors.length} z ${slotIds.length} uložených her`);
  }
  return saves;
}

try {
  loadAll(Object.keys(savedGames));
} catch (error) {
  console.log(error.message);
}

// Naposledy uložená hra ze všech, které jdou načíst.
function newestSave() {
  let newest;
  for (const text of Object.values(savedGames)) {
    const save = tryReadSave(text);
    if (save !== null && (newest === undefined || save.savedAt > newest.savedAt)) {
      newest = save;
    }
  }
  return newest;
}

console.log('Pokračovat ve hře:', newestSave());

// Tlačítko Hrát: buď obrazovka farmy s hrou, nebo menu s hláškou.
function startGame(slotId) {
  try {
    return { screen: 'farm', save: loadGame(slotId) };
  } catch (error) {
    const reason = error.cause ? ` (${error.cause.message})` : '';
    return { screen: 'menu', notice: `${error.message}${reason}` };
  }
}

console.log(startGame('slot-3'));
```
