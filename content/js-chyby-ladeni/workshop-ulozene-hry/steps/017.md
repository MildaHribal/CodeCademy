---
title: Obnovení ze zálohy
see: js-chyby-ladeni/vyjimky#vic-chyb-najednou-aggregateerror
---

# --description--

Záloha má smysl jen tehdy, když ji hra umí použít. Když se pozice nenačte, má se hra pokusit o její zálohu (slot s `-backup` na konci jména) a poškozenou pozici zálohou nahradit.

**Úkol:** Napiš funkci `restoreBackup(slotId)`:

- Když jde pozici načíst funkcí `loadGame`, vrátí ji a nic dalšího nedělá.
- Když ne, načte stejnou funkcí zálohu. Když se to povede, přepíše text poškozené pozice v `savedGames` textem zálohy a vrátí načtenou pozici.
- Když nejde načíst ani záloha, vyhodí `AggregateError` s oběma chybami (nejdřív chyba pozice, pak chyba zálohy) a zprávou `Pozici slot-2 ani její zálohu nejde načíst` (se skutečným slotem).

# --hints--

`restoreBackup` vrátí platnou pozici a `savedGames` nemění.

```js
const before = JSON.stringify(savedGames);
assert.equal(restoreBackup('slot-1')?.coins, 340, "restoreBackup('slot-1') má vrátit pozici ze slot-1 (340 mincí)");
assert.equal(JSON.stringify(savedGames), before, "restoreBackup('slot-1') nemá v savedGames nic měnit — pozice je v pořádku");
```

Když je pozice poškozená a záloha platná, `restoreBackup` vrátí pozici ze zálohy a poškozenou pozici zálohou přepíše.

```js
const backup = '{"player":"Tonda","level":3,"coins":80,"savedAt":"2026-09-10T12:00:00"}';
savedGames['slot-2-backup'] = backup;
const save = restoreBackup('slot-2');
assert.equal(save?.player, 'Tonda', "restoreBackup('slot-2') má vrátit Tondovu pozici ze zálohy");
assert.equal(save?.coins, 80, "restoreBackup('slot-2').coins má být 80 — hodnota ze zálohy");
assert.equal(savedGames['slot-2'], backup, "po restoreBackup('slot-2') má savedGames['slot-2'] obsahovat text zálohy");
```

Když nejde načíst pozice ani záloha, `restoreBackup` vyhodí `AggregateError` s oběma chybami.

```js
let error;
try {
  restoreBackup('slot-3');
} catch (caught) {
  error = caught;
}
assert.ok(error instanceof AggregateError, "restoreBackup('slot-3') bez zálohy má vyhodit AggregateError");
assert.equal(error.message, 'Pozici slot-3 ani její zálohu nejde načíst', "zpráva má být 'Pozici slot-3 ani její zálohu nejde načíst'");
assert.equal(error.errors.length, 2, 'AggregateError má obsahovat 2 chyby: pozice a zálohy');
assert.match(error.errors[0].message, /slot-3/, 'první chyba má být chyba pozice slot-3');
assert.match(error.errors[1].message, /slot-3-backup/, 'druhá chyba má být chyba zálohy slot-3-backup');
```

Poškozená záloha poškozenou pozici nepřepíše.

```js
const broken = savedGames['slot-2'];
savedGames['slot-2-backup'] = '{"player":"Tonda"';
assert.throws(() => restoreBackup('slot-2'), AggregateError, "restoreBackup('slot-2') s uříznutou zálohou má vyhodit AggregateError");
assert.equal(savedGames['slot-2'], broken, "po neúspěšném obnovení má savedGames['slot-2'] zůstat beze změny");
```

# --help--

## --tip--

Dvě chyby, které chceš nahlásit spolu — viz [Víc chyb najednou](see:js-chyby-ladeni/vyjimky#vic-chyb-najednou-aggregateerror). Pokus o zálohu je druhý `try…catch` uvnitř prvního `catch`.

## --tip--

Chyby pojmenuj různě (třeba podle toho, odkud jsou), abys je ve vnitřním `catch` měl obě. Text do `savedGames[slotId]` přepiš až po úspěšném načtení zálohy.

## --tip--

Kostra: `// 1. try: vrať načtenou pozici`, `// 2. catch: try: načti zálohu, přepiš slot, vrať zálohu`, `// 3. vnitřní catch: vyhoď AggregateError s oběma chybami`.

# --seed--

## --file-- script.js

```js
// Uložené pozice hry Farma Lísková. Klíč je slot, hodnota text ve formátu JSON —
// přesně tak, jak hra pozice ukládá do úložiště prohlížeče.
const savedGames = {
  'slot-1': '{"player":"Eliška","level":12,"coins":340,"savedAt":"2026-09-12T18:40:00"}',
  'slot-2': '{"player":"Tonda","level":3,"coins":',
  'slot-3': '{"player":"Mája","level":0,"coins":25,"savedAt":"2026-09-01T09:15:00"}',
  'slot-4': '{"player":"Radek","level":7,"coins":-20,"savedAt":"2026-09-11T16:30:00"}',
  autosave: '{"player":"Eliška","level":12,"coins":355,"savedAt":"2026-09-13T21:05:00"}',
};

// Když je loader.busy true, hra ukazuje točící se ikonu nahrávání.
const loader = { busy: false };

// ----- Od kolegy -----

// Zkontroluje počet mincí v uložené hře a vrátí ho.
function checkCoins(coins) {
  if (!Number.isInteger(coins) || coins < 0) {
    throw new RangeError(`Počet mincí musí být nezáporné celé číslo, ne ${coins}`);
  }
  return coins;
}

// Zkopíruje pozici do zálohy (slot-1 → slot-1-backup).
// Vrátí true, když se záloha povedla, jinak false.
function backupSlot(slotId) {
  try {
    const save = validateSave(readSave(savedGames[slotId]));
    savedGames[`${slotId}-backup`] = JSON.stringify(save);
    return true;
  } catch {
    return false;
  }
}

// ----- Tvoje funkce -----

// Z textu uložené pozice vrátí objekt hry.
function readSave(text) {
  return JSON.parse(text);
}

console.log(readSave(savedGames['slot-1']));

// Zkontroluje úroveň hráče (celé číslo 1–50) a vrátí ji.
function checkLevel(level) {
  if (!Number.isInteger(level) || level < 1 || level > 50) {
    throw new RangeError(`Úroveň musí být celé číslo 1–50, ne ${level}`);
  }
  return level;
}

// Zkontroluje celou uloženou hru a vrátí ji.
function validateSave(save) {
  if (typeof save.player !== 'string' || save.player.trim() === '') {
    throw new TypeError('Uložená hra nemá jméno hráče');
  }
  checkLevel(save.level);
  checkCoins(save.coins);
  return save;
}

// Vrátí uloženou hru, nebo null, když ji nejde načíst.
function tryReadSave(text) {
  try {
    return validateSave(readSave(text));
  } catch {
    return null;
  }
}

console.log('slot-2:', tryReadSave(savedGames['slot-2']));

// Popisek pozice do menu, nebo důvod, proč ji nejde načíst.
function slotLabel(slotId) {
  try {
    const save = validateSave(readSave(savedGames[slotId]));
    return `${save.player} · úroveň ${save.level} · ${save.coins} mincí`;
  } catch (error) {
    return `Nejde načíst: ${error.message}`;
  }
}

console.log(slotLabel('slot-1'));
console.log(slotLabel('slot-3'));

function loadSlot(slotId) {
  loader.busy = true;
  try {
    return validateSave(readSave(savedGames[slotId]));
  } finally {
    loader.busy = false;
  }
}

// Uloží hru do slotu. Neplatnou hru neuloží a vyhodí chybu.
function saveSlot(slotId, save) {
  loader.busy = true;
  try {
    validateSave(save);
    savedGames[slotId] = JSON.stringify(save);
  } finally {
    loader.busy = false;
  }
}

// Načte hru ze slotu. Když to nejde, vyhodí chybu s původní chybou v cause.
function loadGame(slotId) {
  if (!Object.hasOwn(savedGames, slotId)) {
    throw new Error(`Pozice ${slotId} neexistuje`);
  }
  try {
    return loadSlot(slotId);
  } catch (error) {
    throw new Error(`Pozici ${slotId} nejde načíst`, { cause: error });
  }
}

try {
  loadGame('slot-2');
} catch (error) {
  console.log(error.message, '←', error.cause.message);
}

// Pozice vložená ze schránky. Prázdný nebo poškozený text vrátí null,
// jiná chyba (třeba volání s číslem místo textu) vyletí dál.
function readSaveOrNull(text) {
  try {
    return JSON.parse(text.trim());
  } catch (error) {
    if (error instanceof SyntaxError) {
      return null;
    }
    throw error;
  }
}

// Sloty, jejichž pozici nejde načíst.
function brokenSlots() {
  return Object.keys(savedGames).filter((slotId) => tryReadSave(savedGames[slotId]) === null);
}

console.log('Poškozené pozice:', brokenSlots());

// Načte všechny zadané sloty. Když některé nejdou, vyhodí AggregateError se všemi chybami.
function loadAll(slotIds) {
  const saves = [];
  const errors = [];
  for (const slotId of slotIds) {
    try {
      saves.push(loadGame(slotId));
    } catch (error) {
      errors.push(error);
    }
  }
  if (errors.length > 0) {
    throw new AggregateError(errors, `Nejde načíst ${errors.length} z ${slotIds.length} uložených her`);
  }
  return saves;
}

try {
  loadAll(Object.keys(savedGames));
} catch (error) {
  console.log(error.message);
}

// Naposledy uložená hra ze všech, které jdou načíst.
function newestSave() {
  let newest;
  for (const text of Object.values(savedGames)) {
    const save = tryReadSave(text);
    if (save !== null && (newest === undefined || save.savedAt > newest.savedAt)) {
      newest = save;
    }
  }
  return newest;
}

console.log('Pokračovat ve hře:', newestSave());

// Tlačítko Hrát: buď obrazovka farmy s hrou, nebo menu s hláškou.
function startGame(slotId) {
  try {
    return { screen: 'farm', save: loadGame(slotId) };
  } catch (error) {
    const reason = error.cause ? ` (${error.cause.message})` : '';
    return { screen: 'menu', notice: `${error.message}${reason}` };
  }
}

console.log(startGame('slot-3'));

--edit--

--edit--
```

# --solution--

## --file-- script.js

```js
// Uložené pozice hry Farma Lísková. Klíč je slot, hodnota text ve formátu JSON —
// přesně tak, jak hra pozice ukládá do úložiště prohlížeče.
const savedGames = {
  'slot-1': '{"player":"Eliška","level":12,"coins":340,"savedAt":"2026-09-12T18:40:00"}',
  'slot-2': '{"player":"Tonda","level":3,"coins":',
  'slot-3': '{"player":"Mája","level":0,"coins":25,"savedAt":"2026-09-01T09:15:00"}',
  'slot-4': '{"player":"Radek","level":7,"coins":-20,"savedAt":"2026-09-11T16:30:00"}',
  autosave: '{"player":"Eliška","level":12,"coins":355,"savedAt":"2026-09-13T21:05:00"}',
};

// Když je loader.busy true, hra ukazuje točící se ikonu nahrávání.
const loader = { busy: false };

// ----- Od kolegy -----

// Zkontroluje počet mincí v uložené hře a vrátí ho.
function checkCoins(coins) {
  if (!Number.isInteger(coins) || coins < 0) {
    throw new RangeError(`Počet mincí musí být nezáporné celé číslo, ne ${coins}`);
  }
  return coins;
}

// Zkopíruje pozici do zálohy (slot-1 → slot-1-backup).
// Vrátí true, když se záloha povedla, jinak false.
function backupSlot(slotId) {
  try {
    const save = validateSave(readSave(savedGames[slotId]));
    savedGames[`${slotId}-backup`] = JSON.stringify(save);
    return true;
  } catch {
    return false;
  }
}

// ----- Tvoje funkce -----

// Z textu uložené pozice vrátí objekt hry.
function readSave(text) {
  return JSON.parse(text);
}

console.log(readSave(savedGames['slot-1']));

// Zkontroluje úroveň hráče (celé číslo 1–50) a vrátí ji.
function checkLevel(level) {
  if (!Number.isInteger(level) || level < 1 || level > 50) {
    throw new RangeError(`Úroveň musí být celé číslo 1–50, ne ${level}`);
  }
  return level;
}

// Zkontroluje celou uloženou hru a vrátí ji.
function validateSave(save) {
  if (typeof save.player !== 'string' || save.player.trim() === '') {
    throw new TypeError('Uložená hra nemá jméno hráče');
  }
  checkLevel(save.level);
  checkCoins(save.coins);
  return save;
}

// Vrátí uloženou hru, nebo null, když ji nejde načíst.
function tryReadSave(text) {
  try {
    return validateSave(readSave(text));
  } catch {
    return null;
  }
}

console.log('slot-2:', tryReadSave(savedGames['slot-2']));

// Popisek pozice do menu, nebo důvod, proč ji nejde načíst.
function slotLabel(slotId) {
  try {
    const save = validateSave(readSave(savedGames[slotId]));
    return `${save.player} · úroveň ${save.level} · ${save.coins} mincí`;
  } catch (error) {
    return `Nejde načíst: ${error.message}`;
  }
}

console.log(slotLabel('slot-1'));
console.log(slotLabel('slot-3'));

function loadSlot(slotId) {
  loader.busy = true;
  try {
    return validateSave(readSave(savedGames[slotId]));
  } finally {
    loader.busy = false;
  }
}

// Uloží hru do slotu. Neplatnou hru neuloží a vyhodí chybu.
function saveSlot(slotId, save) {
  loader.busy = true;
  try {
    validateSave(save);
    savedGames[slotId] = JSON.stringify(save);
  } finally {
    loader.busy = false;
  }
}

// Načte hru ze slotu. Když to nejde, vyhodí chybu s původní chybou v cause.
function loadGame(slotId) {
  if (!Object.hasOwn(savedGames, slotId)) {
    throw new Error(`Pozice ${slotId} neexistuje`);
  }
  try {
    return loadSlot(slotId);
  } catch (error) {
    throw new Error(`Pozici ${slotId} nejde načíst`, { cause: error });
  }
}

try {
  loadGame('slot-2');
} catch (error) {
  console.log(error.message, '←', error.cause.message);
}

// Pozice vložená ze schránky. Prázdný nebo poškozený text vrátí null,
// jiná chyba (třeba volání s číslem místo textu) vyletí dál.
function readSaveOrNull(text) {
  try {
    return JSON.parse(text.trim());
  } catch (error) {
    if (error instanceof SyntaxError) {
      return null;
    }
    throw error;
  }
}

// Sloty, jejichž pozici nejde načíst.
function brokenSlots() {
  return Object.keys(savedGames).filter((slotId) => tryReadSave(savedGames[slotId]) === null);
}

console.log('Poškozené pozice:', brokenSlots());

// Načte všechny zadané sloty. Když některé nejdou, vyhodí AggregateError se všemi chybami.
function loadAll(slotIds) {
  const saves = [];
  const errors = [];
  for (const slotId of slotIds) {
    try {
      saves.push(loadGame(slotId));
    } catch (error) {
      errors.push(error);
    }
  }
  if (errors.length > 0) {
    throw new AggregateError(errors, `Nejde načíst ${errors.length} z ${slotIds.length} uložených her`);
  }
  return saves;
}

try {
  loadAll(Object.keys(savedGames));
} catch (error) {
  console.log(error.message);
}

// Naposledy uložená hra ze všech, které jdou načíst.
function newestSave() {
  let newest;
  for (const text of Object.values(savedGames)) {
    const save = tryReadSave(text);
    if (save !== null && (newest === undefined || save.savedAt > newest.savedAt)) {
      newest = save;
    }
  }
  return newest;
}

console.log('Pokračovat ve hře:', newestSave());

// Tlačítko Hrát: buď obrazovka farmy s hrou, nebo menu s hláškou.
function startGame(slotId) {
  try {
    return { screen: 'farm', save: loadGame(slotId) };
  } catch (error) {
    const reason = error.cause ? ` (${error.cause.message})` : '';
    return { screen: 'menu', notice: `${error.message}${reason}` };
  }
}

console.log(startGame('slot-3'));

// Načte hru ze slotu; když je poškozená, obnoví ji ze zálohy.
function restoreBackup(slotId) {
  try {
    return loadGame(slotId);
  } catch (originalError) {
    try {
      const save = loadGame(`${slotId}-backup`);
      savedGames[slotId] = savedGames[`${slotId}-backup`];
      return save;
    } catch (backupError) {
      throw new AggregateError([originalError, backupError], `Pozici ${slotId} ani její zálohu nejde načíst`);
    }
  }
}
```
