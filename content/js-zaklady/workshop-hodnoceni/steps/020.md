---
title: První řádek přehledu
see: js-zaklady/promenne-a-typy#sablonovy-retezec
---

# --description--

Přehled studenta začíná řádkem se jménem, body a procenty.

**Úkol:** Pod `gradeOf` napiš funkci `headline(name, earned)`, která vrátí první řádek přehledu: jméno, body se správným tvarem slova a procenta z maxima `maxPoints`. Třeba `Tereza Malá: 36 bodů (90 %)` nebo `Filip Novotný: 1 bod (3 %)`. Kdo nepsal (`null`), dostane `Ema Horáková: nepsal(a)`.

# --hints--

`headline('Tereza Malá', 36)` vrátí `Tereza Malá: 36 bodů (90 %)`.

```js
assert.equal(headline('Tereza Malá', 36), 'Tereza Malá: 36 bodů (90 %)', "headline('Tereza Malá', 36) má vrátit 'Tereza Malá: 36 bodů (90 %)'");
```

`headline` používá správný tvar slova i pro 1, 3 a 0 bodů.

```js
assert.equal(headline('Filip Novotný', 1), 'Filip Novotný: 1 bod (3 %)', "headline('Filip Novotný', 1) má vrátit 'Filip Novotný: 1 bod (3 %)'");
assert.equal(headline('Jakub Dvořák', 3), 'Jakub Dvořák: 3 body (8 %)', "headline('Jakub Dvořák', 3) má vrátit 'Jakub Dvořák: 3 body (8 %)'");
assert.equal(headline('Petr Novák', 0), 'Petr Novák: 0 bodů (0 %)', "headline('Petr Novák', 0) má vrátit 'Petr Novák: 0 bodů (0 %)' — nula bodů není nepsal(a)");
```

`headline` pro studenta bez bodů vrátí `… : nepsal(a)`.

```js
assert.equal(headline('Ema Horáková', null), 'Ema Horáková: nepsal(a)', "headline('Ema Horáková', null) má vrátit 'Ema Horáková: nepsal(a)'");
```

# --help--

## --tip--

Řádek skládáš [šablonovým řetězcem](see:js-zaklady/promenne-a-typy#sablonovy-retezec) z hodnot a volání funkcí; chybějící body vyřiď [brzkým returnem](see:js-zaklady/porovnani-a-logika#rozhodnuti-zabalene-do-funkce).

## --tip--

Tvar slova ti vrátí funkce z kroku „bod, body, bodů" a procenta `percentOf`. Kontrola na `null` patří na začátek funkce.

## --tip--

Stejně jsi skládal souhrnný řádek pro Terezu v kroku s opakováním — hodnoty a volání funkcí v jedné šabloně. Tady ho jen vrátíš místo vypsání.

# --seed--

## --file-- script.js

```js
// Písemka z fyziky, třída 9.A. Maximum je 40 bodů.
const maxPoints = 40;

// Výsledek Terezy z formuláře. Formulář posílá body vždycky jako text.
const studentName = 'Tereza Malá';
const pointsInput = '36';
const attendance = 82; // docházka za pololetí v procentech

// ---- Od kolegy: funkce pro webový přehled třídy ----

// Návrh na pochvalu: aspoň 90 % v písemce a zároveň aspoň 90 % docházky.
function hasPraise(percent, attendancePercent) {
  return percent >= 90 && attendancePercent >= 90;
}

// Barva štítku se známkou v přehledu (barva pro CSS).
function gradeColor(grade) {
  let color;
  switch (grade) {
    case 1:
      color = 'green';
      break;
    case 2:
      color = 'yellowgreen';
      break;
    case 3:
      color = 'gold';
      break;
    case 4:
      color = 'orange';
      break;
    default:
      color = 'red';
  }
  return color;
}

// ---- Tvoje funkce ----

// Úspěšnost v procentech zaokrouhlená na celé číslo.
function percentOf(earned, max) {
  return Math.round((earned / max) * 100);
}

function gradeFor(percent) {
  if (percent < 0 || percent > 100) {
    return null;
  }
  if (percent >= 90) {
    return 1;
  } else if (percent >= 75) {
    return 2;
  } else if (percent >= 50) {
    return 3;
  } else if (percent >= 30) {
    return 4;
  } else {
    return 5;
  }
}

// Slovní poznámka k docházce.
function attendanceNote(attendancePercent) {
  if (attendancePercent >= 90) {
    return 'výborná docházka';
  } else if (attendancePercent >= 75) {
    return 'docházka v pořádku';
  } else if (attendancePercent >= 60) {
    return 'nízká docházka';
  } else {
    return 'nelze klasifikovat';
  }
}

// Slovní název známky na vysvědčení.
function gradeLabel(grade) {
  switch (grade) {
    case 1:
      return 'výborný';
    case 2:
      return 'chvalitebný';
    case 3:
      return 'dobrý';
    case 4:
      return 'dostatečný';
    case 5:
      return 'nedostatečný';
    default:
      return 'neznámá známka';
  }
}

// Text do přehledu: navrhnout pochvalu, nebo ne.
function praiseText(percent, attendancePercent) {
  return hasPraise(percent, attendancePercent) ? 'navrhnout pochvalu' : 'bez pochvaly';
}

// Má si výsledku všimnout třídní učitelka?
function needsAttention(grade, attendancePercent) {
  return grade === 5 || attendancePercent < 60;
}

// Body do tabulky třídy; kdo nepsal, nemá body.
function pointsCell(earned) {
  return earned ?? 'nepsal(a)';
}

// Upozornění k výsledku, nebo null, když není co hlásit.
function warningFor(grade, previousGrade) {
  if (grade === 5) {
    return 'nedostatečná, nabídni opravný termín';
  }
  if (previousGrade !== null && grade - previousGrade >= 2) {
    return `zhoršení o ${grade - previousGrade} stupně`;
  }
  return null;
}

// Tvar slova „bod" podle počtu.
function pointsWord(count) {
  if (count === 1) {
    return 'bod';
  }
  if (count >= 2 && count <= 4) {
    return 'body';
  }
  return 'bodů';
}

// Známka z bodů; kdo nepsal (null), známku nemá.
function gradeOf(earned) {
  if (earned === null) {
    return null;
  }
  return gradeFor(percentOf(earned, maxPoints));
}

--edit--

--edit--

// ---- Výpis pro Terezu ----

const studentPoints = Number(pointsInput);
console.log(`${studentName}: ${studentPoints}/${maxPoints} bodů`);

const studentPercent = percentOf(studentPoints, maxPoints);
console.log(`Úspěšnost: ${studentPercent} %`);

const hasPassed = studentPercent >= 50;
console.log('Splněno:', hasPassed);

if (hasPassed) {
  console.log('Písemka je splněná.');
} else {
  console.log('Písemka není splněná, domluv si opravný termín.');
}

const studentGrade = gradeFor(studentPercent);
console.log(`Známka: ${studentGrade}`);
console.log(`Docházka ${attendance} %: ${attendanceNote(attendance)}`);

console.log(`${studentName}: ${studentPercent} % → ${studentGrade} (${gradeLabel(studentGrade)})`);
```

# --solution--

## --file-- script.js

```js
// Písemka z fyziky, třída 9.A. Maximum je 40 bodů.
const maxPoints = 40;

// Výsledek Terezy z formuláře. Formulář posílá body vždycky jako text.
const studentName = 'Tereza Malá';
const pointsInput = '36';
const attendance = 82; // docházka za pololetí v procentech

// ---- Od kolegy: funkce pro webový přehled třídy ----

// Návrh na pochvalu: aspoň 90 % v písemce a zároveň aspoň 90 % docházky.
function hasPraise(percent, attendancePercent) {
  return percent >= 90 && attendancePercent >= 90;
}

// Barva štítku se známkou v přehledu (barva pro CSS).
function gradeColor(grade) {
  let color;
  switch (grade) {
    case 1:
      color = 'green';
      break;
    case 2:
      color = 'yellowgreen';
      break;
    case 3:
      color = 'gold';
      break;
    case 4:
      color = 'orange';
      break;
    default:
      color = 'red';
  }
  return color;
}

// ---- Tvoje funkce ----

// Úspěšnost v procentech zaokrouhlená na celé číslo.
function percentOf(earned, max) {
  return Math.round((earned / max) * 100);
}

function gradeFor(percent) {
  if (percent < 0 || percent > 100) {
    return null;
  }
  if (percent >= 90) {
    return 1;
  } else if (percent >= 75) {
    return 2;
  } else if (percent >= 50) {
    return 3;
  } else if (percent >= 30) {
    return 4;
  } else {
    return 5;
  }
}

// Slovní poznámka k docházce.
function attendanceNote(attendancePercent) {
  if (attendancePercent >= 90) {
    return 'výborná docházka';
  } else if (attendancePercent >= 75) {
    return 'docházka v pořádku';
  } else if (attendancePercent >= 60) {
    return 'nízká docházka';
  } else {
    return 'nelze klasifikovat';
  }
}

// Slovní název známky na vysvědčení.
function gradeLabel(grade) {
  switch (grade) {
    case 1:
      return 'výborný';
    case 2:
      return 'chvalitebný';
    case 3:
      return 'dobrý';
    case 4:
      return 'dostatečný';
    case 5:
      return 'nedostatečný';
    default:
      return 'neznámá známka';
  }
}

// Text do přehledu: navrhnout pochvalu, nebo ne.
function praiseText(percent, attendancePercent) {
  return hasPraise(percent, attendancePercent) ? 'navrhnout pochvalu' : 'bez pochvaly';
}

// Má si výsledku všimnout třídní učitelka?
function needsAttention(grade, attendancePercent) {
  return grade === 5 || attendancePercent < 60;
}

// Body do tabulky třídy; kdo nepsal, nemá body.
function pointsCell(earned) {
  return earned ?? 'nepsal(a)';
}

// Upozornění k výsledku, nebo null, když není co hlásit.
function warningFor(grade, previousGrade) {
  if (grade === 5) {
    return 'nedostatečná, nabídni opravný termín';
  }
  if (previousGrade !== null && grade - previousGrade >= 2) {
    return `zhoršení o ${grade - previousGrade} stupně`;
  }
  return null;
}

// Tvar slova „bod" podle počtu.
function pointsWord(count) {
  if (count === 1) {
    return 'bod';
  }
  if (count >= 2 && count <= 4) {
    return 'body';
  }
  return 'bodů';
}

// Známka z bodů; kdo nepsal (null), známku nemá.
function gradeOf(earned) {
  if (earned === null) {
    return null;
  }
  return gradeFor(percentOf(earned, maxPoints));
}

// První řádek přehledu, třeba „Tereza Malá: 36 bodů (90 %)".
function headline(name, earned) {
  if (earned === null) {
    return `${name}: nepsal(a)`;
  }
  return `${name}: ${earned} ${pointsWord(earned)} (${percentOf(earned, maxPoints)} %)`;
}

// ---- Výpis pro Terezu ----

const studentPoints = Number(pointsInput);
console.log(`${studentName}: ${studentPoints}/${maxPoints} bodů`);

const studentPercent = percentOf(studentPoints, maxPoints);
console.log(`Úspěšnost: ${studentPercent} %`);

const hasPassed = studentPercent >= 50;
console.log('Splněno:', hasPassed);

if (hasPassed) {
  console.log('Písemka je splněná.');
} else {
  console.log('Písemka není splněná, domluv si opravný termín.');
}

const studentGrade = gradeFor(studentPercent);
console.log(`Známka: ${studentGrade}`);
console.log(`Docházka ${attendance} %: ${attendanceNote(attendance)}`);

console.log(`${studentName}: ${studentPercent} % → ${studentGrade} (${gradeLabel(studentGrade)})`);
```
