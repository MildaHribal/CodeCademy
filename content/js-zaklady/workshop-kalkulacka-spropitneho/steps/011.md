---
title: Kdo platí kartou, doplatí drobné
see: js-zaklady/promenne-a-typy#operatory-a-prednost
---

# --description--

Drobné doplatí ten, kdo platí kartou: zaplatí svůj podíl a k němu zbytek.

**Úkol:** Založ proměnnou `cardPayment`, na začátku s hodnotou `share`. Na dalším řádku k ní přičti `remainder` zkráceným zápisem `+=`. Vypiš ji, třeba `Kdo platí kartou, zaplatí: 309 Kč`.

Do proměnné budeš přiřazovat podruhé — který ze dvou způsobů deklarace to dovolí? Hodí se `const`, nebo `let`?

Tím máš hotové celé rozúčtování: mezisoučet, spropitné, celkovou částku i kdo kolik platí. Klidně si dej pauzu, další kroky přidávají funkce navíc.

# --hints--

`cardPayment` je `309`.

```js
assert.equal(cardPayment, 309, 'cardPayment má být 309 (306 + 3)');
```

`cardPayment` se počítá z podílu a zbytku: pro 5 lidí `247`.

```js
const rerun = (changes, names) => {
  let src = files['script.js'];
  for (const [from, to] of changes) {
    assert.ok(src.includes(from), `Řádek ${from} nech v souboru beze změny — test ho přepíná na jiná data.`);
    src = src.replace(from, to);
  }
  return new Function(`${src}\nreturn { ${names} };`)();
};
const five = rerun([["const peopleInput = '4';", "const peopleInput = '5';"]], 'cardPayment');
assert.equal(five.cardPayment, 247, 'Pro 5 lidí (245 + 2) má cardPayment vyjít 247');
```

K `cardPayment` se přičítá zápisem `+=`.

```js
const src = helpers.stripComments(files['script.js'], 'js');
assert.match(src, /cardPayment\s*\+=/, 'Přičti remainder k cardPayment zkráceným zápisem cardPayment += …');
```

# --help--

## --tip--

Jde o proměnnou, do které se přiřazuje znovu — viz [`const` a `let`](see:js-zaklady/promenne-a-typy#const-a-let) a zkratku `+=` v [Operátory a přednost](see:js-zaklady/promenne-a-typy#operatory-a-prednost).

## --tip--

`const` přiřazení podruhé nedovolí a `+=` je přiřazení. Deklaruj `cardPayment` tak, aby to šlo, a na dalším řádku přičti zbytek.

## --tip--

Rozepsané na podcíle: `// 1. deklarace, která dovolí další přiřazení, s hodnotou podílu` `// 2. zkrácené přičtení zbytku` `// 3. výpis s popiskem`

# --seed--

## --file-- script.js

```js
// Rozúčtování večeře v bistru Lávka. Ceny jsou v korunách.

// Objednávka stolu č. 7 z pokladny
const soupPrice = 89;
const mainPrice = 239;
const drinkPrice = 55;
const soups = 2;
const mains = 3;
const drinks = 4;

// Co hosté vyplnili v aplikaci. Formulář posílá vždycky text.
const tipPercentInput = '10';
const peopleInput = '4';

// Ceník a údaje z rezervace
const yearlyIncreasePercent = 6;
const years = 3;
const visitMinutes = 135;

console.log('Bistro Lávka, stůl 7');

const itemCount = soups + mains + drinks;
console.log('Položek na stole:', itemCount);

const subtotal = soupPrice * soups + mainPrice * mains + drinkPrice * drinks;
console.log('Mezisoučet:', subtotal, 'Kč');

const tipPercent = Number(tipPercentInput);
console.log('Spropitné:', tipPercent, '%');

const tip = subtotal * tipPercent / 100;
console.log('Spropitné:', tip, 'Kč');

const total = Math.ceil(subtotal + tip);
console.log(`Celkem: ${total} Kč`);

const people = Number(peopleInput);
console.log(`Platí lidí: ${people}`);

const share = Math.floor(total / people);
console.log(`Každý zaplatí: ${share} Kč`);

const remainder = total % people;
console.log(`Zbývá doplatit: ${remainder} Kč`);

--edit--

--edit--
```

# --solution--

## --file-- script.js

```js
// Rozúčtování večeře v bistru Lávka. Ceny jsou v korunách.

// Objednávka stolu č. 7 z pokladny
const soupPrice = 89;
const mainPrice = 239;
const drinkPrice = 55;
const soups = 2;
const mains = 3;
const drinks = 4;

// Co hosté vyplnili v aplikaci. Formulář posílá vždycky text.
const tipPercentInput = '10';
const peopleInput = '4';

// Ceník a údaje z rezervace
const yearlyIncreasePercent = 6;
const years = 3;
const visitMinutes = 135;

console.log('Bistro Lávka, stůl 7');

const itemCount = soups + mains + drinks;
console.log('Položek na stole:', itemCount);

const subtotal = soupPrice * soups + mainPrice * mains + drinkPrice * drinks;
console.log('Mezisoučet:', subtotal, 'Kč');

const tipPercent = Number(tipPercentInput);
console.log('Spropitné:', tipPercent, '%');

const tip = subtotal * tipPercent / 100;
console.log('Spropitné:', tip, 'Kč');

const total = Math.ceil(subtotal + tip);
console.log(`Celkem: ${total} Kč`);

const people = Number(peopleInput);
console.log(`Platí lidí: ${people}`);

const share = Math.floor(total / people);
console.log(`Každý zaplatí: ${share} Kč`);

const remainder = total % people;
console.log(`Zbývá doplatit: ${remainder} Kč`);

let cardPayment = share;
cardPayment += remainder;
console.log(`Kdo platí kartou, zaplatí: ${cardPayment} Kč`);
```
