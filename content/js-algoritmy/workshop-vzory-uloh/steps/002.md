---
title: "Valid Palindrome"
kind: step
see: js-algoritmy/postup-reseni#porozumeni
---
<--description-->
Použij two-pointer přístup. Zkontroluj, zda je slovo palindrom (čte se stejně zepředu i zezadu).
Můžeš ignorovat velikost písmen.
</--description-->
<--hints-->
Funkce vrátí boolean.
```js
assert.equal(isPalindrome('Racecar'), true, 'Racecar je palindrom');
```
</--hints-->
<--help-->
## --tip--
Jeden ukazatel dej na začátek, druhý na konec.
</--help-->
<--seed-->
## --file-- script.js
```js
function isPalindrome(str) {
--edit--
--edit--
}
```
</--seed-->
<--solution-->
## --file-- script.js
```js
function isPalindrome(str) {
  str = str.toLowerCase();
  let left = 0;
  let right = str.length - 1;
  while (left < right) {
    if (str[left] !== str[right]) return false;
    left++;
    right--;
  }
  return true;
}
```
