---
title: "Two Sum"
kind: step
see: js-algoritmy/postup-reseni#porozumeni
---
<--description-->
Napiš funkci, která vrátí indexy dvou čísel, jejichž součet je `target`.
K řešení použij `Map`. Můžeš předpokládat, že řešení vždy existuje.
</--description-->
<--hints-->
Musíš vrátit pole s dvěma indexy.
```js
assert.deepEqual(twoSum([2, 7, 11, 15], 9), [0, 1], 'twoSum([2, 7, 11, 15], 9) má vrátit [0, 1]');
```
</--hints-->
<--help-->
## --tip--
Projdeš pole jen jednou.
</--help-->
<--seed-->
## --file-- script.js
```js
function twoSum(nums, target) {
--edit--
--edit--
}
```
</--seed-->
<--solution-->
## --file-- script.js
```js
function twoSum(nums, target) {
  const map = new Map();
  for (let i = 0; i < nums.length; i++) {
    const diff = target - nums[i];
    if (map.has(diff)) {
      return [map.get(diff), i];
    }
    map.set(nums[i], i);
  }
}
```
