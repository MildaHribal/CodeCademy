---
title: Zpět tam, odkud jsi přišel
see: react-aplikace/routovani#odkazy-link-a-navlink
---

# --description--

Poslední drobnost, která dělá rozdíl. Uživatel si vyfiltruje elektrokola, klikne
na detail a pak na „Zpět na katalog" — a je zpátky v **nefiltrovaném** katalogu,
protože ten odkaz vede natvrdo na `/`. Celý výběr je pryč.

Router si přitom historii adres pamatuje, včetně parametrů za otazníkem. Stačí
místo pevného cíle říct „o krok zpátky".

**Úkol:** V `Detail.jsx` nahraď u nalezeného kola odkaz zpátky tlačítkem
`button` typu `button` s třídou `zpet-tlacitko` a s textem „← Zpět tam, odkud
jsi přišel", které uživatele vrátí o jeden krok v historii.
Hook na navigaci z kódu už znáš z lekce. Větev „Tohle kolo nemáme" nech, jak je —
tam odkaz na katalog dává smysl, protože uživatel může přijít odkudkoli.

> [!NOTE]
> Tlačítko „o krok zpět" má jednu nevýhodu: když uživatel přijde přímo
> z odkazu v chatu, žádná předchozí stránka v historii aplikace není. Skutečné
> aplikace proto často nabízejí obojí — tlačítko zpět a odkaz na rozcestí.

## Co teď umíš

Za čtrnáct kroků z jedné stránky vznikla aplikace, která má trasy s parametry,
vnořený layout se společným záhlavím, stránku pro neznámé adresy a filtr
s hledáním, jehož stav jde poslat odkazem. To je kostra, kterou má devět z deseti
aplikací, co kdy budeš psát.

Příště k téhle kostře přibydou **data ze serveru** — a s nimi otázka, kde má
bydlet stav, který ti nepatří, protože ho vlastní backend.

## Udělej po svém

Testy končí, katalog je tvůj. Žádné z rozšíření se nekontroluje:

- **Vizuálně:** dej kartám obrázek nebo barevný pruh podle typu kola a rozsviť
  je na `:hover` — třídu pro typ máš v datech.
- **Funkčně:** přidej do filtru řazení podle ceny a drž ho v adrese vedle typu
  a hledání.
- **Ještě dál:** ukaž nad seznamem větu „Našli jsme 3 kola", která se mění podle
  filtru.
- **Na svoje téma:** přepiš data na to, co půjčuje nebo prodává někdo, koho znáš
  — deskové hry, lyže, vrtačky. Struktura aplikace zůstane, změní se jen `data.js`
  a texty.

# --hints--

V detailu nalezeného kola je tlačítko zpět místo odkazu na katalog.

```js
await helpers.click(document.querySelectorAll('.katalog a')[0]);
await helpers.flush();
const tlacitko = document.querySelector('.detail button.zpet-tlacitko');
assert.ok(tlacitko, 'v detailu má být <button className="zpet-tlacitko">');
assert.match(tlacitko.textContent, /Zpět tam, odkud jsi přišel/, 'tlačítko má mít text „← Zpět tam, odkud jsi přišel"');
```

Tlačítko vrátí uživatele na stránku, ze které na detail přišel.

```js
await helpers.click(document.querySelectorAll('.katalog a')[2]);
await helpers.flush();
assert.ok(document.querySelector('.detail'), 'po kliknutí na kartu má být vidět detail');
await helpers.click(document.querySelector('.detail button.zpet-tlacitko'));
await helpers.flush();
assert.equal(document.querySelectorAll('.katalog li').length, 8, 'po kliknutí na tlačítko zpět má být vidět zase katalog');
```

Zpět z detailu zachová i filtr, ve kterém uživatel byl.

```js
await helpers.click(document.querySelectorAll('.filtr button')[2]);
await helpers.flush();
assert.equal(document.querySelectorAll('.katalog li').length, 2, 'po volbě Elektrokola mají zbýt dvě kola');
await helpers.click(document.querySelectorAll('.katalog a')[0]);
await helpers.flush();
await helpers.click(document.querySelector('.detail button.zpet-tlacitko'));
await helpers.flush();
assert.equal(document.querySelectorAll('.katalog li').length, 2, 'tlačítko zpět má vrátit uživatele do vyfiltrovaného katalogu, ne na holou adresu /');
assert.ok(document.querySelectorAll('.filtr button')[2].classList.contains('zvolene'), 'po návratu má být pořád zvolený typ Elektrokola');
```

Na neexistujícím kole zůstává odkaz zpátky na katalog.

```js
const { default: Trasy } = await helpers.importFile('Trasy.jsx');
const { MemoryRouter } = await import('react-router');
const { createElement } = await import('react');
const { createRoot } = await import('react-dom/client');
const misto = document.createElement('div');
document.body.append(misto);
createRoot(misto).render(createElement(MemoryRouter, { initialEntries: ['/kola/999'] }, createElement(Trasy)));
await helpers.flush();
assert.ok(misto.querySelector('.zpet'), 've větvi „Tohle kolo nemáme" má zůstat odkaz s třídou zpet');
misto.remove();
```

# --help--

## --tip--

Navigaci z kódu (ne z odkazu) dělá hook popsaný v části
[Odkazy: Link a NavLink](see:react-aplikace/routovani#odkazy-link-a-navlink).

## --tip--

Ten hook vrací funkci. Kromě adresy jí můžeš dát i číslo — kladné dopředu,
záporné dozadu v historii.

## --tip--

Jinde: tlačítko „Zrušit" ve formuláři si funkci uloží jako `const zpet = useNavigate()`
a pak dělá `onClick={() => zpet(-1)}` — vrátí uživatele tam, odkud formulář otevřel.

# --seed--

## --file-- App.jsx

```jsx
import { MemoryRouter } from 'react-router';
import Trasy from './Trasy';
import './styles.css';

export default function App() {
  return (
    <MemoryRouter>
      <Trasy />
    </MemoryRouter>
  );
}
```

## --file-- Katalog.jsx

```jsx
import { Link, useSearchParams } from 'react-router';
import { kola, popisekTypu, typy } from './data';

export default function Katalog() {
  const [parametry, setParametry] = useSearchParams();
  const typ = parametry.get('typ') ?? 'vse';
  const hledani = parametry.get('hledani') ?? '';
  const razeni = parametry.get('razeni') ?? 'nejlevnejsi';

  const videt = kola
    .filter((kolo) => typ === 'vse' || kolo.typ === typ)
    .filter((kolo) => kolo.nazev.toLowerCase().includes(hledani.toLowerCase()))
    .toSorted((prvni, druhe) =>
      razeni === 'nejdrazsi' ? druhe.cenaDen - prvni.cenaDen : prvni.cenaDen - druhe.cenaDen,
    );

  function zmenHledani(text) {
    const dalsi = new URLSearchParams(parametry);
    if (text === '') dalsi.delete('hledani');
    else dalsi.set('hledani', text);
    setParametry(dalsi, { replace: true });
  }

  function zmenRazeni(hodnota) {
    const dalsi = new URLSearchParams(parametry);
    dalsi.set('razeni', hodnota);
    setParametry(dalsi);
  }

  return (
    <>
      <div className="filtr">
        <button
          type="button"
          className={typ === 'vse' ? 'zvolene' : ''}
          onClick={() => setParametry({})}
        >
          Vše
        </button>
        {typy.map((moznost) => (
          <button
            key={moznost.hodnota}
            type="button"
            className={typ === moznost.hodnota ? 'zvolene' : ''}
            onClick={() => setParametry({ typ: moznost.hodnota })}
          >
            {moznost.popisek}
          </button>
        ))}
      </div>
      <label className="hledani">
        <span>Hledat kolo</span>
        <input
          type="search"
          value={hledani}
          placeholder="název kola"
          onChange={(udalost) => zmenHledani(udalost.target.value)}
        />
      </label>
      <label className="razeni">
        <span>Řadit</span>
        <select value={razeni} onChange={(udalost) => zmenRazeni(udalost.target.value)}>
          <option value="nejlevnejsi">od nejlevnějšího</option>
          <option value="nejdrazsi">od nejdražšího</option>
        </select>
      </label>
      {videt.length === 0 ? (
        <div className="prazdno">
          <p>Žádné kolo neodpovídá tomu, co hledáš.</p>
          <button type="button" className="zpet-tlacitko" onClick={() => setParametry({})}>
            Zrušit filtry
          </button>
        </div>
      ) : (
        <ul className="katalog">
          {videt.map((kolo) => (
            <li key={kolo.id}>
              <Link className="karta" to={'/kola/' + kolo.id}>
                <h2>{kolo.nazev}</h2>
                <p className="typ">{popisekTypu(kolo.typ)}</p>
                <p className="cena">{kolo.cenaDen} Kč / den</p>
              </Link>
            </li>
          ))}
        </ul>
      )}
    </>
  );
}
```

## --file-- data.js

```js
// Katalog půjčovny. cenaDen je cena za jeden den v korunách.
export const kola = [
  { id: 1, nazev: 'Author Vertigo 29', typ: 'horske', cenaDen: 420, ram: 'hliník', hmotnost: 13.4, popis: 'Horské kolo na lesní cesty kolem Brna i na sjezd k přehradě.' },
  { id: 2, nazev: 'Crussis e-Cross 7.9', typ: 'elektro', cenaDen: 690, ram: 'hliník', hmotnost: 24.1, popis: 'Elektrokolo s dojezdem 120 km. Na výlet do kopců bez potu.' },
  { id: 3, nazev: 'Rock Machine Torrent 30', typ: 'horske', cenaDen: 390, ram: 'hliník', hmotnost: 14, popis: 'Odolné kolo do terénu, zvládne i nezpevněné cesty s batohem.' },
  { id: 4, nazev: 'Favorit Praha', typ: 'mestske', cenaDen: 250, ram: 'ocel', hmotnost: 15.8, popis: 'Městské kolo s košíkem a blatníky. Na nákup i na kafe.' },
  { id: 5, nazev: 'Kellys Avenue 10', typ: 'mestske', cenaDen: 290, ram: 'hliník', hmotnost: 13.9, popis: 'Rychlé městské kolo na cyklostezku podél Vltavy.' },
  { id: 6, nazev: 'Superior eXR 6090', typ: 'elektro', cenaDen: 750, ram: 'karbon', hmotnost: 22.5, popis: 'Elektrokolo na dlouhé trasy, motor v zadním náboji.' },
  { id: 7, nazev: 'Dema Rockie 20', typ: 'detske', cenaDen: 150, ram: 'hliník', hmotnost: 9.6, popis: 'Dětské kolo pro jezdce od 120 cm. Lehké, aby se dalo zvednout.' },
  { id: 8, nazev: 'Author Ultima 24', typ: 'detske', cenaDen: 180, ram: 'hliník', hmotnost: 10.8, popis: 'Dětské kolo s většími koly a osmi převody na první delší výlety.' },
];

export const typy = [
  { hodnota: 'horske', popisek: 'Horská' },
  { hodnota: 'elektro', popisek: 'Elektrokola' },
  { hodnota: 'mestske', popisek: 'Městská' },
  { hodnota: 'detske', popisek: 'Dětská' },
];

/** Popisek typu pro výpis: 'horske' → 'Horská'. */
export function popisekTypu(hodnota) {
  return typy.find((typ) => typ.hodnota === hodnota)?.popisek ?? hodnota;
}
```

## --file-- styles.css

```css
:root {
  --zelena: #1f6f4f;
  --zelena-tmava: #15503a;
  --papir: #f6f5f3;
  --bila: #ffffff;
  --text: #1f2933;
  --seda: #6b7280;
  --linka: #e3e1dd;
  --radius: 14px;
}

* { box-sizing: border-box; }

body {
  margin: 0;
  font: 16px/1.6 system-ui, -apple-system, 'Segoe UI', sans-serif;
  background: var(--papir);
  color: var(--text);
}

.obal {
  max-width: 880px;
  margin: 0 auto;
  padding: 32px 20px 64px;
}

h1 { margin: 0 0 4px; font-size: 1.9rem; letter-spacing: -0.02em; }
.podnadpis { margin: 0 0 28px; color: var(--seda); }

.katalog {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(230px, 1fr));
  gap: 16px;
  margin: 0;
  padding: 0;
  list-style: none;
}

.karta {
  display: block;
  padding: 18px;
  background: var(--bila);
  border: 1px solid var(--linka);
  border-radius: var(--radius);
  color: inherit;
  text-decoration: none;
  transition: transform 160ms ease, box-shadow 160ms ease, border-color 160ms ease;
}

a.karta:hover, a.karta:focus-visible {
  transform: translateY(-2px);
  border-color: var(--zelena);
  box-shadow: 0 8px 24px rgb(31 41 51 / 0.1);
}

.karta h2 { margin: 0 0 6px; font-size: 1.05rem; }
.karta .typ { margin: 0; font-size: 0.8rem; text-transform: uppercase; letter-spacing: 0.06em; color: var(--seda); }
.karta .cena { margin: 12px 0 0; font-weight: 650; color: var(--zelena); }

.hlavicka {
  display: flex;
  flex-wrap: wrap;
  align-items: baseline;
  justify-content: space-between;
  gap: 12px;
  padding-bottom: 16px;
  margin-bottom: 24px;
  border-bottom: 1px solid var(--linka);
}

.nabidka { display: flex; gap: 18px; }
.nabidka a { color: var(--seda); text-decoration: none; padding-bottom: 4px; border-bottom: 2px solid transparent; }
.nabidka a:hover { color: var(--text); }
.nabidka a.active { color: var(--zelena); border-bottom-color: var(--zelena); font-weight: 600; }

.filtr { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 20px; }
.filtr button {
  border: 1px solid var(--linka);
  border-radius: 999px;
  padding: 7px 16px;
  background: var(--bila);
  font: inherit;
  color: var(--text);
  cursor: pointer;
  transition: background 140ms ease, border-color 140ms ease, color 140ms ease;
}
.filtr button:hover { border-color: var(--zelena); }
.filtr button:focus-visible { outline: 2px solid var(--zelena); outline-offset: 2px; }
.filtr .zvolene { background: var(--zelena); border-color: var(--zelena); color: var(--bila); }

.pocet { margin: 0 0 16px; color: var(--seda); font-size: 0.9rem; }

.detail { background: var(--bila); border: 1px solid var(--linka); border-radius: var(--radius); padding: 24px; }
.detail h1 { font-size: 1.5rem; }
.detail dl { display: grid; grid-template-columns: max-content 1fr; gap: 6px 20px; margin: 20px 0; }
.detail dt { color: var(--seda); }
.detail dd { margin: 0; }
.detail .cena-velka { font-size: 1.4rem; font-weight: 650; color: var(--zelena); }

.zpet { display: inline-block; margin-top: 16px; color: var(--zelena); }
.zpet:focus-visible { outline: 2px solid var(--zelena); outline-offset: 3px; }

.prazdno { padding: 32px; text-align: center; color: var(--seda); background: var(--bila); border: 1px dashed var(--linka); border-radius: var(--radius); }

button.zpet-tlacitko {
  margin-top: 16px;
  border: 1px solid var(--zelena);
  border-radius: 999px;
  padding: 8px 18px;
  background: transparent;
  color: var(--zelena);
  font: inherit;
  cursor: pointer;
}
button.zpet-tlacitko:hover { background: var(--zelena); color: var(--bila); }

.hledani { display: block; margin-bottom: 20px; }
.hledani span { display: block; font-size: 0.85rem; color: var(--seda); margin-bottom: 4px; }
.hledani input {
  width: 100%;
  max-width: 320px;
  padding: 9px 14px;
  border: 1px solid var(--linka);
  border-radius: 999px;
  background: var(--bila);
  font: inherit;
  color: inherit;
}
.hledani input:focus-visible { outline: 2px solid var(--zelena); outline-offset: 1px; }

.servis { margin: 0; padding: 0; list-style: none; display: grid; gap: 10px; }
.servis li {
  display: flex;
  flex-wrap: wrap;
  gap: 4px 12px;
  align-items: baseline;
  padding: 14px 18px;
  background: var(--bila);
  border: 1px solid var(--linka);
  border-radius: var(--radius);
}
.servis .ukon { color: var(--seda); }
.servis .stav { margin-left: auto; font-size: 0.8rem; text-transform: uppercase; letter-spacing: 0.06em; color: var(--zelena); }

.razeni { display: inline-flex; align-items: center; gap: 8px; margin: 0 0 20px; }
.razeni span { font-size: 0.85rem; color: var(--seda); }
.razeni select {
  padding: 8px 12px;
  border: 1px solid var(--linka);
  border-radius: 999px;
  background: var(--bila);
  font: inherit;
  color: inherit;
}
.razeni select:focus-visible { outline: 2px solid var(--zelena); outline-offset: 1px; }
```

## --file-- Trasy.jsx

```jsx
import { Routes, Route } from 'react-router';
import Layout from './Layout';
import Katalog from './Katalog';
import Detail from './Detail';
import Pujcovna from './Pujcovna';
import Servis from './Servis';
import Nenalezeno from './Nenalezeno';

export default function Trasy() {
  return (
    <Routes>
      <Route path="/" element={<Layout />}>
        <Route index element={<Katalog />} />
        <Route path="kola/:id" element={<Detail />} />
        <Route path="pujcovna" element={<Pujcovna />} />
        <Route path="servis" element={<Servis />} />
        <Route path="*" element={<Nenalezeno />} />
      </Route>
    </Routes>
  );
}
```

## --file-- Detail.jsx

```jsx
import { Link, useParams } from 'react-router';
import { kola, popisekTypu } from './data';

export default function Detail() {
  const { id } = useParams();
  const kolo = kola.find((polozka) => polozka.id === Number(id));

  if (!kolo) {
    return (
      <article className="detail">
        <h1>Tohle kolo nemáme</h1>
        <p>Kolo číslo {id} v katalogu není. Možná už ho někdo vyřadil.</p>
        <Link className="zpet" to="/">← Zpět na katalog</Link>
      </article>
    );
  }

  return (
    <article className="detail">
      <h1>{kolo.nazev}</h1>
      <p className="typ">{popisekTypu(kolo.typ)}</p>
      <p>{kolo.popis}</p>
      <dl>
        <dt>Rám</dt>
        <dd>{kolo.ram}</dd>
        <dt>Hmotnost</dt>
        <dd>{kolo.hmotnost} kg</dd>
      </dl>
      <p className="cena-velka">{kolo.cenaDen} Kč / den</p>
      <Link className="zpet" to="/">← Zpět na katalog</Link>
    </article>
  );
}
```

## --file-- Layout.jsx

```jsx
import { NavLink, Outlet } from 'react-router';

export default function Layout() {
  return (
    <div className="obal">
      <header className="hlavicka">
        <h1>Půjčovna Na Kolech</h1>
        <nav className="nabidka">
          <NavLink to="/" end>Katalog</NavLink>
          <NavLink to="/pujcovna">O půjčovně</NavLink>
          <NavLink to="/servis">Servis</NavLink>
        </nav>
      </header>
      <Outlet />
    </div>
  );
}
```

## --file-- Pujcovna.jsx

```jsx
export default function Pujcovna() {
  return (
    <article className="detail">
      <h1>O půjčovně</h1>
      <p>Jsme malá půjčovna v Brně-Komíně. Kola půjčujeme od jara do podzimu,
        vždy s helmou a zámkem v ceně.</p>
      <dl>
        <dt>Otevřeno</dt>
        <dd>po–pá 9:00–18:00, so 9:00–12:00</dd>
        <dt>Telefon</dt>
        <dd>541 123 456</dd>
        <dt>Adresa</dt>
        <dd>Veslařská 12, Brno</dd>
      </dl>
    </article>
  );
}
```

## --file-- Nenalezeno.jsx

```jsx
import { Link } from 'react-router';

export default function Nenalezeno() {
  return (
    <div className="prazdno">
      <h2>Taková stránka u nás není</h2>
      <p>Zkontroluj adresu, nebo se vrať do katalogu.</p>
      <Link className="zpet" to="/">← Zpět na katalog</Link>
    </div>
  );
}
```

## --file-- Servis.jsx

```jsx
import { useSearchParams } from 'react-router';

const objednavky = [
  { id: 1, kolo: 'Author Vertigo 29', ukon: 'Seřízení převodů', stav: 'ceka' },
  { id: 2, kolo: 'Favorit Praha', ukon: 'Výměna duše', stav: 'hotovo' },
  { id: 3, kolo: 'Crussis e-Cross 7.9', ukon: 'Diagnostika motoru', stav: 'ceka' },
  { id: 4, kolo: 'Kellys Avenue 10', ukon: 'Centrování kola', stav: 'hotovo' },
  { id: 5, kolo: 'Dema Rockie 20', ukon: 'Seřízení brzd', stav: 'ceka' },
];

const stavy = [
  { hodnota: 'vse', popisek: 'Vše' },
  { hodnota: 'ceka', popisek: 'Čeká' },
  { hodnota: 'hotovo', popisek: 'Hotovo' },
];

export default function Servis() {
  const [parametry, setParametry] = useSearchParams();
  const stav = parametry.get('stav') ?? 'vse';
  const videt = stav === 'vse' ? objednavky : objednavky.filter((polozka) => polozka.stav === stav);

  return (
    <>
      <div className="filtr">
        {stavy.map((moznost) => (
          <button
            key={moznost.hodnota}
            type="button"
            className={stav === moznost.hodnota ? 'zvolene' : ''}
            onClick={() => setParametry(moznost.hodnota === 'vse' ? {} : { stav: moznost.hodnota })}
          >
            {moznost.popisek}
          </button>
        ))}
      </div>
      <ul className="servis">
        {videt.map((polozka) => (
          <li key={polozka.id}>
            <strong>{polozka.kolo}</strong>
            <span className="ukon">{polozka.ukon}</span>
            <span className="stav">{polozka.stav === 'hotovo' ? 'hotovo' : 'čeká'}</span>
          </li>
        ))}
      </ul>
    </>
  );
}
```

# --solution--

## --file-- Detail.jsx

```jsx
import { Link, useNavigate, useParams } from 'react-router';
import { kola, popisekTypu } from './data';

export default function Detail() {
  const { id } = useParams();
  const navigate = useNavigate();
  const kolo = kola.find((polozka) => polozka.id === Number(id));

  if (!kolo) {
    return (
      <article className="detail">
        <h1>Tohle kolo nemáme</h1>
        <p>Kolo číslo {id} v katalogu není. Možná už ho někdo vyřadil.</p>
        <Link className="zpet" to="/">← Zpět na katalog</Link>
      </article>
    );
  }

  return (
    <article className="detail">
      <h1>{kolo.nazev}</h1>
      <p className="typ">{popisekTypu(kolo.typ)}</p>
      <p>{kolo.popis}</p>
      <dl>
        <dt>Rám</dt>
        <dd>{kolo.ram}</dd>
        <dt>Hmotnost</dt>
        <dd>{kolo.hmotnost} kg</dd>
      </dl>
      <p className="cena-velka">{kolo.cenaDen} Kč / den</p>
      <button type="button" className="zpet-tlacitko" onClick={() => navigate(-1)}>
        ← Zpět tam, odkud jsi přišel
      </button>
    </article>
  );
}
```
