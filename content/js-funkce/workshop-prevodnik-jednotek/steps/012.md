---
title: "Opakování: číslo z textového políčka"
kind: recall
see: js-retezce-cisla/cisla
---

# --description--

Z políčka na stránce přijde vždycky **text**, a Češi píšou desetinnou čárku: `'26,2'`. Než převodník začne počítat, musí z textu udělat číslo. Tohle znáš ze sekce o řetězcích a číslech.

**Úkol:** Napiš funkci `parseAmount(text)`, která:

- vrátí číslo z textu, v němž může být desetinná čárka i tečka: `'26,2'` → `26.2`, `'5.5'` → `5.5`,
- ignoruje mezery na začátku a na konci: `' 12 '` → `12`,
- pro prázdný text, text jen z mezer nebo text, který číslo není, vrátí `NaN`.

Tady máš hotový základ převodníku: převody, formátování i kontroly vstupu. Klidně si dej pauzu — v další části přijdou kolegovy chyby, zbytkové parametry a nakonec skutečná stránka.

# --hints--

`parseAmount('26,2')` vrátí `26.2` a `parseAmount('5.5')` vrátí `5.5`.

```js
assert.equal(parseAmount('26,2'), 26.2, "parseAmount('26,2') má vrátit číslo 26.2");
assert.equal(parseAmount('5.5'), 5.5, "parseAmount('5.5') má vrátit 5.5");
```

`parseAmount(' 12 ')` vrátí `12`.

```js
assert.equal(parseAmount(' 12 '), 12, "parseAmount(' 12 ') má vrátit 12");
```

`parseAmount('')` a `parseAmount('   ')` vrátí `NaN`.

```js
assert.ok(Number.isNaN(parseAmount('')), "parseAmount('') má vrátit NaN — pozor, Number('') je 0");
assert.ok(Number.isNaN(parseAmount('   ')), "parseAmount('   ') má vrátit NaN");
```

`parseAmount('abc')` vrátí `NaN`.

```js
assert.ok(Number.isNaN(parseAmount('abc')), "parseAmount('abc') má vrátit NaN");
```

# --help--

## --tip--

Převod textu na číslo a jeho pasti jsou v lekci [Čísla a počítání](see:js-retezce-cisla/cisla).

## --tip--

Nejdřív text ořízni a čárku nahraď tečkou. Pak pozor: `Number('')` vrací `0`, ne `NaN` — prázdný text ošetři zvlášť, nebo použij převod, který z prázdného textu udělá `NaN`.

## --tip--

Postup na podcíle:

```js
// 1. ořízni mezery
// 2. prázdný text → NaN (guard clause)
// 3. čárku nahraď tečkou a převeď na číslo
```

# --seed--

## --file-- script.js

```js
// Převodník jednotek pro nákupy, recepty a závody ze zahraničí.
// Americké weby uvádějí míle, libry, palce a stupně Fahrenheita.
const KM_PER_MILE = 1.609344;
const KG_PER_POUND = 0.45359237;
const CM_PER_INCH = 2.54;

// Převody liber už napsal kolega.
function poundsToKg(pounds) {
  return pounds * KG_PER_POUND;
}

function kgToPounds(kg) {
  return kg / KG_PER_POUND;
}

console.log(poundsToKg(5));

function roundTo(value, decimals) {
  const factor = 10 ** decimals;
  return Math.round(value * factor) / factor;
}

console.log(roundTo(poundsToKg(5), 2));

function milesToKm(miles) {
  return miles * KM_PER_MILE;
}

console.log(milesToKm(26.2));

const kmToMiles = (km) => km / KM_PER_MILE;

console.log(kmToMiles(42.195));

function formatNumber(value, decimals = 2) {
  return String(roundTo(value, decimals)).replace('.', ',');
}

console.log(formatNumber(milesToKm(26.2)));
console.log(formatNumber(milesToKm(26.2), 0));

function formatResult(value, unit, decimals = 1) {
  return `${formatNumber(value, decimals)} ${unit}`;
}

console.log(formatResult(milesToKm(26.2), 'km'));

// Teploty poslal kolega: recepty z USA uvádějí teplotu trouby ve °F.
const ABSOLUTE_ZERO_F = -459.67;
const ABSOLUTE_ZERO_C = -273.15;

function degreesAboveFreezing(fahrenheit) {
  return fahrenheit - 32;
}

function fahrenheitToCelsius(fahrenheit) {
  return degreesAboveFreezing(fahrenheit) * 5 / 9;
}

function celsiusToFahrenheit(celsius) {
  return celsius * 9 / 5 + 32;
}

console.log(formatResult(fahrenheitToCelsius(350), '°C'));

function temperatureLabel(fahrenheit) {
  if (!Number.isFinite(fahrenheit)) {
    return 'Zadej číslo';
  }
  if (fahrenheit < ABSOLUTE_ZERO_F) {
    return 'Pod absolutní nulou';
  }
  return formatResult(fahrenheitToCelsius(fahrenheit), '°C');
}

function distanceLabel(miles) {
  if (!Number.isFinite(miles)) {
    return 'Zadej číslo';
  }
  if (miles < 0) {
    return 'Vzdálenost nemůže být záporná';
  }
  return formatResult(milesToKm(miles), 'km');
}

console.log(distanceLabel(26.2));

function convert(value, conversion) {
  if (conversion === 'mi-km') {
    return milesToKm(value);
  }
  if (conversion === 'km-mi') {
    return kmToMiles(value);
  }
  if (conversion === 'lb-kg') {
    return poundsToKg(value);
  }
  if (conversion === 'kg-lb') {
    return kgToPounds(value);
  }
  if (conversion === 'f-c') {
    return fahrenheitToCelsius(value);
  }
  if (conversion === 'c-f') {
    return celsiusToFahrenheit(value);
  }
  return null;
}

console.log(convert(5, 'lb-kg'));

function targetUnit(conversion) {
  const unit = conversion.slice(conversion.indexOf('-') + 1);
  if (unit === 'c') {
    return '°C';
  }
  if (unit === 'f') {
    return '°F';
  }
  return unit;
}

console.log(targetUnit('f-c'));

--edit--

--edit--
```

# --solution--

## --file-- script.js

```js
// Převodník jednotek pro nákupy, recepty a závody ze zahraničí.
// Americké weby uvádějí míle, libry, palce a stupně Fahrenheita.
const KM_PER_MILE = 1.609344;
const KG_PER_POUND = 0.45359237;
const CM_PER_INCH = 2.54;

// Převody liber už napsal kolega.
function poundsToKg(pounds) {
  return pounds * KG_PER_POUND;
}

function kgToPounds(kg) {
  return kg / KG_PER_POUND;
}

console.log(poundsToKg(5));

function roundTo(value, decimals) {
  const factor = 10 ** decimals;
  return Math.round(value * factor) / factor;
}

console.log(roundTo(poundsToKg(5), 2));

function milesToKm(miles) {
  return miles * KM_PER_MILE;
}

console.log(milesToKm(26.2));

const kmToMiles = (km) => km / KM_PER_MILE;

console.log(kmToMiles(42.195));

function formatNumber(value, decimals = 2) {
  return String(roundTo(value, decimals)).replace('.', ',');
}

console.log(formatNumber(milesToKm(26.2)));
console.log(formatNumber(milesToKm(26.2), 0));

function formatResult(value, unit, decimals = 1) {
  return `${formatNumber(value, decimals)} ${unit}`;
}

console.log(formatResult(milesToKm(26.2), 'km'));

// Teploty poslal kolega: recepty z USA uvádějí teplotu trouby ve °F.
const ABSOLUTE_ZERO_F = -459.67;
const ABSOLUTE_ZERO_C = -273.15;

function degreesAboveFreezing(fahrenheit) {
  return fahrenheit - 32;
}

function fahrenheitToCelsius(fahrenheit) {
  return degreesAboveFreezing(fahrenheit) * 5 / 9;
}

function celsiusToFahrenheit(celsius) {
  return celsius * 9 / 5 + 32;
}

console.log(formatResult(fahrenheitToCelsius(350), '°C'));

function temperatureLabel(fahrenheit) {
  if (!Number.isFinite(fahrenheit)) {
    return 'Zadej číslo';
  }
  if (fahrenheit < ABSOLUTE_ZERO_F) {
    return 'Pod absolutní nulou';
  }
  return formatResult(fahrenheitToCelsius(fahrenheit), '°C');
}

function distanceLabel(miles) {
  if (!Number.isFinite(miles)) {
    return 'Zadej číslo';
  }
  if (miles < 0) {
    return 'Vzdálenost nemůže být záporná';
  }
  return formatResult(milesToKm(miles), 'km');
}

console.log(distanceLabel(26.2));

function convert(value, conversion) {
  if (conversion === 'mi-km') {
    return milesToKm(value);
  }
  if (conversion === 'km-mi') {
    return kmToMiles(value);
  }
  if (conversion === 'lb-kg') {
    return poundsToKg(value);
  }
  if (conversion === 'kg-lb') {
    return kgToPounds(value);
  }
  if (conversion === 'f-c') {
    return fahrenheitToCelsius(value);
  }
  if (conversion === 'c-f') {
    return celsiusToFahrenheit(value);
  }
  return null;
}

console.log(convert(5, 'lb-kg'));

function targetUnit(conversion) {
  const unit = conversion.slice(conversion.indexOf('-') + 1);
  if (unit === 'c') {
    return '°C';
  }
  if (unit === 'f') {
    return '°F';
  }
  return unit;
}

console.log(targetUnit('f-c'));

function parseAmount(text) {
  const trimmed = text.trim();
  if (trimmed === '') {
    return NaN;
  }
  return Number(trimmed.replace(',', '.'));
}

console.log(parseAmount(' 26,2 '));
```
