---
title: "Road trip: libovolný počet úseků"
see: js-funkce/funkce#vychozi-a-zbytkove-parametry
---

# --description--

Na road tripu po Route 66 máš úseky v mílích: Chicago → St. Louis 297, St. Louis → Tulsa 396, Tulsa → Amarillo 370. Kolik je to celkem kilometrů? A co když úseků bude sedm?

**Úkol:** Napiš funkci `totalKm(...milesLegs)`, která dostane libovolný počet úseků v mílích jako samostatné argumenty — `totalKm(297, 396, 370)` — a vrátí jejich součet v kilometrech (nezaokrouhlený). Bez argumentů vrátí `0`.

Na předem neznámý počet argumentů se hodí zbytkový parametr se třemi tečkami. Sebere argumenty do pole, které projdeš cyklem `for…of` a sečteš. Jinou možností by bylo chtít po volajícím jedno pole, ale tady se volá s čísly za sebou.

Převod na kilometry nepiš znovu — máš na něj funkci.

# --hints--

`totalKm(10)` vrátí `16.09344`.

```js
assert.ok(Math.abs(totalKm(10) - 16.09344) < 1e-9, 'totalKm(10) má vrátit 16.09344');
```

`totalKm(297, 396, 370)` vrátí součet úseků v kilometrech.

```js
assert.ok(Math.abs(totalKm(297, 396, 370) - 1063 * 1.609344) < 1e-6, 'totalKm(297, 396, 370) má vrátit asi 1710.7 km (1063 mil)');
```

`totalKm` zvládne libovolný počet úseků, i sedm.

```js
assert.ok(Math.abs(totalKm(1, 1, 1, 1, 1, 1, 1) - 7 * 1.609344) < 1e-9, 'totalKm(1, 1, 1, 1, 1, 1, 1) má vrátit 7 × 1.609344');
```

`totalKm()` bez argumentů vrátí `0`.

```js
assert.equal(totalKm(), 0, 'totalKm() bez úseků má vrátit 0');
```

# --help--

## --tip--

Zbytkový parametr je v části [Výchozí a zbytkové parametry](see:js-funkce/funkce#vychozi-a-zbytkove-parametry).

## --tip--

Tři tečky patří před jméno parametru v hlavičce. V těle si připrav průběžný součet v `let`, v cyklu k němu přičítej kilometry každého úseku a po cyklu ho vrať.

## --tip--

Na jiných datech: `function totalMinutes(...songs)` by sečetla délky písní, `totalMinutes(3, 4, 5)` vrátí `12`.

# --seed--

## --file-- script.js

```js
// Převodník jednotek pro nákupy, recepty a závody ze zahraničí.
// Americké weby uvádějí míle, libry, palce a stupně Fahrenheita.
const KM_PER_MILE = 1.609344;
const KG_PER_POUND = 0.45359237;
const CM_PER_INCH = 2.54;

// Převody liber už napsal kolega.
function poundsToKg(pounds) {
  return pounds * KG_PER_POUND;
}

function kgToPounds(kg) {
  return kg / KG_PER_POUND;
}

console.log(poundsToKg(5));

function roundTo(value, decimals) {
  const factor = 10 ** decimals;
  return Math.round(value * factor) / factor;
}

console.log(roundTo(poundsToKg(5), 2));

function milesToKm(miles) {
  return miles * KM_PER_MILE;
}

console.log(milesToKm(26.2));

const kmToMiles = (km) => km / KM_PER_MILE;

console.log(kmToMiles(42.195));

function formatNumber(value, decimals = 2) {
  return String(roundTo(value, decimals)).replace('.', ',');
}

console.log(formatNumber(milesToKm(26.2)));
console.log(formatNumber(milesToKm(26.2), 0));

function formatResult(value, unit, decimals = 1) {
  return `${formatNumber(value, decimals)} ${unit}`;
}

console.log(formatResult(milesToKm(26.2), 'km'));

// Teploty poslal kolega: recepty z USA uvádějí teplotu trouby ve °F.
const ABSOLUTE_ZERO_F = -459.67;
const ABSOLUTE_ZERO_C = -273.15;

function degreesAboveFreezing(fahrenheit) {
  return fahrenheit - 32;
}

function fahrenheitToCelsius(fahrenheit) {
  return degreesAboveFreezing(fahrenheit) * 5 / 9;
}

function celsiusToFahrenheit(celsius) {
  return celsius * 9 / 5 + 32;
}

console.log(formatResult(fahrenheitToCelsius(350), '°C'));

function temperatureLabel(fahrenheit) {
  if (!Number.isFinite(fahrenheit)) {
    return 'Zadej číslo';
  }
  if (fahrenheit < ABSOLUTE_ZERO_F) {
    return 'Pod absolutní nulou';
  }
  return formatResult(fahrenheitToCelsius(fahrenheit), '°C');
}

function distanceLabel(miles) {
  if (!Number.isFinite(miles)) {
    return 'Zadej číslo';
  }
  if (miles < 0) {
    return 'Vzdálenost nemůže být záporná';
  }
  return formatResult(milesToKm(miles), 'km');
}

console.log(distanceLabel(26.2));

function convert(value, conversion) {
  if (conversion === 'mi-km') {
    return milesToKm(value);
  }
  if (conversion === 'km-mi') {
    return kmToMiles(value);
  }
  if (conversion === 'lb-kg') {
    return poundsToKg(value);
  }
  if (conversion === 'kg-lb') {
    return kgToPounds(value);
  }
  if (conversion === 'f-c') {
    return fahrenheitToCelsius(value);
  }
  if (conversion === 'c-f') {
    return celsiusToFahrenheit(value);
  }
  return null;
}

console.log(convert(5, 'lb-kg'));

function targetUnit(conversion) {
  const unit = conversion.slice(conversion.indexOf('-') + 1);
  if (unit === 'c') {
    return '°C';
  }
  if (unit === 'f') {
    return '°F';
  }
  return unit;
}

console.log(targetUnit('f-c'));

function parseAmount(text) {
  const trimmed = text.trim();
  if (trimmed === '') {
    return NaN;
  }
  return Number(trimmed.replace(',', '.'));
}

console.log(parseAmount(' 26,2 '));

// Kolegův modul pro běžce: tempo z amerických závodů v minutách na kilometr.
function paceLabel(miles, minutes, decimals) {
  const km = milesToKm(miles);
  const pace = minutes / km;
  const places = decimals ?? 2;
  return `${formatNumber(pace, places)} min/km`;
}

console.log(paceLabel(26.2, 210, 0));

--edit--

--edit--
```

# --solution--

## --file-- script.js

```js
// Převodník jednotek pro nákupy, recepty a závody ze zahraničí.
// Americké weby uvádějí míle, libry, palce a stupně Fahrenheita.
const KM_PER_MILE = 1.609344;
const KG_PER_POUND = 0.45359237;
const CM_PER_INCH = 2.54;

// Převody liber už napsal kolega.
function poundsToKg(pounds) {
  return pounds * KG_PER_POUND;
}

function kgToPounds(kg) {
  return kg / KG_PER_POUND;
}

console.log(poundsToKg(5));

function roundTo(value, decimals) {
  const factor = 10 ** decimals;
  return Math.round(value * factor) / factor;
}

console.log(roundTo(poundsToKg(5), 2));

function milesToKm(miles) {
  return miles * KM_PER_MILE;
}

console.log(milesToKm(26.2));

const kmToMiles = (km) => km / KM_PER_MILE;

console.log(kmToMiles(42.195));

function formatNumber(value, decimals = 2) {
  return String(roundTo(value, decimals)).replace('.', ',');
}

console.log(formatNumber(milesToKm(26.2)));
console.log(formatNumber(milesToKm(26.2), 0));

function formatResult(value, unit, decimals = 1) {
  return `${formatNumber(value, decimals)} ${unit}`;
}

console.log(formatResult(milesToKm(26.2), 'km'));

// Teploty poslal kolega: recepty z USA uvádějí teplotu trouby ve °F.
const ABSOLUTE_ZERO_F = -459.67;
const ABSOLUTE_ZERO_C = -273.15;

function degreesAboveFreezing(fahrenheit) {
  return fahrenheit - 32;
}

function fahrenheitToCelsius(fahrenheit) {
  return degreesAboveFreezing(fahrenheit) * 5 / 9;
}

function celsiusToFahrenheit(celsius) {
  return celsius * 9 / 5 + 32;
}

console.log(formatResult(fahrenheitToCelsius(350), '°C'));

function temperatureLabel(fahrenheit) {
  if (!Number.isFinite(fahrenheit)) {
    return 'Zadej číslo';
  }
  if (fahrenheit < ABSOLUTE_ZERO_F) {
    return 'Pod absolutní nulou';
  }
  return formatResult(fahrenheitToCelsius(fahrenheit), '°C');
}

function distanceLabel(miles) {
  if (!Number.isFinite(miles)) {
    return 'Zadej číslo';
  }
  if (miles < 0) {
    return 'Vzdálenost nemůže být záporná';
  }
  return formatResult(milesToKm(miles), 'km');
}

console.log(distanceLabel(26.2));

function convert(value, conversion) {
  if (conversion === 'mi-km') {
    return milesToKm(value);
  }
  if (conversion === 'km-mi') {
    return kmToMiles(value);
  }
  if (conversion === 'lb-kg') {
    return poundsToKg(value);
  }
  if (conversion === 'kg-lb') {
    return kgToPounds(value);
  }
  if (conversion === 'f-c') {
    return fahrenheitToCelsius(value);
  }
  if (conversion === 'c-f') {
    return celsiusToFahrenheit(value);
  }
  return null;
}

console.log(convert(5, 'lb-kg'));

function targetUnit(conversion) {
  const unit = conversion.slice(conversion.indexOf('-') + 1);
  if (unit === 'c') {
    return '°C';
  }
  if (unit === 'f') {
    return '°F';
  }
  return unit;
}

console.log(targetUnit('f-c'));

function parseAmount(text) {
  const trimmed = text.trim();
  if (trimmed === '') {
    return NaN;
  }
  return Number(trimmed.replace(',', '.'));
}

console.log(parseAmount(' 26,2 '));

// Kolegův modul pro běžce: tempo z amerických závodů v minutách na kilometr.
function paceLabel(miles, minutes, decimals) {
  const km = milesToKm(miles);
  const pace = minutes / km;
  const places = decimals ?? 2;
  return `${formatNumber(pace, places)} min/km`;
}

console.log(paceLabel(26.2, 210, 0));

function totalKm(...milesLegs) {
  let total = 0;
  for (const miles of milesLegs) {
    total += milesToKm(miles);
  }
  return total;
}

console.log(formatResult(totalKm(297, 396, 370), 'km'));
```
