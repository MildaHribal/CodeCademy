---
title: "Opakování: řádek s prodejcem"
kind: recall
see: js-retezce-cisla/workshop-textove-utility/011
---

# --description--

Pod inzerátem je řádek s prodejcem. Tentokrát bez návodu, jen z funkcí, které už máš.

**Úkol:** Napiš funkci `sellerLine(name, phone)`, která vrátí iniciály, jméno bez mezer na krajích a naformátovaný telefon, oddělené znakem ` · ` (mezera, tečka uprostřed, mezera): `JN · Jana Nováková · 777 123 456`.

Když telefon není platný, řádek končí jménem: `JN · Jana Nováková`.

# --hints--

S platným telefonem vrátí `sellerLine` iniciály, jméno a telefon po trojicích.

```js
assert.equal(sellerLine('Jana Nováková', '777123456'), 'JN · Jana Nováková · 777 123 456', "sellerLine('Jana Nováková', '777123456') má vrátit 'JN · Jana Nováková · 777 123 456'");
assert.equal(sellerLine('  eva dvořáková ', '603 98 76 54'), 'ED · eva dvořáková · 603 987 654', "sellerLine('  eva dvořáková ', '603 98 76 54') má vrátit 'ED · eva dvořáková · 603 987 654' — jméno bez mezer na krajích");
```

S neplatným telefonem vrátí `sellerLine` jen iniciály a jméno.

```js
assert.equal(sellerLine('Jana Nováková', '777 12'), 'JN · Jana Nováková', "sellerLine('Jana Nováková', '777 12') má vrátit 'JN · Jana Nováková' — telefon je neúplný");
```

# --help--

## --tip--

Všechno potřebné už máš: iniciály z kroku [Vyber nástroj sám: iniciály](see:js-retezce-cisla/workshop-textove-utility/011) a kontrolu i formát telefonu z dřívějších kroků.

## --tip--

Nejdřív si slož začátek z iniciál a oříznutého jména. Podle výsledku kontroly telefonu vrať buď jen začátek, nebo začátek s formátovaným telefonem.

## --tip--

Podcíle:

```js
// 1. začátek řádku: iniciály · jméno
// 2. neplatný telefon → vrať začátek
// 3. jinak → začátek · telefon
```

# --seed--

## --file-- script.js

```js
// Kolobazar: textové utility pro inzeráty v bazaru kol.
// Každá funkce dostane text a vrátí nový text, číslo nebo true/false.

// Popisek prodejce pod inzerátem.
function sellerLabel(name) {
  if (name === '') {
    return 'Prodává: anonymní prodejce';
  }
  return `Prodává: ${name}`;
}

// Titulek bez bílých znaků na krajích.
function cleanTitle(title) {
  return title.trim();
}

// Text s velkým prvním písmenem, zbytek beze změny.
function capitalize(text) {
  return text.slice(0, 1).toUpperCase() + text.slice(1);
}

// Obsahuje titulek hledané slovo? Velikost písmen nerozhoduje.
function hasKeyword(title, keyword) {
  return title.toLowerCase().includes(keyword.toLowerCase());
}

// Zkrácený text s výpustkou, když je delší než maxLength.
function truncate(text, maxLength) {
  if (text.length <= maxLength) {
    return text;
  }
  return text.slice(0, maxLength) + '…';
}

// Kód inzerátu s pevnou délkou, např. INZ-000042.
function listingCode(id) {
  return `INZ-${String(id).padStart(6, '0')}`;
}

// Kolega upravil: snese i telefon zadaný s mezerami, třeba '777 123 456'.
function formatPhone(phone) {
  const digits = phone.replaceAll(' ', '');
  return `${digits.slice(0, 3)} ${digits.slice(3, 6)} ${digits.slice(6)}`;
}

// E-mail s hvězdičkami místo jména, doména zůstane.
function maskEmail(email) {
  const at = email.indexOf('@');
  if (at === -1) {
    return email;
  }
  return email.slice(0, 1) + '*'.repeat(at - 1) + email.slice(at);
}

// Platný telefon: po odstranění mezer přesně 9 číslic.
function isValidPhone(phone) {
  const digits = phone.replaceAll(' ', '');
  if (digits.length !== 9) {
    return false;
  }
  for (const char of digits) {
    if (!'0123456789'.includes(char)) {
      return false;
    }
  }
  return true;
}

// Iniciály ze jména: 'Jana Nováková' → 'JN'.
function initials(fullName) {
  let result = '';
  for (const word of fullName.split(' ')) {
    if (word !== '') {
      result += word.slice(0, 1).toUpperCase();
    }
  }
  return result;
}

// Od kolegy: text bez háčků a čárek, pro hledání a adresy inzerátů.
function removeDiacritics(text) {
  return text.normalize('NFD').replace(/\p{M}/gu, '');
}

// Obsahuje titulek hledaný text? Rozhoduje jen písmeno, ne velikost ani diakritika.
function matchesSearch(title, query) {
  const normalizedTitle = removeDiacritics(title).toLowerCase();
  const normalizedQuery = removeDiacritics(query.trim()).toLowerCase();
  return normalizedTitle.includes(normalizedQuery);
}

function slugify(title) {
  const allowed = 'abcdefghijklmnopqrstuvwxyz0123456789';
  let slug = '';
  for (const char of removeDiacritics(title).toLowerCase()) {
    if (allowed.includes(char)) {
      slug += char;
    } else if (slug !== '' && !slug.endsWith('-')) {
      slug += '-';
    }
  }
  return slug.endsWith('-') ? slug.slice(0, -1) : slug;
}

// Hashtag z textu: jen písmena a číslice bez diakritiky, např. #horskekolo29.
function hashtag(text) {
  const allowed = 'abcdefghijklmnopqrstuvwxyz0123456789';
  let tag = '';
  for (const char of removeDiacritics(text).toLowerCase()) {
    if (allowed.includes(char)) {
      tag += char;
    }
  }
  return tag === '' ? '' : `#${tag}`;
}

// Počet znaků tak, jak je vidí člověk (grafémy).
function countCharacters(text) {
  const segmenter = new Intl.Segmenter('cs', { granularity: 'grapheme' });
  return [...segmenter.segment(text)].length;
}

// Zkrácený text, který nekončí uprostřed slova.
function truncateAtWord(text, maxLength) {
  if (text.length <= maxLength) {
    return text;
  }
  let cut = text.slice(0, maxLength);
  const lastSpace = cut.lastIndexOf(' ');
  if (lastSpace !== -1) {
    cut = cut.slice(0, lastSpace);
  }
  return `${cut}…`;
}

// Porovnání titulků podle české abecedy: záporné, 0, nebo kladné číslo.
function compareTitles(a, b) {
  return a.localeCompare(b, 'cs');
}

--edit--
--edit--
```

# --solution--

## --file-- script.js

```js
// Kolobazar: textové utility pro inzeráty v bazaru kol.
// Každá funkce dostane text a vrátí nový text, číslo nebo true/false.

// Popisek prodejce pod inzerátem.
function sellerLabel(name) {
  if (name === '') {
    return 'Prodává: anonymní prodejce';
  }
  return `Prodává: ${name}`;
}

// Titulek bez bílých znaků na krajích.
function cleanTitle(title) {
  return title.trim();
}

// Text s velkým prvním písmenem, zbytek beze změny.
function capitalize(text) {
  return text.slice(0, 1).toUpperCase() + text.slice(1);
}

// Obsahuje titulek hledané slovo? Velikost písmen nerozhoduje.
function hasKeyword(title, keyword) {
  return title.toLowerCase().includes(keyword.toLowerCase());
}

// Zkrácený text s výpustkou, když je delší než maxLength.
function truncate(text, maxLength) {
  if (text.length <= maxLength) {
    return text;
  }
  return text.slice(0, maxLength) + '…';
}

// Kód inzerátu s pevnou délkou, např. INZ-000042.
function listingCode(id) {
  return `INZ-${String(id).padStart(6, '0')}`;
}

// Kolega upravil: snese i telefon zadaný s mezerami, třeba '777 123 456'.
function formatPhone(phone) {
  const digits = phone.replaceAll(' ', '');
  return `${digits.slice(0, 3)} ${digits.slice(3, 6)} ${digits.slice(6)}`;
}

// E-mail s hvězdičkami místo jména, doména zůstane.
function maskEmail(email) {
  const at = email.indexOf('@');
  if (at === -1) {
    return email;
  }
  return email.slice(0, 1) + '*'.repeat(at - 1) + email.slice(at);
}

// Platný telefon: po odstranění mezer přesně 9 číslic.
function isValidPhone(phone) {
  const digits = phone.replaceAll(' ', '');
  if (digits.length !== 9) {
    return false;
  }
  for (const char of digits) {
    if (!'0123456789'.includes(char)) {
      return false;
    }
  }
  return true;
}

// Iniciály ze jména: 'Jana Nováková' → 'JN'.
function initials(fullName) {
  let result = '';
  for (const word of fullName.split(' ')) {
    if (word !== '') {
      result += word.slice(0, 1).toUpperCase();
    }
  }
  return result;
}

// Od kolegy: text bez háčků a čárek, pro hledání a adresy inzerátů.
function removeDiacritics(text) {
  return text.normalize('NFD').replace(/\p{M}/gu, '');
}

// Obsahuje titulek hledaný text? Rozhoduje jen písmeno, ne velikost ani diakritika.
function matchesSearch(title, query) {
  const normalizedTitle = removeDiacritics(title).toLowerCase();
  const normalizedQuery = removeDiacritics(query.trim()).toLowerCase();
  return normalizedTitle.includes(normalizedQuery);
}

function slugify(title) {
  const allowed = 'abcdefghijklmnopqrstuvwxyz0123456789';
  let slug = '';
  for (const char of removeDiacritics(title).toLowerCase()) {
    if (allowed.includes(char)) {
      slug += char;
    } else if (slug !== '' && !slug.endsWith('-')) {
      slug += '-';
    }
  }
  return slug.endsWith('-') ? slug.slice(0, -1) : slug;
}

// Hashtag z textu: jen písmena a číslice bez diakritiky, např. #horskekolo29.
function hashtag(text) {
  const allowed = 'abcdefghijklmnopqrstuvwxyz0123456789';
  let tag = '';
  for (const char of removeDiacritics(text).toLowerCase()) {
    if (allowed.includes(char)) {
      tag += char;
    }
  }
  return tag === '' ? '' : `#${tag}`;
}

// Počet znaků tak, jak je vidí člověk (grafémy).
function countCharacters(text) {
  const segmenter = new Intl.Segmenter('cs', { granularity: 'grapheme' });
  return [...segmenter.segment(text)].length;
}

// Zkrácený text, který nekončí uprostřed slova.
function truncateAtWord(text, maxLength) {
  if (text.length <= maxLength) {
    return text;
  }
  let cut = text.slice(0, maxLength);
  const lastSpace = cut.lastIndexOf(' ');
  if (lastSpace !== -1) {
    cut = cut.slice(0, lastSpace);
  }
  return `${cut}…`;
}

// Porovnání titulků podle české abecedy: záporné, 0, nebo kladné číslo.
function compareTitles(a, b) {
  return a.localeCompare(b, 'cs');
}

// Řádek s prodejcem: 'JN · Jana Nováková · 777 123 456'.
function sellerLine(name, phone) {
  const start = `${initials(name)} · ${name.trim()}`;
  if (!isValidPhone(phone)) {
    return start;
  }
  return `${start} · ${formatPhone(phone)}`;
}
```
