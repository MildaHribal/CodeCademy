---
title: "Zkrácení u celého slova"
see: js-retezce-cisla/retezce#kousky-textu-at-slice-a-hledani
---

# --description--

`truncate` z pátého kroku řeže uprostřed slova: `Prodám horské k…`. V náhledu inzerátu to vypadá lacině. Lepší je zkrátit u poslední mezery, která se do limitu vejde: `Prodám horské…`.

**Úkol:** Napiš funkci `truncateAtWord(text, maxLength)`:

- když má text nejvýš `maxLength` znaků, vrátí ho beze změny,
- jinak vezme prvních `maxLength` znaků a když je v nich mezera, zkrátí je ještě až k **poslední** mezeře,
- když v nich mezera není (jedno dlouhé slovo), nechá prvních `maxLength` znaků,
- za zkrácený text přidá `…`.

Hodí se `slice`, `lastIndexOf` nebo `indexOf` — jen jedno z posledních dvou hledá od konce.

# --hints--

Delší text `truncateAtWord` zkrátí u poslední mezery, která se vejde do limitu.

```js
assert.equal(truncateAtWord('Prodám horské kolo Author', 15), 'Prodám horské…', "truncateAtWord('Prodám horské kolo Author', 15) má vrátit 'Prodám horské…' — v prvních 15 znacích je poslední mezera za slovem horské");
assert.equal(truncateAtWord('Prodám kolo i přilbu', 12), 'Prodám kolo…', "truncateAtWord('Prodám kolo i přilbu', 12) má vrátit 'Prodám kolo…'");
```

Krátký text zůstane beze změny.

```js
assert.equal(truncateAtWord('Kolo', 10), 'Kolo', "truncateAtWord('Kolo', 10) má vrátit 'Kolo'");
assert.equal(truncateAtWord('Kolo Author', 11), 'Kolo Author', "truncateAtWord('Kolo Author', 11) má vrátit 'Kolo Author' — text má přesně 11 znaků");
```

Jedno dlouhé slovo bez mezery se zkrátí na limit.

```js
assert.equal(truncateAtWord('Celoodpružené', 5), 'Celoo…', "truncateAtWord('Celoodpružené', 5) má vrátit 'Celoo…' — v prvních 5 znacích mezera není");
```

# --help--

## --tip--

Hledání „kde" od konce je v části [Kousky textu: at, slice a hledání](see:js-retezce-cisla/retezce#kousky-textu-at-slice-a-hledani).

## --tip--

Nejdřív si ulož výřez na `maxLength` znaků. V **něm** hledej poslední mezeru. Když ji `lastIndexOf` najde (výsledek není `-1`), vyřízni výřez ještě jednou až k ní.

## --tip--

Na jiných datech: jméno souboru bez přípony z `'foto-kola.final.jpeg'` je výřez od `0` k poslední tečce, kterou najde `lastIndexOf('.')`.

# --seed--

## --file-- script.js

```js
// Kolobazar: textové utility pro inzeráty v bazaru kol.
// Každá funkce dostane text a vrátí nový text, číslo nebo true/false.

// Popisek prodejce pod inzerátem.
function sellerLabel(name) {
  if (name === '') {
    return 'Prodává: anonymní prodejce';
  }
  return `Prodává: ${name}`;
}

// Titulek bez bílých znaků na krajích.
function cleanTitle(title) {
  return title.trim();
}

// Text s velkým prvním písmenem, zbytek beze změny.
function capitalize(text) {
  return text.slice(0, 1).toUpperCase() + text.slice(1);
}

// Obsahuje titulek hledané slovo? Velikost písmen nerozhoduje.
function hasKeyword(title, keyword) {
  return title.toLowerCase().includes(keyword.toLowerCase());
}

// Zkrácený text s výpustkou, když je delší než maxLength.
function truncate(text, maxLength) {
  if (text.length <= maxLength) {
    return text;
  }
  return text.slice(0, maxLength) + '…';
}

// Kód inzerátu s pevnou délkou, např. INZ-000042.
function listingCode(id) {
  return `INZ-${String(id).padStart(6, '0')}`;
}

// Kolega upravil: snese i telefon zadaný s mezerami, třeba '777 123 456'.
function formatPhone(phone) {
  const digits = phone.replaceAll(' ', '');
  return `${digits.slice(0, 3)} ${digits.slice(3, 6)} ${digits.slice(6)}`;
}

// E-mail s hvězdičkami místo jména, doména zůstane.
function maskEmail(email) {
  const at = email.indexOf('@');
  if (at === -1) {
    return email;
  }
  return email.slice(0, 1) + '*'.repeat(at - 1) + email.slice(at);
}

// Platný telefon: po odstranění mezer přesně 9 číslic.
function isValidPhone(phone) {
  const digits = phone.replaceAll(' ', '');
  if (digits.length !== 9) {
    return false;
  }
  for (const char of digits) {
    if (!'0123456789'.includes(char)) {
      return false;
    }
  }
  return true;
}

// Iniciály ze jména: 'Jana Nováková' → 'JN'.
function initials(fullName) {
  let result = '';
  for (const word of fullName.split(' ')) {
    if (word !== '') {
      result += word.slice(0, 1).toUpperCase();
    }
  }
  return result;
}

// Od kolegy: text bez háčků a čárek, pro hledání a adresy inzerátů.
function removeDiacritics(text) {
  return text.normalize('NFD').replace(/\p{M}/gu, '');
}

// Obsahuje titulek hledaný text? Rozhoduje jen písmeno, ne velikost ani diakritika.
function matchesSearch(title, query) {
  const normalizedTitle = removeDiacritics(title).toLowerCase();
  const normalizedQuery = removeDiacritics(query.trim()).toLowerCase();
  return normalizedTitle.includes(normalizedQuery);
}

function slugify(title) {
  const allowed = 'abcdefghijklmnopqrstuvwxyz0123456789';
  let slug = '';
  for (const char of removeDiacritics(title).toLowerCase()) {
    if (allowed.includes(char)) {
      slug += char;
    } else if (slug !== '' && !slug.endsWith('-')) {
      slug += '-';
    }
  }
  return slug.endsWith('-') ? slug.slice(0, -1) : slug;
}

// Hashtag z textu: jen písmena a číslice bez diakritiky, např. #horskekolo29.
function hashtag(text) {
  const allowed = 'abcdefghijklmnopqrstuvwxyz0123456789';
  let tag = '';
  for (const char of removeDiacritics(text).toLowerCase()) {
    if (allowed.includes(char)) {
      tag += char;
    }
  }
  return tag === '' ? '' : `#${tag}`;
}

// Počet znaků tak, jak je vidí člověk (grafémy).
function countCharacters(text) {
  const segmenter = new Intl.Segmenter('cs', { granularity: 'grapheme' });
  return [...segmenter.segment(text)].length;
}

--edit--
--edit--
```

# --solution--

## --file-- script.js

```js
// Kolobazar: textové utility pro inzeráty v bazaru kol.
// Každá funkce dostane text a vrátí nový text, číslo nebo true/false.

// Popisek prodejce pod inzerátem.
function sellerLabel(name) {
  if (name === '') {
    return 'Prodává: anonymní prodejce';
  }
  return `Prodává: ${name}`;
}

// Titulek bez bílých znaků na krajích.
function cleanTitle(title) {
  return title.trim();
}

// Text s velkým prvním písmenem, zbytek beze změny.
function capitalize(text) {
  return text.slice(0, 1).toUpperCase() + text.slice(1);
}

// Obsahuje titulek hledané slovo? Velikost písmen nerozhoduje.
function hasKeyword(title, keyword) {
  return title.toLowerCase().includes(keyword.toLowerCase());
}

// Zkrácený text s výpustkou, když je delší než maxLength.
function truncate(text, maxLength) {
  if (text.length <= maxLength) {
    return text;
  }
  return text.slice(0, maxLength) + '…';
}

// Kód inzerátu s pevnou délkou, např. INZ-000042.
function listingCode(id) {
  return `INZ-${String(id).padStart(6, '0')}`;
}

// Kolega upravil: snese i telefon zadaný s mezerami, třeba '777 123 456'.
function formatPhone(phone) {
  const digits = phone.replaceAll(' ', '');
  return `${digits.slice(0, 3)} ${digits.slice(3, 6)} ${digits.slice(6)}`;
}

// E-mail s hvězdičkami místo jména, doména zůstane.
function maskEmail(email) {
  const at = email.indexOf('@');
  if (at === -1) {
    return email;
  }
  return email.slice(0, 1) + '*'.repeat(at - 1) + email.slice(at);
}

// Platný telefon: po odstranění mezer přesně 9 číslic.
function isValidPhone(phone) {
  const digits = phone.replaceAll(' ', '');
  if (digits.length !== 9) {
    return false;
  }
  for (const char of digits) {
    if (!'0123456789'.includes(char)) {
      return false;
    }
  }
  return true;
}

// Iniciály ze jména: 'Jana Nováková' → 'JN'.
function initials(fullName) {
  let result = '';
  for (const word of fullName.split(' ')) {
    if (word !== '') {
      result += word.slice(0, 1).toUpperCase();
    }
  }
  return result;
}

// Od kolegy: text bez háčků a čárek, pro hledání a adresy inzerátů.
function removeDiacritics(text) {
  return text.normalize('NFD').replace(/\p{M}/gu, '');
}

// Obsahuje titulek hledaný text? Rozhoduje jen písmeno, ne velikost ani diakritika.
function matchesSearch(title, query) {
  const normalizedTitle = removeDiacritics(title).toLowerCase();
  const normalizedQuery = removeDiacritics(query.trim()).toLowerCase();
  return normalizedTitle.includes(normalizedQuery);
}

function slugify(title) {
  const allowed = 'abcdefghijklmnopqrstuvwxyz0123456789';
  let slug = '';
  for (const char of removeDiacritics(title).toLowerCase()) {
    if (allowed.includes(char)) {
      slug += char;
    } else if (slug !== '' && !slug.endsWith('-')) {
      slug += '-';
    }
  }
  return slug.endsWith('-') ? slug.slice(0, -1) : slug;
}

// Hashtag z textu: jen písmena a číslice bez diakritiky, např. #horskekolo29.
function hashtag(text) {
  const allowed = 'abcdefghijklmnopqrstuvwxyz0123456789';
  let tag = '';
  for (const char of removeDiacritics(text).toLowerCase()) {
    if (allowed.includes(char)) {
      tag += char;
    }
  }
  return tag === '' ? '' : `#${tag}`;
}

// Počet znaků tak, jak je vidí člověk (grafémy).
function countCharacters(text) {
  const segmenter = new Intl.Segmenter('cs', { granularity: 'grapheme' });
  return [...segmenter.segment(text)].length;
}

// Zkrácený text, který nekončí uprostřed slova.
function truncateAtWord(text, maxLength) {
  if (text.length <= maxLength) {
    return text;
  }
  let cut = text.slice(0, maxLength);
  const lastSpace = cut.lastIndexOf(' ');
  if (lastSpace !== -1) {
    cut = cut.slice(0, lastSpace);
  }
  return `${cut}…`;
}
```
