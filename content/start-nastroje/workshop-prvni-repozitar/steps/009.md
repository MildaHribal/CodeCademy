---
title: Co přesně se změnilo
timeoutMs: 30000
see: start-nastroje/workshop-prvni-repozitar/005
---

# --description--

Stav říká „index.html je změněný", ale ne **jak**. Před každým commitem se vyplatí podívat se na samotné řádky — je to poslední chvíle, kdy si všimneš, že tam zůstal `console.log` nebo omylem smazaný odstavec.

**Úkol:** vypiš rozdíl mezi pracovní složkou a posledním commitem.

Ve výpisu je řádek s `-` ten původní a řádek s `+` ten nový. Tenhle příkaz ukazuje **jen změny, které nejsou připravené ke commitu** — po `git add` už v něm soubor neuvidíš.

# --hints--

Výstup ukazuje konkrétní změněné řádky.

```js
const SANDBOX = 'cd sandbox/kemp && GIT_CEILING_DIRECTORIES="$(dirname \"$PWD\")" &&';
const out = await helpers.run('bash run.sh');
assert.match(out.stdout, /^\+.*7:30–21:00/m, `Ve výstupu chybí přidaný řádek s letní otevírací dobou. Vypsalo se:\n${out.stdout.slice(-1500)}`);
assert.match(out.stdout, /^-.*8:00–20:00/m, 'Ve výstupu chybí odebraný řádek s původní otevírací dobou');
```

Rozdíl se vypisuje až po výpisu stavu.

```js
const SANDBOX = 'cd sandbox/kemp && GIT_CEILING_DIRECTORIES="$(dirname \"$PWD\")" &&';
const lines = files['commands.sh'].split('\n').map((l) => l.trim()).filter(Boolean);
const diff = lines.lastIndexOf('git diff');
assert.ok(diff !== -1, 'V commands.sh chybí git diff');
assert.ok(lines[diff - 1] === 'git status', 'git diff patří hned pod git status, který jsi psal v minulém kroku');
```

# --help--

## --tip--

Příkaz, který ukáže změněné řádky, má v názvu anglické slovo pro rozdíl — *diff*.

## --tip--

Patří hned pod `git status` z minulého kroku.

## --tip--

Kdybys chtěl vidět, co je připravené ke commitu, byl by to `git diff --staged`.

# --explain--

Vysvětli vlastními slovy, čím se liší `git status` a `git diff`.

## --model--

`git status` odpovídá na otázku **které soubory**: co je nové, co změněné a co připravené ke commitu. `git diff` odpovídá na otázku **co přesně** se v nich změnilo — vypíše konkrétní řádky, odebrané s minusem a přidané s plusem. Status je přehled, diff je detail. Navíc `git diff` bez přepínače ukazuje jen změny, které ještě nejsou ve staging area; po `git add` už soubor v jeho výpisu není.

## --checklist--

- `git status` říká, které soubory se změnily.
- `git diff` říká, které řádky se změnily.
- `git diff` bez přepínače nezahrnuje to, co je už připravené ke commitu.

# --seed--

## --file-- commands.sh

```sh
# Příkazy pro web Kempu Pod Lipou.
# Běží shora dolů v čisté kopii webu (sandbox/kemp), jako bys je psal do terminálu.

pwd
ls -a

git config --global user.name "Jana Nováková"
git config --global user.email "jana.novakova@example.cz"

git init -b main
git status

git add index.html styles.css
git status

git commit -m "Úvodní stránka kempu"
git log --oneline

cp ../upravy/index-sezona.html index.html
git status

--edit--
--edit--
```

## --file-- setup.sh

```sh
# Připraví web kempu tak, jak ti ho předala správkyně, a vedle něj prázdný „GitHub".
# Tenhle soubor neměň: každé spuštění začíná od stejného stavu.
mkdir -p fotky-originaly ../upravy

cat > index.html <<'EOF'
<!doctype html>
<html lang="cs">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Kemp Pod Lipou</title>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <header>
    <h1>Kemp Pod Lipou</h1>
    <p>Stany, karavany a dřevěné chatky přímo u břehu Lipna.</p>
  </header>
  <main>
    <h2>Otevírací doba</h2>
    <p>Od 1. května do 30. září, recepce denně 8:00–20:00.</p>
  </main>
</body>
</html>
EOF

cat > styles.css <<'EOF'
body { margin: 0; font-family: system-ui, sans-serif; color: #1f2a1f; background: #f6f3ea; }
header { padding: 3rem 1.5rem; background: #2f5d3a; color: #fff; }
main { max-width: 40rem; margin-inline: auto; padding: 1.5rem; }
EOF

# Nepořádek, který do repozitáře nepatří: systémový soubor macOS a originály fotek.
printf 'Finder metadata\n' > .DS_Store
printf 'RAW 24 MB\n' > fotky-originaly/molo-pri-zapadu.cr3
printf 'RAW 31 MB\n' > fotky-originaly/chatky-v-lese.cr3

# Úpravy, které bys jinak udělal ve VS Code. Tady je zkopíruješ příkazem cp.
sed 's/recepce denně 8:00–20:00/recepce denně 8:00–20:00, v červenci a srpnu 7:30–21:00/' index.html > ../upravy/index-sezona.html
sed 's#<h2>Otevírací doba</h2>#<p><a href="cenik.html">Ceník ubytování</a></p>\n    <h2>Otevírací doba</h2>#' ../upravy/index-sezona.html > ../upravy/index-s-cenikem.html

cat > ../upravy/cenik-2026.html <<'EOF'
<!doctype html>
<html lang="cs">
<head>
  <meta charset="utf-8">
  <title>Ceník – Kemp Pod Lipou</title>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <main>
    <h1>Ceník na sezónu 2026</h1>
    <ul>
      <li>Místo pro stan: 180 Kč za noc</li>
      <li>Místo pro karavan: 320 Kč za noc</li>
      <li>Chatka pro 4 osoby: 1 450 Kč za noc</li>
    </ul>
  </main>
</body>
</html>
EOF
sed -e 's/2026/2027/' -e 's/180 Kč/200 Kč/' -e 's/320 Kč/350 Kč/' -e 's/1 450 Kč/1 590 Kč/' ../upravy/cenik-2026.html > ../upravy/cenik-2027.html

# „GitHub": prázdný vzdálený repozitář ve vedlejší složce, bez sítě.
git init --bare --quiet -b main ../github/kemp.git
```

## --file-- run.sh

```sh
# Tlačítko Spustit i testy: vytvoří čistou kopii webu v sandbox/kemp a spustí v ní commands.sh.
# Git tu má vlastní nastavení v sandbox/gitconfig, tvoje skutečné nastavení nezmění.
rm -rf sandbox && mkdir -p sandbox/kemp && cd sandbox/kemp || exit 1
: > ../gitconfig
export GIT_CONFIG_GLOBAL="$PWD/../gitconfig" GIT_CONFIG_NOSYSTEM=1 GIT_CEILING_DIRECTORIES="$(dirname "$PWD")"
export LC_ALL=C.UTF-8 GIT_PAGER=cat GIT_EDITOR=true
bash ../../setup.sh
if [ -f ../../.gitignore ]; then cp ../../.gitignore .gitignore; fi
PS4='$ ' bash -x ../../commands.sh 2>&1
```

## --file-- index.js

```js
// Tlačítko Spustit pustí run.sh a ukáže, co by vypsal terminál.
import { spawnSync } from 'node:child_process';

spawnSync('bash', ['run.sh'], { stdio: 'inherit' });
```

# --solution--

## --file-- commands.sh

```sh
# Příkazy pro web Kempu Pod Lipou.
# Běží shora dolů v čisté kopii webu (sandbox/kemp), jako bys je psal do terminálu.

pwd
ls -a

git config --global user.name "Jana Nováková"
git config --global user.email "jana.novakova@example.cz"

git init -b main
git status

git add index.html styles.css
git status

git commit -m "Úvodní stránka kempu"
git log --oneline

cp ../upravy/index-sezona.html index.html
git status
git diff
```
